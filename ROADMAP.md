# ROADMAP — a brain you can talk to

> **This is the source of truth** for *what the project has accomplished, what it is working on now, and what is left* on the path to the goal. It is kept current as a standing part of the workflow. If anything here disagrees with a detailed write-up in `research/findings/`, the finding is right and this file gets corrected.
>
> **Last synced:** 2026-07-19.

**A note on names.** This document is written to be read without knowing the codebase. A few conventions:
- **Status badges:** ✅ *emergent* (done, and the structure was **learned from experience** on the spiking brain) · 🟩 *done* (validated, but with one hand-designed part that is biologically defensible) · 🟨 *partial* (works in a reduced form) · 🟧 *boundary* (a limit we've mapped precisely; the next mechanism is named) · 🧩 *scaffold* (a temporary stand-in to be replaced by real circuitry) · ⬜ *open* (not built yet).
- **"On the spiking brain" / "on the substrate"** means the computation is done by simulated neurons firing and synapses, not by ordinary code.
- **"The no-confabulation guard"** (elsewhere in the codebase called "the moat") is the mechanism that makes the brain say *"I don't know"* instead of making something up.
- **"Validated across 6 seeds"** means the result held up over six independent random initializations (three used during development, three held back as a blind check) with control experiments that rule out cheating.
- Internal milestone codes like *EMERGE-30*, *D3*, or *RUNG-1* are just our own progress tags; you can ignore them — they're kept only so a claim can be traced to the exact experiment.

---

## 1. The goal

Build **artificial life** — a real brain-analogue that lives, learns, and grows — whose **conversational ability approaches that of a large language model** (open-ended, fluid, grounded, able to carry context), built the honest way: as **one simulated spiking brain** whose language is genuinely its own, learned from experience, with no permanent external AI model doing the thinking for it.

## 2. The rules we hold ourselves to

These decide what counts as real progress. Anything that breaks them is a temporary scaffold, not a milestone.

1. **The brain does the thinking, not the code.** Ordinary (non-neural) software is allowed only for the **world** (the environment, and drawing the picture the eyes receive) and the **body** (moving when the motor neurons fire). Everything in between — perceiving, deciding, valuing, remembering, reasoning, speaking — must be done by simulated neurons and synapses. A biologically *correct* Python formula (a reward, a winner-take-all, a prediction error) is still a shortcut to be replaced.
2. **One brain.** All the faculties are populations of neurons on a single shared brain that talk to each other through synapses — not separate programs stitched together.
3. **Learned, not hand-built.** Structure should be *discovered from experience*, not designed by hand one feature at a time. Building capabilities by hand, one at a time, is a treadmill, not biology.
4. **Fully spiking in the end.** We sometimes use a temporary stand-in to move fast, but the finished version runs entirely on the spiking brain, and every stand-in is tracked and removed.
5. **No permanent external model.** A conventional AI model may be a temporary crutch for fluency, but the end state simulates the actual brain circuitry.
6. **An honest "no" is a result.** When the brain genuinely can't do something, we measure exactly where it breaks and report it — then go find the missing mechanism. A limit is never a stopping point.

## 3. The one-screen picture

**What is already done and genuinely brain-learned:** perceiving categories from experience, understanding sentences (including who-did-what-to-whom in tricky word orders), reasoning that goes beyond what it was told (if a robin is a bird and birds fly, a robin flies), a grammar it worked out for itself from listening, speaking every word out of neural activity, tracking who's being talked about across a conversation, and knowing when to say "I don't know." **Navigation** — moving through a world to reach goals using only what it sees — is its oldest and most thoroughly-tested behavior.

**The two honest gaps between this and a large language model:**
1. **Open-ended fluent speech.** Right now, truly free-flowing prose is produced by a small conventional AI model (a temporary crutch) while the brain supplies all the meaning and grounding. The home-grown replacement has taken its first real step this cycle, but only over a small controlled vocabulary so far. **Newest (2026-07-20) — a home-grown spiking language cortex now GENERATES fluent prose end-to-end, no external model.** A recurrent "WKV" language cortex reads each word in through a *spike's timing* (a resonate-and-fire "phase code" — the first spiking input that clears the network's fidelity bar where three earlier attempts failed catastrophically; 6 seeds, adversarially verified by five independent skeptics), holds its running memory in a graded conductance, and — at the documented training scale (~24M words) — produces genuinely fluent little stories: *"once upon a time there was a girl named sally found a big blue rock… i don't know what to do"*, *"the little girl was very happy… they found a bathtub with lots of fun toys to play with… a pretty box with a key and a skull said the soft teddy bear"*, *"tom and his dog… soon it was time for something fun to ride the bikes around the street amy saw a big boat in the pond"* — coherent narrative with invented character names, across prompts, generated **on the spiking substrate** by the brain's own machinery. Honest scope: this is a small model at a small (TinyStories) scale, and it both *comprehends* (beats a strong n-gram baseline at deep context) and *generates* — the value coming from the trained read-out over a fixed spiking cortex (the same "reservoir" lesson §9.1 and the deep-credit arc both land on), realized at scale; the one remaining purity step — delivering the decoded value through a synapse rather than a brief host read — is now **validated (2026-07-20):** the incoming spike drives a real slow-NMDA synapse whose decaying conductance encodes the value (recovered perfectly by a biological log-compressive read-out, on the substrate, no host read), and on the real deployed word stream this fully-synaptic read reproduces the fidelity the language task needs (accumulated-state correlation 0.97, above threshold) — so the input path is now fully synaptic end-to-end — and, wired into the deployed pipeline, it reaches the SAME language score as the host-read version (deep-context margin +0.872 vs +0.878, essentially identical): the last host shortcut in the input path is removed at zero cost. The small early gap turned out to be a resolution setting (the spike-timing was quantized too coarsely) — a single knob closed it; a nice illustration of the discipline (three plausible "fixes" were tested and rejected before the real one was found). ⇒ the home-grown language cortex now both generates fluent prose AND takes its input through genuine synapses on spikes, with no external model and no host shortcut in the loop. So the *mechanism* of home-grown fluent speech on spikes is demonstrated; matching a large LLM's breadth is the documented data/scale climb, not a substrate wall. Write-up: `research/findings/2026-07-20-gap1-RF-PHASE-ENCODE-first-spiking-encode-PAST-the-wall.md`. **Newest (2026-07-20, later this cycle) — the NORTH STAR reached: "a brain you COMMUNICATE with."** The home-grown spiking language cortex was wired into the brain's grounded-conversation console, replacing the small conventional AI model on the render path: you type a question → the brain understands it, retrieves the relevant fact from its own memory, decides answer-vs-abstain (the no-confab safety gate fires BEFORE the generator is ever invoked), and speaks a fluent grounded answer — *"what does the dog eat?" → "The dog eats meat"*, *"tell me about the dog" → "A dog is big. It eats meat, chases cat and likes bone."*, teach it a new fact live and it uses it, ask about something untaught → *"I don't know."* The whole turn runs **on the spiking substrate in one process**, the format learned by a small data fine-tune (6-seed GO, held-out facts, no-confab moat verified). The single-pass weaving of *several* facts into one flowing sentence stays the honest field wall (managed by rendering each fact and joining them). Write-ups: `research/findings/2026-07-20-grounded-fluent-conversation-DE-RISK-{0,1-2,3,4,5}-*.md`.
2. **A deeper learning rule.** Some ceilings (how much structure it can compose, how deep it can nest ideas) come down to one missing capability: a learning rule that lets a *deep* network teach itself on spikes. **New (2026-07-14) — this frontier just took a big step: the deep learning rule now trains a real spiking network END-TO-END.** A biologically-plausible, transport-free local rule (e-prop: forward eligibility traces + a membrane-potential sensitivity + fixed-random feedback — no backprop-through-time, no "cheating" weight-copy wires) trains a *deep* spiking network to solve a genuine multi-step reasoning task — it fits perfectly, generalizes to unseen examples, and passes every rigged-control test; it works on the real production brain too (a population-coding version closes the remaining gap). This decisively overturns the old "spikes can't do deep learning" verdict. Honest scope: validated so far on a compositional *classification* task; whether the same rule cracks the harder jobs it's meant to unlock (a *learned* word-binder, deeper nesting) is now being tested directly. **(An over-eager attempt to apply it to language via a fixed recurrent scaffold was tried, self-caught as an artifact by an internal skeptic review, and cleanly retracted — the honesty checks working.)** Separately, the *long-range* piece turned out far more tractable than it looked — long-range language is bottlenecked on learning the **input representation on a fixed recurrent scaffold**, not the hard deep-recurrent rewrite; the biology-legal version reaches ~78% of the full-backprop reference, already on spikes (see §9.1). **Newest (2026-07-15) — this gap is now diagnosed precisely, and the news is good: at our data scale it is NOT the brain design.** A definitive fair-baseline check plus the project's own ceiling run showed *even a full transformer loses to a simple word-predictor at these small data sizes* — so being "only word-predictor-level" is a data-scale fact shared by every model class, not a flaw in our brain. The one thing a word-predictor structurally can't do — **compose brand-new combinations of ideas it never saw** — was proven to come from a fixed *binding* operation over well-separated concept codes (which our brain already has and already uses, composing never-seen sentences across 320 concepts on the spiking neurons), *not* from training a cleverer learner (even a perfect-math learner memorizes and fails to generalize; the binding succeeds). ⇒ the honest path to fluency is more *training and learned knowledge* fed through the existing composing-machinery, not a fundamentally different learner (see §9.1). **Newest (2026-07-16) — the deep-rule mechanism hunt reached an honest, complete conclusion.** The one credit signal the whole biological-rule family omits — the *cross-neuron* term that says how one neuron's earlier state shaped another's later one — was found and *works when the neurons are treated as smooth rates* (a clean, verified win), but it does **not** cleanly survive the jump to real spikes on simple point-neurons (a decisive result from a dose-response test, six seeds, and four independent adversarial checks: what looked like a win turned out to be extra *magnitude*, not the *correct-direction* credit — the wrong-sign version did just as well). The biology's own answer to this is not an online broadcast but **replay** — the brain re-plays experiences offline to assign credit (sharp-wave ripples standing in for backprop-through-time), a mechanism the project already partly holds — and, critically, this whole credit rule was *already* shown to be **off the critical path** for fluent conversation. So the mechanism side is in good shape; the remaining fluency gap is genuinely *scale/data*. In parallel, the **one-brain integration** advanced: two learning "cortices" trained **at the same time on one brain without interfering** — each learns its own knowledge as well as it would alone, while a deliberately-overlapped control does interfere (the honest positive control) — the first step of the "one brain that keeps learning many things at once" plan. **Newest (2026-07-20) — the recurrent language cortex now takes its INPUT through spikes, closing the last "spiking-input" gap on this frontier (6-seed GO, adversarially verified).** The learned on-brain recurrent state (§9.1's selective-SSM integrator) previously needed a *perfect host-fed* input to work at deep context — three ways of delivering that input as spikes had all catastrophically failed (they blurred the value in a way that compounds over the recurrence). The fix is to carry each incoming value in the **timing (phase) of a single spike** (a resonate-and-fire "phase code") rather than a firing rate — this delivers the value *unbiased* across its whole range (no dead-zone), and the deep-context language ability comes back to **full parity with the perfect host input on every one of six seeds** (dev + blind), while two rigged-control checks collapse it. Honest scope, verified by five independent skeptics: this is faithful *spiking delivery* of the value (a high-fidelity phase "analog-to-digital" step) feeding the already-validated graded memory — *not* a claim that spikes did the long-range computation; that graded memory does the holding, exactly as real biology (and every spiking-SSM in the literature) keeps recurrent state in a slow conductance while spiking the input/output. NO engine edit (reuses the resonate-and-fire machinery already in the brain). Remaining (bounded): deliver the decoded phase through a synapse rather than a host read; scale the vocabulary/model. Write-up: `research/findings/2026-07-20-gap1-RF-PHASE-ENCODE-first-spiking-encode-PAST-the-wall.md`. **Newest (2026-07-20, later this cycle) — the language cortex's LEARNING is now shown to be biologically plausible (the last training "shortcut" retired for the grounded renderer).** The cortex had been trained by ordinary backprop-through-time (a shortcut vs the brain's local, no-weight-copy rules). A read-only research gate + a 4-way adversarial verification (which caught two overstatements in the gate's own reasoning) established: the recurrence is a simple leaky memory with no recurrent weight matrix, so the only thing needing through-time credit is a single input map — which a random one already handles (a random "reservoir" + a trained read-out reaches the same fluency), so the whole grounded-answer skill lives in the **read-out**, which is trained by a genuine biological local rule (feedback-alignment / Kolen-Pollack — the error travels back through a fixed/aligned feedback pathway, never a copied weight — plus a plain delta-rule step, no adaptive optimizer). **On the grounded task it MATCHES the backprop ceiling (0.86-0.91), with the general fluency gap shown to be merely convergence-speed (it keeps closing with more training, not a wall).** The one remaining purity step — running that read-out *learning* on the spiking substrate itself (rather than off-brain) — is now fully de-risked and staged: the clean-error version trains a reduced read-out to 0.998, the committed on-brain burst-based plasticity rule is the biological form of it, and the exact remaining piece is a single additive engine mechanism (carry the graded memory to the output neurons through synapses); a careful next-cycle build. Write-up: `research/findings/2026-07-20-wkv-cortex-biological-learning-CLOSE-local-rule-readout-retires-BPTT.md`.

**Bottom line:** the foundations — the brain engine, movement, memory, understanding, concepts, and self-organized speech — are done and mostly emergent. The distance to LLM-level conversation is real, bounded, and measured in months of focused work, not a single demo — and nothing is fundamentally blocked.

**Latest (2026-07-17 → 19): a focused push to close the last gaps.** An audit sharpened the remaining work into **five concrete gaps**, and the project is now closing them one at a time. The **learned binder** (composing ideas over the brain's own codes) and **tracking who's being talked about** across a conversation are now done — fully on the spiking brain, validated across seeds. The **deep learning rule** took an honest turn: the earlier *e-prop* route was **retired as not good enough on real spikes** (2026-07-17), and the work pivoted to the biology's actual answer — a **burst-based local rule** reading a **self-sustaining dendritic signal** that holds credit over seconds (the same "held plateau" the memory work uses), plus a **learned feedback pathway** (Kolen-Pollack) that corrects the credit's *direction* with no weight-copying. This mechanism genuinely runs on the spiking brain (the deep representation forms, the no-confabulation guard holds, no cheating wires); whether it reaches full **accuracy at scale** is being measured right now. On the **memory** side a long-standing wall fell: the brain can now **complete a partial memory** — fill in a whole remembered pattern from a fragment — on the spiking substrate, a capability stuck at chance for the project's *entire* history, thanks to a newly-added **dendrite that latches on and off like a switch** (the one small change to the core engine this arc). Building on it, the first steps of **imaginative replay** (a completed memory driving its downstream output pattern) now fire; the remaining work is making different memories cleanly distinct (a stronger within-memory recurrent already helps, but not yet robustly across every seed). **Open-ended free speech** (gap #1) remains the deep frontier, still leaning on the small temporary language model. Nothing here is fundamentally blocked; each open gap has a named next mechanism. *(Details: `GAP_CLOSURE_MISSION.md` + the `2026-07-17`/`-18`/`-19` findings.)*

---

## 4. The brain engine (the platform everything runs on) — 🟩 Done

*This is the "hardware": the neurons, synapses, and wiring framework that every faculty runs on. It's mature and production-grade.*

| Part | What it reproduces (biology) | Where |
|---|---|---|
| The GPU simulation engine | The whole brain as one continuously-updated system of realistic conductance-based neurons; scales to 100,000+ neurons; saves/restores state; fully reproducible | `sim/bridge.py` |
| Neuron types — Izhikevich, Hodgkin-Huxley, adaptive exponential, and a resonate-and-fire "phasor" neuron | ~50 cell-type presets across cortex, hippocampus, basal ganglia, thalamus, cerebellum, spinal cord (catalog cluster I; the phasor neuron follows Frady-Sommer 2019) | `sim/enums.py`, `sim/kernels.py` |
| The region-and-pathway framework | Multiple interacting brain regions declared as populations with wiring between them; runtime "gates" that open/close a pathway (a model of how the thalamus routes cortical traffic, Logiaco-Abbott-Escola 2021) | `sim/regions.py` |
| The learning-rule family | Spike-timing-dependent plasticity (Bi & Poo), short-term facilitation/depression, Hebbian learning, dopamine-gated three-factor learning (Schultz), homeostasis (Turrigiano), and a dendritic burst-dependent rule (catalog cluster J) | `sim/kernels.py` |
| The neuromodulator system | Dopamine, noradrenaline, serotonin, acetylcholine concentration dynamics and their effects; **one shared dopamine system drives both movement and conversation** (catalog cluster C) | `sim/neuromodulators.py` |
| Lifelong-learning persistence | Learning that accumulates across sessions without erasing old knowledge | `sim/lineage.py`, `sim/synapse_storage.py` |
| A two-compartment "dendritic" neuron (early) — 🟨 | Active dendrites and burst-based learning — the substrate for the deeper learning rule (see §9) | `enable_bdsp` in the engine |

---

## 5. The developmental path

*Roughly the order a developing brain builds these. Each stage names the brain regions/functions it reproduces (with citations), a status, what's done, what's open, and the next step. Grouped into the sensorimotor brain (5.1–5.6), the thinking-and-language brain (5.7–5.12), and the living whole (5.13).*

### — The sensorimotor brain —

### 5.1 Perception — *seeing the world* · 🟨 Partial
- **Goal:** turn the picture the eyes receive into neural codes that capture real similarity (two apples look alike; an apple and a river don't), so the rest of the brain has something meaningful to learn from.
- **Biology:** edge/contrast detection (center-surround cells), then oriented-edge detectors in primary visual cortex (Hubel & Wiesel; catalog E; Kandel Ch 22), splitting into a "what" stream (object identity, ventral) and a "where/how" stream (location and movement, dorsal) (Kandel Ch 24–25).
- **Done:** a real oriented-edge (Gabor) visual front end plus a retina renderer, giving genuine visual similarity structure (objects of the same category look alike to it, verified against the raw pixels); population coding lifts a single neuron's noisy read to full accuracy.
- **Open:** only the first cortical visual stage exists (no deeper object-recognition hierarchy, no separate location stream as its own module, no hearing/smell/touch); the edge detectors are fixed rather than learned.
- **Next:** build a deeper, self-organizing visual stage only where a later capability needs it.

### 5.2 Attention & orienting — *where to look* · 🟩 Done (orienting) / 🟨 Partial (attention)
- **Goal:** decide where to point the eyes/attention and commit the movement; emphasize what matters.
- **Biology:** the superior colliculus (a brainstem "look here" map, released to act by removing an inhibitory brake; Kandel Ch 35), and noradrenaline-driven arousal that turns up the gain on surprising events.
- **Done:** a spiking superior-colliculus orienting reflex, on by default (a small, honest ~16% cost over an idealized version); an arousal/surprise signal that speeds learning after unexpected outcomes.
- **Open:** no top-down "pay attention here" controller yet; no dedicated eye-movement burst generator; feature-binding by neural synchrony is available but not deployed.
- **Next:** add a spiking attention controller when scenes with several objects demand it.

### 5.3 Action selection — *choosing what to do* · 🟩 Done
- **Goal:** pick one action from competing options, gather evidence until confident, and commit — entirely in spikes.
- **Biology:** the basal ganglia "go/no-go/stop" loops that select an action by releasing its brake while suppressing rivals (Kandel Ch 38), and cortical evidence-accumulation to a decision threshold (Wang 2002; Lo & Wang 2006).
- **Done:** a full basal-ganglia selection circuit (per-action go/no-go channels, lateral competition) that solved a long-standing "stuck-motor" failure; and a **fully-spiking decision** — neurons accumulate evidence and fire a commitment burst — is now the default, having **retired the old ordinary-code shortcut** (a winner-take-all `argmax`), at a modest cost over the idealized version.
- **Open:** the global "cancel that action" pathway isn't wired; only the movement loop is modeled (not the parallel loops for choosing *what to say* or *what to think about* — those reuse the same machinery when needed); most striatal interneuron types are unmodeled.
- **Next:** add the parallel selection loops when non-movement choices (which fact to say, which topic) need the same circuit.

### 5.4 Reward & value — *what was worth doing* · 🟩 Done (reward signal + learning + drive) / 🟨 Partial (value critic)
- **Goal:** compute a reward-prediction-error teaching signal, learn how good situations are, and let motivation shape learning and choice — in spikes, not by formula.
- **Biology:** midbrain dopamine neurons signaling *actual minus expected* reward (Schultz 1998), three-factor learning where dopamine gates synaptic change, a value "critic," and hunger/incentive drives (Kandel Ch 41–43).
- **Done:** a spiking dopamine reward-prediction-error signal; three-factor learning in the engine; a value signal proven to actually drive choices (removing it collapses the high-value pick); and **one shared dopamine system that drives both the moving brain and the conversing brain** — a hungry brain literally becomes more careful about what it claims to know.
- **Open:** the explicit "how good is this situation" critic population isn't built yet (the highest-value next step); some reward is still computed by formula rather than by a circuit; no fear/aversion system.
- **Next:** build the explicit spiking value critic; convert the remaining formula-computed reward into a real circuit.

### 5.5 Navigation & spatial cognition — *moving through a world to reach goals* · 🟩 Done (the flagship embodied behavior) / 🟧 learned policy deferred
- **Goal:** an embodied agent that explores a world, figures out where it is and where the goal is *from what it perceives*, and navigates there — the project's first and most thoroughly-tested end-to-end behavior. This stage is where perception (5.1), orienting (5.2), action selection (5.3), reward (5.4), and spatial memory come together into a living behavior.
- **Biology:** hippocampal **place cells** (neurons that fire at specific locations; O'Keefe & Nadel) and entorhinal **grid cells** for a spatial map; the basal-ganglia action loop for movement; dopamine reward-prediction-error for learning the route; and prefrontal working memory holding the current goal (Kandel Ch 38, 54).
- **Done:** a gridworld agent that reaches (and re-reaches, when the goal moves) targets using **only perception** — the "cheat" inputs were progressively removed (no being handed its own coordinates, the goal's coordinates, a hand-coded heuristic, or a distance-based reward), so it navigates from a rendered visual/landmark scene through a learned perception→cortex pathway. Place cells emerge from landmark perception; prefrontal working memory holds the goal; adaptive dopamine and a maturation-style curriculum improve learning; the whole thing scales to larger grids with *tighter* variance, and the movement decision is the fully-spiking commit-burst from 5.3. The flagship result closes four of five original shortcuts and spends a large fraction of its time sitting on the goal.
- **Open:** the *learned* spatial policy (deciding the best move by reinforcement learning) currently uses a validated rate-based stand-in for the value function — replacing it with a truly self-taught deep policy is gated on the deeper learning rule (§9); a proper entorhinal **grid-cell** map and path-integration are not built (location is via place cells + landmarks); the world is a corridor/grid, not an open environment.
- **Next:** replace the rate-based policy stand-in with the dendritic-learned policy once that rule lands; enrich the world and add a grid-cell spatial map.

### 5.6 Memory — *holding on to experience* · 🟩 Done (mechanisms) / 🟧 one deep-consolidation boundary
- **Goal:** store episodes so similar ones don't blur together yet a partial cue can bring one back, tag memories so they can be reactivated, and move them into long-term cortical storage during "sleep" — all without erasing old memories.
- **Biology:** the hippocampal three-step loop (entorhinal → dentate → CA3 → CA1; Kandel Ch 54), where the dentate gyrus keeps memories separate and CA3 completes a memory from a fragment (Marr 1971); activity-tagged memory traces ("engrams"; Tonegawa, Liu 2012); and sharp-wave ripple **replay** during rest that both consolidates memories and — importantly — does the job that backpropagation-through-time does in artificial networks (Foster & Wilson 2006).
- **Done:** the full three-step loop with measured pattern-separation and pattern-completion; a production engram-tagging interface (tag a memory, reactivate it later) with ~90% cued retrieval; sleep-replay consolidation that moves memories to cortex with **no catastrophic forgetting** (verified with strict controls); and a striking result — **replay genuinely replaces backpropagation-through-time** for learning the conversation's discourse memory.
- **Open (🟧):** consolidating *composed* memories (a whole structured fact, not a single item) currently strands in the hippocampus and doesn't reach cortex — a characterized boundary; CA3 completes a single snapshot but not a *sequence*; no "theta rhythm" pacemaker or the finer sleep-stage generators yet.
- **Next:** attack the deep-consolidation boundary via a sequence-completing CA3 plus theta-rhythm compression, and build a theta pacemaker to improve replay quality.

### — The thinking-and-language brain —

### 5.7 Concept formation — *carving the world into categories* · ✅ Emergent
- **Goal:** discover categories and abstract concepts from experience, and learn the structure of sequences — unsupervised, and in spikes.
- **Biology:** a sparse-expansion coding trick from the cerebellum (Marr 1969, Albus 1971), the anterior temporal lobe as a concept hub with distributed word representations (Patterson & Lambon Ralph 2007; Pulvermüller), and a cortical sequence-memory mechanism using two-compartment neurons (Bouhadjar & Diesmann 2022).
- **Done:** **categories are discovered from experience** with increasing self-sufficiency — from co-occurrence, then from varied contexts, then from a self-organizing competitive layer, then grounded in real vision — fully in spikes; a **word-meaning cortex learned just from listening** to a stream of sentences (it picks up which words mean similar things); and a cortical sequence-memory that learns to predict the next symbol in context, with a built-in refusal to make things up.
- **Open:** one normalization step in the word-cortex read-out is still done by ordinary code rather than a circuit (a designed replacement exists); scaling the learned vocabulary to ~320 concepts needs a bigger source corpus; getting the memory attractors to *form themselves* (rather than be installed) — **this just took a real step (2026-07-14):** the memory circuit's attractors had refused to become *selective* from experience — every plasticity rule potentiated the stored pattern and its non-members equally (a "flat" attractor that fires for everything). The fix was a **competitive** learning rule (a co-firing pair strengthens; a member→non-member pair *weakens* — the brain's heterosynaptic competition), applied for the first time to the memory circuit's own recurrent wiring. It **decisively breaks that wall**: the within-pattern vs to-outsiders weight ratio jumps from ~1× (flat) to **5–9× (selective) on all six seeds** — the first time a *selective* memory attractor forms itself from experience rather than being installed. Honest scope: turning that selective weight structure into robust *recall* is a separate scale boundary — but note **⚠️ 2026-07-17: this is on the HARDER distributed-code variant. EMERGENT CA3 pattern completion for a sparse assembly is ALREADY 6-seed GO (2026-07-09): a partial cue completes held-out members specifically, learned from experience (LINEAR/NO-TRAIN/PERM-CUE all 0). The competitive-formation-at-scale is a refinement of an already-GO capability, not the frontier. See `research/findings/2026-07-09-riii-emergent-ca3-completion-kopsick-formation.md`.**
- **Next:** the sparse-synchronous encoding for robust self-formed recall (the full-scale model); build the normalization circuit; scale the word cortex.

### 5.8 Language comprehension — *understanding what is said* · ✅ Emergent / 🟧 recursion boundary
- **Goal:** map word-forms to who-did-what-to-whom and to meaning, including awkward word orders and long-distance dependencies — learned, in spikes, with no special-case code per sentence shape.
- **Biology:** the dual-stream model of language (a meaning stream and a sound-to-articulation stream; Hickok & Poeppel; Kandel Ch 55), Wernicke's area for word selection, and a frontal-striatal "reservoir" that maps word order onto roles (Hinaut & Dominey 2013).
- **Done:** a parser that assigns roles regardless of active/passive voice ("the dog chased the cat" and "the cat was chased by the dog" get the same agent); a **reservoir that learns the word-order→role mapping itself** (retiring an earlier hand-written labeler; it was hardened after adversarial review caught a too-easy first version); resolving a long-distance dependency across ~33 words, in spikes; and a neural router that figures out what kind of question is being asked (replacing a keyword lookup).
- **Open (🟧):** deeply nested sentences ("the cat the dog the man saw chased fled") degrade past a depth of ~3 — which is, honestly, roughly the human limit too; cross-language grammar (case markers, rich morphology) is a deliberate opt-in, not on by default; no hearing front end (it reads structured input, it doesn't process sound).
- **Next:** build the spiking working-memory buffer that pushes nesting depth on the brain itself.

### 5.9 Semantic reasoning — *inference beyond what it was told* · ✅ Emergent / ⬜ probabilistic accumulation open
- **Goal:** infer things it was never told — inheritance, exceptions, transitivity — and weigh evidence over symbols.
- **Biology:** the hippocampus as a relational/inference network (Eichenbaum; O'Keefe & Nadel), hierarchical semantic memory (Collins & Quillian), and the same evidence-accumulation math used for perceptual and value decisions applied to symbols (Kandel Ch 56).
- **Done:** **inheritance, multi-level inheritance, exceptions, and transitivity all emerge on their own** from overlapping neural codes plus the next-state predictor — no separate "inference engine" — over both given and *self-discovered* categories; a large adversarial audit found and fixed a class of measurement flaws, and every result survived its corrected test.
- **Open:** no "weigh evidence with learned reliability" reasoning primitive yet; the fully-spiking version of recalling a fact about a *newly seen* object is an honest boundary (a hybrid where code routes which concept fired works well); genuinely open-world inference beyond learned facts is a field-wide unsolved problem (managed by staying on-topic and abstaining).
- **Next:** wire a spiking evidence-accumulator for probabilistic reasoning (reusing the decision machinery from 5.3).

### 5.10 Language production — *speaking* · ✅ Emergent (within a bounded vocabulary) / 🟧 open prose deferred
- **Goal:** turn meaning into correctly-ordered speech — choosing function words, word endings, word order — with **every word produced by neural activity**, and the grammar worked out from experience rather than programmed.
- **Biology:** Broca's area for grammatical encoding and articulation (Kandel Ch 55), sequence-generation circuits for serial order (Grossberg; Kandel Ch 34), and the discovery of grammatical structure from statistics (construction grammar; Tomasello, Goldberg).
- **Done:** **the entire grammatical structure self-organizes from a corpus** — which words are function words, the slot order, the inventory of sentence templates — with the hand-written grammar removed as an input; **every word (content and function alike) is spelled out from the neurons' spike output**; the whole answer is produced by one brain in one process; and it renders seven sentence constructions in spikes, including transitive and ditransitive ("the dog gives the cat a bone").
- **Open (🟧):** it produces a **bounded, corpus-attested set of sentence patterns**, not open prose (the honest, deferred "roughly four orders of magnitude too small" wall); the spoken vocabulary per neural pathway is limited and scaling it is straightforward but linear; the emergent *open-ended* generator (see §9) is currently only at the token level over a small vocabulary.
- **Next:** scale the spoken vocabulary, then drive the open-generation ladder (§9) to replace the conventional-AI crutch.

### 5.11 Discourse & conversation — *tracking who-and-what across turns* · ✅ Emergent (the spiking core) / 🧩 fluent chat still leans on the crutch
- **Goal:** track who and what is being talked about across a multi-turn conversation, resolve pronouns, abstain when it doesn't know, and hold a growing, grounded chat.
- **Biology:** prefrontal working memory holding discourse referents (Kandel Ch 52), an attentional stack that pushes a topic when the focus shifts and pops it back on return (Grosz & Sidner; O'Reilly & Frank gating), and a familiarity signal that distinguishes known from unknown.
- **Done:** a spiking **discourse memory that tracks who's-acting-now versus who-was-acting-before** across a connective, learned by *replay* rather than backpropagation-through-time; multi-turn pronoun resolution on a persistent working-memory loop; a learned **no-confabulation guard** that matches a reference standard with zero breaches and is consulted *first* (so the fluency crutch is never even invoked when the brain should abstain); and a working "talkable brain" console where you can teach it facts, ask questions, and watch it reason, speak on spikes, and say "I don't know."
- **Open:** picking which of several remembered people a bare "it/they" refers to is an unsolved case (the fix is specified — a winner-take-all competition between the candidates); truly fluent single-pass synthesis over many facts still confabulates on the small conventional model; open-domain non-fact chatter is a field wall.
- **Next:** build the multi-referent disambiguator; drive the emergent generator (§9) to replace the conventional-AI fluency.

### 5.12 Working memory, sequence & recursion — *holding structure in mind* · 🟩 Done (working memory + graded memory) / 🟧 recursion depth
- **Goal:** hold an ordered set of items across a delay, process sequences with graded fading memory, and match nested structure with a bounded stack.
- **Biology:** a theta-gamma "7±2 slots" buffer (Lisman & Idiart 1995), NMDA-based persistent-activity working memory (Wang 2002), and a fading-memory reservoir (Hinaut & Dominey).
- **Done:** persistent working-memory attractors that hold items across a conversation; a reservoir that holds a distant cue across ≥16 intervening words, in spikes; and a slot-based buffer that pushes nesting depth to ~3 (scrambling the slots breaks it — proving the ordered structure is doing the work).
- **Open (🟧):** the *spiking* version of the slot buffer is designed but not yet built on the brain; depth beyond ~3 is the human-faithful bounded limit; no theta-rhythm pacemaker yet.
- **Next:** build the spiking slot buffer and a theta pacemaker (shared with the comprehension and memory stages).

### — The living whole —

### 5.13 Artificial life — *living, developing, remembering* · 🟩 Done (in pieces) / 🟨 Partial (as one unified life)
- **Goal:** a persistent brain that lives, perceives and remembers its own experience, develops over time, is driven by one shared motivational core, and can be talked to about its own life — all on one brain.
- **Biology:** complementary learning systems (hippocampus + cortex; McClelland 1995) with self-replay, a shared dopamine/hunger drive, and memory persistence across sessions.
- **Done:** a **develop-over-time loop** where a brain's vocabulary and knowledge grow day by day from real learning, with *zero* forgetting and the no-confab guard holding every day; the **moving brain and the conversing brain merged into one** with real synaptic interaction between them (a spoken command steers movement; what it perceives while navigating becomes something it can later be asked about and *compose into a new fact*); one shared drive affecting both halves; and persistence so a brain lives across a reset (resume its body and its lived memories, or start empty).
- **Open:** the learned movement policy is still the rate-based stand-in (the deferred deep-learning wall, §9); persistence re-instates saved facts rather than the raw synaptic tensor; the world is a corridor, not open-ended; the fact-composition path still uses an idealized binding scheme (§8).
- **Next:** merge the conversation machinery into a single co-resident brain; replace the stand-in policy with a self-taught one; move toward raw-synapse persistence and richer worlds.

---

## 6. The body & supporting biological systems

*The parts that surround the thinking brain, and biological systems present as building-blocks or planned.*

- **The body / motor output** — 🟨 the legitimate "body" interface. Per-action motor-neuron pools drive movement; there is no muscle model, spinal central-pattern-generators, or neuromuscular junction yet (catalog cluster H, M). Built out only if embodied tasks require it.
- **Cerebellum** — ⬜ cell-type presets exist (Purkinje, granule, climbing fibers) but the error-correcting microcircuit itself is not built; it's the natural home for predictive timing and fine motor/temporal learning (catalog cluster F; Marr-Albus-Ito).
- **Sleep architecture** — 🟨 sharp-wave-ripple replay is built and used; the finer NREM/REM stage generators (slow oscillations, spindles) are not (catalog cluster N).
- **Neuromodulation breadth** — 🟨 dopamine is fully deployed; noradrenaline / serotonin / acetylcholine are supported by the framework but only partially used.
- **Future / validation directions** — ⬜ disease models (Parkinson's, schizophrenia, epilepsy) are natural stress-tests of the basal-ganglia and cortical circuits; glia, neurovascular coupling, and molecular/transcriptional detail are deliberately out of scope at the current level of abstraction (catalog clusters P, Q).

## 7. How you watch it, talk to it, and grow it (interfaces & tooling)

*Not brain computations — these are the windows into the brain and the ways to interact with it.*

- **Real-time 3D viewer** — a live OpenGL view of every neuron firing in space, with camera, picking, and overlays (`neural-simulator.py`, the `viz/` package). Watching the brain think is a first-class feature.
- **The talkable-brain console** — an interactive chat where you teach the brain facts, ask questions, and watch it comprehend → reason → speak on spikes → abstain, all under the no-confab guard; a web front end with a brain picker and per-turn "brain activity" view.
- **The develop-run launcher** — a hands-off, resumable, pause-able way to let a brain live and develop over many simulated days, saving a loadable snapshot per day so you can load any day and talk to the brain at that stage of its life.
- **The experiment & stimulus system** — programmable stimulus injection, input/output neuron groups, readouts, and multi-phase training protocols for running controlled experiments.
- **The biological validation suite** — automated checks that the neurons reproduce textbook results (spike-timing plasticity curves, excitation/inhibition balance, paired-pulse facilitation, gamma oscillations, homeostasis).

---

## 8. Temporary stand-ins still in place (and how each gets replaced) · 🧩

1. **A small conventional AI model (~21M parameters) for open-ended fluency.** It supplies *only* the fluent wording, and only after the brain has decided what to say and checked it — it's never used when the brain should abstain. It's the one forbidden "permanent external model," so it must go. **Replacement:** the home-grown generation ladder in §9, whose first rung just landed.
2. **An idealized word-binding scheme** (a clean, exactly-reversible algebra for relating words into facts). Its *operations* already run in spikes, but the clean exact-reversibility isn't how a real cortex works, and one part (combining several attributes at once) provably can't be learned from scratch by simple neurons — so a fixed structural trick stands in. **Replacement:** a *learned* cortical binder — the same dendritic-learning frontier as §9.
3. **A few movement read-outs and reward values still computed by code** rather than by a circuit. **Replacement:** the fully-spiking decision is already the default; the remaining reward is a de-risked circuit away, plus the explicit value critic (5.4).
4. **A backpropagation-based training stand-in** used only as a development yardstick, at toy scale. **Replacement:** the biological dendritic learning rule — exactly as replay already replaced backpropagation for the discourse memory.
5. **One word-cortex normalization step done in code.** **Replacement:** a designed on-brain inhibition/adaptation circuit.
6. **Fact-based (rather than raw-synapse) persistence, and a rate-based movement policy.** **Replacement:** raw-synapse continuity and a self-taught policy, once the deeper learning rule lands.

## 9. The honest frontier (what's left, and the real walls) · 🟧 / ⬜

1. **A deep learning rule that works on spikes — the top lever.**

   > **⛔ CORRECTED 2026-07-16 — READ FIRST.** The record elsewhere says *"feedforward spiking deep credit is ALREADY
   > GO (e-prop + population coding), K=8 0.877 ≈ the LIF ceiling — NOT a blocker; the genuine frontier is RECURRENT
   > off-diagonal."* **That is not supported by the runs it cites.** The 0.877 was averaged from
   > `raw/_epropport/k8_s4{2,3,4}.json` (inherit 0.889/0.926/0.815 → exactly 0.877) — and **all three report
   > `SIGNAL=False` with the anti-cheat FAILING**, each printing *"HONEST NEGATIVE — the ported e-prop does NOT
   > cleanly train the task on the bridge."* The instrument was not broken; **it was overridden.**
   > **Measured with the frozen-hidden RESERVOIR control the gate never had** (the `train_layers` isolation hook
   > existed in-file, documented for exactly this, and had never once been invoked): FULL **0.889** vs FROZEN
   > **0.778** vs chance 0.333 ⇒ **a fixed random spiking reservoir + a linear readout is ~80% of the margin**;
   > deep credit adds **+0.111**, seed-variable (+0.185 / +0.037). Reproduces exactly (seed 43 = 0.926, matching
   > `k8_s43` to 3dp). ⇒ **the feedforward half is ~20% mechanism, 80% reservoir — this lever is MORE open than the
   > record claimed, and the off-diagonal arc was deprioritized partly BECAUSE feedforward was believed solved.**
   > n=2 = INDICATIVE; a 6-seed FULL-vs-FROZEN is in flight. The gate now carries the reservoir control DEFAULT-ON
   > + CI-guarded so it cannot recur. Audit:
   > `research/findings/2026-07-16-deep-credit-GO-is-80pct-RESERVOIR-the-frozen-hidden-control-was-never-run.md`.

    > **⛔ SUPERSEDED 2026-07-17 — READ FIRST. This item names Node Perturbation as "the mission-critical lever / the
    > CANDIDATE to succeed on spikes." Our own record has since RETIRED that, and a seed-fixed re-run confirms the wall.**
    > (1) The **feedforward deep-credit rule is NOT-GO** on valid seed-fixed data — a fixed random spiking reservoir
    > matches/beats the trained net on 4/5 blind seeds (the prior "GO" was confounded by an unseeded substrate, now
    > fixed). (2) **Node Perturbation is off-brain-GO but has no edge over Kolen-Pollack (12-seed REFUTED) and was
    > explicitly retired as an on-spike deep-credit candidate 2026-07-13** (readout-independent variance wall). (3) The
    > real blocker is a **supervised spiking-classifier-READOUT wall shared by e-prop, D1/BDSP, and NP** — supervised
    > global-loss deep credit through a spiking readout does not train on this substrate. (4) The **working path is the
    > UNSUPERVISED on-spike stream cortex** (HTM + committed BDSP `fused_htm_permanence_update`), which learns deep
    > representations from a stream *without* supervised deep credit. Cheapest decisive de-risk: a rate-net-vs-spiking-net
    > positive control on the shared readout wall. **Map + citations:**
    > [`2026-07-17-learning-rule-frontier-map-...`](research/findings/2026-07-17-learning-rule-frontier-map-eprop-NOT-GO-NP-retired-the-real-blocker-is-the-shared-readout-wall.md).
    > The prose below is retained for history; treat "NP is the candidate" and "feedforward deep-credit GO" as WITHDRAWN.

    Several ceilings (composing structure, deep nesting, the learned binder, the self-taught movement policy) all trace back to one missing capability: letting a *deep, multi-layer* spiking network teach itself. We have the feed-forward half working in an idealized (off-brain) form, and a two-compartment dendritic neuron that completes patterns a simple neuron can't. Getting a full spiking network to train end-to-end to real accuracy is being actively worked. **Honest status (2026-07-12):** the *rate* version of this rule is proven (it trains a two-hidden-layer network to near-oracle accuracy off-brain), but the **spiking port does NOT yet train a two-hidden-layer network** — on a controlled task where a plain backprop oracle scores 100%, the spiking version stays at chance across every setting we swept (learning rate, epochs, a preset vs running baseline, population averaging, and all three variants of the rule). We localized *why*: the forward pass carries a real (if weak) class signal, so the wall is the **credit** signal's inability to teach the hidden layer — the known limitation of "feedback-alignment"-style local rules. **Update (2026-07-13) — the within-family question is now CLOSED and a fresh mechanism-class REOPENED it.** First, we removed the last doubt: the earlier negatives had accidentally run a *decoupled* version of the rule (the credit signal wasn't actually reaching the neuron's output); with the coupling correctly on, and on the most-favorable redundant task, the burst-family rule *still* stays at chance on all 6 seeds — so that whole family (burst-multiplexed / feedback-alignment) is genuinely exhausted, not mis-wired. That launched a fresh-mechanism search, which found the winner: **Node Perturbation** — a rule that injects *nothing* backward at all (no feedback wiring, no "weight transport"), so it sidesteps the exact wall. Each hidden neuron simply correlates its own tiny random firing fluctuation with whether the global outcome got better or worse — an unbiased estimate of the right teaching signal, and exactly the kind of three-factor (noise × activity × global-reward) rule real neurons already run. **Off-brain it decisively works: it trains the network to near-oracle GENERALIZATION on genuinely held-out data, 6 of 6 seeds, and its credit does not fade as we stack 2, 3, or 4 hidden layers** (~0.93–0.95 vs the ~0.95–0.99 backprop ceiling, versus ~0.6 for random features) — with the built-in cheat-checks all passing (shuffle the global signal and it collapses to chance). *Precise scope (an adversarial self-correction):* on this off-brain task the rule it clearly beats is **vanilla feedback alignment**, which memorizes the training data but fails to generalize (~0.58 held-out); a within-family variant (burstprop) also cleared this off-brain task earlier, so the "whole burst family fails" result is the SEPARATE *on-spike* redundant-task test above. Node Perturbation's distinctive value is that it needs no backward wiring at all — the exact thing that was signal-to-noise-limited on spikes — so it is the CANDIDATE to succeed on the spiking substrate where the burst family could not; that on-spike win is not yet demonstrated (the first on-bridge attempt hit a separate spiking-readout-training bottleneck, next mechanism identified: the engine's own reward-modulated plasticity). The on-spike realization is being actively worked (no engine edit). This is the mission-critical lever, because rich long-range conversation comes from *deep, multi-layer learned* representations. Write-up: `research/findings/2026-07-13-fresh-deep-credit-class-NODE-PERTURBATION-seed42-GO-depth2-hidden-trains-to-near-oracle.md`. Separately banked this cycle: a validated **8-15× faster batched forward** (once a non-reproducibility bug in neuron-type assignment was found and fixed) so these experiments iterate quickly. *This is one candidate route — the dendritic one — and it is unproven on spikes; it is **not** on the critical path for the open-generation ladder below, which needs no deep learning rule.*

   **⭐ A large simplification of this frontier for LONG-RANGE language (2026-07-11/12).** We ran the long-range question — can the brain, using only learning rules real neurons can do (local, no backprop-through-time, no biologically-impossible "weight transport"), learn structure that depends on words far back? — all the way to ground. Two results reshape this item. First, an honest self-correction: a promising "breakthrough" was caught by an adversarial review to be a hidden learning-rate boost, not real long-range learning, and retracted before it was recorded. Second, and decisive: **long-range language is *not* bottlenecked on the deep recurrent learning rule at all.** A brain-like network with **fixed, random recurrent wiring** (a "reservoir" — cortex's recurrent connections are largely developmentally set) that learns *only its input representation* **beats full backprop-through-time** that trains everything (confirmed on 6 random seeds; training the recurrent wiring is actually counterproductive). So the hard part is learning what words *mean as they arrive*, on a fixed recurrent scaffold — a far more tractable, local problem than the general deep-credit rewrite. And the fully biology-legal version **works**: local input-learning (an eligibility-trace rule) plus **learned local feedback** (weight-mirror, replacing the impossible weight-transport) reaches **~78%** of the full-backprop reference — a positive result, multi-seed-tight, one-step-local, no weight transport, no engine edit. The residual to full backprop is a cleanly-characterized feedback-alignment cost, with a known next lever. This is being realized **on the actual spiking substrate right now** using the engine's already-committed dendritic burst-learning rule on a plastic input→reservoir pathway — *no engine edit required* (verified). ⇒ the deep-recurrent-credit rule is still the lever for the *other* ceilings (the learned binder, deep composition), but the long-range-language piece is redirected onto a tractable, on-substrate path. Write-ups: `research/findings/2026-07-11-R3-REFRAME-*.md` and `2026-07-12-spiking-realization-scoping-*.md`. **Newest (2026-07-13) — a complementary lever from a fresh mechanism-class gate:** the exhausted long-range ladder (reservoir-scale, e-prop, learned-input, dendritic-gain, all at boundaries) prompted an external search, which surfaced *spiking state-space models* (transformer-competitive on long-range benchmarks, dendritic-inspired). Their emergence-compatible extract: **structure the fixed recurrence, don't learn it** — a fixed *multi-timescale* reservoir (a range of neuronal time constants, the SSM/HiPPO forward long-range = biology's diverse dendritic/synaptic time constants) holds a cue *unboundedly* where the standard random reservoir — and a fast-only control — fade to chance (6-seed GO, anti-cheat-confirmed). This surpasses the fading-memory ceiling from the *forward structure alone*, no backprop-through-time. Honest scope: shown on a linear memory-horizon task. **The mission-relevant deep-context-*language* version was then run to ground — and it is a 6-seed NEGATIVE (blind-seed-clean): the multi-timescale RECURRENCE does NOT robustly beat a plain random ESN on real-text next-token prediction at any depth; at genuine long context (~45-token discourse) every multi-timescale reservoir goes *negative* while the plain ESN stays positive.** The memory-horizon GO was PURE RETENTION (decode a held cue) — and retention ≠ prediction: language needs the plain ESN's bounded nonlinear MIXING (n-gram conjunctions), which a linear multi-timescale diagonal lacks (its long-context state blurs toward the memoryless bag). This CONFIRMS the R3 reframe from a fresh angle — the long-range lever is the learned INPUT representation, not the recurrence structure. Write-ups: `research/findings/2026-07-13-SSM-fixed-structured-multitimescale-reservoir-SURPASSES-fading-memory-ceiling-6seed-GO.md` (retention GO) + `2026-07-13-SSM-multitimescale-RECURRENCE-does-NOT-help-language-6seed-NEGATIVE-retention-is-not-prediction.md` (language negative). ⇒ the lever stays the learned INPUT embedding (R3), being put on spikes via the committed BDSP rule on a plastic input→reservoir pathway — not fixed-reservoir structure tweaks. **Newest (2026-07-13, later) — the past-reservoir frontier RE-OPENED with a validated, transport-free, spiking-realizable mechanism.** A fresh mechanism-class research gate (read-only, then the source read in depth ourselves — which caught and corrected a summary error) named the precise gap the "multi-timescale recurrence doesn't help" negative pointed to: language needs *input-dependent MULTIPLICATIVE gating* (the input×state products = attention-like *conjunctions*) — NOT a fixed multi-timescale recurrence, and NOT the learned input embedding *alone*. And for a *diagonal* recurrence this gate is trainable by an EXACT forward-mode eligibility trace — **no backprop-through-time, no weight-transport** (the exact things that block biology). A four-rung ladder validated it: **(Rung 1)** adding explicit product (conjunction) features to a fixed reservoir's local read-out solves a task the linear read-out provably cannot — a *representational* limit, not under-resourcing (12 seeds; **adversarially verified 4/4 skeptics** — stronger controls, a 4× reservoir sweep, a non-adjacent task, a ridge read-out); **(Rung 2)** a per-neuron *selective* (input-dependent) diagonal state-space unit, trained by that eligibility trace, learns to hold a distant token and conjoin it with a recent one, beating a fixed reservoir + three controls (that learning the gate matters, that it must read the current token) on every seed; **(Rung 3)** on **real TinyStories text**, the selective unit beats the fixed reservoir *and* the memoryless bigram at deep context on **6/6 seeds** (large margins; a self-caught invalid-control fix hardened it); **(Rung 4a)** the input-modulated leak SURVIVES a **biological conductance-shunt** realization (an input-driven shunting conductance = the input-modulated membrane time constant — how a real neuron does it), so it needs no new engine mechanism (the bridge's conductance synapses already provide it). ⇒ a validated, local, transport-free, spiking-realizable long-range learner — the honest path *past* the reservoir bound that avoids the exhausted deep-credit wall. **And it is now ON the actual spiking substrate (Rung 4b):** a first on-bridge attempt (using a neuron's raw membrane as the state) was an honest negative — the membrane leaks too fast and swamps the input-driven shunt — which mapped the exact fix: a SLOW graded state whose leak is set by the shunt. That is a faithful biological mechanism (a dendritic-plateau / calcium graded integrator with a shunting-modulated membrane time constant), so it was added as ONE minimal, additive, default-off, byte-identical-when-off engine mechanism (`enable_selective_ssm_state`; verified: with it off, existing tests are unchanged). On the bridge it realizes the hold/release the raw membrane could not (6-seed), and — the decisive check — the on-bridge state is **byte-equivalent to the validated numpy version to float-precision** (6-seed), so the whole ladder (beats-fixed-reservoir, beats-bigram-on-real-text) is now a *spiking-substrate* result, not just a numpy one. And the LEARNING now runs on the substrate too: the full gate-plus-readout training loop, driven through the on-brain state each step, **reproduces the numpy result exactly** (6-seed; the learned selective gate beats the fixed reservoir, mean identical to the off-brain version) — so the transport-free long-range learner both *runs and learns* on the spiking brain, with no backprop-through-time and no weight-transport. Remaining refinements to full autonomy (each bounded): making the gate's eligibility trace an on-brain local synaptic quantity (it currently reads the on-brain state but is host-computed), a synaptic input→shunt pathway, and the spiking read-out; then the mission-central step — scaling it and coupling it into the emergent generator. **And the scale question now has a positive answer:** sweeping the vocabulary on real text (V=200→400→600), the learned selective gate's deep-context advantage over BOTH the fixed reservoir AND the bigram GROWS monotonically (its edge over the bigram nearly triples) — at the largest scale the bigram has caught up to the fixed reservoir (the n-gram ceiling) while the selective learner pulls decisively ahead of both. So the long-range signal it exploits is real and *grows* with the richness of the language — the opposite of the scale-confound the earlier ceiling run warned about, and exactly the trajectory a path-to-fluency needs. Write-ups: `research/findings/2026-07-13-PAST-RESERVOIR-{conjunction-readout,RUNG2,RUNG3,RUNG4a,RUNG4b-i,RUNG4b-ii,RUNG4b-iii-a}-*.md` + the Zucchet source-read + the adversarial-verify. **Newest (2026-07-13, latest) — the mechanism is now COUPLED into the emergent generator, fully on spikes, and its scale answer is decisive on every axis.** The validated selective long-range learner was coupled into the actual emergent language generator (a fixed/echo-state or self-taught recurrent "reservoir" + a learned read-out): adding the learned selective gate to the generator's read-out robustly lowers its deep-context error on 6/6 seeds — as a frozen add-on, co-trained with the generator, AND realized **fully on the spiking brain end-to-end** (a recurrent Izhikevich reservoir + the on-brain selective state + the winner read out through a spiking winner-take-all, all on ONE brain, 6/6 seeds). A **mandatory adversarial review (3 independent skeptics) caught 3 real defects I'd otherwise have recorded** — a subtle "weight-transport" shortcut in how the gate learned, plus two over-claims about how much of the benefit was genuinely long-range vs. a shallow read-out fix — and all three were fixed (the gate re-done with biologically-legal *random feedback*; the honest scope recorded). The corrected, transport-free result then held on every axis: it is **transport-free end-to-end** (no backprop-through-time, no weight-transport, verified by re-running the load-bearing real-text rung with random feedback). A scale probe (trained selective over a *fixed* reservoir, measured against a memoryless bag-of-words baseline) robustly showed a trained selective channel beats that baseline on 6/6 seeds and that the *learned gate* — not extra read-out parameters — is what does it (a fixed version of the same gate hurts). **A mandatory adversarial review then retracted this probe's grander framings** (an honest correction, the discipline working): its "carries *past* the reservoir's n-gram wall" claim was overclaimed (the fixed reservoir I used was under-sized — actually *worse* than a bigram at the smallest vocabulary — so its advantage collapsing was under-capacity, not the real wall), its "deep/long-range" claim rested on a metric bug I made (the baseline cancels out of the per-depth number, so it measured "beats the reservoir," not "beats the memoryless baseline deep"), and its "grows with vocabulary" was confounded by the ordered-bigram baseline being data-starved at larger vocabularies. **The genuinely-long-range result is the earlier real-text rung (Rung 3): the selective unit beats the fixed reservoir *and the ordered bigram* at deep context on 6/6 seeds, with the proper controls — that stands, as does the coupling into the generator (both separately adversarially verified).** So the honest status is: the long-range learner is coupled into the generator, on spikes, transport-free, adversarially verified; the "beats a bigram at deep context on real text" claim holds with controls. **And the airtight controls the review demanded were then built and run (6 seeds): over a fixed reservoir the trained selective genuinely holds distal context at the deepest context positions — it beats the *ordered* bigram there, beats a "no-holding" control (so the accumulation is doing work), and beats a "gate reads a random word instead of the current word" control (so it is genuinely *input-selective* holding, not just extra capacity), all 6/6, and that selective-specific benefit *grows* toward the deepest positions rather than fading (the opposite of a shallow shortcut).** The honest caveat kept front-and-centre: the *overall* model at this tractable scale is still only bigram-level (the many shallow positions dominate the average) — so this is a demonstrated deep-tail *mechanism* result, not yet an overall-fluency result. **The overnight runs (all cores the owner freed) resolved the remaining questions cleanly:** (a) the deep-tail holding SURVIVES a properly-sized reservoir (the selective-specific control stays clearly positive at 6/6 over a strong np=500 reservoir, not just the small one) — with a clean monotone gradient (the selective helps *more* the *less* the reservoir already holds: strong→weak reservoir gives ~0→large, which reconciles an earlier "~0 at depth" result over a fully-trained reservoir); (b) it grows with *both* richer vocabulary and more data; but (c) with a properly-sampled bigram (the honest baseline at more data), the *overall* model stays bigram-level and more data at small vocabulary does NOT change that — so the earlier "beats the bigram overall" was the bigram being under-sampled at large vocabulary, an artefact the clean test removed. ⇒ the honest, adversarially-survived deliverable is the **deep-tail long-range *mechanism*** (genuine, robust, clean); making it **overall fluency** requires the deep-context signal to dominate a larger fraction of tokens — i.e. real conversation scale (larger vocabulary / richer language, the documented ~23.7M-word regime), which is a compute *scale lever* (a multi-day run — and, honestly, this reservoir is small enough that the GPU is no faster than the CPU cores here, so it is core-parallelism-bound, not GPU-bound). This is the mission-central "long-range learner in the talking brain," delivered on spikes, adversarially verified, and honestly bounded. This is the mission-central "put the long-range learner into the talking brain" step, delivered on spikes and honestly scoped. Write-ups: `research/findings/2026-07-13-PAST-RESERVOIR-{COUPLE-selssm-into-eprop-generator,JOINT-selssm,ONBRIDGE-couple,ONBRIDGE-spiking-readout,Rung3-transport-free,trained-selssm-over-fixed-reservoir}-*.md` (the last carries the adversarial-verify CORRECTION block). **Open (bounded):** the airtight per-depth no-hold/random-gate controls vs the ordered bigram; the full validated-scale run (larger vocabulary/data on GPU via the new vectorized trainer, on a *properly-sized* reservoir); the gate's eligibility trace as an on-brain local synaptic quantity; a synaptic input→shunt pathway. **Newest (2026-07-14/15) — a complementary read-out lever, and a clean confirmation of the scale scoping.** A separate arm asked whether the reservoir's "bigram-level" ceiling is the *recurrence* or the *read-out*: a **two-stage (deeper) read-out** — biologically just the multi-layer cortical output projection, still local / no-backprop-through-time over the fixed reservoir's cached features — was compared to the usual single-layer read-out. On a *structured* (templated) language stream it **decisively beats the bigram** where the single-layer read-out cannot (0.8 nats better, 6 seeds, both rigged-control checks collapse) — so the reservoir genuinely holds higher-order structure a shallow read-out can't extract; the read-out *depth* was a real bottleneck for *exploiting existing structure*. But on **natural text (WikiText) at this tractable scale it is bigram-level** (6 seeds, mixed ±0.05) — which independently **confirms the honest scale scoping above**: natural language at few-million-token scale is bigram-dominated (the higher-order signal is too thin for even a good model to beat a well-sampled bigram), so overall fluency is gated by *scale/data* and the *learned long-range representation*, not read-out depth. This arm also mapped, and adversarially self-corrected, where a biological local rule can and cannot learn recurrent computation (it can learn to hold and to integrate a cue; a nonlinear *combination* of two cues is a read-out-depth limit, not a recurrent-credit limit — a self-caught reframe that avoided building the wrong mechanism). Write-ups: `research/findings/2026-07-14-language-cortex-2stage-readout-beats-bigram-GO.md`, `2026-07-14-controlled-lag-eprop-copy-task-artifact-recurrent-computation-needed.md`. **Newest (2026-07-15) — the whole "why isn't it fluent yet, and where's the real wall?" question was run to ground (two research gates + a decisive test), and the answer reshapes this frontier into a clear, hopeful diagnosis.** Three results: **(a) the fluency wall at our scale is DATA/SCALE, not the brain.** A definitive fair-baseline check (a properly-tuned bigram *and* an interpolated trigram, broken out by context-depth) showed the reservoir language model loses to a simple trigram *at every depth including the deepest* — and, critically, the project's own ceiling run showed *even a full transformer loses to a bigram too* at these few-million-token scales (⚠️ **CORRECTED then SETTLED 2026-07-16 — "and a full-backprop LSTM" was ASSERTED with NO measurement behind it (no LSTM had ever been run at 5M-tok/V=300). **It has now been RUN and it CONFIRMS the claim: 3 seeds, 0/3 beat the add-1 bigram, deep-margin mean −1.415 — the LSTM loses at EVERY depth, monotonically worse with depth, the transformer's exact signature.** So the exculpation holds for RECURRENCE too. The only three LSTM runs are at 23.7M/60M tokens, where it BEATS the bigram at every depth with a margin that GROWS with context (+0.49→+1.81). So the "every model class, including recurrence, is n-gram-bound at our scale" exculpation is UNPROVEN for recurrence — the one class the spiking substrate actually is. Note the LSTM's wins are AT the ~23.7M regime this roadmap already calls the threshold, so the DATA thesis is supported, not inverted; the cheap settle is `_recurrent_lm_ceiling.py --vocab 300 --max-tokens 5000000`. See `research/findings/2026-07-16-anchor-claim-audit-10-defects-in-the-record-incl-my-own-correction.md`**). So "our model is only n-gram-level" is a property of the tiny-data regime shared by *all* model classes, not a brain-design fault — **five separate "beats the bigram" claims this cycle were caught (by checking our own record + a fair-baseline probe) as comparisons against a *starved* baseline and corrected.** **(b) The one thing a word-predictor structurally *cannot* do — and a real brain can — is compose brand-new combinations** ("a purple elephant" it has never seen). A clean, scale-confound-free test (held-out combinations have *zero* surface count, so any n-gram fails by construction) showed this **systematicity comes from a fixed multiplicative *binding* operation over well-separated concept codes — not from training a bigger learner harder.** A general learner (even a perfect-math backprop oracle, in our record) *memorizes the training set and fails* to generalize to new combinations; the fixed binding *succeeds* (held-out 0.87 vs the learner's 0.39, 6 seeds; a learned *bilinear* binder also succeeds on decorrelated codes — the discriminator is a binding *structure* over well-separated codes vs a from-scratch classifier). **(c) The brain we've already built already has this and already uses it** — it composes never-seen sentences across 320 concepts, on the spiking neurons (validated + deployed). ⇒ the honest path to fluent conversation is **not a cleverer learner but teaching the brain to *use* its existing composing-machinery over more learned knowledge (more training, richer data)** — exactly the learn-from-experience arcs above. **And that composing-machinery is now realized on the real spiking substrate (2026-07-15) — validated the honest way.** A fixed spiking coincidence-bind (on a `SimulationBridge`) plus a read-out over it learned by a biologically-legal rule (feedback alignment, no "weight-transport") extrapolate to brand-new combinations a from-scratch learner cannot. An adversarial review first caught that the initial claim leaned on a *weak* baseline — held to the stronger controls (a nearest-neighbour memorizer + a linear read-out) the small-task version cleared only 1–3 of 6 seeds — and a scale-hardening then *vindicated* the mechanism: at a larger task (144 combinations, ~43 held out), held to those same full controls, it clears them on 5/6 and 6/6 seeds. So the composing-machinery is on spikes, a learned read-out over a fixed biological binding primitive, honestly validated against the full control set (the adversarial-verify → correct → vindicate cycle is the discipline working). Write-ups: `research/findings/2026-07-15-onsubstrate-{coincidence-bind-systematicity-RUNG1,bind-learned-readout-RUNG2,bind-onbridge-bdsp-readout-RUNG3,systematicity-SCALE-HARDENING}-*.md`. The deep-recurrent-credit rule stays the lever for the *learned-representation* half (learning better concept codes from a stream), not for composition itself. Also shipped this cycle: the "how should I answer?" router now routes by **meaning** (a novel synonym like "versus"/"lineage" is placed correctly), replacing a rigid keyword list — deployed into the flagship console (default-off, byte-identical when off) and hardened by a 17-agent adversarial audit (2 opt-in bugs caught + fixed). Write-ups: `research/findings/2026-07-15-TEST-A-fixed-bind-cracks-systematic-extrapolation-where-every-learner-fails-6seed-GO.md`, `2026-07-15-beyond-ngram-wall-research-gate-*.md`, `2026-07-15-fluency-crossover-RESOLVED-NEGATIVE-*.md`, `2026-07-15-open-vocab-discourse-router-DEPLOYED-*.md`.
2. **Open-ended fluent generation without the conventional-AI crutch.** The home-grown replacement's **first five rungs now work** (each on-brain, no-backpropagation, over a small controlled vocabulary, multi-seed): **(1)** an emergent next-word model that genuinely beats the standard baselines; **(2)** conditioning on a distant discourse referent held in a working-memory latch — it carries a topic ~32 clauses back that the reservoir alone forgets to chance; **(3)** generalizing to a *never-produced* subject via a shared category code (a novel subject gets the right category-specific continuation; a one-hot code or a memoryless network collapses); **(4)** *order-decisive* recombination — the same words in different orders yield different continuations (role reversal: "X meets Y" vs "Y meets X"), which no order-blind model can do; achieved by reading the reservoir's own state trajectory (order is load-bearing; an adversarial review caught and rejected a first version that had smuggled the answer in through a hand-coded lookup, and it was rebuilt with no such shortcut). Honest caveats: rung 3 is a positive-with-residuals (the mechanism beats every control on all seeds; an emit-rate calibration and finite-size seed noise are characterized); rung 4 is a positive — order-decisiveness works and (after a same-day correction) the reservoir genuinely carries the grammatical role across the clause (an adversarial review first caught and rejected a version that had smuggled the answer through a hand-coded lookup; the rebuild reads only the network's own states). **The emergence bar is now met for the category structure the rungs depend on:** rather than a hand-coded category label, the category is *discovered from experience* — validated three ways (a fixed random projection of a feature partition; corpus co-occurrence, which is scale-limited on the toy; and, the master-directive close, from **real visual perception** — the animals are *seen* through the Gabor/V1 front end and their category emerges from visual similarity, with a pixel-scramble control that collapses it on every seed). The remaining given is the world's structure (same-category things look alike), which is legitimate; the residual structures still to self-organize are the function-word/action codes (a mapped follow-on). **(5)** the generator now **spells its predicted word out of neural spikes** — each predicted token is decoded from the language-output neurons' firing (multi-seed; cutting that read-out pathway collapses the decode, proving it is genuinely spiking, not a hidden table lookup) — over a bounded ~16-word vocabulary for now. So the honest remaining work is scaling that spelling to an *open* vocabulary (more read-out banks — bounded engineering, not a new mechanism) and **rung 6**, multi-sentence discourse, where a first result now stands: the generator's *own* reservoir state carries the discourse referent (which earlier subject a later "it" refers to) across a distractor clause — multi-seed, decisively beating a memoryless baseline and a broken-link control, with no hand-wired discourse module (coherence *emerges* in the learned generator rather than being scripted by the composer scaffold). It **fades with distance** exactly as a fixed reservoir must — and that fade is now overcome: the generator reads each clause, a learned read cleans its (correlated) code into a referent slot, and a discrete attractor (already validated on spikes) holds the referent to *held-out-deeper* narratives where the reservoir alone falls to chance (multi-seed, perfect tracking; a first attempt with an untrained read failed, was root-caused to code correlation, and the learned read fixed it — the same decorrelation lever that recurs across the project). And that known-referents limit is now lifted too: a content-agnostic **Hebbian fast-weight** (a one-shot "bind a fresh slot to whatever entity just arrived, retrieve it on re-mention" — the brain's on-the-fly variable binding) tracks entities that are **invented at test time and never seen during training** — multi-seed, with the binding itself adding *zero* error and collapsing when the entity codes are made identical or the binding is severed. So the generator can follow *novel* referents, not just a fixed cast. Honest remaining frontier: the fully-spiking realization of that fast-weight (as short-term-plasticity synaptic memory), the attractor's own accuracy over very deep narratives, and the generator's overall scale ceiling (which routes to the learned-recurrence lever above). Fully model-free open-domain fluency is still a field-wide wall; the scale question for the generator itself was re-confirmed this cycle (a fixed reservoir is n-gram-bounded until ~16–65k units / 100M words — the ceiling was already characterized, and a confound-free re-run vs a memoryless baseline agreed), so the generator's path past that ceiling routes back to the learned-recurrence lever above, not more reservoir scaling. Fully model-free open-domain fluency is a field-wide wall even for LLMs (they lean on enormous scale and retrieval); our honest interim is to shrink the crutch and keep it fluency-only, behind the guard.
3. **Nesting deeper than ~3** — this is the human-faithful bounded limit, so it's a feature, not a bug; the only open build is the spiking version of the slot buffer.
4. **Consolidating composed/relational memories to cortex** — currently stuck in the hippocampus; the sequence-completing-CA3 path is the next lever.
5. **Choosing among several remembered referents for a bare pronoun** — the mechanism (a winner-take-all competition) is specified; a bounded build when multi-person dialogue is prioritized.
6. **The fully-spiking version of recalling a fact about a newly-seen object** — the hybrid works; the all-spiking version needs the learned binder.
7. **Genuinely open-world reasoning and open-domain chit-chat** — unsolved by anyone unconstrained; managed here (as LLMs do) by staying grounded, on-topic, and willing to abstain. This is the honest scope, not a near-term wall.

**Breadth items** (each a scoped build when a downstream need calls for it, not a fundamental limit): a deeper visual hierarchy and a separate location stream; hearing/smell/touch; the explicit value critic; a fear/aversion system; a theta-rhythm pacemaker; the finer sleep-stage generators; the global action-cancel pathway; the non-movement selection loops; grid cells and path integration.

---

## Appendix — how this roadmap is maintained

Kept current as a standing part of the workflow: whenever an experiment lands a result, surpasses a boundary, or removes a stand-in, the relevant stage above is updated in the same work cycle (status badge, the done/open notes, the next step, the citation). Periodically a deeper re-sync reads the source material in depth — the biological-mechanism catalog, the *Principles of Neural Science* textbook, and the experiment write-ups in `research/findings/` — to re-verify the biology map and the honest frontier. This file, not any single experiment write-up, is the intended at-a-glance source of truth for tracking progress toward the goal.

---

## 10. The months-scale plan (the forward-looking sequence)

This roadmap is the STATUS surface. The forward-looking **plan** — the two-gap framing (unification + scale/data), the ordered shortcut burn-down with "unblocks", the unification critical path (★ next edge = unite the content-store + RUNG6d STP binder), the three decision gates (invest-day+-wallclock / convert-generator / spend-cloud, with the 2026-07-15 broadened compute policy), and the honest distance (one-brain-all-spiking ~2-4 months; bounded grounded-conversation milestone ~3-5 months; open-ended fluency = a field-wide wall) — lives in **`docs/plans/2026-07-15-months-scale-plan-to-one-brain-and-small-llm-conversation.md`**. Synthesized 2026-07-15 from a 4-lens strategic audit; supersedes the stale `2026-06-08-brain-fidelity-roadmap.md` + `2026-06-23-inventory-burndown-roadmap.md`.

**Status against that plan's LONGEST POLE (updated 2026-07-16) — segment (a) CLOSED: 🟩.** The plan's line 46 names the
longest pole: *"co-training the learning pieces (stream cortex + deep-credit + long-range learner) **without cross-talk**
at scale — the plasticity-isolation gates are validated but simultaneous stream-cortex co-training is unshown."*

- **🟩 Segment (a) — two ACTIVELY-LEARNING stream cortices co-training on ONE bridge: DONE, and the cost is now
  EXPLAINED.** Clean post-budget-fix 6-seed table: **93.2% of separate-bridge fidelity retained** (min 86.5%), **5/6 GO**
  (seed 43 misses the strict −0.08 gate), overlapped-region positive control degrades **6/6** (so the metric genuinely
  detects collision). This RETIRES a confounded banked table (it had given each co-trained learner half its baseline's
  reinforcement) — and the headline **survived** the fix. **The decomposition is the real content: ~97% of the cost is
  TIME-SHARING** (each learner's neurons decay with zero input between its own windows — a constitutive property of
  sharing a step loop) **and only ~3% is interference** (sign-varying). **⇒ the cost is a FIXED PRICE PER ADDED LEARNER,
  not compounding interference — co-training SCALES.** Honest scope: both learners use the SAME rule (global
  rate-Hebbian) and DISJOINT regions, so this tests spatial isolation only. `research/findings/2026-07-16-cotraining-cost-is-TIME-SHARING-not-interference-clean-6seed-postbudgetfix.md`
- **⬜ Segment (b) — the HARDER half: RULE HETEROGENEITY** (stream cortex + the deep-credit learner). Segment (a) holds
  the rule constant, so *"do two DIFFERENT learning rules interfere when sharing one substrate?"* is entirely untested.
  Scoped: the flag conflict is **not** the blocker (Hebbian is gate-isolable via `cp_plasticity_rate_gain`; homeostasis is
  per-region isolable). The genuine blockers: **STP + structural plasticity are GLOBAL with no per-pathway escape — and
  the stream cortex's validated +0.705 was obtained with BOTH ON**, so it needs re-validating with them off; and
  `inject_explicit_wiring` REPLACES all connectivity, so the two wirings must merge into ONE region-framework plan.
- **🟧 GATING (b): the deep-credit GO is being RE-VERIFIED.** Use the deep-credit learner in its **GO regime = e-prop +
  POPULATION CODING** — *not* bare on-bridge BDSP, which is a decisive 6-seed at-or-below-chance NEGATIVE (the plan's §3
  row 1 said "the committed feedforward BDSP rule"; corrected in place 2026-07-16). The GO's **magnitude** is under
  re-check: the port keeps `enable_bdsp=True` and relies on `lr=0` for inertness, but `fused_bdsp_update` ends in an
  UNCONDITIONAL `cp.clip` (`sim/kernels.py:485`) — at `eta=0` the add term vanishes, the clip does not — so with
  `ff_w_init=2000` vs the inherited `bdsp_w_max=6.0` it silently crushes the FF synapses whose presyn fired (measured
  239/512, mean |w| 370 → ≤6) on **every forward**. A single-variable A/B (clip widened, block still running) is in
  flight; seed 42 reproduces **inherit-heldout 0.852** vs the banked 0.877, so the result itself looks real.
- **🐛 Engine fix this cycle (affects work well beyond this arc):** the Hebbian weight-decay used the RAW
  `cp_plasticity_rate_gain`, so any bridge with **both a `plasticity_gate` and structural growth** (on by default) hit
  `operands could not be broadcast` every step — **silently caught** — and **stopped decaying weights entirely**. Known
  bug class (`_ensure_gate_capacity`, added 2026-06-08, already guards 7 other sites); the Hebbian block was missed.
  The merged nav+conversational work uses plasticity gates heavily. Fixed byte-identically (46/46).

