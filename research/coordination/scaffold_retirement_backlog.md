# SCAFFOLD-RETIREMENT BACKLOG — the owner's #1 arc, mapped (2026-09-05)

Produced by the ultracode scaffold-shortcut-map workflow `w9sn9wn4b` (6 subsystem auditors + synthesis; full
output in the workflow transcript). **58 distinct host shortcuts → 24 ranked backlog items.** This is the durable
sequenced plan for retiring host shortcuts → emergent + on the shared ONE-brain (the brain-based-only standard).

## THE CORE INSIGHT
On the DEPLOYED brain the live conversation is **already NEURAL in its DECISIONS almost everywhere** — every
faculty's own lesion proves its threshold/ignition/WTA/veto verdict is spiking + load-bearing. The host residual is
at the **two ENDS of nearly every turn**: (a) the **INPUTS** that reach those decisions (which stored fact matches
a cue; which words are affect-bearing; how novel/salient/rewarding a turn is; which tokens/turn-class route where),
and (b) the **OUTPUT words** the user reads (recall prose + no-confab moat on Qwen-0.5B/templates; affect/discourse
framing on f-strings). **The shortest path to the most-neural conversation is mostly CONFIG-FLIP + re-verify, not
new research**, and it matches the owner's mouth-first arc.

## THE SHORTEST-PATH CHAIN (highest leverage; ranks 1-5)
1. **Rebuild the deployed `scale787` bundle OFF `composer_kind='rf'` ONTO `onebrain`** — MAXIMAL load-bearing (the
   live composer for every recall/store/abstain), NEAR-ZERO risk. The bundle is pinned to pure host FHRR, so even
   the spiking unbind + NEF/Izhikevich cleanup that `OneBrainComposer` enables BY DEFAULT are NOT running on any
   live recall. Path A = flip `local_reciprocal_unbind`+`enable_spiking_cleanup` (already onebrain defaults) +
   rebuild the bundle. **The single largest, cheapest reclaim.**
2. **Thread `integrated_loop=True`** (webapp/server.py → brain_chat_tui → OneBrainComposer) — the spiking K-way
   cue-match SELECTION (GO 4/4 at V=320) replaces the host string-`==` for-loop under ~15 faculties. Config-flip +
   re-verify at production vocab.
3. **Promote `BRAIN_TOUCHPOINT_A_FACT_CLAUSE`** (the Stage-2 GO landed this session) — the recall ANSWER words are
   neurally produced (EMERGE-59/61 SpikingClauseProducer) instead of Qwen. Needs the full n=4 battery + a
   punctuation/capitalization fix + a spiking moat clause-decomposition.
4. **Wire ONE shared spiking novelty/salience/value afferent to the live turn** — retires 3 default-on rows
   (da-mode-drives-response, da-gated-encoding, da-gated-curiosity) + the ONLY live motor decision (SPEAK/STAY) +
   value-choice in one move. Both halves de-risked; this is INTEGRATION, not a new mechanism.
5. **Route Gate-B appraisal through the board-#84 interoceptive CURRENT afferent** (already 6/6-validated, running
   in production for the same substrate) instead of the direct host `set_concentration` write — **the readiest
   high-load-bearing win** (an adaptation, not a fresh mechanism).

## THE FULL 24-ITEM RANKED LIST (abridged; load-bearing · readiness)
1 composer bundle pinned to rf (MAXIMAL · config-flip+rebuild) · 2 host cue-match for-loop (MAXIMAL · config-flip) ·
3 recall words on Qwen/Touchpoint-A (MAXIMAL-minus · wired, hardening) · 4 no spiking salience/novelty/reward
afferent (HIGH-broad · fresh wiring) · 5 Gate-B appraisal host set_concentration (HIGH · READY) · 6 78k-fact core
written by closed-form host (HIGH-provenance/behavior-neutral · fresh research) · 7 Warriner affect lexicon+fixed
threshold (HIGH · partial: value-half GO, gate-half failed) · 8 GNW congruence host string-id (HIGH · fresh) ·
9 metacog confidence host formula (HIGH · fresh) · 10 curiosity novelty binary host constant (HIGH · de-risked
start) · 11 topic-swap regex+host (HIGH · fresh, self-referential) · 12 GNW STOP host boolean-OR (HIGH · fresh) ·
13 QuestionRouter self/identity+anaphora host (MED-HIGH · DE-RISKED 2026-09-05, both flags default-OFF, 6/6-seed
GO on plain gate() AND the installed GNW-bus gate() — see `2026-09-05-rank13-selfid-anaphora-scaffold-derisk-GO-6of6.md`;
PRODUCTION-FLIP attempted TWICE 2026-09-05, still NO-GO both times, flags STAY default-OFF — a seed=43 SELFID
over-fire (missing `v=="isa"` guard) is fixed; the seed=44 regression is a discourse-WM referent-misidentification
with no narrow fix, banked as the next lever — see `2026-09-05-rank13-selfid-anaphora-production-flip-CORRECTED-DIAGNOSIS.md`;
residual is now flag-scoped not architecture-scoped) · 14 NL question-routing host comprehension
(MED-HIGH · fresh) · 15 self-schema authorship host (LOW-actual/nominal-on · fresh) · 16 DA→write-magnitude host
linear (MED · partial: homeostasis-half retired) · 17 pronoun detect/substitute host (MED · fresh) · 18 cross-turn
persistence host state (MED · architecture change) · 19 per-turn fact WRITE host closed-form (MED · SlotBinder
candidate exists) · 20 value-choice reward-context host recency (MED-narrow · low-risk) · 21 verdict→English
template bank (OWNER-SANCTIONED Broca scaffold · deprioritized) · 22 curiosity multi-concept SELECTOR host dict
(LOW/dormant · partial) · 23 vision cluster host (LOW/no live consumer · mixed) · 24 off-path/dormant/doc tail
(LOW · flip-ready to fresh).

## RANK 7 UPDATE (2026-09-05) — gate-half attempted a SECOND time, still failed, now with a sharper diagnosis
A second, mechanistically-independent gate-retirement attempt (arousal co-gate, habituation/frequency
exclusion, cross-resample stability, neighbor affect-purity, plus the strongest 2-way combination — none a
re-threshold of the refuted learned-magnitude lever) was calibrated + validated jointly across the full corpus
and 6 bootstrap resamples. Best achieved: 29.4% worst-case recall at zero false positives (below the 50%
usability bar) — see `research/findings/2026-09-05-affect-learned-gate-retry-register-confound-BOUNDARY.md`.
Diagnosis sharpened from "unseparable by any threshold" to a REGISTER confound: every candidate reads the SAME
co-occurrence graph, and TinyStories' narrative voice frames ordinary actions/settings inside emotionally-
resolved scenes about as consistently as it uses real emotion words, so no statistic of that graph separates
them. Removed from "ready to build now" below (two independent misses is not a quick win); the standing
surpass is the fully-spiking experience-bound opponent V+/V- appraisal population (D1's own named next rung),
not a further statistic of the text co-occurrence graph. Production `_STRONG_MARGIN` threshold UNCHANGED.

## READY TO BUILD NOW (build-ahead wave-2 fodder) — RE-AUDITED 2026-09-05 (hygiene pass; see STATUS UPDATES below for citations)
⛔ **Rank-5 (appraisal-via-interocept) and Rank-4 (shared-afferent wiring) are DONE, not ready-to-build** — both
flipped to PRODUCTION DEFAULT-ON later the same session (verified in the current checkout). ⛔ **The "d5
depth_hold direct-read" example under Rank-24 is NO-GO/falsified** — do not re-attempt it; Rank-24's SC-flag
half is the part that actually flipped GO. ⛔ **"Rank-20 worldmodel-state WTA" was a mislabel** — rank-20 in the
ranked list above is value-choice reward-context, not a worldmodel-state WTA (no finding or ranked item matches
that name); rank-20 is now de-risked GO, default-OFF, not wired.

Still genuinely ready: **Rank-1** bundle-rebuild de-risk (the literal rebuild is confirmed still NOT done —
`composer_kind` defaults to `'rf'` in `brain_conversational_agent.py` and `webapp/server.py`'s own comment still
describes the scale787 bundle's manifest as `'rf'` — but the retrieval-latency sub-blocker to doing it is now
resolved) · **Rank-2** integrated_loop re-verify AT SCALE (the plumbing thread is GO/wired-default-off; the
V=320 answer-preservation re-verify came back UNDEFINED, and the fix it named — the DG-CA3 sharded composer
wire-in — has since landed, so the re-verify is now the genuinely-ready rung) · **Rank-3** Touchpoint-A
hardening (full battery + punctuation fix; unchanged, no new finding) · **Rank-23** vision compose (dormant,
idle-compute; now has a 6-seed derisk GO, still no live consumer) · **Rank-15** self-schema neural-turn-class
(now PARTIAL 5/6, residual characterized — not a blank start).

## ALREADY IN FLIGHT / DONE (this session)
flip (linattn mouth) · recall-gate→LTM · affect→neural (WKV) · Stage-1 Qwen-fallback retire · composer L2/L3
(SlotBinder) · LC-NE eviction · num/den (satdiv/Tier-1) · Touchpoint-A Stage-2 (structural GO) · Rank-13
QuestionRouter self/identity+anaphora DE-RISKED (both flags default-OFF; 6/6-seed GO, see the finding). ⚠️ Several had
HEADLINE overclaims corrected by the adversarial-verify (`w3qhweujd`) — see the anchor; the flip "clean default" is
being re-verified.

## STATUS UPDATES (per-rank, append-only — keep terse; do not rewrite the ranked list above)
- **Rank 6 (78k-fact knowledge-core WRITE, closed-form host) — CHARACTERIZED (mixed), 2026-09-05.** Premise
  confirmed WITH CORRECTION: the per-fact VSA bind is already genuinely neural (RF resonate-and-fire, `fast:
  false` in `curation_report.json`, confirmed for both the shipped 15k default and the ~78,857-fact
  `wikidata_100k` bundle) — the real host residual is DATA PERSISTENCE (`sharded_phasor_store.py` forces
  `enable_substrate_store=False` at LTM scale) plus CURATION SELECTION (unaddressed, named as the deeper rung).
  The already-validated synaptic-weight write (`enable_substrate_store=True`, Phase-2 GO 2026-06-05) is
  re-confirmed **GO 6/6 seeds** for answer+moat parity against the numpy-kb path, now over 240 REAL curated
  wikidata facts (not synthetic). MEASURED (not estimated) marginal memory cost: 50.26 KB/fact, projecting to
  **3.78 GB at the full 78,857-fact core — affordable, not a blocker**. STRUCTURAL GAP found and confirmed
  empirically: `ShardedPhasorStore.save()` FAILS (`TypeError: cannot pickle 'mappingproxy'`) under
  `enable_substrate_store=True` — the concrete, previously-undocumented reason the LTM excludes this path.
  Literature check (Crawford-Gingerich-Eliasmith 2016) shows the per-fact-cleanup-population ARCHITECTURE
  already holds 117,659 facts in spiking neurons (more than our target) at ~1/6 the neurons/fact our generic
  `SimulationBridge`-per-fact implementation spends — the residual is engineering efficiency, not architecture.
  Also confirmed: `TieredFactStore.query_patient_source()` already exposes a genuine buffer-vs-ltm
  tier-of-origin signal in production, UNCONSUMED by the shipped provenance-honesty framing (#129/#140) — the
  cheapest "honest, provenance-neutral" next rung is wiring that EXISTING signal, not a new mechanism. Query-
  time cost (loses the batched scan under `enable_substrate_store`) confirmed by code-reading, not separately
  benchmarked (this run's own wall-clock timings were contention-inflated 2.7x-5x by concurrent box load,
  cross-checked against the bundle's own uncontended build record — flagged explicitly, not papered over). See
  `research/findings/2026-09-05-rank6-knowledge-core-substrate-write-scaled-derisk-mixed.md`. Default-off; the
  production knowledge-core WRITE path is unchanged.
  **UPDATE 2026-09-05 (later): the `ShardedPhasorStore.save()` pickle bug is FIXED (`fede9d596`).** `save()` now
  reads the composite back via `_retrieve_substrate` before numpy (and `load()` rebuilds via `_store_substrate`),
  so the GO'd `enable_substrate_store=True` write path is save/load-clean END-TO-END at real 78,857-fact scale
  (smoke: save_ok/load_ok/answers_match all true). No `sim/` edit; production default still OFF. The two remaining
  rank-6 residuals are unchanged: query-time batching loss + per-fact `SimulationBridge` footprint vs a lean
  Crawford-Eliasmith-sized cleanup population. See `2026-09-05-rank6-shardedphasorstore-pickle-fix-GO.md`.
- **Rank 12 (GNW STOP host boolean-OR) — DE-RISKED + WIRED DEFAULT-OFF, 2026-09-05.** An ACC/BG hyperdirect circuit
  (two afferent relay pools → ACC → STN → GPi, reusing the STN-veto de-risk's chain shape) reads the SAME
  `n_ignited` / `mm_peak` afferents `detect_trigger`'s boolean-OR read, directly as synaptic input, and decides the
  STOP trigger via spiking integration (GPi rate crossing a fixed threshold) instead of a host comparison. 6/6-seed
  GO on parity with the host decision (18/18 turn-classes), both afferents independently load-bearing, full
  afferent-lesion reverts to no-stop on all 6 seeds; a second 6/6-seed check confirms the production dispatch
  (`webapp/gnw_global_stop.detect_trigger` behind `BRAIN_GNW_STOP_TRIGGER_SPIKING`, default-off) is byte-identical
  when off. See `research/findings/2026-09-05-gnw-stop-trigger-accbg-circuit-derisk-GO.md`. NOT flipped default-on
  (owner call); the STOP *clear* itself is unchanged (still the distributed-overwrite workspace's own depression).
- **Rank 8 (GNW congruence host string-id) — DE-RISKED + WIRED DEFAULT-OFF, 2026-09-05.** The N-organ bus's organ-B
  (VERIFY re-check) / organ-C (reverse-binding VERIFY) congruence checks in `webapp/gnw_bus_shadow.py::_organ_reads`
  decided "does this second read match the first" with a host `==`; a `SpikingCongruenceReader` reuses the
  ALREADY-6/6-GO'd swap-intention circuit's `pred_k->mm_k` MATCH VETO (the same ignition-workspace populations
  already load-bearing on the neural thought-swap decision) to read the verdict off spiking population dynamics
  instead. 6/6-seed GO on 100% parity vs the host `==` on a REAL organ-B/organ-C battery (32 pairs/seed, real
  `query_patient`/`query_agent` reads on stored facts), the trigger-lesion collapses discrimination on every
  genuine mismatch (96/96 pairs flip), and build-twice determinism holds. A second 6/6-seed check confirms the
  production dispatch (behind `BRAIN_GNW_CONGRUENCE_SPIKING`, default-off) is byte-identical to a FROZEN pre-edit
  reference when off, matches the host on genuine real-fact agreement when on, and — on a manufactured mismatch —
  lesioning via `BRAIN_GNW_CONGRUENCE_LESION` makes `bus_combine`'s own COMMITTED decision WRONGLY corroborate a
  fact the host correctly withheld (the load-bearing collapse at the full production-decision level). See
  `research/findings/2026-09-05-gnw-congruence-spiking-read-rank8-derisk-GO.md`. NOT flipped default-on (owner
  call); the ignition/WTA consensus and organ A's forward recall are unchanged.

## HYGIENE RE-AUDIT, 2026-09-05 (later cycle) — stale-pointer corrections, cited against the current checkout

**Every claim below was checked against the actual file/flag state of the checkout at re-audit time, not just
re-cited from a finding's own headline.** Corrections to the ranked list (lines above) and to READY TO BUILD
NOW are recorded here, append-only, per the section's own convention.

- **Rank 1 (composer bundle pinned to rf) — SUB-BLOCKER RESOLVED, the literal rebuild STILL NOT done.** Verified
  in the current checkout: `BrainConversationalAgent.__init__`'s default is still `composer_kind="rf"`
  (`research/runners/brain_conversational_agent.py:175`), and `webapp/server.py`'s own comment still describes
  the scale787 bundle's persisted manifest as `'rf'` (`webapp/server.py:3925`). What HAS landed: the DG-CA3
  sparse-index sharded retrieval — the mechanism that makes an onebrain-scale bundle's O(k_max) recall scan
  tractable — is now wired into `OneBrainComposer` itself and reachable from `/api/brain-chat` (additive,
  default-OFF via `BRAIN_FACT_SHARD_RETRIEVAL`). See
  `research/findings/2026-09-05-onebrain-fact-shard-wirein-production-composer.md`. Do not re-derive "is onebrain
  fast enough to be the production bundle" — it now is; the remaining work is the rebuild itself.
- **Rank 2 (integrated_loop thread) — Part A GO (wired default-OFF), Part B UNDEFINED at production scale, the
  named next step is now UNBLOCKED.** Threading `integrated_loop=True` through `webapp/server.py ->
  brain_chat_tui/MultiTurnAgent -> BrainConversationalAgent -> OneBrainComposer` is GO (6/6 checks, default-OFF)
  — see `research/findings/2026-09-05-rank2-integrated-loop-webapp-thread-derisk.md`. The at-scale re-verify
  (V=320/K=32, 6-seed) came back UNDEFINED: patient recall matches the host on the large majority of cues but
  the spiking path ABSTAINS where the host answers on a substantial minority (honest recall-incompleteness, not
  confabulation; 1 moat FA in 210 cue-seeds) — see
  `research/findings/2026-09-05-rank2-integrated-loop-at-scale-answer-preservation-UNDEFINED-recall-incomplete.md`.
  That finding names its own fix — re-verify AFTER the DG-CA3 sharded composer is wired to the live recall path
  — and that wire-in has SINCE landed (the rank-1 citation above). The re-verify is the genuinely-ready rung now,
  not a fresh integrated_loop build.
- **Rank 4 (shared spiking novelty/salience/value afferent) — FLIPPED TO PRODUCTION DEFAULT-ON; the ranked
  list's "not yet integrated" framing and the old READY-TO-BUILD-NOW entry are STALE.** De-risk GO
  (`research/findings/2026-09-05-shared-spiking-salience-afferent-wired-GO.md`) was followed, same session, by
  the production-default flip (`research/findings/2026-09-05-shared-salience-afferent-production-default-flip-GO.md`).
  Verified in the current checkout: `shared_salience_afferent.py::shared_salience_enabled()` returns `True` when
  `BRAIN_SHARED_SALIENCE` is unset, and `git merge-base --is-ancestor 04cbd1bec HEAD` confirms the flip commit is
  an ancestor of the checked-out HEAD. Do not re-list this as build-ahead fodder.
- **Rank 5 (Gate-B appraisal via interoceptive afferent) — FLIPPED TO PRODUCTION DEFAULT-ON; "the readiest" was
  true and has since been ACTED ON.** De-risk GO
  (`research/findings/2026-09-05-gateB-appraisal-interoceptive-afferent-derisk-GO.md`) was followed, same
  session, by the production flip
  (`research/findings/2026-09-05-gateB-appraisal-interoceptive-production-flip-GO.md`). Verified in the current
  checkout: `affect_production_organ.py::appraisal_interoceptive_enabled()` returns `True` when
  `BRAIN_AFFECT_APPRAISAL_INTEROCEPTIVE` is unset. Do not re-list this as build-ahead fodder.
- **Rank 9 (metacog confidence host formula) — moved from "fresh" (unattempted) to de-risked PARTIAL,
  default-OFF.** A spiking winner-vs-runner-up recall-margin (reusing the existing Izhikevich cleanup
  competition) tracks the host confidence formula (Pearson r=0.959) and is load-bearing by lesion on 6/6 seeds;
  the honestly-characterized residual is precision in the ambiguous middle band (~50% agreement there), not an
  unexamined gap. See `research/findings/2026-09-05-metacog-spiking-recall-margin-derisk-PARTIAL.md`. Not wired
  into production; `mean_role_confidence`'s host formula stays the default.
- **Rank 10 (curiosity novelty binary host constant) — moved from "de-risked start" to a full 6/6-seed GO
  de-risk, default-OFF.** `TopicNoveltyGate` reads a graded [0,1] novelty for the SPECIFIC topic off the same
  Bogacz-Brown anti-Hebbian familiarity projector already validated for the v320 familiarity gate, retiring the
  production call site's `NOVEL_SIGNAL=0.95` constant. Additive, byte-identical-off, lesion load-bearing. See
  `research/findings/2026-09-05-rank10-curiosity-graded-novelty-familiarity-scaffold-derisk-GO.md`. Not flipped
  on.
- **Rank 11 (topic-swap regex+host) — the ranked-list entry (line 42 above) is MIS-SCOPED, describing a
  capability that is already done.** The swap-vs-hold DETECTION this item names is already the spiking
  mismatch/salience `mm_k` + prediction-veto `pred_k` comparator, wired PRODUCTION DEFAULT-ON since 2026-08-19/20
  (`webapp/gnw_thought_swap.py`, `webapp/swap_drives_chat.py`) and reused a second time by rank-12's ACC/BG STOP
  circuit. Re-verified against TODAY's code (not just re-cited from the old finding): a fresh 6-seed run
  reproduces the tracked artifact byte-identical, and the real `/api/brain-chat` handler still fires the correct
  swap/hold triad. See
  `research/findings/2026-09-05-rank11-topic-swap-scaffold-backlog-item-already-integrated.md`. Do NOT schedule
  build work against rank 11 as described in the ranked list — it is already neural, wired, and on-by-default.
- **Rank 12 UPDATE — SUPERSEDES the "NOT flipped default-on (owner call)" line in the rank-12 entry above: it
  WAS subsequently flipped.** Later the same session, `stop_trigger_spiking_enabled()` was flipped to
  DEFAULT-ON in production (6/6-seed GO, no-regression + load-bearing-not-hollow through the real entry points;
  two real bugs found and adversarially disambiguated en route, only one load-bearing for the observed
  regression). Verified in the current checkout: the function returns `True` when
  `BRAIN_GNW_STOP_TRIGGER_SPIKING` is unset. See
  `research/findings/2026-09-05-gnw-stop-trigger-accbg-circuit-PRODUCTION-FLIP-GO.md`. The STOP *clear* itself is
  still unchanged, as the original entry said.
- **Rank 15 (self-schema authorship host) — moved from "fresh" to de-risked PARTIAL (5/6 seeds), default-OFF.**
  A per-trial neural signal (real generative-draw spiking activity + a Hebbian familiarity/novelty read)
  replaces the host `authored=True` constant and classifies real self-authored vs recalled trials at 0.9542 mean
  accuracy (chance 0.5), load-bearing on 6/6 seeds by lesion + shuffle anti-cheat; the one characterized miss is
  a generated candidate sharing 2/3 role-fillers with a co-resident stored fact. See
  `research/findings/2026-09-05-selfschema-authorship-neural-turnclass-derisk-PARTIAL.md`. NOT wired into
  `webapp/server.py`; the host regex ROUTING (whether a turn enters the generation branch at all) is separately
  unretired.
- **Rank 16 (DA→write-magnitude host linear) — the remaining LEAF half is now also de-risked, default-OFF; the
  "partial: homeostasis-half retired" framing in the ranked list is out of date.** A spiking population read off
  the existing DA/neuromodulator machinery retires the per-write `_gain_map()` closed-form leaf (6/6-seed GO,
  correlation 0.996-0.9999 vs the host formula, lesion collapses the gain span to zero). See
  `research/findings/2026-09-05-da-write-gain-spiking-derisk-GO.md`. Not flipped; `_gain_map()` stays the
  production default — so BOTH halves of rank-16 are now de-risked, though neither is wired on.
- **Rank 20 (value-choice reward-context host recency) — de-risked further, default-OFF; this also corrects the
  "Rank-20 worldmodel-state WTA" label in the old READY-TO-BUILD-NOW list, which matched no finding or ranked
  item.** Rank-4's shared salience afferent already wired `value_choice_production_organ.py`'s context function
  through the spiking salience organ; this finding closes rank-4's own single-seed waiver by running the REAL
  trained `striosome_value` critic end-to-end at all 6 project-standard seeds across 4 candidate/recency
  scenarios (24 pairs), GO. See
  `research/findings/2026-09-05-value-choice-real-critic-neural-salience-context-6seed-GO.md`. No code changed;
  `BRAIN_VALUE_CHOICE` stays default-ON, `BRAIN_SHARED_SALIENCE` stays at its rank-4 setting (now default-ON per
  the rank-4 update above).
- **Rank 23 (vision cluster host, grouping-decision sub-piece) — de-risked GO, no live consumer.** A 2-pool
  spiking lateral-inhibition WTA circuit (board #86's reused N-pool motif) reproduces the EMERGE-36 pipeline's
  host argmax grouping decision: parity 1.00 mean, mis-route shuffle flips the category as predicted, 84.4% of
  the winner margin is attributable to the real dendritic signal vs. a symmetric control, 6/6 seeds. See
  `research/findings/2026-09-05-rank23-vision-cluster-grouping-decision-spiking-wta-derisk-GO.md`. Pure de-risk;
  nothing wired, no live consumer — the other 3 host residuals rank-23 bundles (V1 Gabor front end, argsort
  feature-selection, hand-written receptive fields) are untouched.
- **Rank 24 (quick flips) — split verdict; the "d5 depth_hold direct-read" example in the old
  READY-TO-BUILD-NOW list is FALSIFIED, do not re-attempt it.** (a) d5 depth_hold direct-read is NO-GO: each
  topic's DG-formed CA3 assembly has its own baseline apical-latch magnitude, so raw `depth_hold` is not
  comparable ACROSS topics and no single threshold can replace the per-topic `_CONSOLIDATED_TOPICS` host-set
  gate (measured, 6/6 seeds, `GO=False`) — see
  `research/findings/2026-09-05-d5-depth-hold-direct-read-NO-GO-cross-topic-baseline-not-comparable.md`. (b)
  `enable_spiking_sc` is a split verdict across two distinct flags: `sc_orienting_production_organ.py`'s
  `BRAIN_SPIKING_SC_ORIENT` is genuinely flip-ready and is now DEFAULT-ON; `g11_bg_runner.py`'s own
  library-level `enable_spiking_sc` default was tried, found to introduce a real no-regression problem, and
  REVERTED — see
  `research/findings/2026-09-05-enable-spiking-sc-split-verdict-organ-flag-GO-library-default-reverted.md`.
