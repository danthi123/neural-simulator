{"issues": ["1. [high, fail-open, cap not enforced] tools/claim_check.py:282,297 checks MAX_ARTIFACT_VALUES only between files, so one cited file is never capped. The tracked 26 MB artifact research/findings/raw/v14_snr_stageB_fast_channel_clamp_cupy_v1.json pools 732,007 distinct values, and 99.7% of random wrong 4-decimal numbers match. Even under the cap, chance acceptance stays high: 10.8% at the largest legitimate battery (8,266 values), 18.3% at 15,000 values, and a visibly cited set of 4 raw directories (2,101 values) passed 12 of 40 wrong numbers. The calibration only measured glob citations. Fix: enforce the budget inside _flatten_numbers. Better, measure each doc's discriminating power directly: probe about 200 random decoy numbers against the pool, and block with 'citation too broad to verify' when more than 5% pass.", "2. [medium-high, fail-open, issue-4 bypass] _is_table_row (claim_check.py:341-343) only recognises rows that start with '|'. Round 5's exact repro passes unchanged in two other GFM table forms: a pipe-less row (`42 | 0.1525 | 0.104615 | <!--derived-->` under `---|---|---|---`) and a blockquoted row (`> | 42 | 0.1525 | ... |`, which 2 existing docs use). _BR_RE (:197) also misses `<br class=x>`, and `</p><p>` is not split at all. Fix: strip leading `>` and list markers before the test, treat any line in a block that has a GFM delimiter row as a row, and use `<br\\b[^>]*>`. Splitting only narrows the exemption, so misclassifying a prose line fails closed.", "3. [medium, NEW fail-open regression vs r5 and main] Deleting `*` and `_` and decoding HTML entities (:337, :331) can glue a number to the letter before it, and then the `(?<![\\w.])` lookbehind drops it. `gain*0.1525`, `**acc**0.1525`, `&Delta;0.1525`, `x\\*0.1525` and `n*-0.1625` all PASS on r6 and FAIL on r5 and main. Stripping comments per segment (:463) also removes text that renders visibly: `` `<!--` 0.1525 `-->` `` and `\\<!-- 0.1525 -->` pass on r6 and fail on r5 and main. Only 1 corpus instance (`4*0.03972`). Fix: replace an emphasis character with a space unless digits sit on both sides; use an ASCII-only lookbehind `(?<![A-Za-z0-9_.])`; leave comments inside code spans or after a backslash in place, or keep checking in-comment numbers while excluding them from the coverage numerator.", "4. [medium, issue-6 incomplete] The same class of hole remains open through characters the normalizer does not handle. A sign-flipped -0.1625 is read as +0.1625 and passes when the minus is U+2010, U+2011, U+2012, U+2014, U+FE63, U+FF0D, U+02D7 or U+2796, or when a letter precedes it (`\u0394\u22120.1625`). The number is hidden entirely by invisible characters that are not category Cf (U+034F, U+FE0F, U+FE00, U+3164, U+115F), by non-empty or non-span inline tags (`0.15<b>25</b>`, `<wbr>`, `<i></i>`, `<sup></sup>`, `<a></a>`), and by other decimal points (U+FF0E, U+2024, U+FE52, U+00B7). Fix (claim_check.py:326-338): apply NFKC; map every Pd character plus U+2212, U+2796 and U+02D7 to '-'; strip Mn, Me and Cf characters and the Hangul fillers; strip `<[^>]+>` in the scan-only copy.", "5. [medium, fail-open, issue-5 bar bypassable] The synthesis verdict bar is bypassable 8 ways; every variant below got synthesis=True with a wrong 0.1525. _TITLE_RE.search (:210, :380) takes the FIRST `# ` line anywhere, so a leading `# Notes` H1, or a `# comment` inside a code fence or YAML, wins over the real title. It also ignores setext H1s and is case- and ZWSP-sensitive (`NOGO`, `G\u200bO`). It never reads the filename, a frontmatter `verdict:`, or body lines, while claim_verdict_consistency scans every line of a status:live doc case-insensitively, so it does not mirror that gate. Separately, the `\\s*` in _SYNTH_REASON_RE (:209) crosses a newline, so an empty `claim_check_reason:` captures the next key as the reason. Fix: use `[ \\t]*` in that regex; bar synthesis when the verdict pattern matches the filename, any heading or frontmatter value, or when status is live.", "6. [medium, guidance contradicts the new cell rule, same class as r5 issue 2] Cell scoping made the row-trailing marker `| ... | <!--derived--> |` inert. That form has 140 uses in 41 findings. Every author-facing string still says 'same line': pre-commit:53, check()'s closing message at claim_check.py:564-566, finding_lint.py:292, and .claude/skills/neural-simulator/SKILL.md:294 ('including every row of a derived table'), which r6 left unchanged. No WARNING fires for a marker alone in a cell with no numbers. The 8-per-cell cap and `claim_check_reason:` are documented nowhere outside claim_check.py. Fix: say 'in the SAME table cell or <br> segment as the number, at most 8', and warn when a marker's cell or segment holds no number.", "7. [medium, retro claim inaccurate] The docstring (claim_check.py:230-241) and the commit message say the 73 flips are 'EVERY one via ... cap, normalization and citation fixes' and are candidate real errors. I tested this by reverting all r6 changes and re-adding one at a time. Causes: synthesis tightening 32 (0 of the 42 synthesis docs has claim_check_reason), cell segmentation 24, NUM_RE widening 14, normalization 2, comment citations 1; the per-line cap and the artifact caps each cause 0 on their own. Of the 1,074 numbers newly flagged in those docs, 597 are correct roundings of a cited value and 53 are arXiv/DOI ids; 29 of the 73 flips have nothing else flagged. The TSV reason column holds only 'unsupported(N)', and the script that generated it is not committed. Fix: correct the claim, commit the r6 retro script, and add a cause column.", "8. [medium, false positives on legitimate docs, mostly pre-existing but amplified] Since 2026-09-01, 155 of 353 findings FAIL under r6 (main 31, r5 135). Of the 1,651 flagged numbers, 1,019 (62%) are correct roundings at the stated precision: relative tolerance 1e-4 rejects 0.477 for 0.4774. 47 docs fail on rounding alone. Sample of 10 failing docs: 6 are rounding-dominated (vision-satdiv, ca3-superposed, affect-grounded-stream, d6-capacity-prereg, spiking-expectation-rpe, crossedge-d6wm-retuned), 1 is arXiv ids (affect-tone-neural-coupling), 1 is unmarked derived differences (d5-depth-hold: 0.499 = 30.240 - 29.741), and 2 are genuinely unsupported (content-addressable-read, ai-teacher-prereg). The widened NUM_RE also reads `1.088B params` as 1.088. Fix: precision-aware matching, |a - v| <= 0.5*10^-k where k is the stated number of decimals (tighter than today for claims with 4 or more decimals); skip numbers after `arXiv:` or `doi:` or inside URLs; do not strip B/M/k magnitude suffixes.", "9. [low-medium, coverage floor and hidden carriers] A doc with 79 claims, all marked, still passes (round-5 repro, unchanged). 11 DISTINCT real values hidden in a multi-line `<!--...-->` still count as checked, because comments are stripped per segment at :463 but per whole text for citations at :451; so do values in `[//]: # (...)` or `<div hidden>`. In each case 200 wrong numbers plus 11 hidden values PASS. `[//]: #` and `<div hidden>` also still carry a citation the reader cannot see. Numbers inside multi-line comments are scanned as visible, which causes false flags in 4 docs that use `<!--derived: ...\\n...-->`. Fix: strip comments from the whole text (keeping the newlines) before segmenting, also strip link-reference definitions and hidden elements, and lower the floor for smaller docs.", "10. [low, tests do not pin the fixes] Issue 7's distinct counting is pinned by nothing: mutating :498 back to `checked / total_numeric` leaves the selftest clean and 98 of 98 tests passing (4 history tests skipped in that copy, which had no git). Issue 9's comment-citation fix is tested only in pytest, not in SELFTEST_CASES, so the new CCT registry gate cannot catch a regression there. Fix: add a selftest case with 80 marked values plus 6 VISIBLE copies of 0.170000, and move the hidden-citation case into SELFTEST_CASES with its own artifact.", "11. [low, UX] When claim_check itself regresses, the gate's selftest (gates/claim_check_selftest.py:56-59) reports 'problems against the UNMODIFIED registry (false positive)'. It labels a real regression as a false positive, and because the registry then skips check(), the clear message never prints. Fix: pass claim_check's selftest problems through verbatim, labelled as a broken instrument."]}