1789581767	SIM_BACKEND=numpy OMP_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 MKL_NUM_THREADS=2 .venv/bin/python -u -m research.runners._gnw_stop_conflict_scaled_boost_derisk --seeds 42 43 44 100 101 102 --json research/findings/raw/_gnw_stop_conflict_scaled_boost_6seed.json  #checked:verify-first (this session): webapp/gnw_global_stop.py:_StopWorkspace.run() passes fixed BOOST_GAIN/MARGIN_REF into run_conflict_stop (confirmed at source); detect_trigger() already reads chat._last_gnw_delib[n_ignited] and gnw_deliberation.py populates chat._last_gnw_delib[conf] at the SAME call site (confirmed reachable); BRAIN_GNW_STOP_LESION zeroing boost_gain->0 is the shipped load-bearing proof (2026-08-26-gnw-global-workspace-stop-production-wirein-GO.md). RAG: no prior lever against THIS constant found; the only related precedent is 2026-09-04-gnw-lc-ne-adaptive-gain-swap-eviction-GO.md, a DIFFERENT module (gnw_thought_swap BOOST_GAIN) via a NEW lc population -- this lever is smaller (reuse-only, no new circuitry). New flag BRAIN_GNW_STOP_CONFLICT_SCALED default-off, wiring verified locally via mocked bridge (zero neuron compute); this queues the REAL spiking 6-seed verification.
