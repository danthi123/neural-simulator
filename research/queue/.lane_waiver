Pool provision is a KNOWN deeper infra issue (not a quick fix): the git-archive source set is a narrow subset (sim + research/{runners,specs,fixtures,findings} + experiment/tools/tests/docs + root md), but the nodes carry a full old clone with many extra dirs (config/ experiments/ raw/ references/ ui/ viz/ research/biology webapp/ bridges/) the strict manifest verify rejects. Fix = prune-nodes-to-subset OR widen-archive+full-tree-rsync (a design decision needing its own test). GUARD IMPROVEMENT LANDED 1f478ff60 (data/ + *.out/*.log no longer false-flagged). Meanwhile CPU de-risks run LOCALLY (proven: mouth + read-fidelity both ran local->verdict). Captured as a focused task. Auto-expires 6h.
