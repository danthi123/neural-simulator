Pre-registered pool sweep (gap4 n_prop=5) is SUPERSEDED by the 2026-08-11 "gap4 wall was a hyperparameter" finding (re-running = stale-pointer make-work). Launching an ultracode workflow to BUILD + stage frontier-aligned CPU-lane (E-Language: mouth repetition/looping residual, the A/B GO next lever) de-risks; this waiver bridges the doc-sync commit until those land. Auto-expires 6h.
