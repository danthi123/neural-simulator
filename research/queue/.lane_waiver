Single agent session on frontier-B continuous-drive-coupling arc (research/continuous-drive-coupling-B3), GPU-bound (cupy anti-hollow E2E verify, 4 sequential ~15min runs). No idle local/pool CPU capacity was available to stage the 5 CPU lanes concurrently — the session was continuously blocked on serialized GPU verification + iteration on a single test-harness race, not idle.
