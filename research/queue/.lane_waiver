Pool idle but NO high-value pool-runnable de-risk this cycle: the frontier CPU work needs LOCAL assets (the WKV mouth checkpoint, gitignored — not on remote pool nodes, which is why the E-Language mouth de-risk crashed there and was re-run LOCALLY to GO) or the cupy GPU handler (xedge/honesty/wander verifies -> gpu.queue). The staged gap4 lines are superseded (2026-08-11 hyperparameter finding) + stale-skipped >12h. Structural: current frontier is local-asset/GPU-bound, not remote-CPU-bound. Auto-expires 6h.
