Isolated worktree subagent committing a single language-lane (E) CPU de-risk (on-bridge spiking naming map); it has no access to the shared pool queue and the parent session owns cross-lane queue orchestration. The GPU/pool lanes are managed outside this worktree.
