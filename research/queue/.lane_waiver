Lanes actively being served IN-FLIGHT (real work, not neglect): gap#4-larger-task + Curiosity reward-omission veto RUNNING; Perception 6-seed just completed (RETIRE banked); C·W4 recursive-ToM + H·spiking-gamma-replay agents BUILDING their runners and will queue to the pool. The staged queue is momentarily empty only because this work runs via lane-agents/local, not as pool.queue lines — the lane_starvation gate reads the queue, not running procs (a known blind spot to fix). This names real in-flight serving, NOT a priority/focus rationalisation; auto-expires 6h.
