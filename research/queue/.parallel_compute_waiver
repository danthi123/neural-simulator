2026-09-16 ~10:10: compute_idle counter is stale from the 5-day break + this session. Current reality: the LOCAL GPU
is running the token _n2M bend-test sweep (88% util, 6/18 done), and the POOL just COMPLETED its 14-cell batch
(vision satdiv-refine + Touchpoint-A) — being harvested in this very commit. Not idle. Genuine reason: no more
verified-unrun pool work is staged this instant (the batch just finished); next pool stocking is a deliberate pick,
not a churn-fill. Auto-expires 6h.
