FRONTIER COMPUTE-COVERED (2026-09-21 ~04:58 UTC, autonomous overnight) — the owner-ratified #1 metric
(lesion-verified LOAD-BEARING FRACTION) is being computed on TWO genuine 6-seed lanes in flight:
  1. AWS r7i.4xlarge: adequate-probe 6-seed, 7 verified drive-flags, numpy 6-parallel (the ROBUST headline fraction).
  2. LOCAL (19 cores): prospective-memory-v2 6-seed, --only prospective-memory (makes the verified single-seed flip
     robust per the 6-seed constraint; consolidation branch research/lb-consolidation @ 179c3c566).
POOL (pool41/42 idle) + local GPU idle have NO genuine ready job — VERIFIED, not assumed:
  - The queued pool job (GNW stop-conflict scaled-boost 6-seed) is ALREADY COMPLETE: verdict=GO, n_seeds=6
    (research/findings/raw/_gnw_stop_conflict_scaled_boost_6seed.json, 2026-09-17) -> stale entry, cleared.
  - The named vision next-lever (conjbind heldout-position 6-seed) is ALREADY DONE + the arc CONCLUDED:
    "divisive normalization is the decisive lever; conjunctive binding is NOT (GO 6/6)" (96ac8b3aa, 2026-09-17).
  - The load-bearing arc does not need GPU (small brains; numpy parallel + BLAS-cap is the proven backend;
    cupy per-build overhead crawls for this setup-bound battery -- 2026-09-20 finding).
Genuine forward build queued for the consolidation (NOT make-work now, to respect the token-leak flag):
  wire open-ended-generation's DISTRIBUTIONAL lesion metric (draw_many plausible-frac 0.83->0.04, already GO'd)
  as its load-bearing ruler -- the open-ended agent's own recommendation (finding 2026-09-21, commit 41575dc86).
Holding pool/GPU is EARNED here: no ready job exists for them, and the frontier metric is fully covered.
