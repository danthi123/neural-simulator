# TRUE one brain, BOTH knowledge dimensions fully spiking in ONE cupy process (GO): the property HTM reasoner AND the relational RF-FHRR composer co-execute, answering property (inherit/cancel) AND relational (SVO what/who) questions over the brain's OWN real-corpus codes, no-confab moat intact. The both-dimension analog of EMERGE-70/71. NO `sim/` edit.

**Date:** 2026-07-08
**Runner:** `research/runners/_realcorpus_one_brain_both_dims_smoke.py` (reuse-by-import: `CancellingPoolerProbe` [property, committed HTM coincidence kernel] + `RFPhasorComposer` [relational, RF resonate-and-fire + complex synapses] + the breadth discovery). `SIM_BACKEND=cupy`. NO `sim/` edit.
**Verdict:** GO — both spiking reasoners co-execute + answer + abstain in one process.

## Why this ran (the TRUE one brain, both dimensions)
Both dimensions had spiking realizations separately (property on HTM, CYCLE 984; relational on RF-FHRR, CYCLE 1002), but on different substrates/runs. The owner's "one brain" directive means they should co-execute in ONE process. Both are `SimulationBridge`s; on the SAME cupy backend they co-exist. First the key uncertainty was resolved (the property HTM reasoner runs on cupy — seed 42 GO, same as numpy), then both were built + answered in one cupy process.

## The result — seed 42 (K=1024 emergent property clusters, D=64 relational, cupy)
```
=== ONE BRAIN, BOTH DIMENSIONS, ONE cupy PROCESS ===
[property/HTM] taught cluster 0 class-property + exception 'bad'
  Q: does 'bad' have the class property?  -> NO (exception overrides)   [apical competition, HTM]
  Q: does 'loud' have the class property? -> YES (inherits)
[relational/RF] stored: 'played found went' (+5 more)
  Q: what does 'played' found?  -> went     [RF-FHRR object recovery]
  Q: who found 'went'?          -> played    [RF-FHRR subject recovery]
  Q: what does 'can' found? (unstored) -> None  [moat]
VERDICT: GO
```
- **PROPERTY (HTM):** a member's exception overrides its inherited class property (apical competition on the committed HTM coincidence kernel, read from `cp_v_apical`); another member inherits.
- **RELATIONAL (RF-FHRR):** SVO what- and who-questions recover the object/subject (bind/unbind on the RF resonate-and-fire + complex-synapse store).
- **MOAT:** an unstored relation → abstain (no confabulation).
Both answered in ONE cupy process, over the brain's OWN real-corpus co-occurrence codes (the property SDR-ification and the relational grounded-phase both derive from the same seed-deterministic codes).

## Multi-seed (3-seed: co-execution robust; cancellation-override member-dependent)
seeds 42/43/44 (K=1024 emergent, cupy): the CO-EXECUTION (both spiking reasoners build + answer in one cupy process) + the RELATIONAL SVO (what/who) + PROPERTY INHERITANCE + the MOAT are robust on ALL 3 seeds. The property CANCELLATION *override* fires on seed 42 but NOT on seeds 43/44 — the smoke picks the FIRST inheriting cluster member as the exception, and on 43/44 that member ('cried', 'happy') is a strongly-codon-categorized member whose override saturates below its inherited drive (the exact CYCLE-985 emergent-cluster boundary: the HTM permanence ceiling can't exceed a very-tightly-inherited member's codon→class drive). So all seeds demonstrate the one-brain co-execution + relational + inheritance + moat; the cancellation override is member-dependent (GO on a weakly-inherited member, saturates on a strongly-inherited one — CYCLE 985).

## Honest scope
- A smoke demonstrating the co-execution; the individual mechanisms are each multi-seed-validated (property cancellation 6/6 labeled / 5/6 emergent; relational SVO 3-seed substrate). The point is that both fully-spiking reasoners co-exist + answer in one process — robust all 3 seeds.
- The cancellation override's member-dependence (CYCLE-985 emergent-cluster saturation) is orthogonal to the co-execution: choosing a weakly-inherited exception member (as the console does) fires it; the smoke's arbitrary first-member choice sometimes lands on a saturating member.
- The emergent-cluster words here are whatever co-clusters ('bad'/'loud' adjectives; the relational facts are over discovered verbs) — the MECHANISM (both dimensions, one process, spiking, moat) is what's demonstrated, not the words' semantics.
- Two separate `SimulationBridge`s on one cupy backend (one-backend-per-process satisfied); the A→W speaker (a 3rd bridge) folds in the same way (a follow-on for the fully-spiking spoken both-dimension turn).

## What this establishes
The TRUE one brain: BOTH of the talkable brain's knowledge dimensions — property (inheritance + cancellation) and relational (SVO what/who) — reason FULLY ON SPIKES over the brain's OWN real-corpus codes, CO-EXECUTING in ONE cupy process, with the no-confab moat. The both-dimension analog of EMERGE-70/71's one-brain milestone. Follow-on: fold the spiking A→W speaker in for the fully-spiking spoken both-dimension turn; multi-seed; scale.

## Files
`research/runners/_realcorpus_one_brain_both_dims_smoke.py`; `research/findings/raw/_one_brain_both.log`. Composes: the spiking cancellation `2026-07-08-cancellation-ON-SPIKES-real-corpus-apical-competition-GO.md` + the spiking relational `2026-07-08-relational-SVO-on-spiking-substrate-RFPhasorComposer-GO.md`; EMERGE-70/71 (the one-brain-one-process pattern).
