# EMERGE-33 / toward-semantics — GO (6/6 seeds): a SELF-ORGANIZED emergent superordinate. A competitive HTM Spatial Pooler DEVELOPS a NEW shared column block for same-category members from varied experience (an internal learned representation, not the raw input-feature overlap), and the validated on-bridge inheritance rides those self-organized cells. The research gate's TOP mechanism. NO `sim/` edit.

**2026-07-02 (autonomous; the emergent-structure research gate's top-ranked mechanism, HTM Spatial Pooler).** Runner `research/runners/_emerge33_spatial_pooler_emergence_derisk.py`; CI guard `tests/test_emerge33_spatial_pooler_emergence.py` (3 tests). Reuse-by-import (`_emerge14` + `_emerge12`); NO `sim/` edit; CPU numpy-backend; 6-seed.

## What it deepens
EMERGE-30 rode a single shared context token; EMERGE-32 rode the raw input-feature overlap — both are the ENVIRONMENT's structure. EMERGE-33 forms an INTERNAL, self-organized representation: a competitive Spatial Pooler (Cui-Ahmad-Hawkins 2017) with k-winners-take-all + homeostatic boosting maps each member (its varied feature subset) to a sparse column code and — because the pooler preserves input similarity — same-category members converge on an OVERLAPPING column block (the emergent superordinate) while different categories stay disjoint. This is the closest thing to a hand-assigned "BIRD block", but LEARNED by the cortex from experience.

## The claim (6/6 seeds)
The competitive pooler forms each member's column code; a category property is taught on the TRAINING members' self-organized codes (the committed `sim/` three-term kernel on the real spiking bridge) → it binds to the shared column block; a HELD-OUT member (its own code overlaps the shared block) INHERITS — read on-bridge:
- **Held-out inheritance ≥ 0.90:** a member whose property was never taught inherits via the emergent self-organized block.
- **Moat 1.00:** a code disjoint from every block abstains.

## Anti-cheats (6-seed) — the LOAD-BEARING control is the input-destruction permuted-features
**Methodology correction (2026-07-02):** the original finding gated on a NO-POOLER (fixed-random-code) control that read 0.00 — but a fixed-random-code control is UNRELIABLE in a small column space (a random held-out code coincidentally inherits, or not, by luck: EMERGE-34's equivalent read 1.00). See `2026-07-02-anti-cheat-control-validity-methodology.md`. The runner was strengthened (**HOLD=3 members/category** for a finer metric; random-code control made **seed-dependent** + demoted to a reported secondary check) and the GO now gates on the reliable input-destruction control:
- **PERMUTED-FEATURES** (members draw from the MIXED pool → no category structure can form → the pooler builds no shared block) — the LOAD-BEARING control: held-out inheritance collapses to **~chance (~0.5 mean)**, the reliable isolation of the learned category structure.
- **dAP-LESION** (bridge coincidence off → no priming): deterministically collapses to **0.00** — a clean mechanism-ablation control.
- **RANDOM-CODES** (no pooler, now seed-dependent): reported, ~chance-ish but noisy (the small-column-space coincidence); NOT a strict gate condition.
- **MOAT 1.00.** 6-seed. The GO rests on the input-destruction permuted control + the deterministic lesion, not on a fixed-random-code control.

## Significance
The cortex LEARNS a shared category representation from experience (self-organized, not hand-assigned, not merely the input overlap) AND infers over it — the research gate's top-ranked mechanism (competitive representation learning + inheritance). It reaches the same inference the hand-assigned is-a codes gave (EMERGE-26), but the superordinate is now a learned internal block. Grounded in the HTM Spatial Pooler (Cui-Ahmad-Hawkins 2017, *Front. Comput. Neurosci.*) and the feature-prediction theory of taxonomic inheritance (Saxe-McClelland-Ganguli 2019).

## Honest scope + next
- The Spatial Pooler is a RATE-reference for the representation step (competitive Hebbian + kWTA + boosting — a biologically-grounded competitive layer); the FULLY-SPIKING pooler (lateral-inhibition kWTA + homeostatic excitability realized on the bridge) is the flagged follow-on. The INHERITANCE itself runs on the real spiking bridge over the self-organized codes.
- With EMERGE-30 (co-occurrence token) and EMERGE-32 (feature overlap), this completes the emergence-from-experience story at three levels of increasing internality: shared token → input overlap → self-organized column block.
- Next: the fully-spiking pooler; cancellation on emergent codes; couple into the experiential console; PERCEPTION-grounded emergence (categories from perceived objects via the visual cortex).

## Artifacts
`research/runners/_emerge33_spatial_pooler_emergence_derisk.py`, `tests/test_emerge33_spatial_pooler_emergence.py`, `research/findings/raw/_emerge33_spatial_pooler_emergence.json`. Prior: `2026-07-02-emerge32-varied-context-emergence-GO.md`, `2026-07-02-emergent-structure-from-experience-research-gate.md`.
