# The e-prop-recurrent-language SYNTHESIS is REFUTED by the must-run controls — the "deep-context" improvement is a credit-direction-INDEPENDENT structural artifact (memory-timescale nudge), it is LOCAL not long-range, and it LOSES to a proper trigram. (The SEPARATE deep-credit-on-classification core result is untouched and STANDS.)

**Date:** 2026-07-14 (overnight; adversarial-verify follow-through)
**Runner:** `research/runners/_emerge_reservoir_lm_eprop_CONTROLS_derisk.py` (subagent-built, controller-verified) · raw `research/findings/raw/_eproplm_controls/controls_s{42,43,44}.json`. numpy CPU; NO `sim/` edit.
**Status:** ❌ REFUTED — the synthesis GO (`2026-07-14-eprop-trained-recurrent-language-cortex-...GO.md`, already walked back to "needs controls") does NOT survive. Honest negative.

## The claim tested + why it fell
The synthesis claimed an e-prop-TRAINED recurrent language cortex "beats the fixed reservoir AND the bigram at deep context" on WikiText. A 5-skeptic adversarial-verify flagged three confounds; the four must-run controls now resolve ALL of them AGAINST the claim:

| control | result (3-seed) | verdict |
|---|---|---|
| **1. distal-prefix scramble** (randomize tokens 0..t-4, keep the local window) | plastic-vs-fixed deep margin RETENTION 0.94 / 0.86 / 0.55 (mean ~0.78) — the margin SURVIVES scrambling the distal context | ✗ the gain is **LOCAL** structure at late positions; "deep context" is a MISNOMER |
| **2. strongest n-gram** (add-k bigram, interpolated trigram, 4-gram) | plastic deep token-weighted CE **3.180** vs interpolated-trigram **2.804** (loses −0.376, 3/3) and add-k bigram **2.871** (loses); plastic only ever beat the deliberately-weak add-1 bigram (3.226) | ✗ plastic LOSES to a proper n-gram — the "beats bigram" leg was a weak-baseline artifact |
| **3. coherent credit-irrelevant `random_signal`** (structured eligibility, task-decoupled random L) | random_signal deep CE 4.088 (much WORSE than plastic 3.180) | (fully-random L destroys it — but see control 4) |
| **4. sign_flip + symmetric ceiling** (the decisive test) | **sign_flip (L:=−B·δ) gives the SAME deep margin as plastic** (+0.069/+0.099/+0.093 vs plastic +0.067/+0.098/+0.088); **symmetric (true-gradient direction) HURTS deep** (−0.165/−0.846/−0.473); plastic FA-cosine tiny (0.03→0.08) | ✗✗ the benefit is **NOT credit-driven** — it is INDEPENDENT of the credit direction (negating the signal helps equally; the true gradient hurts) |

## ⇒ honest conclusion
**The e-prop "improvement" on the language reservoir's `W_rec` is a credit-direction-INDEPENDENT structural artifact** — any structured (non-random) weight perturbation nudges the fixed reservoir's effective memory-timescale / spectral radius toward a regime that lowers CE at LATE sentence positions and raises it at early ones (the shallow-hurt/deep-help crossover). It is NOT genuine long-range deep-credit capture: (a) it survives distal-prefix scrambling (local, not long-range), (b) it loses to a proper trigram, (c) sign-flipping the learning signal reproduces it while the true-gradient direction hurts — the hallmarks of an operating-point side-effect, not credit assignment. `random_signal` (fully-random L) destroys it only because random L destabilizes the reservoir, not because the plastic L carries correct credit. The two original anti-cheats (shuffle_elig ≈ fixed, zero_signal == fixed) looked clean precisely because they miss this class of confound — exactly what the adversarial verify predicted.

**REFUTED cleanly + honestly.** This is the adversarial-verify + must-run-control discipline working end-to-end: an exciting first read was over-claimed, self-flagged, walked back, and then decisively refuted by the pre-registered controls. A clean negative is a first-class result — it removes a false lead.

## What this does NOT touch (important scope)
This refutes ONLY the SYNTHESIS (applying e-prop to a fixed reservoir's recurrent weights for a WikiText next-token task, where the "win" was a tiny CE margin). It does **NOT** affect the SEPARATE, SOLID deep-credit-on-spikes core result (`2026-07-14-deep-credit-spiking-training-wall-...md`): there, transport-free e-prop + population trains a depth-2 compositional-INHERITANCE CLASSIFICATION task to **train 1.0 + held-out generalization 0.89**, with permuted-label at chance AND a positive-control gate — a **genuine-learning signature a credit-direction artifact cannot produce** (you cannot fit 5-class labels + generalize with a wrong-direction update). Different task, different net, different evidence class. The emergence-engine core (a spiking brain that learns deep credit the biological way) STANDS; the language-reservoir synthesis is the honest negative.

## NEXT (honest, for the recurrent-language-cortex direction)
The refutation says: nudging a FIXED reservoir's recurrent weights with a local rule does not, by itself, buy real long-range language structure — the reservoir's fixed random dynamics are the bottleneck, not the recurrent-weight learning. The honest path to a LEARNING language cortex needs the recurrent learning to be validated on a task with a GENUINE-LEARNING signature (fit + generalize + credit-direction-dependent), as the classification core has — i.e. bring the validated deep-credit-classification machinery to a sequence task designed so correct credit is load-bearing (not a tiny CE margin a memory-timescale nudge can fake). The emerge6b boundary (autonomous-generation stability) + a genuine-credit sequence task, NOT the reservoir-CE margin, are the real frontier.
