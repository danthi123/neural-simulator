# GNW Rung-3 — the REPORT == REASONING identity + the dissociation on the spiking workspace (6-seed GO): the ignited workspace content drives BOTH a "report" and a "reason" consumer, a causal swap flips both together, and a workspace lesion collapses the (workspace-only) reasoning while a peripheral direct path keeps the report alive

**Date:** 2026-07-07
**Runner:** `research/runners/_gnw_rung3_report_reasoning_identity_derisk.py` (reuse-by-import of the Rung-1 assembly-loop + snapshot-restore + the Rung-2 shared-inhibition mutual exclusion; NO `sim/` edit).
**Verdict:** 6-seed GO (42/43/44/100/101/102), numpy/CPU. The GWT paper's KEY result (Gurnee et al. 2026, §3.5.2/§7: "the representations used for verbal report are the same ones that govern how the model silently reasons") demonstrated on the spiking substrate. Builds on Rung-1 (ignition GO) + Rung-2 (mutual exclusion GO).
**Adversarial-verify:** Workflow `wtiri7pu8` (trivial-copy lens + lesion/dissociation-validity lens + synthesizer) — **both SURVIVES_WITH_SCOPE, no refutation.** Confirmed STRENGTHS (each with the skeptic's own probes): (a) the sustained-ignition dependence is REAL, not a drive artifact — reason stays lit in the late post-drive window when intact (asm 0.333 / rea 0.15) and goes to EXACTLY 0.0000 under lesion (the A_to_reason weight is UNCHANGED at 12.0; reason dies only because the lesioned workspace can't self-sustain); (b) the lesion is SURGICAL — it zeroes ONLY the two ws_loop recurrence populations (189600→0 each); every other projection is byte-identical (43645 nnz identical intact-vs-lesion); (c) content-specificity is CLEAN — zero off-content cross-talk on all 6 seeds; (d) the GO gate is a REAL test — three nulls all correctly FAIL identity (no-broadcast; zeroed broadcast edges; SCRAMBLED reason read A→rea_B). Sharpened SCOPE (folded in below): the identity is by-construction symmetric wiring (report & reason are structurally-identical, byte-identical-rate projections), NOT an emergent shared representation; and "who survives the lesion" is a pure wiring choice (giving reason a peripheral path makes it survive too — a fair but constructed model of the paper's hypothesis).

## The result (6-seed)
A `workspace` region with two content assemblies (A, B), each a dense self-recurrent loop (weight 30) sharing an inhibitory `workspace_fs` pool (only one ignites at a time). Two downstream regions read the workspace via fixed content-specific projections: `report` (report_A/report_B = what the brain would SAY — which content is in the workspace) and `reason` (reason_A/reason_B = a downstream reasoning consumer). `report` ALSO has a peripheral DIRECT external-current path; `reason` reads ONLY the workspace. All 6 seeds:
- **IDENTITY** — ignite content A → report=A ∧ reason=A; ignite B → report=B ∧ reason=B. The report and the reasoning are driven by the SAME ignited assembly (content-specific, clean: rep_A/rea_A ≈ 0.15, rep_B/rea_B = 0.00 on ignite-A, and mirror-image on ignite-B — no cross-talk).
- **CAUSAL SWAP** (the paper's membership test) — swapping which content ignites flips BOTH report and reason together.
- **WORKSPACE LESION collapses reasoning** — with the assembly self-recurrence → 0 (the workspace cannot ignite/sustain), reason goes silent (the reasoning consumer depends on the SUSTAINED workspace, measured in the late post-drive window — not on the transient drive).
- **DISSOCIATION** (the load-bearing §3.5.2 result) — under the SAME lesion, the peripheral DIRECT path keeps `report` alive (a peripheral faculty survives) while `reason` stays dead → **the workspace reps ARE the reasoning reps** (report-reps == reasoning-reps).
- `identity ∧ causal_swap ∧ workspace_lesion_collapses_reason ∧ dissociation` = True on all 6 seeds.

## Honest scope (the cheap-first framing — what is and isn't demonstrated)
This is the cheap-first de-risk with ABSTRACT downstream regions. What is genuinely demonstrated (adversarially confirmed): the GNW **broadcast + sustained-ignition-dependence + dissociation ARCHITECTURE** on the spiking substrate — the ignited workspace content is broadcast to multiple downstream consumers; a causal swap flips all together; the consumers depend on the SUSTAINED workspace attractor (not the input drive), verified by the surgical recurrence-only lesion; and the GO gate rejects the nulls (no-broadcast, scrambled-read).

What is a STAND-IN / by-construction (the sharpened scope):
- **`reason` is a content-selective consumer, NOT a genuine multi-premise/multi-hop reasoner.** The A→report and A→reason projections are STRUCTURALLY IDENTICAL (same 4000 edges, weight 12.0, frozen gate) → report and reason rates are BYTE-IDENTICAL (|rep−rea| < 1e-9 every seed). So the "report == reasoning identity" is **architecturally guaranteed by symmetric wiring of two readouts of the same source**, NOT an emergent shared representation of a genuine reasoning computation.
- **The dissociation is a deliberately-wired demonstration, not an emergent property.** "Who survives the workspace lesion" is a pure wiring choice: report survives only because it has a peripheral direct path; give the SAME peripheral path to `reason` and it survives too (the skeptic showed roles cleanly reverse). This is a FAIR model of the paper's structural hypothesis (Dehaene-Changeux / Gurnee: peripheral faculties — perception/recall/speech — have non-workspace pathways, reasoning is workspace-dependent), stated explicitly in the runner docstring, but it is constructed, not discovered.

The genuine strengthening — wire the project's REAL faculties: the A→W read-out as the `report` (the brain SPEAKS the ignited content) and the EMERGE inference/multi-hop `query_chain` as the `reason` (a genuine reasoning op, NOT a symmetric readout) — is **Rung-3b**, and it is the direct project-value payoff: it makes "the concepts the brain can SAY are the ones it REASONS with" literally true for the actual conversational brain, on one workspace. Only Rung-3b can demonstrate an EMERGENT shared representation (vs the by-construction symmetric wiring here).

## What this establishes toward the unified spiking GNW (+ project value)
Rung-1 gave ignition; Rung-2 gave single-content mutual exclusion; Rung-3 gives the **report==reasoning broadcast + dissociation** — the architecture by which one ignited workspace assembly is simultaneously what the brain SAYS and what it REASONS with, made true by construction. This is the GNW gate's core deliverable: the spiking substrate supplies the recurrent ignition + broadcast a feed-forward transformer only emulates over depth (and, per the paper §9.3, cannot confirm as true ignition/broadcast). The project payoff (Rung-3b): unify the existing SPEAK (A→W) + REASON (EMERGE inference) + moat (access gate) faculties into ONE explicit workspace — the communication-with-a-reasoning-brain north star.

## Files
`research/runners/_gnw_rung3_report_reasoning_identity_derisk.py`; `research/findings/raw/_gnw_rung3_seed{42,43,44,100,101,102}.json` (6-seed). Adversarial-verify Workflow `wtiri7pu8`.
