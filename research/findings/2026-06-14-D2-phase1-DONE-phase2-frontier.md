# D2 dendritic-cortex build — Phase 1 DELIVERED + fully verified (the protected on-bridge dendritic divisive gain, byte-identical when off); Phase 2 (the learned graded cortex) first realization is an honest NEGATIVE that localizes the real frontier: the concept→code readout on the spiking substrate

**Date:** 2026-06-14. **Owner-approved** (the D2 build, after the D1/D1.5/D1.6/D1.7 de-risk ladder landed GO/SURVIVES). **Plan:** `docs/plans/2026-06-14-D2-dendritic-cortex-build-plan.md`. **Phase-0 gate:** `2026-06-14-dendritic-D1-cheap-derisk-GO.md` (D1.7 SURVIVES).

> **Phase 1 (the protected `sim/` edit) is DELIVERED and fully verified.** A dendritic per-presynaptic-source divisive gain now exists on the bridge: each presynaptic source's spike is scaled by a bounded gain `g_i = σ/(σ + a_i)` (`a_i` = that source's firing-rate EMA), suppressing high-frequency common sources toward 0 and passing rare informative ones near 1 — the per-input divisive normalization a single-soma point neuron provably cannot do, realized at the synaptic input. It is a small, guarded, default-OFF edit (five sites; the gain stays unreached unless `enable_dendritic_divisive_gain=True`). **Verified:** the mechanism works on a real bridge (a high-activity source's drive is suppressed to 44.6%); byte-identity-when-off is confirmed at the project's bar — **18/18 GPU conversational + composition tests pass verbatim** (incl. the no-confab moat), plus 4 dedicated tests + the numpy step-loop integration suite. **Phase 2 (the learned graded cortex embedding) — first realization NEGATIVE, and it localizes the genuine frontier:** the cortex-scale concept→code readout on the spiking substrate, NOT the gain mechanism (which is verified).

## Phase 1 — the protected edit (DELIVERED)
**Five guarded sites** (`sim/config.py` flag + 3 fields; `sim/bridge.py` None-declaration + guarded allocation + the gain at the conductance matvec + the per-source EMA update). The gain scales `prev_fired_float` (the presynaptic firing vector) before the excitatory/inhibitory conductance matvec; the coincidence/GABA_B/NMDA blocks re-cast from `cp_prev_firing_states`, so the gain is scoped to the main drive. The gain is in (0,1] (pure suppression, never amplifies → cannot destabilize the hot path). All sites guard on `cp_dendritic_source_activity is not None` (None unless the flag is on) → byte-identical when off, mirroring `enable_gabab`/`enable_coincidence_detection`.

**Verification:**
- `tests/test_dendritic_divisive_gain.py` 4/4: (1) off → EMA None + deterministic across builds; (2) on → EMA allocated + grows; (3) the gain SUPPRESSES a high-activity source (target excitatory conductance off 0.0042 → on 0.0019, **ratio 0.446** = the divisive normalization on the real bridge); (4) σ-huge ≈ off (spike mismatch 0.000000).
- **Byte-identity-when-off at the project's bar:** `tests/test_brain_conversational_agent.py` + `tests/test_core_sim_composition.py` = **18/18 pass verbatim on GPU** (incl. the no-confab moat assertions) — the protected edit does not alter the conversational pipeline when off.
- Stash-comparison confirmed the 10 pre-existing numpy-incompat `test_regions`/`test_neuromodulators` failures are NOT introduced by the edit (identical on clean code); `test_numpy_backend_integration` passes identically with the edit.

The diff is small + reviewable for the owner's byte-level review (the standing rule for protected edits).

## Phase 2 — first realization NEGATIVE; the frontier is the spiking concept→code readout
The plan's Phase-2 reframe: the Option-C readout computed codes from the learned recurrent `W` (a numpy spreading-activation the synaptic-current gain doesn't touch), so Phase 2 must read codes via a **bridge forward pass**. The first realization (`research/runners/dendritic_cortex_forward_codes_derisk.py`): drive a hub region by each concept's co-occurrence pattern → a random dense projection to a readout region → read the readout's gain-normalized activity as the code; gain ON vs OFF (the point-neuron control), structure recovery vs the a-priori taxonomy.

**Result: NEGATIVE across six diagnostic iterations** (firing-read → conductance-read; raw-count → presence drive; drive-scale sweep; fast → slow EMA for marginal-vs-instantaneous normalization). The readout codes are consistently **anti-correlated** with `S_true` (≈ −0.07, vs host +0.96), and the gain has **≈ zero differential effect** (dendritic ≈ point-neuron at every setting). Diagnosis (the root causes, per systematic-debugging — stopped tweaking at the iron-law threshold):
1. **The rheobase threshold silences the rare, informative category hubs** (low count → below firing threshold) while the common hubs fire — the category-distinguishing signal is lost at the hub layer before the gain can act.
2. **The spiking forward pass + random readout projection do not linearly preserve the hub-profile cosine** — even the raw (gain-off) readout codes are anti-correlated with the structure that is +0.96 in the counts, so the readout architecture itself is scrambling/inverting the linear structure.
3. **The per-source EMA does instantaneous, not marginal, normalization** unless very slow — and even a slow EMA didn't rescue it, because (1)+(2) dominate.

**Precise localization (CPU diagnostic `research/runners/_dendritic_cortex_diagnose.py`, + a drive sweep):** the failure is the spiking READOUT, not the gain or the input. The presence-drive input pattern carries strong structure (**Pearson +0.923** vs S_true); as the hub drive rises above rheobase the **hub firing** climbs from anti-correlated toward 0 (it partially preserves the input), and the dendritic-vs-point-neuron gap emerges (drive 250: dendritic +0.022 vs point-neuron +0.004); but the **readout `g_e` code stays ≈0/anti-correlated at every setting** — the random projection + Izhikevich readout dynamics do not linearly preserve the hub-profile cosine. So the structure is present in the input and partially in the hub layer, and is lost at the spiking readout. (At very low drive the near-silent hubs also leave the per-hub EMAs barely differentiated — common 0.0065 vs category 0.0049 — so the gain is near-uniform; reliable firing at higher drive fixes that part but the readout-scrambling dominates.)

**This is an honest, valuable frontier finding, not a failure of the mechanism.** The gain *mechanism* is verified (Phase-1 bridge tests + D1.7 spiking two-compartment). What Phase 2's first attempt localizes is that the **cortex-scale concept→code readout on a spiking substrate is the genuine open problem** — the spiking dynamics (threshold, the projection) don't behave as the rate-level D1.x toys assumed. This is the same *class* of problem the dual/CLS pipeline (Phase 3) exists to handle (raw spiking activity is not a clean linear code → decorrelated codes + attractor cleanup). The next Phase-2 iteration needs a different concept→code architecture (candidates: a learn-based route where the gain modulates the recurrent Hebbian growth during co-fire — the reframe may have been too quick that the gain can't touch the learn; an above-threshold rate-preserving encoding; or reading codes through the dual/CLS cleanup rather than a raw projection).

## Status + honest next
- **Phase 0: PASS. Phase 1: DELIVERED + verified (the protected edit is clean + byte-identical when off).** These are solid, committed deliverables.
- **Phase 2: the first forward-pass realization is an honest NEGATIVE that maps the real frontier** (the spiking concept→code readout). The next step is a fresh Phase-2 architectural design for how the cortex reads graded concept codes from the gain-normalized spiking activity — best done with fresh design thought, not a parameter hunt. Per the project standard, this honest NEGATIVE + diagnosis IS the deliverable; it narrows the Phase-2 design space.
- The owner's byte-level review of the Phase-1 protected diff is invited (5 guarded sites, default-OFF, byte-identical-when-off proven by the 18-test conversational suite).
