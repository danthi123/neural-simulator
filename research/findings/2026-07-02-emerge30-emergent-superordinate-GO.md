# EMERGE-30 / toward-semantics — GO (6/6 seeds): EMERGENT STRUCTURE FROM EXPERIENCE (the master-directive core). The shared superordinate is NOT host-designed — it EMERGES from a co-occurrence stream, unsupervised, and the validated inheritance rides the LEARNED grouping. This closes the last honest residual of the whole inference arc (EMERGE-26/27/28 all rode hand-assigned is-a codes). NO `sim/` edit.

**2026-07-02 (autonomous; the emergent-structure-from-experience research gate's recommended de-risk).** Runner `research/runners/_emerge30_emergent_superordinate_derisk.py`; CI guard `tests/test_emerge30_emergent_superordinate.py` (4 tests). Reuse-by-import (`_emerge14` + `_emerge12`); NO `sim/` edit; CPU numpy-backend; 6-seed.

## The residual it closes
EMERGE-26/27/28 proved the substrate INFERS over relational structure — but the structure was HOST-DESIGNED (`SUPER = {"robin":"BIRD", ...}`, a dict literal; the shared "BIRD" columns hand-assigned). The master directive requires EMERGENCE: the structure must be LEARNED FROM EXPERIENCE, not told. EMERGE-30 discovers the category from co-occurrence: members that appear in the same contexts DEVELOP a shared representation, and a property taught to that emergent shared representation is inherited by a member that only ever CO-OCCURRED with the context (never told the category, never told the property).

## The claim
Latent categories BIRD{robin,sparrow,canary}, FISH{trout,salmon,pike} — **NEVER labeled**. The brain only OBSERVES each member co-occurring with its category's context tokens (the environment); the committed `sim/` three-term kernel learns member-content → context (on-bridge Hebbian co-occurrence). All members of a category learn the same context cells → those cells are the EMERGENT superordinate. A class property is taught on the emergent shared cells ("bird-context flies", "fish-context swims"). Asking about a member is a 2-hop read: member-content primes its emergent context (learned), the context primes the property. 6/6 seeds:
- **Inheritance 1.00:** every member infers its class property — `robin/sparrow/canary → flies`, `trout/salmon/pike → swims` — though NONE was told its category or property.
- **Held-out 1.00:** the highlighted held-out members (canary, pike) inherit purely via the co-occurrence-learned grouping.
- **Moat 1.00:** a never-observed member has no learned context → abstains (no confabulation).

## Anti-cheats (all airtight, 6/6)
- **PERMUTED-CONTEXT** (each member co-occurs with an independently random context every stream epoch → no consistent per-category grouping): inheritance collapses to **0.50 (chance)** on every seed — isolating the LEARNED co-occurrence structure as the cause (the category is discovered from experience, not from anything hand-designed about the members).
- **NO-LEARNING** (skip the stream → no member→context): abstains, inheritance **0.00**.
- **dAP-LESION** (coincidence off): collapses to **0.00**.
- **MOAT 1.00**; 6-seed unanimous.

## Significance — the last honest residual of the inference arc is CLOSED
Every prior inference finding (EMERGE-26/27/28) closed with the SAME caveat: "the is-a structure is host-designed; this is inference-OVER-structure, not acquisition-OF-structure-from-experience." EMERGE-30 removes that caveat: the substrate now ACQUIRES the relational grouping from a co-occurrence stream AND infers over it. This is the master-directive core — a brain that learns its structure from experience — on the emergent spiking substrate, NO `sim/` edit. Grounded in HTM Spatial-Pooler competitive learning (Cui-Ahmad-Hawkins 2017: input-similarity-preserving SDRs) and the feature-prediction theory of taxonomic inheritance (Saxe-McClelland-Ganguli 2019, PNAS), realized with the project's validated on-bridge Hebbian co-occurrence learning.

## Honest scope + next
- The context TOKENS are the environment (legitimate world/experience); the category GROUPING (which members belong together) is DISCOVERED from co-occurrence, not told — the permuted-context control isolates exactly that. One shared token per category is the clean cheap-first; overlapping/varied contexts (each member a random subset of a category's feature pool) is the robustness follow-on.
- Follow-ons: an HTM Spatial Pooler that forms a NEW shared column block (rather than reusing the context cells as the superordinate); cancellation on emergent codes; coupling the emergent grouping into the EMERGE-29 conversational console (learn categories by observation, then answer inference questions about them).

## Artifacts
`research/runners/_emerge30_emergent_superordinate_derisk.py`, `tests/test_emerge30_emergent_superordinate.py`, `research/findings/raw/_emerge30_emergent_superordinate.json`. Prior: `2026-07-02-emergent-structure-from-experience-research-gate.md`, `2026-07-02-emerge28-transitive-inference-GO.md`, `2026-07-02-emerge26-emergent-inheritance-GO.md`.
