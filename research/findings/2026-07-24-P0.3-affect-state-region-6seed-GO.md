# P0.3 persistent affect-STATE region — 6-seed GO: a slow-NMDA opponent attractor holds a mood that persists, integrates history, and CAUSALLY biases cognition (the EMOTION keystone) (2026-07-24)

The genuine-cognition pivot's emotion keystone. A real on-bridge spiking affect region (3 opponent NMDA pools
affect_vplus/affect_vminus/affect_arousal on a numpy SimulationBridge; reuse-by-import, **NO `sim/` edit**) that holds
a persistent core-affect (valence×arousal, Russell circumplex; Namburi-Tye opponent BLA populations; catalog O.58),
updated by the DR-2 learned valence tags via the diffuse neuromodulator bus.

## Result — GO 6/6 (`_affect_state_region_derisk`)
- **PERSISTENCE (slow-NMDA attractor):** after a same-sign appraised burst then drive-OFF, mood retains **0.68 of peak
  with NMDA-on vs 0.00 with NMDA-off** — the persistence is the slow-NMDA reverberatory attractor (cloned from the
  dlPFC WM latch operating point), not residual input. The attractor holds a GRADED mood (circumplex continuum), not a
  bistable good/bad latch — the open-risk resolved on the good side.
- **HISTORY INTEGRATION:** the mood tracks the running-mean appraised valence at **r=0.68 vs shuffled-history 0.12** —
  it genuinely integrates the event sequence, not the last event / a static lookup.
- **CAUSAL BIAS (the load-bearing proof):** mood-congruent recall advantage **Δ +0.030 → 0% under affect-LESION**
  (Bower mood-congruent memory, causal not correlational); high-arousal **speak-margin +0.055** vs baseline
  (Damasio somatic bias gains the spiking speak/silence accumulator). Yoked-random affect misdirects the bias.
- **DOMAIN DISSOCIATION (value⟂plausibility):** corr(valence axis, factual plausibility) **r=+0.102** (|r|<0.15) — the
  affect axis is its own circumplex dimension, NOT relabeled how-likely-a-statement-is. Emotion ≠ probability.
- All anti-cheats hold 6/6 (persistence NMDA-on≥0.5 & off<0.1, congruent-recall Δ>0, lesion-collapse≤0.2, yoked-misdirect,
  speak-margin>0, value⟂plausibility). cfg.seed set.

## Significance
The affective world-model keystone the pivot named: a brain-based (V+/V- rate differential, never a host variable)
persistent emotional STATE that is set by learned appraisals and causally colours memory + speech. Everything downstream
in the pivot (mood-congruent reasoning, felt-emotion correlates, the GNW-broadcast affect) reads from this region.
Follow-on: wire it into the conversational loop + the GNW workspace; the felt-emotion correlate battery (walls-ledger).
