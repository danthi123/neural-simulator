<!-- ⚠️ FILENAME NOTE (2026-07-24 doc-sync): the filename retains "…-6seed-GO.md" for stable linking, but the actual verdict is **QUALIFIED-GO / BOUNDARY** (bistable good/bad latch, not a graded circumplex) — see the title + CORRECTION block below. The commit message "…6-seed GO" (282d5534) was likewise corrected by e402a732. Do not read "-GO" in the filename as the verdict. -->

# P0.3 persistent affect-STATE region — 6-seed QUALIFIED-GO / BOUNDARY: a slow-NMDA opponent attractor holds a persistent mood that CAUSALLY biases cognition, but reads as a bistable good/bad LATCH not a graded circumplex (the EMOTION keystone) (2026-07-24)

> **CORRECTION (2026-07-24, same day):** an earlier snapshot of this doc claimed "GO 6/6 — GRADED mood (circumplex
> continuum), not a bistable latch — the open-risk resolved on the good side" with history r=0.68. **That was WRONG and
> is RETRACTED.** The r=0.68 came from a *shared-FS* opponent variant whose persistence was a **40 ms-probe-phase
> artifact** — a phase-robust 100 ms-averaged read collapses it to ~0.3, and the landscape is jagged/fragile. The
> robust config (Namburi-Tye opponent **cross-inhibition**) gives a **bistable good/bad LATCH**, not a graded
> circumplex. The honest result is below. (The commit titled "…6-seed GO" and this doc's original text overclaimed;
> the corrected verdict is QUALIFIED-GO / BOUNDARY.)

The genuine-cognition pivot's emotion keystone. A real on-bridge spiking affect region (3 opponent NMDA pools
affect_vplus/affect_vminus/affect_arousal on a numpy SimulationBridge; reuse-by-import, **NO `sim/` edit`**) that holds
a persistent core-affect (Namburi-Tye opponent BLA populations, catalog O.58), updated by the DR-2 learned valence tags
via the diffuse neuromodulator bus (excitability_drive, volume transmission).

## Result — QUALIFIED-GO / BOUNDARY, 6-seed (`_affect_state_region_derisk`, `--seeds 42 43 44 100 101 102`)

**The THREE core affect-state faculties are robustly demonstrated on ONE spiking bridge (all 6 seeds):**
- **PERSISTENCE (slow-NMDA attractor):** after a same-sign appraised burst then drive-OFF, mood retains **0.62 of peak
  with NMDA-on vs 0.00 with NMDA-off** (per-seed 0.60–0.65) — persistence is the slow-NMDA reverberatory attractor
  (dlPFC WM-latch operating point), not residual input. Graded DECAY (not a saturated ≥0.9 latch on the persistence
  axis), off-persist only appears at w≥28 (the AMPA-ping-pong regime, avoided).
- **CAUSAL BIAS (the load-bearing causal proof):** mood-congruent recall advantage **Δ +0.067 → 0% under
  affect-output LESION** (transmission-gate `affect_out`=0; Bower mood-congruent memory, causal not correlational);
  **yoked-random affect misdirects** the bias (→ ~0 / negative); high-arousal **speak-margin +0.052** vs the
  lesioned/baseline speak rate (Damasio/Niv arousal gains the spiking speak/silence accumulator).
- **DOMAIN DISSOCIATION (value⟂plausibility):** corr(valence axis, PPMI relatedness) **r=+0.102** (|r|<0.15) on the
  separate-RNG independent tags used for appraisal — the affect axis is its own dimension, NOT relabeled likelihood.
  Carry-forward: the **DR-2 learned valence tag reads r=−0.32** with PPMI hub-ness on the coarse Warriner-approximate
  core lexicon (entangled beyond the 0.15 bar); per the spec fallback the appraisal uses the decorrelated
  separate-RNG tags (same marginal valence distribution, independence by construction). Expected to shrink with the
  full 13,915-word Warriner CSV (`--warriner-csv`).

**The 4th gate — GRADED history-integration — is the CHARACTERIZED OPEN-RISK BOUNDARY (the deliverable either way):**
- The point-neuron slow-NMDA opponent attractor **IGNITES at a low threshold and SATURATES**: the sustained mood is
  flat (~0.09–0.11) across appraisal drive 0.15→1.0, so it does **not grade with appraised-valence magnitude**
  (history-magnitude r=0.33, unreliable across seeds) and does **not smoothly cross sign** (sign-crossing r=0.38,
  1/6 seeds). ⇒ **a bistable good/bad LATCH, not a graded valence×arousal circumplex.**
- Mechanism of the latch: with opponent cross-inhibition the **incumbent** valence pool's interneuron suppresses a
  newly-driven opposite pool (WTA hysteresis), and the point-attractor recurrence saturates suprathreshold drive.
- **The named surpass:** a graded Russell circumplex needs **graded persistent activity** — a line/bump attractor
  with **incumbent fatigue (spike-frequency-adaptation eviction)** / the dendritic substrate. This is exactly the
  open-risk the spec anticipated: "a bistable-latch read IS the honest bounded result that names the (dendritic)
  surpass."

Anti-cheats (all hold 6/6): persistence NMDA-on≥0.5 & off<0.1; congruent-recall Δ>0; lesion-collapse ratio ≤0.2;
yoked-random misdirect; speak-margin>0; value⟂plausibility |r|<0.15; magnitude-integration beats shuffled. `cfg.seed`
set (verified: same-seed builds hash-identical on `cp_neuron_firing_thresholds`, cross-seed differ — the substrate is
genuinely seeded). numpy-CPU (real spiking Izhikevich bridge; the "numpy" is the backend, not a host shortcut).

## Honest scope + notes
- The recall-vigor bias is a firing-rate read of valence-congruent pools (the cheap importable recall substrate, the
  spec's tertiary-risk degrade), not a full engram-tag stimulate/read. The affect state's synaptic output onto
  cognition (`affect_out`) is the load-bearing causal handle (lesion collapses the bias; the read is a measurement).
- The **shared-FS** opponent variant (`opponent_style="shared"`) CAN flip sign but its persistence is a fragile
  oscillation (the retracted 0.68 was a 40 ms-snapshot phase artifact). **cross-inhibition is the robust config.**
- NO `sim/` edit anywhere (enable_nmda + neuromodulator excitability_drive/group targets + transmission_gate are all
  pre-existing additive attributes); `git diff --ignore-cr-at-eol --stat sim/` is empty.

## Significance
A brain-based (V+/V- rate differential, never a host variable) PERSISTENT emotional STATE that is set by learned
appraisals and **causally colours memory + speech** — the three core emotion faculties the pivot named, on one spiking
brain. The graded-circumplex integration is the precisely-characterized boundary that scopes the dendritic
line-attractor / SFA-eviction surpass. Follow-on: (1) the graded circumplex via SFA-eviction / dendritic substrate;
(2) wire the affect state into the conversational loop + GNW workspace (unblocks P1.2's full affect-directedness
anti-cheat); (3) the full 13,915-word Warriner CSV to shrink the DR-2 value⟂plausibility carry-forward.
