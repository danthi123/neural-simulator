# GNW Rung-1 — a spiking Global Neuronal Workspace shows a CLEAN ALL-OR-NONE IGNITION THRESHOLD (6-seed GO, adversarially verified): a recurrent workspace assembly latches a sustained self-reverberating limit cycle above threshold, dies out below, and the recurrence is load-bearing

**Date:** 2026-07-07
**Runner:** `research/runners/_gnw_rung1_ignition_curve_derisk.py` (reuse-by-import of the dlPFC self-attractor recipe + FS lateral inhibition; NO `sim/` edit).
**Verdict:** 6-seed GO (42/43/44/100/101/102), numpy/CPU. Cheapest-first de-risk of the GNW research gate (`2026-07-07-GNW-spiking-global-workspace-research-gate.md`, Rank 1) — the load-bearing "IGNITION" property of a Global Neuronal Workspace on the spiking substrate.
**Adversarial-verify:** Workflow `wm01lvqaz` (3 independent skeptics + synthesizer, each running its own controls). **Outcome: the core ignition claim SURVIVED; the verify caught + I fixed a real anti-cheat bug (a leaky reset) and corrected two overclaims (NMDA-specificity, "fixed point").** This is the discipline working — a confounded GO caught and corrected BEFORE commit.

## The result (6-seed, after the adversarial-verify corrections)
A `workspace` region (300 exc neurons; a dense self-recurrent ASSEMBLY of 80 at E→E weight 30, `enable_nmda=True`) + an inhibitory `workspace_fs` lateral-inhibition region shows the Dehaene-Changeux (2011) **die-out-vs-ignite** property: a brief drive pulse into the assembly either latches a SUSTAINED self-reverberating state (persisting after the drive is removed) or dies to a low residual — bistable, all-or-none.

Measured on the **late-window settled rate** (mean over the last third of a 100-step free run AFTER the drive is removed), with the corrected **snapshot-restore reset** (each amplitude starts from the identical quiescent state — order-invariant, == a fresh bridge), all 6 seeds are **byte-identical in structure**:
- **Clean MONOTONE ignition threshold** — only the top two of 12 swept amplitudes ignite (`ignited_idx = [10, 11]`, ~2273 and ~2500 pA) on EVERY seed. No basin-dependence, no phantom mid-range ignitions (see "Adversarial verification" — the earlier "basin-dependent non-monotonic" narrative was a leaky-reset artifact, now removed).
- **Ignited plateau = 0.3333** on every seed — a persistent self-sustaining state.
- **Off-state ≤ 0.053** (0.12–0.16× the plateau) — a clear BIMODAL GAP: every settled state is either the ignited plateau or a low residual, nothing in the intermediate band → **all-or-none**.
- **LESION (recurrent weight → 0) → 0.0000 at every amplitude** → the sustained state is the RECURRENCE, not the input (recurrence load-bearing, 6/6).
- **A=0 → 0.0000** → no spontaneous ignition.
- `ignites` ∧ `bimodal_all_or_none` ∧ `recurrence_load_bearing` ∧ `baseline_ok` = True on all 6 seeds.

## Adversarial verification (Workflow wm01lvqaz — 3 skeptics, each running own controls)
All three returned SURVIVES_WITH_SCOPE (none REFUTED the core), and each surfaced a real correction I applied before committing:

1. **Attractor-genuineness → SURVIVES (with a wording fix).** The 0.3333 plateau holds at exactly 0.3333 over 600+ free steps with NO decay (not a slow transient the 100-step window missed) and collapses to ~0.0008 under lesion — a genuine persistent recurrence-dependent state. BUT the raw per-step counts show it is a **period-3 synchronous LIMIT CYCLE** (all 80 assembly neurons fire together every 3rd step: `[80,0,0,80,0,0,…]`), an Izhikevich-refractory + synchronous-recurrent limit cycle, not a stationary async fixed point. **Fix:** "settled fixed point" → "settled limit cycle" throughout.

2. **NMDA-specificity → REWORDED (the framing was wrong; the ignition is not).** The runner framed ignition as NMDA-dependent, but the lesion only zeroes the recurrent WEIGHT. The skeptic ran the missing control — **NMDA OFF at the same weight 30 (two independent methods, byte-identical): AMPA-only recurrence STILL ignites on all 6 seeds** (plateau ~0.147, off-state 0.000 — an even cleaner all-or-none, verified a flat non-decaying plateau over 300 free steps). The recurrent E→E synapses always carry a fast AMPA component (bridge.py:6338-6357); the NMDA mask only gates the *added* NMDA current. **⇒ the ignition is GENERIC recurrent-attractor bistability; NMDA MODULATES (raises the plateau ~0.15→~0.33, narrows the ignition band) but is NOT necessary.** The CLAUDE.md/nav note "30 = genuinely NMDA-dependent; 50 = AMPA ping-pong" is contradicted on this substrate. **Fix:** removed the NMDA-specificity claim from the runner docstring + this finding; the load-bearing control is the weight→0 lesion (recurrence), not NMDA.

3. **Basin-reset-integrity → a REAL anti-cheat BUG, fixed.** The "basin-dependent, non-monotonic" narrative (3–5/12 amps ignite) was an ARTIFACT of a leaky reset: `_reset_quiescent` zeroed conductances + settled 40 steps but NEVER reset v/u/firing, so a persisting limit-cycle latch survived the "reset" and carried over between amplitudes → sweep-order-dependent ignited sets (ascending ≠ descending ≠ shuffled). The skeptic's controls: **fresh-bridge-per-amplitude gives a clean monotone threshold `{10,11}` on all 6 seeds**; a direct carry-over test (identical amplitude, opposite outcome depending on the prior amplitude) and a state dump (v/u/g_nmda all non-quiescent after the "reset") isolated the cause; a clean hard reset collapses the ignited set back to `{10,11}`. **Fix (root cause, systematic-debugging):** `_reset_quiescent` now snapshots the post-init quiescent state (v/u/firing/conductances) and RESTORES it each trial (the EMERGE-61 wash-out pattern) → the sweep is order-invariant == fresh-bridge, and the result is the clean monotone threshold reported above. This is a *better, more rigorous* result than the artifact version.

## What this establishes (toward the unified spiking GNW)
The GNW gate's genuine residual — "no single explicit workspace pool with an all-or-none IGNITION" — now has its load-bearing primitive demonstrated + adversarially verified on the spiking substrate: a recurrent + FS-lateral-inhibition assembly that IGNITES all-or-none at a clean threshold, holds a sustained self-reverberating limit cycle, requires its recurrence, and never fires spontaneously. This is the recurrent ignition a feed-forward transformer only emulates over depth and (per the GWT paper §9.3) cannot confirm — the project HAS it. Honest scope: the ignited state is a synchronous period-3 limit cycle (a reduced-model artifact of the Izhikevich refractory + synchronous recurrence — a richer async attractor would need heterogeneity/noise, an optional polish); the ignition is generic recurrent-attractor bistability, with NMDA a modulator not a cause.

## Next rungs
- **RANK 2 (next, building):** competitive access — ≥2 candidate assemblies on the workspace pool with mutual inhibition; only ONE ignites (WTA crossover); the causal-swap membership test (swapping the salience flips which is reported).
- **RANK 3:** broadcast-back drives multi-hop reasoning — the ignited assembly is what the brain SAYS (via A→W) AND what it REASONS with (report-reps == reasoning-reps by construction; the paper's §3.5.2 dissociation).

## Files
`research/runners/_gnw_rung1_ignition_curve_derisk.py`; `research/findings/raw/_gnw_rung1_seed{42,43,44,100,101,102}.json` (6-seed). Adversarial-verify Workflow `wm01lvqaz`.
