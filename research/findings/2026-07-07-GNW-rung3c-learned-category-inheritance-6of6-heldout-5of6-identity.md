# GNW Rung-3c — the workspace reasons by inheritance through a LEARNED (not hand-wired) category: 6/6 clean (identity, held-out inheritance, permuted-collapse, no-learning-abstain), closing Rung-3b's hand-wired-structure scope

**Date:** 2026-07-07
**Runner:** `research/runners/_gnw_rung3c_learned_inheritance_derisk.py` (reuse-by-import of Rung-1 assembly-loop + Rung-3 dense-projection + the on-bridge rate-Hebbian; NO `sim/` edit).
**Verdict:** **6-seed GO 6/6** (42/43/44/100/101/102), numpy/CPU — identity 6/6, held-out learned inheritance 6/6, permuted-collapse 6/6, no-learning-abstain 6/6. Closes the adversarial-verified scope of Rung-3b (whose taxonomy was HAND-WIRED).
**Adversarial-verify:** Workflow `wvfbcc9sn` (learned-genuineness + het-load-bearing/caveat lenses) — the discipline WORKED: it **REFUTED an earlier het+noise-necessity overclaim BEFORE commit and drove the result from 5/6 → a clean 6/6.** See "The correction" below.

## The result (6-seed, 6/6)
The category structure (member→superordinate) is LEARNED from CO-OCCURRENCE, not hand-placed. The workspace ignites a member; a `context` region (the emergent superordinate) is driven by a PLASTIC member→context pathway (the ONLY learning pathway); `report` reads the member's distinctive identity (fixed), `reason` reads context→property (fixed). STREAM: co-drive each member + its category's context tokens → the on-bridge rate-Hebbian (bridge.py:6923-6931, pre(t-1)∧post(t) coincidence) LEARNS member→context (bird-members converge on the bird-context cells). QUERY: ignite a member → its LEARNED member→context fires the emergent superordinate → context→property.
- **HELD-OUT LEARNED INHERITANCE (6/6, the killer claim):** the held-out `canary` is streamed with bird-context but NEVER taught the property; ignite canary → report=canary ∧ **reason=flies**, inherited via the LEARNED shared BIRD-context, on all 6 seeds. The category itself was ACQUIRED (verified: floor 0.010 before streaming → canary→BIRD grows to ~16, canary→FISH stays at the 0.05 floor; no direct canary→property edge exists).
- **IDENTITY (6/6):** every taught member also infers correctly (robin→flies, salmon→swims) on all 6 seeds.
- **PERMUTED-CONTEXT collapse (6/6):** re-stream with each member's category-context SHUFFLED (the held-out displaced to a different category) → canary ACTIVELY infers the displaced property (swims), not flies — the inference FOLLOWS the learned co-occurrence (a stronger result than a bare collapse).
- **NO-LEARNING abstain (6/6):** skip the stream → member→context stays at floor → reason rates a true 0.0 → abstain.

## The real mechanism (corrected — het+noise was a red herring)
The on-bridge rate-Hebbian learns member→context because during streaming BOTH the member assembly AND its category's context tokens are **force-driven every step** (MEMBER_DRIVE_PA / a strong CONTEXT_DRIVE_PA=2200), so both populations fire densely and the pre∧post coincidence fires abundantly — the internal firing PHASE is irrelevant. The two fixes that made it work: **(1) `hebbian_weight_decay=0`** — the default every-step decay (bridge.py:6948) otherwise collapses the sparse potentiation to the ~0.05 clip floor; **(2) a strong context drive** so the driven category fires densely. The member→context all-to-all is FLOOR-init (0.01) so it does not itself drive the context (keeping the coincidence selective: robin fires with the DRIVEN bird-context → robin→BIRD grows; fish silent → robin→FISH stays at floor).

## The correction (adversarial-verify wvfbcc9sn — drift-mode-#8 caught before commit)
An earlier draft of this finding claimed het+noise DESYNC was **load-bearing** ("the synchronous period-3 limit cycle makes the coincidence never fire → no learning"). The verify REFUTED this: building with `heterogeneity=False, ou_noise_pA=0.0` (deterministic), the learning is **6/6 GO on every gate** — strictly BETTER than the het+noise config (5/6). Under sync the streaming actually produces MORE coincidence (6400 vs 3640), because the strong external drive dominates the dynamics. Worse, het threshold-variance was the SOLE cause of the earlier seed-44 failure (a taught member locking in a spurious SECOND category, the pure-potentiation Hebbian having no depression to prune it). **Fix applied:** het+noise defaulted OFF → clean 6/6; the docstring's synchrony-rationale corrected. The verify also flagged that the FS mutual-inhibition pathways (declared `plastic=False`) still drifted under the global Hebbian (they lacked a freeze gate) — confirmed NOT to route the inheritance (a single shared 50-neuron pool, near-uniform across members), but **fixed cleanly** by tagging them with the frozen `WS_LOOP_GATE` (zeroes their plasticity rate, leaves inhibitory transmission intact). member→context is now the ONLY learning pathway. Learned-genuineness SURVIVES: floor-before / selective-after weight audit, no direct held-out→property path, permuted follows-scramble, no-learning true-zero abstain.

## What this establishes
Rung-3b showed a genuine 2-hop held-out inheritance on a HAND-WIRED taxonomy; Rung-3c makes the taxonomy **LEARNED from co-occurrence experience** — the held-out member inherits via a category the brain DISCOVERED, on the spiking workspace, with the permuted + no-learning controls proving the learned co-occurrence (not hand-placement) carries it. Combined with Rung-1/2/3/3b, the GNW workspace now: ignites one content, mutually excludes, reports==reasons with a dissociation, and reasons by inheritance through a discovered category — all 6-seed GO, transformer-free, NO `sim/` edit. The GNW gate's thesis (the spiking substrate supplies the workspace + emergent structure) is validated end-to-end.

## Files
`research/runners/_gnw_rung3c_learned_inheritance_derisk.py`; `research/findings/raw/_gnw_rung3c_seed{42,43,44,100,101,102}.json` (6-seed). Adversarial-verify Workflow `wvfbcc9sn`.
