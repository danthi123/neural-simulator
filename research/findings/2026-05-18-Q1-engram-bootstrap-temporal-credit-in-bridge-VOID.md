# Q1 engram-bootstrapped temporal-credit GENERATIVE composition (in-bridge) — honest VOID at the pre-registered full scale: the reward-FREE engram bootstrap dissolves n_rewarded=0 at TOY scale but NOT at feasible full scale (the SAME spiking-bootstrap blocker, 6th-direction triangulation); NOT scale-confident, NOT a refutation of the engram/temporal-credit principles, NOT spun; autonomous non-stop pivot to Q2 (NO owner-deferral)

## TL;DR

After the owner reframed the deliverable to **scale-confidence** (a
working local PoC at small capacity + evidence of NO architectural
ceiling across a pre-registered local scale ladder, so scale-up is
justified) and corrected the process (a VOID/BOUNDARY must trigger an
immediate PIVOT to the next genuinely-distinct architecture, NOT a
stop or owner-deferral), Q1 of the pivot queue was built end-to-end:
the validated reward-FREE Tonegawa engram bind (catalog D.14) used to
BOOTSTRAP the rewarded episode the compose-bridge VOID provably lacked
(`n_rewarded=0`), so the thrice-validated temporal-credit/eligibility
mechanism could GENERATIVELY refine it, in a MINIMAL slice of the real
spiking concept-pool architecture.

The full disciplined arc ran: design -> writing-plans ->
subagent-driven build (fresh subagent per task; Task-0 grounding pin;
pure scale-confidence aggregator; the engram-bootstrap in-bridge
wiring) -> **dedicated adversarial reviewer BEFORE Phase B** (7 probes:
6 CLEAN with RNG-stream tracing + 18 adversarial verdict inputs + smoke
re-run; 1 WEAK -> STRENGTHEN-only dedicated `perm_rng`, no frozen bar
moved) -> Phase B LOAD-BEARING no-harm (protected 0-byte diff, moat
7/7, suite 23 green) -> controller-only decisive multi-seed multi-rung
in-sim run + MANDATORY anti-cheat smell-test.

**Recorded result (recomputed from the single recorded JSON
`research/findings/raw/engram_bootstrap_gate.json`; NO re-run, NO
bar-tuning, NO config-crank), seeds 42-46, scale ladder B in {4,8,16}:**

- **GATE = VOID at EVERY rung; scale_confident = False; classification
  = VOID.** Pre-registered reason: "V1 unmet: in-bridge TD did NOT
  learn the no-gap verb->motor bind (instrument unsound)" (+ at B=4/B=8
  also non-discriminating).
- **The decisive evidence: `n_rewarded_td = 0` at ALL 15 seed x rung
  cells.** The reward-FREE engram bootstrap produced rewarded episodes
  at TOY scale (the pre-decision smoke: n_rewarded_td = 1/2/1 at B=4
  tiny) but **ZERO at the pre-registered full scale** (B=4/8/16,
  larger pools/steps). With no rewarded episode, the bridge's native
  eligibility->reward path never engages -> `td` is identical to
  `engram_only` (= the inherited `hebbian_no_trace` key) and to
  `permuted`/`wrongsign`, all collapsing to the chance floor (~1/B:
  B=4 ~0.25-0.5, B=8 ~0.125, B=16 ~0.06-0.19). V1 (`nogap_td`)
  0.0-0.25, far below the frozen 0.80 -> genuinely unsound, not a
  marginal miss.
- This is the **SAME `n_rewarded=0` blocker** that VOIDed the prior
  compose-bridge integration. Engram-bootstrap DISSOLVES it at toy
  scale but NOT at feasible full scale: the population-vote
  readout/teacher/reward loop still never bootstraps a rewarded
  episode at the pre-registered scale.

**Verdict (no spin):** Honest **VOID** -- instrument-not-soundly-
constructible at the pre-registered full scale. NOT scale-confident.
NOT a science FAIL in the strong sense (a sound discriminating
instrument with the mechanism nonetheless failing); nothing was
validly measured because V1 itself is unmet and no condition learns.
NOT a refutation of the engram principle (independently multi-seed
validated: 87.5% stim-recall, 90% multitag) NOR of the temporal-credit
principle (thrice-validated: TD-critic PASS, compose-abstract PASS,
pop-transfer GREEN). It is the **6th independent triangulation** that
the recurring blocker is the spiking-dynamics-integration bootstrap,
robust enough that a reward-FREE one-shot engram does not dissolve it
at feasible full scale.

## Honest scope (no overclaim, no underclaim, no spin)

- **NOT scale-confident** (the owner's actual deliverable was NOT
  met): there is no local PoC here -- the instrument is unsound at
  the smallest pre-registered rung (B=4), so there is no "works-small"
  to extrapolate, let alone a non-degrading scale curve. Reported as
  an honest non-success, never as a win.
- **NOT a refutation of engram OR temporal-credit.** Both are
  independently validated. What is NOT constructible at feasible full
  scale is the COMPOSITION instrument (engram bootstrap -> n_rewarded>0
  -> temporal-credit refinement) inside the real spiking concept-pool
  readout/teacher/reward loop -- the same boundary the compose-bridge
  VOID localized, now confirmed robust against the reward-free
  engram-bootstrap dissolution attempt at scale.
- **The toy-vs-full split is the precise, honest diagnosis:**
  n_rewarded_td > 0 at B=4 tiny (8-neuron pools, 4 steps) but = 0 at
  the pre-registered full config (40-neuron pools, B up to 16,
  64*B lang_input, 20/24 encode/stim steps). The engram
  `stimulate_tag` reactivation does not make the population-vote
  argmax select the target pool reliably enough to ever score
  reward=1 once the competing-pool count and pool size grow -- so the
  delayed-reward TD delta stays r-V with r identically 0.
- **NOT config-cranked.** Two root-caused, scope-true subagent
  iterations occurred earlier (Task-1 plan-prose 9->8 corrected
  transparently; Task-3 STRENGTHEN-only `perm_rng`); the frozen
  `_CBR_*` (0.80/0.80/0.35) + `_SCALE_LADDER (4,8,16)` + `_SCALE_TOL
  0.05` are byte-UNCHANGED. Tuning encode/stim/top_k/engram_stim_pA
  until `n_rewarded>0` at full scale would be config-cranking the
  instrument toward a desired outcome past the pre-registered
  terminus -- forbidden. So this honest VOID is propagated and the
  arc PIVOTS (it is NOT deferred to the owner and NOT stopped, per the
  CORRECTED OPERATING MODE the owner mandated this session).

## Why this is NOT an owner-deferral, and the autonomous pivot (CORRECTED OPERATING MODE)

The owner's explicit process correction this session: prior
VOID/BOUNDARY outcomes (dendritic, compose-bridge, PC) were each
followed by a "handed to the OWNER eyes-open" deferral that functioned
as a STOP. That is corrected. An honest VOID is propagated (this doc +
pillar + both remotes) and IMMEDIATELY triggers the autonomous pivot
to the next genuinely-distinct architecture in the durable pivot queue
(`docs/plans/2026-05-18-option-B-...-design.md` CORRECTED OPERATING
MODE section), NON-STOP, no owner-deferral, no pause.

**Pivot: Q1 VOID -> Q2 (two-module CONSTRAINED DECODING).** The Q2
design is already PRE-STAGED durably (commit c494d15,
`docs/plans/2026-05-18-Q2-two-module-constrained-decoding-design.md`):
the validated coherent-simple generator (Generator-F) PROPOSES tokens;
the validated no-confab grounded memory VETOES ungrounded next-tokens
PER-TOKEN at decode time -- genuinely distinct from the Generator-G
NEGATIVE (which measured ~89% drift POST-HOC; Q2 prevents drift at
generation time). Same scale-confidence target + own pre-registered
THREE-STATE + scale ladder + honest ceiling. The arc proceeds to Q2
now.

## What is preserved / validated (unaffected)

Net-new only: `research/runners/engram_bootstrap_gate.py` +
`tests/test_engram_bootstrap_{grounding,scale,smoke,no_harm}.py`. NO
protected/validated module created, modified, or touched: the
no-confab moat (`abstention_gate` + `tests/test_abstention_gate.py`,
7/7 byte-identical throughout), `sim/td_value_critic.py`,
`sim/compose_temporal_bind.py`, `sim/kernels.py`, `sim/bridge.py`,
`sim/neuromodulators.py`, `sim/train_checkpoint.py`,
`research/runners/text_minimal_isolation.py`,
`research/runners/compose_bridge_core.py` (REUSED byte-UNMODIFIED -
the THREE-STATE verdict; no new movable bar), every frozen `*_core`.
`git diff bda6e46..HEAD` on the full protected set is EMPTY. NO
autograd anywhere. The prior validated results (TD-critic PASS,
compose-abstract PASS, pop-transfer GREEN, engram multi-seed, the
no-confab moat) are entirely unaffected.

## Anti-cheat discipline (why this VOID is trustworthy)

The pre-registered THREE-STATE + V1-instrument-validity-FIRST design
+ the pre-registered scale-confidence criterion did exactly their job:
an instrument whose V1 positive control did not learn (and whose
conditions did not separate) produced VOID at every rung, never a
fabricated PASS or a spun "works-small". The dedicated adversarial
reviewer (7 probes, RNG-stream tracing, 18 adversarial verdict inputs)
found 6/7 CLEAN + 1 WEAK -> STRENGTHEN-only, frozen bars byte-
unchanged. Every number recomputed from the single recorded JSON (no
re-run, no bar-tuning, no config-crank); the `n_rewarded_td=0`
evidence is the honest decisive diagnosis. The deeper instrument-
engineering question (a readout/teacher/reward loop that bootstraps a
rewarded episode at full scale) is NOT autonomously config-cranked
and NOT deferred-as-a-stop -- the arc PIVOTS to the genuinely-distinct
Q2 architecture, non-stop. The validated no-confab moat remained
byte-identical and 7/7 green throughout.

## Files / evidence

- Recorded gate output: `research/findings/raw/engram_bootstrap_gate.json`
  (GATE VOID all 3 rungs; per-seed nogap_td 0.0-0.25 << 0.80 [V1
  unmet]; td == engram_only == permuted == wrongsign == chance;
  n_rewarded_td = 0 all 15 cells; recompute-from-JSON, no re-run).
- Pre-decision toy smoke evidence (NOT propagated): n_rewarded_td =
  1/2/1 at B=4 tiny -> the engram bootstrap works at toy scale; the
  VOID is specifically a FULL-scale bootstrap failure.
- Build commits (all controller-verified, protected byte-empty in
  every commit-scoped diff): `979239f` Task-0 grounding pin ->
  `01c2043` Task-1 pure scale_confidence aggregator -> `1fbc8e7`
  plan-prose 9->8 honest correction -> `560e819` Task-2 engram-
  bootstrap in-bridge wiring -> `7611a02` Task-3 STRENGTHEN-only
  `perm_rng` -> `7dce882` Task-4 Phase B no-harm.
- Design/plan: `docs/plans/2026-05-18-option-B-engram-bootstrap-
  temporal-credit-generative-{design,implementation}.md` (CORRECTED
  OPERATING MODE + pre-registered scale ladder + scale-confidence
  criterion).
- Converges with / does NOT refute:
  `2026-05-18-compose-temporal-credit-spiking-VOID.md` (the same
  n_rewarded=0 blocker, now confirmed robust against the reward-free
  engram-bootstrap dissolution at full scale),
  `2026-05-18-td-value-critic-temporal-credit-PASS.md`,
  `2026-05-18-compose-temporal-credit-PASS.md`,
  `2026-05-18-predictive-coding-cheap-gate-VOID-with-durable-V1-positive.md`.
- PIVOT (autonomous, non-stop): Q2 design pre-staged at
  `docs/plans/2026-05-18-Q2-two-module-constrained-decoding-design.md`
  (commit c494d15).
