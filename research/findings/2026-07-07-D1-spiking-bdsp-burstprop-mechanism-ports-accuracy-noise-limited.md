# D1 — the biologically-plausible deep-credit rule (Burstprop / BDSP) PORTS TO SPIKES on the real substrate: the deep representation forms (probe 0.92, apical load-bearing, all anti-cheats hold); held-out ACCURACY is noise-limited below the bar (the EMERGE-5c-predicted burstprop noise-sensitivity) → the MICROCIRCUIT variant is the named noise-robust fix

**Date:** 2026-07-07
**Runner:** `research/runners/_gnw_d1_spiking_bdsp_derisk.py` + the `sim/` burst-dependent-plasticity mechanism (`fused_bdsp_update` + burst detector + apical→P routing; additive/default-off/byte-identical-when-off — verified). Cheapest-first de-risk of the deep-lever gate (`docs/plans/2026-07-07-D1-spiking-bdsp-build-spec.md`).
**Verdict:** QUALIFIED — the deep-credit MECHANISM ports to spikes (validated); the Burstprop ACCURACY is noise-limited below the pre-registered 0.75 bar (consistent with EMERGE-5c); the microcircuit variant is the noise-robust next build.

## The `sim/` mechanism (built + verified)
Burst-Dependent Synaptic Plasticity as an ADDITIVE, DEFAULT-OFF `sim/` mechanism (173 insertions, **0 deletions** across `config.py`/`kernels.py`/`bridge.py`): `fused_bdsp_update` (`dw = eta·Etilde_j·(B_i − Pbar_i·E_i)`, Payeur M1.2) + a burst detector (2nd-spike-within-ISI = burst; per-neuron E/B low-pass) + the apical→burst-probability read (`P = sigmoid(β·v_apical + logit(p0))` — the rest-bias makes P(rest)==p0, the P0 moat) + a single-phase Pbar EMA baseline; gated by `cp_plasticity_rate_gain` + the plastic mask; the fixed-random apical feedback is a runner-side `RegionPathway(plastic=False)` (no weight transport). **Verified byte-identical-when-off:** pure-insertion diff (0 deletions), the block is unreached with `enable_bdsp=False`, determinism + kernel + numpy-integration suites 52 passed. Reuses `cp_v_apical` (the committed two-compartment dAP), the eligibility machinery, and the plasticity gate.

## The result (3-seed GPU, hidden=128; the oracle learned 0.854 → a VALID test)
- **The deep representation FORMS on spikes** — the level-1 XOR probe = **0.923 mean** (0.94/0.92/0.91) on all 3 seeds → credit genuinely reached the deep layer (the direct measure of deep credit assignment).
- **Beats the floors by +0.16** — BDSP held-out 0.664 vs single-layer 0.503 and apical-lesion 0.504 (both ~chance) → depth is needed AND the apical teaching-channel is load-bearing (apical-lesion probe collapses 0.92→0.51).
- **Every anti-cheat holds:** wrong-sign anti-learns (0.45-0.48 ~chance), the no-teaching-null P0 moat is flat (0.45-0.54 ~chance), permuted is chance (0.48-0.54), and it is provably **no weight transport** (the fixed-random feedback is never written and never equals a forward weight).
- **Stage A (multiplexing) GO on the real bridge (3/3):** E~basal 0.999, E-invariance-to-apical ~0.06 on the bridge, P~apical 0.97-0.99, **P_rest = 0.300 exactly** (the P0 moat holds on the actual substrate), separability R² 0.93.

## The honest scope (the accuracy caveat = the EMERGE-5c prediction reproduced on spikes)
The held-out ACCURACY (0.664 mean, 0.59-0.72, below the pre-registered 0.75 bar) is noise-limited and seed-variable. This is EXACTLY the EMERGE-5c finding, now reproduced on the spiking substrate: raw Burstprop must LOCALLY ESTIMATE credit from a noisy burst fraction (`Binomial(k,p)/k` — the noise IS the credit), so its readout accuracy is noise-sensitive on the finite-sample spiking substrate. (The `--hidden 384` run was INCONCLUSIVE — the oracle itself underfit at that width/LR, correctly gated by the oracle-ceiling guard; 128 is the width where the oracle learns.) **⇒ the deep-credit MECHANISM ports to spikes (the representation forms, the apical is load-bearing, no transport, all anti-cheats hold); the Burstprop-arm ACCURACY is at the substrate's noise floor.**

## The named next build (the noise-robust fix, EMERGE-5c-decided)
The MICROCIRCUIT variant (Sacramento-Senn 2018 + Urbanczik-Senn 2014): an SST-like interneuron learns to CANCEL the predictable top-down feedback so the apical carries a clean ERROR (not a raw noisy teaching burst) — EMERGE-5c showed it is dramatically more noise-robust than raw Burstprop (0.98 vs 0.62 under the same finite-sample noise). Build the interneuron population + the 3 local Urbanczik-Senn rules (M2.6/2.7/2.8) on the same substrate (reusing the built burst/apical machinery); expected to clear the 0.75 held-out bar where Burstprop is noise-limited. This is D1's completion + the entry to D2 (depth-stability).

## What this establishes toward the deep lever
The research gate said the deep lever is ~80% built (the mechanism proven at rate, the apical substrate on the bridge). D1 closes the biggest remaining question: **the rate-proven biological deep-credit rule SURVIVES the rate→spike transition on the real substrate** — the deep representation forms, the apical is load-bearing, no weight transport, all anti-cheats hold. The residual is the noise-robustness (the microcircuit fix) + then depth-stability (D2) + the honest recurrent/scale frontiers. NO expensive training touched.

## Files
`research/runners/_gnw_d1_spiking_bdsp_derisk.py`; `sim/{config,kernels,bridge}.py` (the additive `enable_bdsp` mechanism); `research/findings/raw/_gnw_d1_128.json` (3-seed) + `_gnw_d1_384.json` (INCONCLUSIVE, oracle-underfit). Spec: `docs/plans/2026-07-07-D1-spiking-bdsp-build-spec.md`.
