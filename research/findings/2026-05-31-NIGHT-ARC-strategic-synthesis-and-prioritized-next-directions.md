# Night-arc strategic synthesis (2026-05-30/31) + prioritized next-direction queue

Durable cross-arc narrative + an honest strategic assessment + a queue of concrete next directions, so autonomous work always has a prioritized next thing regardless of how the in-flight CA3 diagnostic lands. Goal frame: artificial life / biology-translatable insight / conversational capability; capabilities instrumental; honest negatives under strict biology ARE the deliverable; spend GPU/time on the highest-value decisive questions.

## The coherent thread (one question, resolved step by step)

1. Integrated-loop wm-emergence arc -> VOID (two horns): binding-retrieval needs a selectivity carrier that is BOTH stable and lesion-ablatable; STDP is emergent-but-unstable, the engram store is stable-but-not-emergent. (2026-05-30)
2. Conversational-ceiling audit -> the ~0.46 ceiling is REPRESENTATIONAL not readout-limited; composition is not decodably present in the rate-coded state. (2026-05-30)
3. Phase-coded VSA survey -> the FHRR composition algebra is already BUILT + VALIDATED (Orchard + Frady-Sommer + attractor TPAM; identity-level 0.96-0.99). The only engineered shortcut left is the ORACLE LOOKUP (a fixed clean separable symbol per concept).
4. Denoiser arc (biologize the oracle lookup via activity grounding) -> NEGATIVE: the substrate's concept activity is SEPARABILITY-limited (between-concept cosine 0.82). Temporal integration denoises variance (CV~1/sqrt(k)) but not separability; the attractor cleanup collapses on overlapping inputs.
5. THREE arcs converge (integrated-loop, D-arc capacity, denoiser) -> the missing substrate mechanism is DG-style PATTERN SEPARATION. Premise confirmed: concept activity overlaps at 0.82 == the P1 DG-input regime (0.800 -> DG 0.218).
6. DG separation gate -> PASS: DG orthogonalizes the concept activity 0.82 -> 0.18-0.30, matching P1. The mechanism works.
7. DG-composition test -> NULL: DG separation is NECESSARY-BUT-NOT-SUFFICIENT. The sparse k-WTA code separates concepts but is UNRELIABLE within-concept (a concept's two observation-halves give near-disjoint sparse DG codes; ~1/3-1/2 words silent). The classic separation-vs-reliability tension.
8. CA3 completion diagnostic (IN FLIGHT, PID 24452, watcher b81ge7zqi) -> the decisive test of whether the next trisynaptic region (CA3 recurrent attractor = pattern completion) makes a concept's storage/query codes CONVERGE (reliability) while keeping concepts DISTINCT (separation). RESOLVES / PARTIAL / NULL.

## Honest strategic assessment

The trisynaptic line (DG separates the substrate's own activity, CA3 completes/stabilizes it) is the GENUINE biology-grounded attempt to biologize the oracle lookup -- it is on the core goal (a brain-faithful compositional symbol from the substrate's own activity, no external code table). It is worth the decisive CA3 test. Predicted risk (stated before the result): CA3 can complete an active-but-unstable code but cannot rescue a SILENT code (no cue), and the gate's dose-response showed no obvious sparsity sweet-spot -- so a PARTIAL is plausible and would point to a fundamental separation-vs-reliability tension at this bridge scale.

Key realization to AVOID a dead end: the G.20 sparse-distributed codes (validated, near-orthogonal, 160-320 concepts) and the engram/multitag mechanism (validated, 90% FULL / 87.5%) are SEPARABLE -- but they are separable because the code is EXTERNALLY GENERATED / tag-stored, i.e. they ARE the oracle lookup in another form, not a biologization of substrate activity. So they do not bypass the grounding problem; they re-instantiate the shortcut. The biologization question (can the SUBSTRATE's own activity yield a separable+reliable symbol) is genuinely answered only by the trisynaptic line. This is why CA3 is the right decisive test, not a pivot to G.20/engram codes.

## Prioritized next-direction queue (pick the top viable item whenever a gate lands)

P1 (CONDITIONAL on CA3 = RESOLVES): full CA3-composition build -- derive the composition symbol from the COMPLETED CA3 code, re-test composition {2,3,5}. If it clears the bar, the trisynaptic loop biologizes the oracle lookup -> all 3 engineering shortcuts removable -> the artificial-life milestone (a brain-faithful compositional system grounded in substrate activity). Cheap-first the symbol-quality before the full FHRR loop.

P2 (CONDITIONAL on CA3 = PARTIAL): diagnose the residual. The silent-word problem is the prime suspect -> test a per-word adaptive concept->DG drive (so every concept fires in the sparse band) + whether CA3 then resolves the active set. If only silent words fail, the fix is drive-uniformity (a wiring/homeostatic problem), not a fundamental tension.

P3 (CONDITIONAL on CA3 = NULL): characterize the separation-vs-reliability tension as an honest biology-translatable BOUNDARY (this substrate's hippocampus separates OR stabilizes but not both for compositional symbols at this scale), and assess whether a different biological mechanism could provide both -- candidates: (a) grid/place-cell conjunctive coding (a different separable+stable code), (b) cortical sparse coding with lateral inhibition tuned for both, (c) accepting the oracle lookup as an irreducible engineering component and advancing the goal via the VALIDATED grounded-memory/multitag conversational capability (extend/scale it as the deliverable).

P4 (INDEPENDENT, always-available): the validated conversational capability (multitag 90% / engram 87.5% / G.20 160-320 concepts) is the project's genuinely-working artifact. A high-value parallel direction: push its SCALE + usability (a larger vocab end-to-end ensemble, or a cleaner interactive chat surface) -- instrumental capability progress that does not depend on the biologization question resolving.

## Discipline

Synthesis only; no code/bars/protected modules touched. The queue is pre-staged so autonomous work never stalls at a gate. Each conditional branch has a concrete first step. Honest assessment recorded (the trisynaptic line is the genuine biologization test; G.20/engram codes re-instantiate the oracle shortcut rather than bypass it).
