# Primary-emergence scoping: the emergent language cortex is MOSTLY built; the single biggest gap is open-ended GENERATION — closable to bounded-but-emergent fluency WITHOUT deep credit via a fixed spiking reservoir + a shallow one-step-local read-out. Rung 1 = an on-bridge reservoir-readout next-token generator.

**Date:** 2026-07-10
**Method:** Ultracode deep-research + inventory Workflow (`wd7mrln2p`, 3 parallel agents + synthesizer). This directly answers the owner's 2026-07-10 whack-a-mole question by mapping the EMERGENT path forward instead of more hand-built capabilities.
**Trust-but-verify:** the load-bearing machinery the synthesis names was confirmed to exist (`_emerge82_onbridge_lsm_derisk.py` OnBridgeLSM reservoir, `sim/predictive_coding.py` PredictiveCoder, `m62.build_stream` corpus, `_d3_delta_cleanerror_derisk.py` clean-error channel). The reservoir API was read by the controller directly (a0).

## Already EMERGENT + learned-from-experience on ONE spiking substrate (no BPTT, no deep credit)
1. **Sequence prediction/production** — the spiking HTM temporal-memory cortex (committed `fused_htm_permanence_update` three-term rule) learns high-order next-symbol prediction + autoregressive rollout unsupervised from the corpus stream (EMERGE-15..25).
2. **Form→role comprehension** — the fronto-striatal RESERVOIR realized as `OnBridgeLSM` on a real `SimulationBridge` (fixed-random recurrent BrainRegion + a shallow trained read-out) resolves NON-LOCAL relative-clause dependencies (EMERGE-82, non-local rel-head 1.000 vs baselines at chance, 6-seed).
3. **Self-organized grammar** — function-word inventory, slot order, construction inventory all discovered from corpus distributional statistics (EMERGE-62..65), then SPOKEN on spikes (Broca producer + spiking A→W read-out, EMERGE-59..71). Categories/inheritance/transitivity discovered from co-occurrence + real Gabor/V1 perception (EMERGE-30..55).
All of this is fixed-reservoir / local-Hebbian / one-step-local-credit — **none needs deep multi-layer credit** (which does not train on spikes at cheap scale — the parallel D1 frontier).

## THE single biggest gap: open-ended, context-conditioned fluent GENERATION
The system comprehends, reasons, and speaks — but only a BOUNDED, corpus-attested, router-selected CONSTRUCTION INVENTORY (7 constructions incl. ditransitive; the honestly-named "R4 ~4-orders-too-small wall"). Genuine fluency is currently supplied either by the bounded frame router (not open) OR by the **external 21M TinyStories transformer** — a PERMANENT EXTERNAL MODEL, the forbidden scaffold under the fully-emergent-on-one-substrate directive. The missing emergent piece: an **on-bridge GENERATIVE sequence model** whose next-token distribution is conditioned on open prior context and generalizes compositionally, learned on spikes **without BPTT**. Every upstream faculty is already emergent, so the pipeline's throughput is capped precisely at the generation stage — this is the last non-emergent / bounded seam on the critical path.

## Closable WITHOUT deep credit — the recipe maps onto validated machinery
Classical reservoir/ESN language modeling trains ONLY a single read-out over a fixed reservoir. So an autoregressive next-token generator = the EMERGE-82 fixed spiking reservoir (temporal expansion on spikes) + a **shallow population read-out trained ONLINE by a one-step next-token error** (the output-layer delta / clean-error rule — local, no weight transport, no BPTT; reuse `sim.predictive_coding` + the D3 clean-error channel). Longer context via the theta-gamma WM buffer (EMERGE-85). Reservoir weights stay fixed-random; only the linear read-out learns. **Honest ceiling (a GO outcome, not a failure):** the reservoir's fading memory (~recursion depth-3, EMERGE-84/85) bounds long-range coherence + deep abstraction — so this reaches SHORT, locally-coherent, grammatical, grounded, moat-safe open-vocabulary generation (bounded-but-emergent fluency), NOT open-domain LLM fluency. That well-characterized bounded ceiling is exactly where the deferred dendritic deep-credit lever becomes the only path past it — a clean demarcation, and an honest-negative-maps-the-substrate deliverable.

## The ranked de-risk ladder (cheapest-first, reuse-by-import, NO sim/ edit)
- **RUNG 1 (decisive, IN BUILD):** on-bridge reservoir-readout AUTOREGRESSIVE next-token generation on the EMERGE-82 fixed spiking reservoir; shallow read-out trained online by the one-step next-token delta rule (no BPTT). GO: held-out next-token cross-entropy strictly below a bigram baseline, 6-seed. Anti-cheats: shuffled-reservoir-state → collapse to bigram; permuted-corpus → collapse; frozen read-out → chance; silenced reservoir → collapse. `_emerge_reservoir_lm_derisk.py` (subagent `a0b898ec067f9be17` building the 1-seed smoke; controller fans 6-seed).
- **RUNG 2:** context-conditioned generation — feed the theta-gamma WM buffer (EMERGE-85) into the reservoir so a distal referent conditions the continuation; buffer-slot-scramble collapses it.
- **RUNG 3:** compositional generalization of GENERATION — a never-seen subject×verb combo produced grammatically (the generative mirror of EMERGE-22 comprehension).
- **RUNG 4:** open-vocabulary SPIKING spell-out — route generated tokens through the EMERGE-67/68 A→W read-out (host-token-free); lesion collapses the decode.
- **RUNG 5:** multi-clause DISCOURSE generation — chain generated clauses through the D3 event-register push/pop gates (who/what across turns); no-confab moat holds.

## ⇒ the answer to the whack-a-mole concern
The path forward is NOT another hand-built conversational capability — it is the **emergent generative reservoir-readout** that LEARNS to produce language from the corpus stream, on the spiking substrate, with a local no-BPTT rule, reusing the already-emergent comprehension/grammar/perception faculties. Rung 1 tests whether this emergent-generation path is reachable before any deeper build; the honest fading-memory ceiling it will hit is itself the scientific deliverable and demarcates exactly where dendritic deep credit becomes necessary.

## Files
Workflow `wd7mrln2p`; reuses `_emerge82_onbridge_lsm_derisk.py`, `sim/predictive_coding.py`, `_emerge62_...` (build_stream), `_d3_delta_cleanerror_derisk.py`. Parallel deep-credit frontier: `2026-07-10-D1-onbridge-deep-credit-poolk-already-run-confounded-by-weight-blowup.md`.
