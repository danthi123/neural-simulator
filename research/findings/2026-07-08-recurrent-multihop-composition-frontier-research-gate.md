# RECURRENT / MULTI-HOP COMPOSITION frontier — research gate: the substrate ALREADY does multi-hop composition (EMERGE HTM transitive chaining + reservoir comprehension + WM-buffer stack-recursion, all spiking, all 6-seed GO). The honest verdict is CONSOLIDATE-AND-STRESS, not a new mechanism — with ONE cheap gap worth a de-risk (multi-hop over EMERGENT/perceived structure, not host-designed codes)

**Date:** 2026-07-08
**Type:** read-only 3-part deep-research gate (project inventory in depth · external ML/neuro field · 4 surpass moves) + synthesizer. NO build, NO `sim/` edit, NO GPU. The ONE file written is this doc.
**Trigger:** the feedforward deep-credit arc converged (`2026-07-08-deep-credit-feedforward-arc-COMPLETE-*.md`) on "the genuine depth-required LANGUAGE capability is MULTI-HOP / RECURSIVE / SEQUENTIAL composition = the recurrent frontier." This gate asks: is that capability MISSING, or already covered by EMERGE HTM (unsupervised) + the reservoir?

---

## THE HEADLINE (say it plainly)
**Recurrent multi-hop composition is already SUBSTANTIALLY ACHIEVED on the spiking substrate, three independent ways, each 6-seed GO with anti-cheats and NO `sim/` edit.** The "recurrent frontier" the feedforward arc pointed at is not an open capability gap — it is a *consolidated, validated* region of the project. The deep lever's genuine, un-redundant contribution is the FEEDFORWARD mechanism validation (the nonlinear-conjunction-over-pool credit), NOT a new recurrent-credit result. The one honest residual is narrow and cheap: multi-hop composition currently rides **host-DESIGNED** codes/premises; making the multi-hop structure ride **EMERGENT / perceived** structure is the only genuinely-open cheap de-risk — and even that reuses machinery already GO in pieces.

---

## PART 1 — DIAGNOSIS: what multi-hop composition is DONE vs genuinely OPEN

### The three DONE pillars (each the multi-hop / recursive / sequential composition the frontier named)

**(A) Multi-hop TRANSITIVE CHAINING — UNSUPERVISED, spiking, on the real bridge — DONE.**
EMERGE-28 (`2026-07-02-emerge28-transitive-inference-GO.md`, 6/6): teach ONLY adjacent premises A>B, B>C, C>D, D>E; the never-trained non-adjacent B>D / A>C / … / A>E are inferred by **chaining overlapping premises into an integrated order**, read by **autoregressive rollout** (EMERGE-16 rollout) on the committed `sim/` HTM three-term kernel. The critical internal pair **B>D = 1.00** is unsolvable by associative strength (Dusek-Eichenbaum 1997; catalog D.02) — it *requires* integrating the chain, i.e. genuine multi-hop composition. Anti-cheats airtight: **BROKEN-CHAIN** (drop C>D → B and D uncomparable) collapses B>D to 0.00 (isolates the chaining), dAP-lesion → 0.00. This is exactly "bind → re-bind → non-shortcut-able transitive chain" — the frontier's canonical example — and it is GO, unsupervised, on spikes. The whole EMERGE inference triad (generalization EMERGE-17, inheritance EMERGE-26/27, transitivity EMERGE-28) is multi-hop composition emergent from overlapping codes × the HTM next-state predictor, NO inference engine.

**(B) Recurrent COMPREHENSION of structure (form→role, non-local, recursion-capable) — spiking, on the bridge — DONE.**
The fronto-striatal reservoir arc EMERGE-78..91 (Hinaut-Dominey cortico-striatal reservoir): learned form→role (78), **uncontingent NON-LOCAL dependency** ≈33 tokens vs windows-at-chance (79), ported to spikes / LSM (80/81), realized on the real `SimulationBridge` as a recurrent `BrainRegion` (`OnBridgeLSM`, internal_density recurrent conductance synapses driven through `_run_one_simulation_step`, read from `cp_firing_states`, EMERGE-82), and — decisively — **comprehension DRIVES composition** (EMERGE-88: reservoir parses a sentence → the production composer stores + answers, who/what 1.000, no-confab moat 0 FA, comprehension-lesion collapses recall). This is the recurrent, over-time holding + re-composing of intermediate structure the frontier named — a genuine capability COMPOSITION, not an isolated score.

**(C) Bounded RECURSION (stack / nested pair-matching) — SPIKING WM buffer — BOUNDARY-THEN-SURPASS DONE.**
The genuine stack-recursion test (EMERGE-84, nested center-embedded pair-matching with reversed verbs + multiset-preserving-swap so the count shortcut is at chance): a plain reservoir does depth-1 perfectly then degrades (d\*=2 — fading memory, not a push/pop stack). The RANK-3 mechanism surpassed it: EMERGE-85 (rate) → **EMERGE-86 (ON SPIKES)** a theta-gamma WM buffer + stack-match (Lisman-Idiart, catalog N.15; on the project's RF ordered-WM `OrderedPositionWM`) reaches **d\*=3**, then boundaries at buffer capacity = the biologically-faithful human ~2-3-embedding bound. Slot-scramble + unbind-lesion + count-shortcut all collapse to chance. This is the hardest sense of "recursive composition holding intermediate bindings over time" — and it is GO on spikes, with the boundary correctly located at the *bounded* human limit (not a wall).

### What is genuinely OPEN (the tiny, precisely-located residual)
1. **Multi-hop over EMERGENT / PERCEIVED structure (the ONE cheap gap).** EMERGE-28's items/premises are **host-DESIGNED disjoint codes** (its own honest scope: "inference-OVER-structure, NOT acquisition-OF-structure-from-experience" — the deferred R-c residual). The pieces to close it already exist and are GO in isolation: the PPMI stream cortex learns codes from experience (CLAUDE.md CYCLE 88-96), EMERGE-30..36 discover categories from co-occurrence/perception, and EMERGE-17 gives similarity-structured word codes for the HTM. The open question is the *composition*: does multi-hop transitive chaining still hold when the chain rides codes DISCOVERED from a stream/percept rather than hand-assigned? Nobody has run that exact test.
2. **SUPERVISED recurrent-WEIGHT credit that beats a fixed reservoir — a robustly CLOSED boundary, not open work.** The 5-probe boundary (EMERGE-6/6b/7/8: target-based, e-prop first-order eligibility, RFLO, Predictive Alignment) all fail to beat a fixed reservoir + trained read-out at toy rate scale; EMERGE-6b RE-LOCALIZED the wall to *autonomous-generation stability*, not credit quality. **This is not a to-do; it is a documented dead-end for this vehicle.** Do NOT re-run it.
3. **Open-domain surface fluency + unbounded (vs bounded) recursion** — the field-wide walls (EMERGE-16's honest residual; EMERGE-85/86's bounded-by-capacity note). Managed by scope, not a cheap experiment.

---

## PART 2 — THE EXTERNAL FIELD (does the field consider recurrent multi-hop OPEN, and how does it do it?)

- **The reservoir route is the field-standard for structured sequence comprehension — and the project is on it.** Hinaut & Dominey's fronto-striatal reservoir does exactly on-line thematic-role assignment of grammatical structure with a trained read-out (Hinaut-Dominey 2013 PLoS ONE; a **2025** neurobiological sentence-comprehension reservoir model, Tandf 10.1080/23273798.2025.2473537, extends it). The project's EMERGE-78..91 IS this mechanism, ported to spikes and to the real bridge. So the project is not behind the field here — it has the field's canonical mechanism running on its own substrate.
- **Fading memory is the reservoir's INTRINSIC recursion limit — confirming EMERGE-84 is a real property, not a bug.** The echo-state property (spectral radius < 1 → contractive → graded decay of past state) is definitional to reservoir computing (Scholarpedia; the ESN math reviews). "A reservoir has fading memory, not a stack" (EMERGE-84) is the field's own account; the field's fix for genuine multi-item ordered memory is exactly a theta-gamma nested-slot buffer (the 2024 theta-gamma WM models: gamma sub-cycles nested in theta = the item-capacity code) — which is precisely EMERGE-85/86. **The project independently arrived at, and implemented on spikes, the field's remedy.**
- **Deep × recurrent × spiking × bio-plausible credit is STILL open field-wide — confirming the 5-probe boundary is not a project failure.** The 2023 survey (Ororbia, arXiv 2312.09257) states credit assignment across BOTH time and depth is unknown; most bio-plausible work is feedforward-only. The newest attempt, **"Generalising E-prop to Deep Networks" (Millidge, arXiv 2512.24506, Dec 2025)**, extends e-prop to deep recurrent SNNs with a local rule / no weight transport — but it is a fresh, un-validated-on-compositional-language result (no multi-hop/language eval), matching the gate's prior note "deep-eprop Dec-2025 is theory-only." So supervised recurrent-weight credit for compositional tasks is a genuine field frontier — which is exactly why the project's *unsupervised* HTM path (that sidesteps it) is the right call, and why re-attacking supervised recurrent credit would be chasing an unsolved field-wide problem on a vehicle already shown 5× to be a false economy.
- **Multi-hop composition in the mainstream (Transformers) is itself brittle** — recent work shows latent two-hop reasoning appears only after phase transitions and has hop-timing failures (arXiv 2501.15857, 2510.01685). The project's *symbolic-structural* multi-hop (transitive chaining, thematic-role parsing) is a cleaner, validated capability than the mainstream's emergent-but-brittle latent hops.

---

## PART 3 — THE 4 SURPASS MOVES

**(a) ISOLATE the true residual.** The multi-hop composition capability is NOT missing — it is covered three ways (transitive chaining EMERGE-28; recurrent comprehension EMERGE-78..91; bounded stack-recursion EMERGE-85/86), all spiking, all 6-seed GO. The genuine residual is a SINGLE thing: multi-hop composition currently rides **host-designed** codes/premises (EMERGE-28's own flagged R-c residual). Everything else the frontier named is done or is a documented dead-end (supervised recurrent credit) or a field-wide wall (open-domain fluency, unbounded recursion).

**(b) REFRAME — is "supervised recurrent credit" even the right question?** No. The 5-probe boundary + the Dec-2025 deep-e-prop field state both say supervised recurrent-weight credit is the wrong vehicle at this scale. The RIGHT answer is the HYBRID the project already runs: **feedforward deep-credit conjunction WITHIN a timestep** (the deep lever's validated feedforward mechanism) **+ UNSUPERVISED HTM / theta-gamma-WM recursion ACROSS time** (EMERGE-16/28/86) **+ a fixed reservoir + trained read-out for structure comprehension** (EMERGE-88, exactly what the 5-probe showed WINS). The recurrent multi-hop is done by *unsupervised self-organization + reservoir + bounded WM buffer*, not by supervised recurrent credit. The deep lever's contribution is the feedforward half; it does not need to (and should not try to) re-own the recurrent half.

**(c) RANK cheap-first mechanisms for a multi-hop de-risk that does NOT re-tread the 5-probe boundary.**

| # | de-risk | reuses | cost | what it proves | verdict |
|---|---|---|---|---|---|
| **1 ★** | **Multi-hop transitive chaining over EMERGENT/stream-learned codes** — repeat EMERGE-28 but the item codes are DISCOVERED (PPMI stream cortex / EMERGE-17 similarity codes / EMERGE-30 co-occurrence) instead of hand-assigned. Gate: B>D still 1.00; broken-chain + dAP-lesion collapse; codes-from-stream (not host) asserted. | EMERGE-28 runner + EMERGE-17/30 + PPMI stream cortex, all GO in isolation | cheap (CPU, reuse-by-import, NO `sim/`) | multi-hop composition holds on structure ACQUIRED from experience — closes EMERGE-28's own R-c residual, the master-directive "structure self-organizes" bar | **RUN FIRST if any new test is run** |
| 2 | Consolidate the three pillars into ONE spiking pipeline: reservoir comprehends (EMERGE-88) → HTM stores/chains (EMERGE-28) → WM-buffer handles nesting (EMERGE-86), one bridge, one turn, moat intact. | EMERGE-88/28/86 + EMERGE-89/91 co-residence | cheap-moderate (integration, no new mechanism) | the multi-hop capabilities interact on one brain (not isolated demos) — a capability-COMPOSITION headline | strong 2nd |
| 3 | Spiking theta/gamma OSCILLATOR driver for the WM buffer (EMERGE-86 RUNG 2) — a literal time-domain theta-nested-gamma slot multiplex. | OrderedPositionWM + StimulusManager SINUSOIDAL + CORTEX_GAMMA_FS_NETWORK | moderate (thin additive default-off `sim/` oscillator) | the bounded stack is realized by real oscillatory nesting, not a functional slot array | nice-to-have, NOT critical-path |
| 4 | Deep-e-prop-on-multi-hop (arXiv 2512.24506) | — | expensive, high-variance, field-unsolved | supervised recurrent credit on compositional tasks | **DO NOT** — re-treads the 5-probe boundary + an unsolved field problem |

**(d) VERDICT.** The honest answer is **"already substantially achieved — CONSOLIDATE, with ONE cheap new test worth running."** Recurrent multi-hop / recursive / sequential composition is DONE three independent ways on the spiking substrate (transitive chaining, recurrent comprehension, bounded stack-recursion), all 6-seed GO, all reuse-by-import. The deep-lever's genuine contribution is the FEEDFORWARD mechanism validation, not a recurrent-credit result. If a new de-risk is run, it is **#1: multi-hop transitive chaining over EMERGENT/stream-learned codes** — the only genuinely-open, cheap, master-directive-aligned gap (closes EMERGE-28's own host-designed-codes residual by riding structure ACQUIRED from experience). Do NOT re-attack supervised recurrent credit (a 5×-confirmed dead-end + a field-wide open problem).

---

## THE 10-LINE RANKED VERDICT
1. **Recurrent multi-hop composition is NOT an open gap — it is DONE 3 ways on spikes, 6-seed GO.** The frontier the feedforward arc pointed at is already a consolidated project region.
2. **Pillar A — transitive multi-hop CHAINING:** EMERGE-28 (B>D=1.00, broken-chain collapses), unsupervised, on the committed HTM `sim/` kernel. The frontier's canonical "non-shortcut-able transitive chain" — GO.
3. **Pillar B — recurrent COMPREHENSION of structure:** the fronto-striatal reservoir (EMERGE-78..91) does form→role + non-local (~33 tok) + drives the composer (EMERGE-88), on the real bridge — GO.
4. **Pillar C — bounded RECURSION (stack):** the theta-gamma WM buffer on RF spikes reaches d\*=3 then boundaries at the human ~2-3-embedding limit (EMERGE-86) — GO.
5. **The ONE genuinely-open residual:** multi-hop currently rides HOST-DESIGNED codes (EMERGE-28's own R-c flag). Making it ride EMERGENT/perceived structure is the only cheap open test.
6. **Supervised recurrent-WEIGHT credit is a CLOSED dead-end (5 probes: EMERGE-6/6b/7/8), re-localized to generation-stability — do NOT re-run.** Field confirms it (deep-e-prop is Dec-2025 theory-only, no compositional eval).
7. **Field alignment is strong:** the project runs the field's canonical structure-comprehension mechanism (Hinaut-Dominey reservoir) on spikes, and independently implemented the field's fading-memory remedy (theta-gamma nested slots, Lisman-Idiart) on spikes.
8. **Reframe:** the right architecture is the HYBRID already running — feedforward deep-credit WITHIN a timestep + unsupervised HTM/theta-gamma-WM ACROSS time + reservoir-readout for comprehension — NOT supervised recurrent credit. The deep lever owns the feedforward half only.
9. **#1 recommended de-risk (if any):** repeat EMERGE-28 transitive chaining over EMERGENT/stream-learned codes (reuse EMERGE-17/28/30 + PPMI cortex; CPU; NO `sim/`) — closes the host-designed-codes residual, master-directive-aligned. **#2:** consolidate the 3 pillars into one spiking turn.
10. **Bottom line: CONSOLIDATE, don't invent.** Recurrent multi-hop composition is substantially achieved; the deep lever's un-redundant deliverable is the feedforward-conjunction mechanism it already validated. Cite: EMERGE-16/28/78/82/85/86/88; Hinaut-Dominey 2013 + Tandf 2025.2473537; Lisman-Idiart 1995 (catalog N.15); Ororbia 2312.09257; Millidge deep-e-prop 2512.24506; ESN echo-state-property (Scholarpedia).
