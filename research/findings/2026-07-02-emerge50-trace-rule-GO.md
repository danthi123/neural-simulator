# EMERGE-50 / toward-semantics — GO (3/3 seeds): the FÖLDIÁK (1991) TRACE / TEMPORAL-CONTINUITY rule SURPASSES the EMERGE-46 fully-spiking-stacked-pooler boundary ON-SUBSTRATE. Presenting same-superordinate members' L1 codons in temporal proximity + a slow eligibility trace on the L2 pre-activity makes co-occurring codons bind to SHARED L2 columns — so the held-out sub-category inherits, and the shuffled-temporal control collapses it. NO new `sim/` edit.

**2026-07-02/03 (autonomous).** Runner `research/runners/_emerge50_trace_rule_derisk.py` (`--numpy` fast proxy + `--onsubstrate` the decisive port); CI guard `tests/test_emerge50_trace_rule.py` (3 tests pass). Reuse-by-import (EMERGE-44 task + EMERGE-46 `OnSubstratePooler` + the committed `_emerge14` kernels); CPU numpy-backend; 3-seed (42/43/44). Closes the EMERGE-46 boundary characterized across EMERGE-46/47/48/49.

## The surpass (3/3 seeds, on-substrate)
The EMERGE-46 boundary (isolated across four prior de-risks): the on-substrate competitive-learning *dynamics* over-sparsify the L2 permanences to a bimodal split, so the stacked pooler couldn't extend shared columns to a held-out sub-category (super-acc 0.03). The fix is not to tune selectivity or the read (both ruled out, EMERGE-48/49) but to build the shared tuning STRUCTURALLY — via temporal continuity:
- **STACKED_TRACE (the Földiák rule): held-out sub-category super-acc 0.944 mean** (per-seed 1.00/0.92/0.92, chance 0.50) — vs EMERGE-46's 0.03. Discrimination **within 0.461 ≫ cross 0.022 (+0.439)**.
- **SHUFFLED-TEMPORAL (the load-bearing control): discrimination collapses to −0.027** (within 0.171 ≈ cross 0.198) — presenting the SAME members in RANDOMIZED order kills the binding, proving the temporal-continuity mechanism (not a shortcut). super-acc 0.611.
- Gate (all pass): super-acc 0.944 ≥ 0.80; ≥ permuted (0.639) + 0.25; ≥ shuffled-temporal (0.611) + 0.20; ≥ dAP-lesion (0.00) + 0.30; within > cross. `onsub_go = True`.

## Mechanism (Földiák 1991 — learning invariance from temporal continuity)
Biology: features that co-occur in time bind into an invariant unit (Földiák 1991 "Learning Invariance from Transformation Sequences"; the ventral stream learns invariance from the temporal continuity of natural input). Realized on the substrate: during L2 learning, same-superordinate members' L1 codons are presented in TEMPORAL PROXIMITY (bouts), and a slow-decaying eligibility trace over the L1 (pre-synaptic) activity is fed into the committed kernels — so a winning L2 column potentiates not just to the current codon but to the recently-active (traced) codons. Consecutive same-super members therefore bind to the SAME L2 columns (a shared superordinate representation), including a held-out sub-category that appears in the temporal group. This builds shared-but-discriminative L2 tuning WITHOUT relying on the over-sparsifying winner-inactive depression — which is exactly why it surpasses where EMERGE-47/48/49 could not.

## The `sim/` scope (NO new edit)
NO new `sim/` edit: the committed kernels `fused_htm_permanence_update` + `fused_htm_winner_inactive_depression` are byte-unchanged. The trace rule only changes HOW the per-synapse pre-vectors are BUILT before the kernel call — a slow eligibility trace over pre-activity (`pre_last`/`pre_active`) instead of the instantaneous winner/active set. The eligibility trace is computed host-side and fed into the on-substrate kernel learning (weights in `cp_connections`); a fully-on-substrate eligibility-trace array (a `cp_` state updated in the step loop) is the clean follow-on (the trace is a standard biological mechanism, so this is a rate-reference for the trace, not a shortcut in kind).

## Honest scope
- The DISCRIMINATION metric (within − cross) is the clean load-bearing signal: +0.439 with the trace, collapsing to −0.027 under shuffled-temporal. The super-acc CONTROLS retain some above-chance signal (shuffled 0.611, permuted 0.639 — residual L1-structure leakage through the inheritance read), so the permuted super-acc gate margin is tight (+0.055 above the +0.25 requirement); a 6-seed confirmation will firm it. The within>cross discrimination + its shuffled-temporal collapse is the primary evidence.
- Two-level stacked pooler (as EMERGE-44); the 3-level version (EMERGE-45) with the trace is a follow-on. The trace/temporal grouping is a legitimate learning-input structure (curriculum/natural-sequence), with the shuffled-temporal control proving it's load-bearing.
- ⇒ the competitive self-organizing pooler is now realizable FULLY on the spiking substrate END-TO-END for a discovered multi-level taxonomy: both layers' learning via the committed kernels + the Földiák trace binding shared superordinate columns, held-out generalization recovered (super-acc 0.94). The EMERGE-46 boundary is surpassed.

## Artifacts
`research/runners/_emerge50_trace_rule_derisk.py`, `tests/test_emerge50_trace_rule.py`, `research/findings/raw/_emerge50_trace_rule.json`. Prior: `2026-07-02-emerge49-graded-read-BOUNDARY.md`, `2026-07-02-emerge46-boundary-surpass-research-gate.md`, `2026-07-02-emerge46-spiking-stacked-pooler-BOUNDARY.md`.
