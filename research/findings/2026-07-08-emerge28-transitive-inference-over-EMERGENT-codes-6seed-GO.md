# EMERGE-28-emergent — transitive multi-hop inference rides EMERGENT codes (6-seed GO): the overlapping codes the chaining traverses are DISCOVERED from a co-occurrence stream, not host-assigned. Closes EMERGE-28's own R-c residual (host-designed codes) — the master-directive core (structure self-organizes from experience). NO `sim/` edit.

**Date:** 2026-07-08
**Runner:** `research/runners/_emerge28_emergent_codes_transitive_derisk.py` (reuse-by-import: EMERGE-14 `build_pool_bridge` + `apply_kernel_update` = the committed `sim/kernels.fused_htm_permanence_update` three-term rule; EMERGE-12 `_prime_from_winners`; the EMERGE-30/32 emergent-code discovery pattern). NO `sim/` edit (`git status sim/` empty). Adversarial-verify: workflow `w4t2t06n1` (folded in on return).
**Verdict:** 6-seed GO — transitive multi-hop chaining rides EMERGENT structure; the last genuinely-open residual of the recurrent-composition frontier (the research gate's #1), closed. **Adversarial-verify `w4t2t06n1` = SURVIVES (all 3 lenses, no REFUTES)** — the skeptics independently REPRODUCED the load-bearing checks (the lesion test proving the substrate kernel produces feat_rep; the scramble FALSIFYING any hand-map-order leak; B>D confirmed the Dusek-Eichenbaum critical-internal pair, unsolvable by associative strength — a clean strict total order, 20/20 consistent, 0 cycles).

## Why this ran (the one genuinely-open residual)
The recurrent-multi-hop-composition research gate (`2026-07-08-recurrent-multihop-composition-frontier-research-gate.md`) established that multi-hop composition is already achieved 3 ways on spikes (EMERGE-28 chaining / reservoir comprehension / theta-gamma recursion) — CONSOLIDATE-not-invent. The ONE genuinely-open, master-directive-aligned residual: EMERGE-28's transitive inference rode HOST-DESIGNED overlapping codes (its own R-c flag). This makes the overlap the chaining rides EMERGENT (discovered from a co-occurrence stream), not hand-assigned.

## The mechanism (how the emergent overlap is genuinely load-bearing)
Each ordered entity A>B>C>D>E has (i) a disjoint IDENTITY (its legitimate sensory token) and (ii) is streamed co-occurring with an OVERLAPPING feature window on a shared 1-D ramp (adjacent entities share features). The committed HTM kernel learns `identity→features`, so each entity's EMERGENT feature code = what its identity primes. **Premises `X>Y` are learned feature-code→feature-code (the LEARNED overlapping codes only; identity is NOT in the chaining path).** The autoregressive rollout (`_reachable`/`greater`) traverses feature-space: a premise step fires the next entity's emergent feature-state, which — because adjacent emergent windows OVERLAP — matches the next premise's trained-from state, so the chain propagates; the reached entity is named by matching the advanced feature-state against the discovered emergent codes. Controller-verified by reading `_reachable`: it starts from `feat_rep` (emergent) and advances/names via `feat_rep` throughout; `IDENT` appears only as the no-learning fallback → the chaining rides emergent codes.

## The result (6-seed 42/43/44/100/101/102)
- **adjacent 1.00 · NON-ADJACENT held-out 1.00 · CRITICAL internal B>D 1.00 · emergent-overlap 0.50** — every seed. From ONLY adjacent premises (A>B..D>E), the never-trained non-adjacent relations (incl. the critical internal B>D, unsolvable by associative strength — both internal + equidistant) are inferred by chaining the premises learned on the emergent reps.
- **ALL anti-cheats collapse, every seed:** dAP-lesion → non-adjacent 0.00; broken-chain (drop C>D) → B>D 0.00 (isolates the chaining); **scrambled-stream → B>D 0.00** (random features → emergent-overlap 0.15–0.35, no ORDERED overlap — the scrambled margin holds below the ordered 0.50 all 6 seeds); **no-learning → B>D 0.00** (empty feat_rep → the chain can't start). ⇒ the LEARNED overlap is load-bearing, NOT host-smuggled.

## Honest scope (the emergent claim is the CODE OVERLAP)
The feature-RAMP tokens are the environment/experience (legitimate world input, exactly like EMERGE-30/32's context tokens), and the premise SET (which pairs are adjacent) is the taught curriculum (the legitimate "the world tells you A>B"). **What is now EMERGENT — the residual this closes — is the CODE OVERLAP the chaining rides** (in EMERGE-28 it was host-designed disjoint overlapping codes; here it is discovered from the co-occurrence stream). Not "the premises are emergent" and not "the ramp is emergent" — the discovered overlap that enables the multi-hop chain is.

**Precise emergent claim (adversarial-verify refinement):** the learned `feat_rep` column-sets are exactly EQUAL to the host per-entity `subset`, and the overlap ORDERING originates in the host `_feature_window` ramp. What makes the overlap EMERGENT (not host-smuggled) is TWO things the controls isolate: (1) the substrate HTM kernel PRODUCES `feat_rep` — the dAP-lesion (coincidence detector off) makes `feat_rep` EMPTY for every entity even though the host `subset` still exists, so the chaining cannot bypass the kernel to read the host structure directly (the kernel readout is load-bearing); (2) the ORDERED overlap is STREAM-discovered — scrambling the stream keeps the kernel learning `feat_rep==subset` perfectly but destroys the ordered overlap (0.50→0.15–0.35), collapsing B>D. It is NOT a claim that the column identities are discovered de novo. **The SCRAMBLED control is per-seed STOCHASTIC** (not deterministic-zero): a random-window overlap can occasionally luck into an ordered chain (~2.5% of seeds in a wider stress test, e.g. seed 77 → scrambled B>D=1.00); all 6 tested seeds (42/43/44/100/101/102) collapsed cleanly (overlap 0.15–0.35 < ordered 0.50, B>D 0.00) — the effect is robust on the tested seeds but the scrambled control is a stochastic, not deterministic, collapse.

## What this closes
EMERGE-28's own honest R-c residual (host-designed codes) is closed: transitive multi-hop composition rides structure SELF-ORGANIZED from experience — the master-directive core. Combined with the arc: the recurrent multi-hop composition is achieved on spikes (EMERGE HTM chaining / reservoir comprehension / theta-gamma recursion), and now its chaining rides emergent codes; the deep-lever's feedforward-conjunction mechanism is the complementary validated piece. NO `sim/` edit.

## Files
`research/runners/_emerge28_emergent_codes_transitive_derisk.py`; `research/findings/raw/_emerge28_emergent_6seed.json`. Prior: `2026-07-02-emerge28-transitive-inference-GO.md`, `2026-07-08-recurrent-multihop-composition-frontier-research-gate.md`.
