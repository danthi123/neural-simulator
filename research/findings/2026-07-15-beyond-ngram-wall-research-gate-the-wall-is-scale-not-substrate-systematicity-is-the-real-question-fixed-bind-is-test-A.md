# Research gate (4-angle Workflow): the fluency n-gram-wall at achievable scale is DATA/SCALE (a transformer loses to a bigram too), NOT a substrate indictment; the genuine substrate question is SYSTEMATICITY (compositional extrapolation), and it comes from a FIXED binding STRUCTURE + learned codes, not a richer LEARNER — TEST A = fixed-FHRR-bind on the hard dispatch split

**Date:** 2026-07-15 (compute unlocked, ultracode) · **Status:** deep-research gate DONE (read-only, 4 parallel angles [our record · ML scale-laws · biology/compositional · honest reframe] → SURPASS synthesis). Grounded in our own record (trust-but-verified). Reconciles this session's convergence + names the cheap-first mission-central de-risk. Workflow `wf_da582b08-395`.

## The decomposition (do not hedge)
- **At achievable scale (~2M tokens, V=120–300): the wall is DATA/SCALE, and as of 2026-07-16 it is MEASURED ON BOTH MODEL CLASSES.** The CEILING finding (`2026-07-11-CEILING-...`): a full GPU TRANSFORMER **loses to an add-1 bigram at every depth** at 5M-tok/V=300, monotonically worse with depth (overfitting, not long-range). **And so does a full-backprop LSTM** — 3 seeds, **0/3 beat the bigram**, deep-margin mean **−1.415**, losing at every depth and monotonically worse with depth (`2026-07-16-D5-SETTLED-*.md`). ⚠️ **PROCESS CORRECTION 2026-07-16:** this bullet originally asserted the LSTM half **with no measurement behind it**, and **contradicted its own next line** (which correctly says *"a full-backprop LSTM reaches it"* — at 23.7M). The anchor-claim audit caught it; the settle then took ~4 min of GPU and **confirmed the claim**. **So the bullet is now true AS EVIDENCE where it had been an assertion** — and the two lines are consistent: the LSTM **loses at ~2M** and **wins at 23.7M+** (+0.494→**+1.813**), which is exactly the DATA thesis. *(Caveat kept honest: "5M-tok" is a misnomer — `wikitext.txt` is 2.045M words, so both runs cap at ~2M identically; and both models are over-parameterized for that corpus, so this shows the DATA REGIME, not a recurrence-specific limit.)* When the strongest content models of **both classes** can't beat a bigram, "our levers are n-gram-bound at ~2M tokens" is a property of the **data regime shared by ALL model classes** — it does NOT isolate our substrate, **and that now covers recurrence, the one class the spiking substrate actually is.** (This session's fluency-crossover NEGATIVE + the by-depth trigram check are the same wall from the reservoir-LM side.)
- **At valid scale (23.7M+, content vocab): it becomes a substrate question with exactly ONE unmeasured cell.** Long-range signal is real there (+0.5→+1.9 growing with depth) and a full-backprop **LSTM reaches it** (~matches the transformer) → the recurrent architecture CLASS is sufficient; the missing ingredient is **learned deep multi-layer credit**. Measured: fixed reservoir/R3-W_in ≈ +0.03 (representation-bound); LSTM-backprop = yes. UNMEASURED: our LEARNABLE spiking deep-credit at valid scale — a months-scale multi-LAYER RECURRENT spiking build we do NOT have (recurrent off-diagonal e-prop refuted/open; feedforward deep-credit is GO but is not the recurrent net the ceiling requires), off the deployed critical path (ROADMAP §12).

## The reframe: perplexity is the wrong instrument; SYSTEMATICITY is the discriminator
Perplexity-vs-n-gram is scale-confounded (no model wins at achievable scale) AND systematicity-blind (natural-text next-token is dominated by LOCAL predictability — our by-depth trigram gets BETTER with depth, 2.34→1.96). The honest discriminator is **systematic compositional generalization**: held-out combinations have ZERO surface count, so any n-gram backs off and fails BY CONSTRUCTION — removing the scale confound STRUCTURALLY (more same-distribution data never fixes it).
- **Reconciling the feedforward-deep-credit systematicity wall (this session):** deep e-prop fails compositional EXTRAPOLATION (dispatch hard-split held-out **0.264 ≈ chance**; binder held-out **0.002**) **AND the BPTT true-gradient ORACLE ALSO fails** (binder 0.007). So NO learner — not our spiking deep-credit, not ideal backprop — cracks systematic extrapolation on a point-neuron feedforward net. The ONLY thing in-record that generalizes systematically is a **FIXED structural primitive** (fixed-FHRR bind = **0.99** positive control). ⇒ **systematicity comes from a FIXED binding/store STRUCTURE the substrate HOSTS + learns to USE — not from a richer learner, not from scale.** (The interpolative deployable dispatch is ALREADY a learned GO 0.793 replacing the hand router — the emergence-bar win on the mission path is in hand; only EXTRAPOLATION is open.)

## TEST A (the verdict — run first): fixed structural binder on the HARD compositional split
Reuse `research/runners/_learned_dispatch_derisk.py --hard` (baselines already in place: deep-eprop 0.264, 1-NN memfloor 0.222, linear 0.245, permuted 0.190 — a clean, scale-confound-FREE bench; the hard rule is `intent = class(a[cat] XOR b[qtype])`). ADD ONE arm: encode (category, qtype) as a **FIXED ±1/FHRR bind of LEARNED codes**, read intent via unbind + cleanup, with a linear readout trained ONLY on attested combos (NOT role-conditioned — that was the prior dendritic-negative confound). ±1 bind IS the XOR-structured coincidence → this tests whether fixed binding makes held-out compositions **LINEARLY READABLE** (→ the readout extrapolates) or still per-combo memorized.
- **Gate:** held-out extrapolation > max(deep-eprop 0.264, memfloor 0.222, linear 0.245). **Anti-cheats:** fixed-FHRR positive control on the EASY binder reproduces ~0.99; permuted-code → chance; assert the readout does not learn per-role filler priors. 6-seed, rate numpy, reuse-by-import, NO `sim/` edit, hours.
- **Discriminates decisively:** GO → systematicity is **architectural structure the point-neuron substrate ALREADY hosts** (the FHRR composer runs on the bridge) → route effort to "learn to USE a fixed bind/store," NOT to scale, NOT to a dendritic *learner*. The on-substrate follow-on is the fixed **two-compartment dendritic-conjunction** realization (`enable_two_compartment_dap`) — ONLY if the rate-level fixed bind is GO. FAIL → systematicity is deeper → provision the structure + constrain the domain.

## Secondary (off critical path)
- **TEST B (heavier):** our learnable deep-credit as a by-depth language read-out at VALID scale (23.7M) vs the fair trigram + the in-record transformer/LSTM ceiling — the literal scale-vs-substrate cell; but the ceiling says the required object is a multi-LAYER RECURRENT net, so the feedforward read-out is a weak proxy + the real build is months-scale.
- **TEST C (cheapest, tangential):** the Stage-0 delayed-cue recalibration gate (recurrent off-diagonal frontier, not the fluency wall).

**⇒ NEXT: build TEST A** — the single cheap, scale-confound-free, mission-central discriminator of whether a fixed binding structure + learned codes cracks systematic composition where every learner (incl. ideal backprop) sits at chance.
