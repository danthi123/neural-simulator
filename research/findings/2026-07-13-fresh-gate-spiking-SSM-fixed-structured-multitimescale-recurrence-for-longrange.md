# Fresh mechanism-class gate (the exhausted reservoir/e-prop long-range ladder → a genuinely-NEW class): FIXED STRUCTURED multi-timescale (HiPPO/SSM) recurrence + local read-out, attacking the fading-memory ceiling without BPTT

**Date:** 2026-07-13
**Gate:** the emergence-engine long-range core (the reservoir's fading-memory ceiling that blocks the whole generation ladder) is at a thoroughly-exhausted frontier — reservoir-scale (Ueda-bounded), e-prop (R1 16% deep, BOUNDARY), learn-W_in (rate-works / spiking-BDSP-coarseness-bounded), dendritic-gain (developmental-normalization, modest), cross-neuron decorrelation (red-herring). Per the workflow (exhausted ladder → FRESH mechanism-class gate, don't re-tread), an external search surfaced the field's spiking long-range answer.

## The new mechanism class (external, cited): SPIKING STATE SPACE MODELS
Spiking SSMs (S4/Mamba-style structured recurrence + LIF) are **transformer-competitive on Long-Range Arena** and surpass spiking LLMs on WikiText-103 at 1/3 the size — the field's spiking long-range mechanism the project has NOT tried (we used random reservoirs + e-prop). Sources: [SpikingSSMs (arXiv:2408.14909, AAAI)](https://arxiv.org/abs/2408.14909), [P-SpikeSSM / Rethinking SNNs as SSMs (arXiv:2406.02923)](https://arxiv.org/html/2406.02923v2), [SPikE-SSM (arXiv:2410.17268)](https://arxiv.org/pdf/2410.17268), [Learning long sequences in SNNs (Sci. Reports)](https://www.nature.com/articles/s41598-024-71678-8).

## The source-read (SpikingSSMs, arXiv:2408.14909) — the load-bearing detail
- SSM block + LIF, hierarchically integrated; **explicitly DENDRITIC-inspired** (multi-timescale dendritic integration = the SSM's diverse time constants).
- **TRAINING is a non-spiking SURROGATE + parallel scan (BPTT-like)** — the *training* is a gradient scaffold, NOT a local rule.
⇒ **the emergence-compatible EXTRACT (the mechanism, not their training):** the long-range comes from the **structured multi-timescale FORWARD state** (HiPPO-initialized SSM = a principled basis of leaky integrators spanning fast→slow time constants), NOT from learned recurrent credit. A **FIXED** such recurrence + a **LOCAL** read-out gets the long-range from the forward structure — no BPTT through the recurrence.

## Why this is the RIGHT fresh class (it converges with our own validated results, not a re-tread)
- **The R3 REFRAME** (`2026-07-11-R3-REFRAME-...`): learning W_rec is COUNTERPRODUCTIVE; a FIXED reservoir + learned input/read-out is the path. The SSM insight is the missing piece: **STRUCTURE the fixed recurrence** (HiPPO multi-timescale) so its forward state exposes long-range (which a RANDOM reservoir's fading memory cannot) — then the local read-out extracts it. Fully consistent with R3 (no learned W_rec).
- **The multi-timescale reservoir GO** (`2026-07-12-multitimescale-reservoir-read-...6seed-GO`): multiple leak-rates already beat a single/flat reservoir. The SSM/HiPPO is the PRINCIPLED version — a structured basis (not just a few hand-picked leaks) with a proven long-range capacity.
- **Biology:** diverse neuronal/synaptic/dendritic time constants ARE the biological substrate for multi-timescale memory (the SSM's dendritic-inspired framing). Emergence-compatible: fixed developmental structure + a local read-out.

## THE CHEAP-FIRST DE-RISK (the gate's output — building now)
`_ssm_fixed_structured_reservoir_derisk.py`: on the reslm/ceiling stream-LM harness (`_recurrent_lm_ceiling.py` / `_emerge_reservoir_lm_*`, the by-depth deep-context metric + bigram baseline), replace the RANDOM reservoir recurrence with a **FIXED DIAGONAL HiPPO-structured SSM recurrence** (state `x_{t+1}=A x_t + B u_t`, `A`=diag of decay rates spanning a log range of time constants ≈ HiPPO-LegS init, `B`=random input, read-out `C` LEARNED = the local read-out) and measure DEEP-context capture vs (a) the random reservoir (same n_units), (b) the bigram. ARMS: {fixed-random-reservoir, fixed-HiPPO-SSM, bigram}; GATE (deep 17-128 bucket, reported separately from shallow): HiPPO-SSM deep margin over the random reservoir > 0 AND grows with depth ⇒ the structured recurrence surpasses the fading-memory ceiling from the FORWARD structure alone (no BPTT). Anti-cheats: permuted-A (shuffle the time-constant assignment → collapses if the STRUCTURE is load-bearing); shallow-bucket control (the gain should be DEEP-specific). Reduced scale first (TinyStories 2M, V=2000, n=256, block=128), then multi-seed. If GO → the spiking realization (LIF + the fixed structured recurrence on-bridge) is the next rung; the emergence-compatible long-range mechanism the whole ladder needed. NO `sim/` edit (fixed recurrence + linear read-out; reuse-by-import).
