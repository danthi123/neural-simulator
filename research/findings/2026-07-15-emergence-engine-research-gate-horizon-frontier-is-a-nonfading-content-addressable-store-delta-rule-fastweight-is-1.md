# Emergence-engine research gate (4-angle Workflow, our-record-first): the recurrent-cortex long-range frontier is a NON-FADING CONTENT-ADDRESSABLE STORE (not recurrent credit) — the cheap-first #1 = a DELTA-RULE fast-weight store (STP-realized) read into the 2-stage read-out; it structurally dissolves the off-diagonal-credit / RTRL wall

**Date:** 2026-07-15 (compute unlocked) · **Status:** deep-research gate DONE (read-only; 4 parallel research angles [our record · biology · ML online-learning · reservoir/SSM] → SURPASS-4-moves synthesis). Grounded in our own cited findings (trust-but-verified). Decision-INDEPENDENT of the fluency crossover (the fixed reservoir is a scaffold either way). NO build yet — this scopes the next de-risk. Workflow `wf_ecf8f9e3-83a`.

## MOVE 1 — the true residual, isolated
The current stack (frozen ALIF reservoir + learned W_in + 2-stage nonlinear read-out) ALREADY does HOLD / INTEGRATE / nonlinear COMBINE (XOR of two temporally-separated cues) — but **only within the fixed-ALIF window (~5–15 tokens)**. Two axes of "long-range" do NOT survive equally:
1. **Off-diagonal cross-neuron credit is NEAR-DEAD (not the binding constraint).** Within the horizon it's already dissolved by the 2-stage read-out (symmetric-true-grad == fixed W_rec is the decisive tell). Beyond the horizon there is *nothing to credit* (the item is already gone). Not yet FORMALLY closed with the exact ceiling (see #2 below).
2. **The FADING STATE is the genuine irreducible residual.** A leaky/adaptation/heterogeneous-τ state is a diluting 1/t AVERAGE, not a specific-item hold (refuted on real text two ways: extend-credit-horizon AND extend-state-τ both leave d10–99 unchanged, 2026-07-11). The only thing that crossed it in our own record is a NON-FADING latch (RUNG-2, dependent-token 0.125→0.97).
- **Residual stated exactly:** the stack lacks a **non-fading, content-addressable, specific-item STORE** that (a) survives past the ~5–15-token window and (b) is retrievable by CURRENT RELEVANCE. That single lack accounts for the entire long-range gap.

## MOVE 2 — biology reframe
Biology does NOT extend the horizon with longer membrane/adaptation constants (that IS the diluting average our record refuted). It uses **activity-silent synaptic WM** (Mongillo-Barak-Tsodyks 2008: the held item = the pattern of facilitated synapses, residual presynaptic Ca²⁺, τ_f ~1–2 s — non-fading, specific-item, interference-resistant; substrate already present as `kernels.fused_stp_decay_recovery` + `stp_U/tau_d/tau_f`, **never yet read as memory**) + **CA3 pattern completion / modern-Hopfield** (Ramsauer 2020: attention == modern-Hopfield == CA3, content-addressed retrieval). These are the SAME mechanism: Ba-Hinton fast weights == STP == linear-attention fast-weight programmer (Schlag 2021) == Hopfield read. **The outer-product store supplies off-diagonal cross-neuron binding STRUCTURALLY (the write is pairwise) → long-range binding needs NO learned recurrent off-diagonal credit → it architecturally DISSOLVES the RTRL/e-prop-diagonal wall rather than paying O(n⁴).**
- **Two literature refinements our prior "additive-store" note LACKED (genuinely new):** (a) **DELTA-RULE write** (error-correcting Widrow-Hoff; Schlag DeltaNet / DeltaProduct arXiv:2502.10297) not additive Hebbian — additive SATURATES under interference; delta restores capacity and is biologically local. (b) **SELECTIVE (input-gated) WRITE** (Mamba selectivity / KOSS arXiv:2512.16723): at 50% distractor interference passive SSMs collapse to 13–17% while a selective hold keeps ~79% — selective WRITE is the capacity lever against same-category interference.

## MOVE 3 — ranked cheap-first (bio-plausibility × cheapness)
- **#1 DELTA-RULE fast-weight content-addressable store (STP-realized), read into the 2-stage read-out.** Bio HIGH, drop-in. Stage-0 (~1 hr, controlled-lag harness): on copy/XOR at T=15–30 (PAST the ALIF window), maintain a delta-rule fast-weight matrix over the last-N reservoir states — `M += η·(v − M k)·kᵀ`, read `v̂ = M q` (q = current state) as extra read-out features; single variable = store ON/OFF. Then Stage-1 (real text): same store on WikiText context-depth buckets; does it lift d10+ CE where longer-τ provably could not? **Anti-cheats (mandatory):** (a) **CACHE-BAG arm** — our 2026-07-11 caution: the apparent cross-sentence win was a position-agnostic cache, not content-addressing; content-addressing MUST beat the cache-bag. (b) additive-vs-delta-vs-no-store ablation. (c) key-shuffle → collapse. (d) tuned add-k bigram + deleted-interpolation trigram on the same split. Positive control: within-horizon copy still solved. **Why #1:** the ONE mechanism past the fading-state wall AND structurally sidestepping off-diagonal credit; O(n²)/step, local, no transport, no BPTT; reuses STP + FHRR machinery.
- **#2 Exact RTRL restricted to W_in on the frozen reservoir (off-diagonal CEILING diagnostic, run in parallel).** W_rec frozen → off-diagonal credit enters only the W_in gradient; exact input-weight RTRL is O(n³·n_in) — affordable. If it ties diagonal e-prop-W_in at a lag past the ALIF horizon, the off-diagonal-credit axis is FORMALLY dead → the frontier is entirely horizon/memory. One cheap run retires half the question.
- **#3 Input-selective time-constant (Mamba-extract) on the diagonal reservoir.** Bio HIGH, drop-in, but still a DIAGONAL state (no content-addressed retrieval) — tests horizon-via-selection, cannot retrieve "the token at t−15" by content. Run if #1 ambiguous or as a selection-vs-averaging control.
- **#4 Bare STP-facilitation-as-features (sub-case of #1, 30-min Stage-0 warm-up):** feed the reservoir's own `u·x` facilitation vector as extra read-out features, sweep `stp_tau_f`, ON/OFF on copy; zero-τ_f → fading baseline, shuffled-facilitation → loses.

**Excluded per our record (do NOT re-run):** plain diagonal e-prop / SnAp-1 / RFLO for cross-cue (diagonal-class); dense SnAp-2 / RTRL (O(n⁴)); node-perturbation on spikes + the FA/burstprop/microcircuit/graded/DECOLLE family (0/6); passive multi-timescale / heterogeneous-leak as a LANGUAGE lever; predictive-coding / iPC / EqProp (need a settling phase).

## VERDICT
**Surpassable — cheaply.** The frontier is NOT off-diagonal recurrent credit (near-dead) but a non-fading content-addressable STORE, and biology's answer (STP synaptic WM = delta-rule fast weights = Hopfield read) dissolves the RTRL wall STRUCTURALLY at O(n²)/step, local, transport-free. **De-risk #1 first** (delta-rule fast-weight store on the controlled-lag harness, past the ALIF window, with the cache-bag anti-cheat load-bearing), run #2 (W_in-RTRL ceiling) in parallel to formally retire the off-diagonal axis. This SHARPENS our own RUNG6c/6d fast-weight binder + STP-window findings — the new content is the delta-rule write, the selective write, and the LANGUAGE-HORIZON application (not just referent binding).
