# The last within-family deep-credit-on-spikes probe (dense-redundant, BurstCCN's exact regime) is a decisive 6-seed NEGATIVE even with PROPER DIRECTED CREDIT — plus a methodological catch: the prior depth-2 negatives were on the DECOUPLED apical path (no directed credit); exposing + enabling the coupling (B rises), the boundary HOLDS

**Date:** 2026-07-13
**Runner:** `research/runners/_d1_onbridge_learn_to_accuracy_derisk.py` (the committed on-bridge 3-region BDSP net; I ADDED a `dense` task + a `--soma-g` flag that wires the 2026-07-10 apical→soma coupling into `_run_bridge_arm`; numpy-CPU, 6-seed fanned per the parallelism gate). Prompted by the CEILING finding's reframe (deep multi-layer learned representations = the lever for rich long-range) + the deep-research gate's "the dense-redundant probe is the one decisive-either-way gate left."
**Status:** ❌ decisive 6-seed NEGATIVE (scale-confound-PROOF) + a methodological CORRECTION. The within-family (feedback-alignment / burst-multiplexed) spiking deep-credit is now EXHAUSTED with proper directed credit; this LAUNCHES the fresh-mechanism-class search (a boundary is an undiscovered mechanism, not a wall).

## The methodological catch (load-bearing): the prior depth-2 negatives were DECOUPLED
`_run_bridge_arm` constructed `OnBridgeBDSPNet` WITHOUT `couple_soma`/`soma_g` → the defaults (`False`/`0`) = the apical-DECOUPLED path: the apical drive raises the burst PROBABILITY read `P` (0.30→1.00) but NOT the MEASURED burst rate `B` (stays 0.000), so the FF update `dev = B − P̄·E` gets NO apical-directed credit. Confirmed via git: the historical D1 depth-2 boundary recorded **`apical_couples_to_bursts: False`**. So the prior "on-bridge BDSP doesn't train depth-2 / the credit can't amplify" negatives (FA-family-exhausted, lr-sweep, task-difficulty) were largely on the path where there is **no directed credit to amplify with** — a confound. I added `--soma-g` (wiring the committed `bdsp_apical_couples_soma` 2026-07-10 edit into `_run_bridge_arm`; byte-identical at `soma_g=0`), and `soma_g=120` makes **B rise (0.000→~0.24, `B_rises True`)** → directed credit IS delivered.

## The result — dense-redundant task, COUPLED path (B_rises True), 6-seed (42/43/44/100/101/102), full compute (18 epochs, hidden 24)
The `dense` task = BurstCCN's trainable regime: 2 latent features each a MAJORITY VOTE over a random REDUNDANT subset of dense input bits (each latent robustly encoded by many bits, like MNIST's redundant pixels); label = XOR of the latents (nonlinear → needs a hidden layer). The single variable vs `parity` is the REDUNDANCY (parity needs every bit exact). Task VALID every seed: numpy oracle (a real MLP) 0.78–0.81 ≥ 0.80; single-layer linear floor ≈ chance.
| seed | oracle | chance | **on-bridge BDSP held** | lesion held |
|---|---|---|---|---|
| 42 | 0.800 | 0.731 | **0.731** (= chance) | 0.269 |
| 43 | 0.781 | 0.661 | **0.661** (= chance) | 0.661 |
| 44 | 0.806 | 0.700 | **0.300** (< chance) | 0.300 |
| 100 | 0.806 | 0.681 | **0.681** (= chance) | 0.681 |
| 101 | 0.783 | 0.706 | **0.314** (< chance) | 0.294 |
| 102 | 0.806 | 0.667 | **0.336** (< chance) | 0.333 |
- **BDSP is AT-or-BELOW chance on ALL 6 seeds** — it NEVER gets above chance. The hidden layer adds nothing (performs at the linear-floor level). The oracle reaches ~0.80, so the task IS learnable — the on-bridge BDSP simply cannot learn it, even on the most-favorable (redundant) regime, even with directed credit.
- The moat does NOT hold (BDSP ≈ lesion on 5/6 — the directed credit adds no productive learning signal beyond the undirected drift). **`--task emerge1 --soma-g 120` corroboration (the ORIGINAL task, coupled): also NEGATIVE** — oracle 0.989 (very learnable), `B_rises True` (directed credit delivered), yet on-bridge BDSP held **0.451 < chance 0.549** (BELOW chance), moat absent (BDSP = lesion 0.451). So the coupled path does NOT rescue depth-2 training on EITHER task family (dense-redundant + emerge1) — the boundary holds with directed credit across tasks.

## ⇒ Verdict + the mission-critical redirect (a boundary = a fresh-mechanism-class launch)
The within-family spiking deep-credit — **feedback-alignment / burst-multiplexed BDSP on point neurons** — is now EXHAUSTED with proper directed credit: NEGATIVE across lr, `P̄` (EMA + preset), pool-k, task-difficulty, AND the dense-redundant BurstCCN regime, coupled. The prior decoupled negatives are superseded by this proper coupled test that STILL fails → the boundary is a genuine credit-QUALITY limit (the delivered burst-credit can't train a hidden layer), NOT the fixable apical-decoupling wiring.

**This is mission-critical, not a side-quest** (per the CEILING finding, `2026-07-11-CEILING-*`): the rich long-range signal at scale (a transformer captures +0.5→+1.9 nats growing-with-depth on TinyStories 23.7M/WikiText-103) comes from **DEEP MULTI-LAYER LEARNED representations** — a shallow reservoir + read-out captures only +0.03 (a 60× gap). So learning multi-layer representations on spikes = deep credit IS the lever for LLM-like conversation. Per the boundary-surpassing workflow (exhausted the ranked within-family ladder → FRESH deep-research gate for a genuinely-NEW mechanism CLASS, don't re-tread the family): the next move is a fresh gate for spiking deep credit BEYOND feedback-alignment/burst — the named candidates: the **dendritic two-compartment substrate** (`enable_two_compartment_dap`, finer continuous-error credit — the owner's standing dendritic priority, a DIFFERENT credit mechanism), BurstCCN's **STP-demux** (the last within-family lever), or a beyond-family class (target-propagation, forward-forward, predictive-coding deep nets — the deep-research gate's literature agent's leads).

## Files
`_d1_onbridge_learn_to_accuracy_derisk.py` (added the `dense` task + `--soma-g` flag); `raw/_d1_dense_s{42,43,44,100,101,102}.log`. Corrects the coupling confound in the prior depth-2 negatives (`2026-07-12-onbridge-BDSP-does-not-train-depth2-*`, `2026-07-12-deep-credit-on-spikes-FA-family-exhausted-*`). Ties to `2026-07-11-CEILING-*` (deep-credit = the rich-long-range lever). NO `sim/` edit (the coupling is the committed 2026-07-10 edit; I only wired the existing flag into the runner).
