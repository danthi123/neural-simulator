# Laptop session raw artifacts — 2026-07-05

Raw research-workflow outputs preserved so the desktop session has the full detail behind the curated summary in
[`docs/2026-07-05-laptop-session-handoff.md`](../../../../docs/2026-07-05-laptop-session-handoff.md). These are verbatim
workflow results (per-angle findings + synthesis, with file:line citations + literature) — **not curated**; cleanup/
folding-into-findings can happen later.

| File | What it is |
|---|---|
| `workflow-dendritic-direction-research.txt` | The **key redirect**: is the objrel structural read dendritic-mandated, or a red herring? Verdict = **RED HERRING** — the residual is a rank-1 common-mode; the fix is a somatic feedforward common-mode subtraction. Inventories the existing two-compartment dAP groundwork. |
| `workflow-objrel-surpass-research-gate.txt` | Earlier surpass research gate (mean-subtracted / divisively-normalized WTA, opponent signed deploy). Its low-floor #1 fix was empirically refuted; superseded by the dendritic-research verdict above. |
| `workflow-phase-domain-read-design.txt` | The FHRR/phase-domain read design (high-confidence) — later **refuted empirically** by the numpy concept test (`raw/signed_conductance/step10_phase_concept.py`): phase-coherence is a similarity classifier, not the linear one objrel needs. Kept as the honest negative. |
| `workflow-adversarial-audit-delta-readout.txt` | The adversarial audit that reframed the arc (found D1 = the canonical test is position-trivial; D2 = the dead source-check). |

The delta-rule read-out + objrel probes are in `research/findings/raw/signed_conductance/` (`step5`…`step11`).
The full narrative + planned work: the handoff doc linked above.
