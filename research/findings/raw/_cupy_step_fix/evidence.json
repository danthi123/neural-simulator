{"backend":"numpy (byte-identity) + cupy (fix target)","device":"CPU determinism + RTX 3090 for the cupy step",
"provenance":{"runner":"sim/bridge.py fix via parallel-attack workflow wf_ad189f54 lane1 (opus) + adversarial verify (opus); orchestrator re-verified determinism","seed":42,"git_sha_parent":"939be514e","command":"SIM_BACKEND=numpy pytest tests/test_determinism.py -q ; SIM_BACKEND=cupy build+step surprise organ + full brain_chat turn"},
"root_cause":"the surprise (and similar) organ homeostat/block-gain write path does 'import scipy.sparse as sp; bridge.cp_connections = sp.csr_matrix(...)', so on the cupy backend cp_connections is a scipy CSR; the step's effective_connections_matrix.T @ fired_2col becomes scipy @ cupy -> 'Implicit conversion to a NumPy array is not allowed' (caught+logged CRITICAL -> the full-faculty GPU chat turn hangs)",
"fix":"_ensure_connections_on_backend() called first in _run_one_simulation_step coerces cp_connections to the active backend's csp sparse type (isinstance(csp.spmatrix) guard -> no-op on numpy since csp IS scipy.sparse -> byte-identical; rebuild once as cupyx csr on cupy via cp.asarray, handling host + hybrid data); + max(1,counter) guard at the 3 mock-stat // sites",
"verification":{"numpy_byte_identity":"determinism 9/9 (ran 3x incl. orchestrator) + sha256 of a 200-step numpy firing hash matches pre-fix","cupy_step":"surprise organ steps on cupy (0.0 vs contradict 4.98 Hz, correct discrimination); load-bearing proof: monkeypatch the coercion to no-op -> the exact crash reproduces","full_faculty_cupy_turn":"webapp.server.brain_chat returns a grounded answer over 2 turns on SIM_BACKEND=cupy; grep 'Implicit conversion' = 0"},
"impact":"unblocks the FULL-faculty fluent GPU server (all organs) AND the continuous tick on cupy AND idle BTSP consolidation"}
