---
type: finding
status: mixed
date: 2026-09-02
mechanism: onebrain-integration-6seed-cupy-harvest
board: 108 / one-brain integration program (Phase 0 harvest)
seeds: [42, 43, 44, 100, 101, 102]
artifact: research/findings/raw/_onebrain_single_pool_flip_6seed.json
---

# One-brain integration Phase-0 harvest (6-seed cupy): single-pool soak GO, C1 surprise→world-model GO, C2 surprise→metacog NO-GO

**2026-09-02, cupy, 6 seeds (42/43/44/100/101/102).** Three queued verifies from the integration program's
Phase-0 harvest landed together. Two GO, one honest NO-GO. Each is a MIGRATION/de-risk verdict (byte-identity /
emergence + vary-lesion), not yet a production-default flip.

## 1. Single-pool merge soak — GO 6/6 (the Wave-1 FOUNDATION is validated)

`research/findings/raw/_onebrain_single_pool_flip_6seed.json`: `go: true`, `status: "GO"`, `n_go: 6/6`, cupy,
per-seed all True, with a `preconditions` block. The `BRAIN_ONEBRAIN_SINGLE_POOL` merge — the substrate that
co-locates the core organs on ONE shared pool (default-OFF, byte-identical-off) — passes its full 6-seed soak.
This was the open question gating Wave 1 (merge comprehension + source_provenance onto the single pool): the
foundation is now proven, so Wave 1 no longer rests on an unverified base.

## 2. C1 D2-surprise → E2-world-model — GO 6/6 (promotes from 2-seed SMOKE)

`research/findings/raw/_crossedge_surprise_worldmodel_6seed.json`: `GO: true`, `n_go: 6/6`, cupy. The world-model's
OWN spiking prediction-error unit (D2 surprise) GATES an online update of the E2 forward model's state→prediction
transition — 6/6 seeds grow the transition toward the observed next state, load-bearing (vary/lesion). This is a
genuine learned spiking cross-edge (surprise informs the world-model), promoted from its earlier SMOKE-GO PARTIAL.

## 3. C2 D2-surprise → E1-metacog — NO-GO 3/6 (honest negative; the METHOD, not the capability)

`research/findings/raw/_crossedge_surprise_metacog_6seed.json`: `GO: false`, `n_go: 3/6`, cupy. A learned
D2-surprise → E1-metacog-confidence edge grows from the substrate's own rate-Hebbian rule and is load-bearing on
only 3 of 6 seeds — not robust. Per NO-DEFER this is a verdict on the METHOD (the plain rate-Hebbian three-factor
rule for THIS edge), not the capability (surprise lowering confidence). NEXT MECHANISM (not deferred): the C1 edge
that DID pass 6/6 uses a prediction-error-GATED update; C2 should adopt the same DA/error-gated three-factor form
(or a normalized/competitive write) rather than the plain Hebbian growth, then re-verify 6-seed. Banked as the
next rung, not a wall.

## What this unblocks / opens
- Wave 1 (comprehension + source_provenance onto the single pool) now has a GO foundation — the build agent's
  numpy smoke validates the merge logic on proven ground.
- C1 is ready for its default-ON flip sequence (6-seed GO → flip → host-path retire), gated by the Phase-1 battery.
- C2's next rung is the error-gated rewrite (mirroring C1's winning form).
