# gap#4 learned self-predicting microcircuit (Sacramento Eq.9) — CPU-rate 6-seed GO: apical-silent-when-correct is EARNED; the learned-vs-FA accuracy separation is spiking-only (2026-07-24)

## Result — CPU-rate 6-seed GO (6/6), `_gap4_learned_microcircuit_selfpredict_derisk` (NO `sim/` edit; MicroNet plastic-Eq.9 W^PI in the runner)
- **credit ≫ reservoir at full data:** best credit (fixed_fa/micro) **0.694 vs reservoir 0.117** (+0.577; every seed ≥ +0.10; weakest seed +0.314). Depth genuinely required (depth-sep True 6/6; 1-layer→deep-oracle 0.19→0.93). Transport-ceiling 0.960 above the learned arms.
- **THE GENUINELY-NEW OBSERVABLE — apical-silent EARNED:** the plastic-Eq.9 self-predicting interneuron W^PI gives silent_ratio **0.087** (|apical|_correct 0.09 ≪ incorrect 1.07; selfpred_cos **0.89** → W^PI LEARNED to predict Y from a noisy init) vs the frozen-noisy W^PI ratio **1.004** (cos 0.01, never cancels). ⇒ the Sacramento (2018) "apical silent when correct" property is EARNED by learning, not initialized. This is the load-bearing new capability.
- **All anti-cheat guards pass:** no-weight-transport 3-guard (AST no-forward-W + W-scramble byte-identity + micro/kp post-hoc transport-free); transport-ceiling guard CORRECTLY fails (Y==W^T detected = the guard works); triple lesion collapses (apical 0.117, freeze-deepest 0.210, freeze-learned-fb==fixed_fa); double permuted (shuffled-target 0.145 ≤ chance; shufE 0.130 collapses); input-selectivity non-degeneracy (code_div 0.98, frac_diff 1.00). cfg.seed set.

## HONEST SCOPE (G2 — do not overclaim)
- **At RATE the learned arms are accuracy-indistinguishable from fixed-FA:** micro is byte-identical to fixed_fa every seed (0.870/0.870, 0.407/0.407, …); kp at-or-below fixed_fa. So the CPU-rate run confirms credit>reservoir + all guards + the earned apical-silent, but **cannot separate the learned surpass from fixed-FA on accuracy** — that separation is a SPIKING op-point question.
- **Op-point gap EXISTS but its clean form is spiking, not rate:** depth sweep — depth-2 fixed_fa 0.858 ≫ reservoir 0.130 (no gap); depth-3/4/5 fixed_fa collapses to 0.11–0.12 BELOW reservoir 0.15 (the FA depth-wall = the "reservoir ≥ frozen-B FA" regime the learned feedback could close). BUT at depth-3 (a) kp/micro also collapse at rate (the learned feedback does NOT close the gap at rate — reconfirms the separation is spiking-only), and (b) the depth-3 rate regime is CONFOUNDED (depth-sep False, transport-ceiling only 0.33 = unstable at this budget). ⇒ the clean fixed-FA-fails-while-credit-succeeds regime is the SPIKING op-point (sparsity), not rate depth.

## Next — the 3090-gated decisive phase (gates i CPU-GO ✓ + iii op-point-gap ✓ met; ii freed-lane pending)
Build the `--on-bridge` arms (reuse the committed default-False `enable_bdsp_microcircuit`/`cp_v_apical`/`cp_bdsp_int_drive` plumbing — additive/guarded; the on-bridge wiring is a follow-up the CPU runner doesn't yet implement). Establish a CLEAN spiking regime at depth-2 (healthy oracle/ceiling ~0.96, healthy fixed-FA at rate) where spiking SPARSITY degrades fixed-FA's alignment to ≤ reservoir, then test whether the LEARNED transport-free feedback (kp / plastic-Eq.9 micro) climbs held-out back above the reservoir toward the ceiling (held-out ≥ reservoir ≥ frozen-B FA). If no reachable sparsity yields that clean regime → honest verdict "scale-frontier at this budget," not a wash-closer. Substrate discipline: `cfg.seed` (never `actual_seed_used`); hash `cp_neuron_firing_thresholds` across two builds before trusting any seed number. Lowest GPU-tail priority (longest negative history).
