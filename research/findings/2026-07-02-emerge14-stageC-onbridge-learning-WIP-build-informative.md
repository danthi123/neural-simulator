# EMERGE-14 / rung-4 Stage C — WIP / build-informative: the HTM permanence LEARNING partially self-organizes on the real bridge (the committed `sim/` `fused_htm_permanence_update` kernel updates `cp_connections.data` over a pre-allocated potential pool → context-specific connectivity, branch 0.000→0.500), but the de-risk's PREDICTION currently reads `cp_connections.data` HOST-side (not via the bridge's coincidence), so it is not yet a faithful on-substrate test and the dAP-lesion control does not collapse. The precise next pass: route prediction through the bridge's coincidence recurrence (Stage-B2 `_prime_from_winners`) + resolve the residual branch-merge.

**2026-07-02 (autonomous).** Runner `research/runners/_emerge14_stageC_onbridge_learning_derisk.py`. Reuse-by-import EMERGE-9b/12/13 + the committed `sim.kernels.fused_htm_permanence_update`. CPU numpy-backend.

## What works (validated this pass)
- **The `sim/` kernel LEARNS on the substrate array.** Over a dense cross-column potential pool pre-injected into a real `SimulationBridge`'s `coincidence_detector` pathway at sub-connected `p_init=0.24`, the committed `fused_htm_permanence_update` kernel (Bouhadjar three-term rule), applied to `cp_connections.data` each symbol with per-synapse `pre_last`/`post_now`/`hfac` gathered from the cached COO, self-organizes CONTEXT-SPECIFIC connectivity: the learned-connectivity trace shows col2 correctly splits into a cue0 sub-SDR (cells 32-35 ← col0) and a disjoint cue1 sub-SDR (36-39 ← col1). The kernel + the on-substrate weight array are doing the learning.
- **Two bugs root-caused + fixed (systematic, not tuned):**
  1. **Dense-pool matching/allocation** — EMERGE-13's `perm>0` "wired"/match metrics are FULL for every cell on a pre-allocated dense pool (every cross-column pair starts at `p_init`), so nothing ever allocates. Fix: matching + prediction use CONNECTED (`perm>=perm_conn`).
  2. **Allocation RACE** — keying allocation on CONNECTED count differentiates only AFTER a cell connects (several epochs of slow permanence rise), so during epoch 0 both contexts allocate onto the SAME cells before either connects → merge (branch 0.000). Fix: the dense-pool "committed" metric = has incoming perm ABOVE the initial `p_init` (a winner is potentiated above `p_init` after ONE step → differentiates IMMEDIATELY, the dense-pool analogue of EMERGE-13's grow-from-zero `perm>0`). This lifted branch 0.000 → 0.500.

## What's NOT yet right (the precise remaining work)
- **Prediction is host-side, not on-substrate.** `connected_predict` reads `cp_connections.data` and counts connected-active synapses in host numpy — it does NOT run the bridge's coincidence recurrence. Consequence: the **dAP-LESION control does not collapse** (`lesion 0.500 == htm 0.500`), because the lesion turns off the coincidence mechanism but the host readout ignores it. For a FAITHFUL on-substrate test (and a valid lesion anti-cheat), prediction must go through the bridge's WEIGHTED coincidence — reuse Stage-B2 `_prime_from_winners` (set the prior winners as `cp_prev_firing_states`, step, read the primed apical cells) so the graded permanence pool grades the plateau and the lesion severs it.
- **Residual branch-merge (0.500).** Even with the immediate-committed fix, half the branches still merge (the eval read `predict=[6,7]` for a cue0 sequence — both branches primed from col5). Likely the LAST-hop allocation (col6 vs col7 from col5's two sub-SDRs) or the eval winner-selection; to resolve alongside the coincidence-prediction rework.

## Honest status
The on-substrate LEARNING mechanism is validated in principle: (a) EMERGE-13 gated the flat three-term rule (6-seed GO); (b) the committed kernel's math is validated; (c) this pass shows the kernel self-organizing context-specific connectivity on the bridge's `cp_connections.data`. The remaining work is de-risk PLUMBING (route prediction through the bridge coincidence so it's a faithful on-substrate test with a working lesion control) + the residual merge — NOT a wall. rung-4 Stage C is close: the kernel learns on the substrate; the test harness needs the bridge-coincidence prediction path.

## Status of the rung-3 → rung-4 arc
Rung-3 fully validated + fully spiking (EMERGE-9b/c/d). Rung-4: Stage A' (two-compartment dAP GO, `sim/`) → Stage B2 (INFERENCE on-substrate GO, 6-seed) → Stage C design gate (flat-permanence three-term == 9d, 6-seed GO) → Stage C kernel + config (committed, byte-inert) → **Stage C on-bridge learning (this, WIP: kernel learns on `cp_connections.data`, branch 0.500; needs bridge-coincidence prediction + residual-merge fix)** → rung-4 complete.

## Artifacts
`research/runners/_emerge14_stageC_onbridge_learning_derisk.py`, `research/findings/raw/_emerge14_stageC_onbridge_learning.json`, `sim/kernels.py` (`fused_htm_permanence_update`), `sim/config.py` + `sim/bridge.py` (`enable_htm_learning` + `cp_htm_z`, byte-inert). Prior: `2026-07-02-emerge13-stageC-flat-permanence-design-gate-GO.md`, `2026-07-02-emerge12-stageB2-bridge-tm-on-substrate-GO.md`.
