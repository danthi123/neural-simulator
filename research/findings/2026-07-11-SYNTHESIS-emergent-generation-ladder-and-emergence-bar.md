# SYNTHESIS (2026-07-11) — the emergent-generation ladder (Rungs 2–4) + the emergence-bar close: a transformer-free, on-spikes next-token generator with LLM-like properties whose category structure is DISCOVERED from experience, not hand-installed

A single map of the 2026-07-11 arc (see the per-result findings for the load-bearing detail). The through-line: a **fixed spiking reservoir** (EMERGE-82 `OnBridgeLSM`) + a **shallow one-step-local-delta read-out** (Widrow-Hoff on the clean next-token error — NO backprop-through-time, NO deep credit) is an on-brain next-token generator, and it has the properties that distinguish a language model from a lookup table. Every result is 6-seed (dev 42/43/44 + blind 100/101/102), reuse-by-import, **NO `sim/` edit**, and — the discipline the owner asked for — **adversarially verified**, which caught real defects (below).

## The ladder (each a distinct LLM-like property, on the same generator)
- **Rung 1** (prior day): the reservoir+read-out predicts the next token, beating bigram/trigram — dynamics-earned, not memorized.
- **Rung 2** (GO, adversarially verified SURVIVES): a non-fading **working-memory latch** carries a *distal* discourse referent the fading reservoir forgets — it holds a topic ~32 clauses back (reservoir-only decays to chance; the buffer restores it 0.97; slot-scramble/referent-shuffle/bag/bigram all collapse).
- **Rung 3** (positive-with-residuals): the generator **generalizes to a never-produced SUBJECT** via a shared category code (a novel subject gets the right category-specific continuation); the shared CODE and the reservoir's MEMORY are each load-bearing on every seed (one-hot codes → chance; a memoryless reservoir → 0).
- **Rung 4** (positive; a host-latch shortcut caught + rejected, then rebuilt honestly + self-corrected): **order-decisive systematic recombination** — the same token multiset in different orders yields different held-out continuations (role reversal), which no order-blind model can do. A 3-skeptic adversarial-verify **rejected a first version** that had smuggled the answer through a hand-coded `ANIMAL_CAT[prefix[0]]` lookup (the reservoir was decorative); the rebuild reads only the reservoir's own state trajectory, and a same-day correction showed the reservoir **genuinely carries the thematic role** (a role-targeted read-out extracts the agent category from the clause-final state 0.72 / all-taps 1.0; permuted collapses) — the EMERGE-78 form→role property on the generation path.

## The emergence-bar close (the owner's core concern: does the structure EMERGE, or is it hand-installed?)
Rungs 3–4 depended on a hand-coded category block. That structure is now **discovered from experience**, validated at three increasing levels of grounding, each with a load-bearing discovery-scramble control:
- **Step 1** (GO, 6-seed, adversarially verified): a **fixed random Marr-Albus codon (F.12)** of category-structured features surfaces a category the ladder generalizes on (0.96), and destroying the feature co-occurrence collapses it. The verify caught an **overclaim** (a learned pooler is NOT needed — the fixed codon is ≥ it) and a **reproducibility bug** (a `hash()` process-salt), both fixed.
- **Step 2** (honest boundary): corpus **distributional** co-occurrence over OVERLAPPING contexts is too weak at toy scale (both a fixed codon and a learned pooler fall to ~0.5) — distributional induction is a large-corpus phenomenon; only the disjoint case works.
- **Step 3** (positive, the master-directive close): the category is discovered from **REAL PERCEPTION** — the animals are SEEN through the Gabor/V1 front end and their category emerges from visual similarity (0.77); a per-image PIXEL-SCRAMBLE collapses it on every seed, isolating the visual shape as the cause.
- **Spiking follow-on** (positive): the category surfacing is now **fully on spikes** — a Marr-Albus coincidence codon on a real `SimulationBridge` (no numpy kWTA) matches the numpy version (0.72), so the pipeline is pixels → Gabor/V1 → **spiking codon** → **spiking reservoir** → generalization.

## What this establishes
A transformer-free, on-spikes generator with the LLM-defining properties — next-token prediction, distal-context working memory, generalization to novel content, order-decisive recombination — whose **category structure emerges from experience (perception), not a hand label**, with the reservoir + read-out unchanged throughout. This is the direct, rigorously-verified answer to the "are we playing whack-a-mole?" concern: the mechanisms are learned/emergent and the hand-installed structure has been removed and replaced by discovery, with adversarial verification catching every place a shortcut crept in.

## Scale — first real-text evidence + the honest lever map (2026-07-11, owner-prompted)
- **The generator beats the bigram on REAL TEXT (3/3 GO).** Off the controlled template grammar, on TinyStories words (V=200), the reservoir generator's held-out next-token cross-entropy beats the bigram on all 3 seeds (mean +0.18 nats), permuted-corpus control collapsing — so the dynamics-earned generation is not a toy-corpus artifact. Honest: the edge is on CE/perplexity, not top-1 accuracy; modest but real. (`2026-07-11-SCALE-emergent-generator-beats-bigram-on-real-text.md`)
- **The scale lever is DATA, not raw reservoir capacity.** At fixed data (1400 sentences) the CE margin over the bigram PEAKS at a moderate reservoir and DECLINES with size (+0.152 @300 → −0.020 @600 → −0.420 @1000); regularization only partly mitigates (wd=0.01 recovers 1000 from −0.42 to −0.25, not to positive) → the large reservoir is data-starved, not merely over-regularized. Co-scale data with reservoir size. (`2026-07-11-SCALE-reservoir-size-vs-data-levers-on-real-text.md`)
- **GPU/infra (measured):** the reservoir-LM runs CPU-correctly (cupy is ~5–10× SLOWER — the tiny reservoir's Izhikevich `_run_one_simulation_step` is launch-bound + a per-step `to_host` sync). The existing `enable_rf_cudagraph` megakernel is RF-resonate-specific and does NOT cover this path. The enabling infra for the data-scale lever = an Izhikevich-step CUDA-graph + on-device firing accumulation + sentence batching (all precedented, none built for the reservoir path). DEFERRED until the data-scale lever is proven (the decisive n_pool×data run is a heavy CPU job).

## Honest scope + the OPEN frontiers (this is not a closed chapter)
- **Scale is the big open question — now with a first data point and a lever map.** Everything above the real-text probe is a TOY grammar. Whether this ladder SCALES to fluent open-domain conversation — the actual replacement for the ~21M transformer scaffold — is unproven and is the major frontier; the first real-text evidence (generator beats bigram) + the data-is-the-lever map (above) are the opening moves. The honest interim remains: shrink/keep the scaffold fluency-only behind the no-confab gate while the emergent path matures.
- **Remaining hand-structure + spiking gaps (mapped):** the perceptual vocabulary is tiny (firming the accuracy needs a grammar/vocab extension, not a bigger codon — tested); the Gabor/V1 front end and the linear read-out are still numpy (EMERGE-36 has spiking V1); MEETS/action codes are fixed; Rung 5 (open-vocab spiking spell-out, GPU) + Rung 6 (multi-clause discourse via the D3 register) are mapped but unbuilt.
- **Deep-credit-on-spikes** (dendritic BDSP) stays DEPRIORITIZED — the reservoir path needs no deep learning rule.

## Findings (this arc)
`2026-07-11-RUNG2-*`, `-RUNG3-*`, `-RUNG4-*` (+ its correction), `-EMERGENT-category-codes-*` (step 1), `-corpus-cooccurrence-scale-boundary` (step 2), `-perception-grounded-*` (step 3), `-fully-spiking-perception-codon-*`. Runners `_emerge_reservoir_lm_*`; CI guard `tests/test_reservoir_lm_emergence.py`. Builds on the prior-day Rung-1 + EMERGE-33/34/35/62/78/82.
