# The reslm batched reservoir — the mission-critical SCALE enabler — is VALIDATED (65× + copy-consistent), usable for the decisive larger-data co-scale (2026-07-12)

**Context (the strategic re-anchor this session):** deep-credit-on-spikes is a thoroughly-mapped NON-critical-path boundary (parked); the critical path is the open-generation ladder, whose one big open question is SCALE (does the fixed-reservoir + shallow-read-out generator's CE margin over the bigram GROW with co-scaled data, or is it bounded?). The tractable co-scale probe already went NEGATIVE at (n_pool=800, 2400 sentences) — data-starved. The blocker to running MORE data is that the on-bridge reslm reservoir (`ReservoirStates(OnBridgeLSM)`) collects each sentence's state per-token through `_run_one_simulation_step`, a heavy CPU job. The 2026-07-11 SYNTHESIS named "sentence batching" as the missing enabler.

**⇒ BUILT + VALIDATED (this session's block-diagonal batched-forward technique, applied to the reslm reservoir):** `research/runners/_reslm_batched_reservoir_derisk.py` — M sentences collected AT ONCE as M disjoint block-diagonal copies of the SAME reservoir on ONE bridge (clone the reference reservoir's recurrent wiring + W_in + init into every copy; drive each copy with its own sentence, zero-pad after it ends; step all M in LOCKSTEP → one `_run_one_simulation_step` per position amortizes the per-call overhead M×). NO `sim/` edit.
- **SPEEDUP: 65× (M=6, n=60)** — far above the BDSP net's 2–13× (the reservoir's per-token stepping is overhead-dominated, so batching wins big). This makes the larger-data co-scale AFFORDABLE.
- **COPY-CONSISTENCY: 0.0** — all M copies driven by the same sentence give IDENTICAL states ⇒ the batched reservoir is a valid, consistent, deterministic reservoir. Since the read-out trains AND evaluates on the batched states, the CE-vs-bigram TREND (the scale question) is valid on it.
- **Serial residual: 0.028** vs the standalone `OnBridgeLSM.final_state` — a small wiring-extraction detail (the block-diagonal reservoir differs slightly from the standalone). It does NOT affect the scale question (a consistent reservoir is all that's needed). Chasing it to bit-exact (the BDSP-net residual-hunt pattern) is optional polish if a faithful == the shipped reservoir is later wanted.
- Prerequisite confirmed: the reslm reservoir is reproducible across builds (OnBridgeLSM sets `heterogeneity_seed`; no `num_traits=1` fix needed, unlike the BDSP net).

**NEXT (the decisive mission-critical run):** wire `final_state_batch` into `_emerge_reservoir_lm_realcorpus_derisk.py`'s state collection, then run the larger-data co-scale — (n_pool, n_train) pushed well past (800, 2400) now that it's 65× cheaper — and answer: does the CE margin over the bigram RECOVER/GROW with more data (→ the emergent generator SCALES toward fluency) or stay bounded (→ the reservoir's fundamental limit, consistent with Ueda's ~60% BLiMP ceiling)? This is THE open question for the emergent-generation path, and it is now affordable.
