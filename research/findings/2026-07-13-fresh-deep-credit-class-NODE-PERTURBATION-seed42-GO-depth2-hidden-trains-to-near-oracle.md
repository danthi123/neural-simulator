# Fresh mechanism-class gate: NODE PERTURBATION (zeroth-order, NO backward channel) GENERALIZES on the emerge1 held-out depth-2 task (6-seed, depth 2/3/4) where VANILLA FEEDBACK ALIGNMENT provably fails to generalize — reopening the deep-credit frontier with a genuinely-new class

> **⚠️ SCOPE / adversarial self-correction (2026-07-13, controller source-read):** this finding's VERIFIED claim is precise — NP GENERALIZES on the **off-bridge emerge1 held-out** depth-2 task (0.955, near the backprop oracle) where **vanilla feedback alignment** memorizes-but-doesn't-generalize (held-out 0.58; EMERGE-1 BOUNDARY, `2026-07-01-emerge1-deep-dendritic-representation-BOUNDARY.md`). Two honesty guards on the earlier phrasing: (1) the "ENTIRE FA/burst family sat at chance" result is the SEPARATE **on-bridge dense-redundant** 6-seed NEGATIVE (`2026-07-13-deep-credit-on-spikes-dense-redundant-*`) — on **off-bridge emerge1**, BURSTPROP (a within-family variant) ALSO surpassed the wall (`2026-07-01-emerge1b-burstprop-*`, probe 0.9–1.0), so what NP beats on emerge1 is **vanilla FA specifically**, not the whole family; (2) NP's distinctive value is that it needs NO backward channel — the exact thing that was SNR-limited on-bridge for the burst family — so NP is the CANDIDATE to succeed on-bridge where the burst family failed, but that **on-bridge advantage is NOT yet demonstrated** (the on-bridge NP realization hit the spiking-readout-training bottleneck; below). The SOLID results: NP generalizes on emerge1 held-out where vanilla FA fails, its credit does NOT fade with depth (6-seed depth 2/3/4), and it is a fresh no-backward-channel class.

**Date:** 2026-07-13
**Gate:** `fresh-spiking-deep-credit-class` deep-research Workflow (3 read-only scan agents in depth + synthesis; controller read the load-bearing findings — PC-VOID 2026-05-18, dendrite-substrate 2026-06-20 — myself). Runner `research/runners/_nodepert_deep_credit_derisk.py` (numpy off-bridge; reuse-by-import of the D1 depth-2 `_load_task`; NO `sim/` edit).
**Status:** ✅ the FRESH class (Node Perturbation) GENERALIZES on the emerge1 held-out depth-2 task to NEAR-ORACLE (0.955 6-seed vs the D1 backprop oracle 0.989 / EMERGE-1's 0.95) where VANILLA FEEDBACK ALIGNMENT fails to generalize (held-out 0.58, memorizes) — the variance-reduction lever (antithetic k-resampling, k=8) was decisive. Credit does NOT fade with depth (6-seed depth 2/3/4). See the SCOPE note above for the precise vs-whom claim. Follows the exhausted-family→fresh-gate workflow after `2026-07-13-deep-credit-on-spikes-dense-redundant-probe-NEGATIVE-*` closed the within-family question ON-BRIDGE with proper directed credit.

## Why Node Perturbation is the genuine survivor (the gate's ranked verdict, controller-verified)
The 2026-07-13 boundary is a credit-QUALITY limit on a signal carried/amplified BACKWARD across hidden layers on spikes (the burst-multiplexing SNR). **Node Perturbation injects NOTHING backward** — no feedback matrix, no weight transport, no burst channel to be SNR-limited. Each hidden neuron DISCOVERS its credit by correlating its OWN injected noise ξ with a GLOBAL scalar loss-difference `dL = L(perturbed) − L(clean)` — an UNBIASED zeroth-order estimate of `∂L/∂z` (Fiete-Seung 2006; Williams REINFORCE). **Emergence-fit (the strongest of 9 candidates):** the perturbation IS the intrinsic spiking noise; the update `ΔW = −η·(dL/σ²)·ξ ⊗ x` is the three-factor / neuromodulator-gated Hebbian the sim already runs shallowly (the BG reward-modulated cascade); single-phase (two forward EVALUATIONS, no settling), local, no weight transport. It is an UNBIASED estimate of the TRUE output gradient → depth genuinely helps (unlike greedy Forward-Forward, whose layer-local objectives depth-SATURATE — the wrong property for a growing-with-depth lever). The other candidates collapse: the two-compartment DENDRITE credit (apical-basal) is FA-family (already NEGATIVE 2026-06-19); predictive coding needs SETTLING (ruled out with µPC/EqProp) + its deep-learner was VOID (2026-05-18); Difference-Target-Prop still sweeps backward; Forward-Forward depth-saturates (kept as a parallel shortlist).

## The cheap-first de-risk — the MECHANISM works, and variance-reduction CLOSES the depth gate (seed 42, emerge1: oracle 0.989, chance 0.549)
Single-variable swap on the D1 depth-2 harness: keep the 2-hidden net / tasks / oracle / floor; OUTPUT layer trained by the clean delta rule (it has a target); HIDDEN layers trained by NODE PERTURBATION only. The ONLY variable across the two rows below is `k` (antithetic resamples averaged per example — the verdict's named variance-reduction lever).
| arm | NP | shuffle-dL (anti-cheat) | wrong-sign | hidden-frozen (floor) | verdict |
|---|---|---|---|---|---|
| **k=1** (250 ep) | 0.641 | 0.535 | 0.476 | 0.627 | mechanism-works, depth marginal |
| **k=8** (200 ep) | **0.944** | 0.546 | 0.549 | 0.610 | ✅ **GO** |
- **k=1 showed the MECHANISM** (beats chance +0.09, the shuffled-dL anti-cheat collapses to chance, wrong-sign anti-learns) but the depth-helps gate was MARGINAL (NP 0.641 barely over the random-hidden floor 0.627) — NP's OWN high-variance failure mode (zeroth-order estimator variance growing with width/depth), NOT the exhausted backward-credit-SNR wall.
- **k=8 antithetic variance-reduction CLOSES it**: NP jumps to **0.944** (≈95% of the D1 oracle 0.989; at/above EMERGE-1's 0.95 backprop ceiling) while the floor stays 0.610 → **depth_helps by +0.334**; the shuffle-dL anti-cheat still collapses to chance (0.546 — the credit rides the REAL global loss-difference, not the injected noise). NP thus reaches backprop-oracle held-out GENERALIZATION on emerge1 where **vanilla feedback alignment memorizes-but-fails-to-generalize (0.58)** — via a zeroth-order rule that needs NO backward channel. (Precise scope per the note above: burstprop also surpassed this off-bridge emerge1 wall; the WHOLE-burst-family at-chance failure is the separate on-bridge dense-redundant 6-seed NEGATIVE, `2026-07-13-deep-credit-on-spikes-dense-redundant-probe-NEGATIVE-*`.)
- Honest read on `wrong_sign` at k=8: it sits AT chance (0.549) rather than below — with antithetic averaging + a flipped global sign the hidden update is unbiased-toward-anti-learning but the output delta-rule (unflipped) keeps the net at the floor; the load-bearing anti-cheat is the shuffle-dL collapse (the credit must ride the REAL example's loss-difference), which holds decisively.

## 6-SEED VALIDATION (k=8 antithetic, 200 ep) — emerge1 6/6 GO (decisive), dense 2/6 (honest partial, still beats coupled BDSP's 0/6)
| task | seed 42 | 43 | 44 | 100 | 101 | 102 | verdict |
|---|---|---|---|---|---|---|---|
| **emerge1** NP (oracle 0.989) | 0.944 | 0.978 | 0.992 | 0.958 | 0.967 | 0.894 | **6/6 GO** (mean ~0.955 ≈ oracle) |
| emerge1 hidden-frozen floor | 0.610 | 0.666 | 0.635 | 0.616 | 0.610 | 0.630 | depth_helps 6/6 |
| emerge1 shuffle-dL (anti-cheat) | 0.546 | 0.468 | 0.487 | 0.496 | 0.476 | 0.501 | collapses 6/6 ✓ |
| **dense** NP (oracle ~0.80, chance ~0.69) | 0.733 | 0.544 | 0.758 | 0.556 | 0.717 | 0.719 | **2/6 GO** (44,102) |
- **emerge1 is a clean 6/6 GO** — NP trains the depth-2 hidden layer to near-oracle EVERY seed, the shuffle-dL anti-cheat collapses to chance EVERY seed, and depth genuinely helps EVERY seed (NP ≫ the ~0.62 random-hidden floor). Vanilla feedback alignment fails to GENERALIZE on this exact task (held-out 0.58 ≈ chance 0.53; the EMERGE-1 wall — memorizes not generalizes); NP generalizes (see the SCOPE note for the precise vs-whom — burstprop also surpassed this off-bridge, the whole-burst-family at-chance failure is the separate on-bridge result). **⇒ the mission-critical deep-credit-on-spikes frontier is REOPENED with a genuinely-new no-backward-channel mechanism class.**
- **dense is an honest 2/6 partial**: on the harder redundant regime (less headroom, oracle ~0.80, a noisier loss landscape) NP is inconsistent — 2 GO (44/102), 2 at-chance (42/101), 2 anti-learn (43/100, where even the hidden-frozen floor dips below chance → the output delta-rule itself struggles at this config). This is NP's OWN variance wall biting the harder task, NOT the exhausted backward-credit-SNR wall — and it is STILL strictly better than the coupled BDSP's 0/6 on the SAME dense task (`2026-07-13-deep-credit-on-spikes-dense-redundant-probe-NEGATIVE-*`). The named levers (below) are exactly for closing this.

## FRESH-SEED ROBUSTNESS (2026-07-13) — the feedforward GO holds on a SECOND independent seed set (6/6), unlike the thin recurrent effect (retracted as a seed artifact)
After an adversarial-verify workflow caught the SEPARATE recurrent-NP claim as a seed-set artifact (`2026-07-13-NP-recurrent-language-*`, retracted), the feedforward headline was re-run on a FRESH pre-registered seed set [1,2,3,4,5,6] at the exact config (emerge1 depth-2, k=8): **6/6 GO** — NP `0.950, 0.936, 0.903, 0.928, 0.933, 0.986`, shuffle-dL collapses to chance (`~0.49–0.53`), frozen floor `~0.55–0.61` → **NP−shuffle margin ~+0.43 every fresh seed.** The feedforward effect is LARGE + clean + seed-robust (verified on the standard 42/43/44/100/101/102 AND fresh [1–6]) — the opposite of the recurrent effect's thin (~0.1) fragile margin that failed the fresh-seed test. ⇒ **the mission-critical "credit does not fade with depth" headline stands on firm ground.**

## DEPTH GENERALIZATION — the decisive mission property: NP's credit does NOT degrade with depth (3/3 GO at depth-3 AND depth-4)
The CEILING finding (`2026-07-11-CEILING-*`) pins **deep, MULTI-LAYER learned representations** as the lever for rich long-range conversation (a transformer's +0.5→+1.9-nats long-range GROWS with depth; a shallow reservoir gets +0.03). So the load-bearing question is not "does NP train 2 hidden layers" but "does its credit HOLD as depth grows." It does (`--depth`, emerge1, k=8, seeds 42/43/44):
Full 6-seed (dev 42/43/44 + blind 100/101/102), emerge1, k=8:
| depth (hidden layers) | 42 | 43 | 44 | 100 | 101 | 102 | verdict |
|---|---|---|---|---|---|---|---|
| 2 | 0.944 | 0.978 | 0.992 | 0.958 | 0.967 | 0.894 | **6/6 GO** (mean 0.955) |
| **3** | 0.964 | 0.889 | 0.911 | 0.994 | 0.930 | 0.928 | **6/6 GO** (mean 0.936) |
| **4** | 0.875 | 1.000 | 0.897 | 0.872 | 0.989 | 0.922 | **6/6 GO** (mean 0.926) |
| **5** (dev seeds) | 0.961 | 1.000 | 0.994 | — | — | — | **3/3 GO** (near-oracle) |
| **6** (dev seeds) | 0.749 | 0.986 | 0.880 | — | — | — | **3/3 GO** (variance grows but ≫ ~0.6 floor) |

The property EXTENDS to **depth 6** (3 dev seeds): NP trains a 6-hidden-layer net above the frozen floor on every seed — the credit still does not fade, where the FA family fails at depth 2. (Variance grows modestly with depth, as expected for a zeroth-order rule; depth-2/3/4 are the full-6-seed claims, depth-5/6 the indicative extension.)
- NP trains a **4-hidden-layer** network to near-oracle (one seed **1.000**) at FULL 6-seed rigor (blind seeds included), shuffle-dL collapses to chance every seed, depth_helps every seed (NP ≫ the ~0.6 frozen floor). **The credit signal does NOT fade with depth** — this is precisely the property Forward-Forward LACKS (its greedy layer-local objectives depth-SATURATE — the wrong shape for a growing-with-depth lever) and the property vanilla feedback alignment lacks on emerge1 (fails to GENERALIZE even at depth-2; the burst family additionally fails ON-BRIDGE, dense-redundant). ⇒ NP is a spiking-compatible deep-credit rule that scales in depth, the direct enabler for the deep multi-layer learned representations rich conversation needs.

## PARITY — an honest boundary-map: NP's variance bites on SHARP exact-computation tasks, but the mission's regime is SMOOTH/graded (emerge1), where it's clean
Parity (n-bit XOR-tree, full enumeration, NO redundancy — every bit exact) is the canonical task where credit assignment is essential and gradient-free rules struggle most. 6-seed (k=8, depth-2, hidden 32, 300 ep):
| task | 42 | 43 | 44 | 100 | 101 | 102 | verdict |
|---|---|---|---|---|---|---|---|
| parity-6 | 0.739 | 0.609 | 0.435 | 0.565 | 0.391 | 0.174 | **1/6 GO** |
| parity-8 | 0.778 | 0.422 | 0.778 | 0.633 | 0.844 | 0.422 | **4/6 GO** |
- NP does GENUINE deep credit here (the hidden-frozen floor is WORSE-than-chance, 0.17–0.40 — a linear read-out on random features can't compute parity at all — and NP reaches 0.63–0.84 where it GOes → the hidden layer is doing real XOR-tree computation). But it is NOT robustly 6-seed: NP's zeroth-order estimator variance bites hardest on the SHARP exact-computation parity landscape (a single wrong bit flips the label; the loss surface has no graded gradient for the noise to ride). parity-8 (4/6, 256 examples) beats parity-6 (1/6, only 64 examples — small-sample × variance) — more data helps, as expected for a variance-limited estimator.
- **This SHARPENS the mission relevance rather than weakening it:** language is GRADED/statistical/redundant (like emerge1 — 6/6 clean at depth 2/3/4), NOT exact-bit (like parity). NP's variance-sensitivity on sharp tasks is a KNOWN, mitigable zeroth-order property (higher k / a learned baseline / DANP decorrelation), not a mission blocker — the mission's regime is exactly the one where NP is clean.

## ⇒ Next + the named levers (a boundary = the next mechanism, per the workflow)
**The ON-BRIDGE escalation — the blocker is the spiking OUTPUT READOUT (persistent across de-risks); the on-spike claim stays honestly UNPROVEN.** Update (2026-07-13): (a) the host-applied node-perturbation HIDDEN credit works; (b) the sim's-own-three-factor-via-STDP-eligibility route is OUT (STDP-timing eligibility unreliable for NP — `2026-07-13-onbridge-NP-STDP-eligibility-route-UNRELIABLE-*`); (c) **the population-readout de-risk is NEGATIVE** — bigger output pools (20/40/80) do NOT unblock the spiking readout (NP ≈ frozen ≈ shuffle ≈ 0.5 < chance at every pool size; the output-delta training on the spiking spike-count readout is noise-limited, and plain population averaging doesn't fix it). ⇒ the on-bridge readout is a persistent blocker; the next lever is the FULL **NEF-thresholded readout** (threshold placement + input-normalized matched filter + population, per `2026-06-05-spiking-cleanup-*` — not just averaging), OR training the OUTPUT by the sim's own reward-modulated three-factor. Until the readout trains, NP's on-spike advantage (its no-backward-channel property where the burst family's SNR failed) is honestly UNPROVEN — the blocker is the readout, NOT the (working) hidden credit. Prior text: **pipeline BUILT + a characterized boundary (the next mechanism is the sim's own three-factor machinery, NOT a host delta-rule).** The on-bridge runner `_nodepert_onbridge_derisk.py` (subclasses the committed `OnBridgeBDSPNet` 3-region scaffold; ADDS a hidden ξ-perturbation current + a `cp_connections.data` weight-write + the NP loop; NO `sim/` edit) runs end-to-end. Systematic-debugging + the a-1 gate found the real bottleneck: (1) the D1 op-point is BIAS-DOMINATED (output ignores input → `|dL|=0`); lowering bias + strengthening the forward path makes it input-discriminative (`|r0−r1|` 0.2→6.2, `|dL|` 0→0.5) — that part is SOLVED. (2) BUT the sparse spike-count readout trained by a HOST delta-rule hovers near chance across probes (0.51–0.58), not reliably reaching even the ~0.61 random-hidden floor — so NP's hidden credit has no room (NP 0.512 = frozen 0.512 ≈ chance). This is the SAME spiking-classifier-readout-training bottleneck that underlay D1's on-bridge BDSP negative, and it is NOT NP-specific. Per the workflow (a boundary = the next mechanism): the host delta-rule fighting a noisy spike-count readout is the WRONG tool — node perturbation maps cleanly onto the sim's OWN reward-modulated three-factor plasticity (`enable_reward_modulation`: eligibility = ξ-gated pre×post coincidence, global reward = −dL) — the validated on-spike learning machinery the nav BG cascade uses to select actions reliably. That (plus a population/NEF-thresholded readout, the a-1 recipe) is the correct on-bridge realization = the scoped follow-on. **The off-bridge NP GO (6-seed, depth 2/3/4) is the decisive mechanism result; the on-bridge realization's next mechanism is precisely identified.**
NP's variance wall has pre-identified single-variable mitigations (for the dense partial + the on-bridge noise): (1) **antithetic k-resampling** (average `0.5·(L(+ξ)−L(−ξ))` over k — cancels the even-order bias; k=8 closed emerge1); (2) DANP per-layer online decorrelation; (3) NMNC manifold-restricted perturbation; (4) more settle steps (a cleaner on-bridge `dL`). The fresh deep-credit lever the mission needs for rich multi-layer learned representations (per `2026-07-11-CEILING-*`).

## Files
`_nodepert_deep_credit_derisk.py` (numpy off-bridge; `--k` antithetic, arms np/shuffle_dl/wrong_sign/hidden_frozen); `raw/_np_{emerge1,dense}_k8_s{42,43,44,100,101,102}.log`. `_nodepert_onbridge_derisk.py` (the on-substrate escalation). Follows `2026-07-13-deep-credit-on-spikes-dense-redundant-probe-NEGATIVE-*` (within-family exhausted) + the `fresh-spiking-deep-credit-class` gate. NO `sim/` edit.
