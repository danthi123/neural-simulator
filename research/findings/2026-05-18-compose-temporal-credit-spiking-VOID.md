# Compose temporal-credit -> spiking-bridge integration — honest VOID: the minimal-slice in-bridge instrument is not soundly constructible at the pre-registered config (the pre-registered THREE-STATE gate refused to fabricate a science verdict from an instrument whose positive control did not learn); the thrice-validated temporal-credit principle is NOT refuted — the remaining composition blocker is sharply localized to the spiking-dynamics integration; the deeper-engineering decision is handed to the OWNER eyes-open

## TL;DR

Converging prior evidence, each anti-cheat-maxed with the honest
ceiling firmly stated: **TD value-function critic = VALIDATED PASS**
(temporal credit is a sound substrate); **compose x temporal-credit =
VALIDATED PASS** (it is the missing composition ingredient on an
abstract task); **compose pop-transfer = cheap-GREEN, scrutinized
harder than a FAIL** (the mechanism TRANSFERS to distributed
sparse-population codes + noisy readout). The single remaining honest,
non-config-cranked question -- the one the compose-PASS ceiling
explicitly named a SEPARATE later gated increment -- was: does the
validated temporal-credit/eligibility mechanism still bridge the
verb->motor compositional bind-gap when wired into a MINIMAL slice of
the REAL spiking `sim.bridge` concept-pool architecture (the actual v16
BOUNDARY setting)?

The full disciplined arc ran: design -> writing-plans ->
subagent-driven build (fresh subagent per task; controller
trust-but-verify every commit-scoped diff with the protected set
byte-empty; **dedicated adversarial reviewer** on the two load-bearing
modules before Phase B; a dead-agent mid-task failure recovered cleanly
with no partial commit). The decisive controller-only kill-safe
multi-seed in-sim run COMPLETED on the real spiking bridge (864
neurons, 67510 synapses).

**Result: GATE = VOID, 5/5 seeds.** Recomputed from the recorded JSON
(mandatory anti-cheat smell-test, no re-run, no bar-tuning, no
config-crank), seeds 42/43/44/45/46:

- **V1 positive control NOT met:** the in-bridge true-TD critic on the
  NO-GAP verb->motor binding scored **0.0 / 0.25 / 0.375 / 0.0 /
  0.125** heldout (frozen `_CBR_V1_ACC_MIN = 0.80`; 8-way chance =
  0.125). The no-gap positive control does NOT learn the bijection in
  the minimal spiking concept-pool harness.
- Consequently `td` (gapped) and all controls sit at/near chance
  (uninformative -- nothing learns because V1 itself fails).
- The pre-registered instrument-validity-FIRST gate correctly returned
  **VOID -- explicitly NOT a science PASS/FAIL** (reason recorded:
  "V1 unmet: in-bridge TD did NOT learn the no-gap verb->motor bind
  (instrument unsound)"). Frozen `_CBR_*` byte-unchanged in the JSON.

The dedicated adversarial review had already predicted this with
FULL-scale evidence BEFORE the decisive run (and found NO
science-integrity holes across 8 vectors -- the decisive Vector-1
"discrimination genuinely isolated to the temporal-credit mechanism"
held: `hebbian_no_trace` is a verified-faithful v16-cold-start analog,
RNG/eligibility-charging parity exact, not a strawman). The
diagnosed cause: the minimal-slice spiking population-vote
readout/teacher/reward loop never bootstraps a single rewarded episode
during training (`n_rewarded = 0`), so the TD error delta is
identically 0, the bridge's native eligibility->reward weight path is
never engaged, and even the no-gap positive control cannot learn. The
THREE-STATE gate did exactly its job: it refused to fabricate a science
verdict from an instrument that did not soundly measure.

## Honest scope (no overclaim, no underclaim, no spin)

- **VOID is NOT a science FAIL of temporal-credit composition.** A
  science FAIL would require a SOUND, DISCRIMINATING instrument
  (V1 >= 0.80, controls failing) with the temporal-credit `td`
  nonetheless not bridging the gap. Here V1 itself is unmet -- nothing
  was validly measured. Reporting this as "temporal credit fails to
  compose in-sim" would be a lie.
- **VOID does NOT refute the temporal-credit principle.** That
  principle is thrice-validated and anti-cheat-maxed: the TD-critic
  PASS (sound substrate), the compose-abstract PASS (the missing
  composition ingredient), and the pop-transfer cheap-GREEN
  (transfers to distributed sparse-population codes + noisy readout).
  Those stand unaffected.
- **What this VOID DOES establish (the decision-relevant finding):**
  at the pre-registered minimal-slice config, a SOUND discriminating
  in-bridge instrument is **not constructible** -- the minimal spiking
  concept-pool harness cannot bootstrap a learnable rewarded signal
  through its population-vote readout/teacher/reward loop (the no-gap
  true-TD positive control does not learn). This **sharply localizes
  the remaining composition blocker to the spiking-dynamics
  integration itself** (closing a learnable rewarded readout loop in
  the spiking concept-pool substrate), NOT the temporal-credit
  principle -- exactly as this increment's design pre-stated its
  honest ceiling: "a faithful FAIL/VOID sharply triangulates that the
  remaining blocker is spiking-dynamics integration, not the
  thrice-validated temporal-credit principle, NOT a license to
  escalate."
- This is an honest **VOID / instrument-not-soundly-constructible-at-
  minimal-slice** boundary, propagated WITHOUT spin.

## Why this is NOT config-cranked, and the genuine remaining option (OWNER decision)

The design pre-registered the minimal slice and the honest ceiling
verbatim, and that "a FAIL/VOID is the strongest honest triangulation
... NOT a license to escalate." Autonomously enlarging/retuning the
in-bridge harness now to force the V1 positive control to bootstrap
would be config-cranking past the pre-registered minimal-slice
terminus toward a desired outcome -- exactly what the discipline
forbids. So this honest VOID is propagated and the genuine remaining
option is handed to the OWNER as an eyes-open strategic decision, NOT
taken autonomously (mirroring the dendritic fair-scale VOID's
owner-decision handoff):

> The only thing that would make the in-bridge V1 positive control
> soundly constructible is a deeper instrument-engineering pass on the
> spiking concept-pool readout/teacher/reward loop (so a rewarded
> episode bootstraps and the bridge's native eligibility->reward path
> engages) -- a larger speculative investment with no cheap precursor
> for THIS sub-problem (the cheap pop-transfer GREEN validated the
> mechanism, NOT the spiking-readout bootstrapping). Honest facts for
> the decision: (a) the temporal-credit PRINCIPLE is thrice-validated;
> (b) the blocker is now sharply localized to closing a learnable
> rewarded readout loop in the spiking substrate; (c) it is a further,
> larger, separately-gated engineering effort. Authorize it
> deliberately (eyes-open), or accept this boundary. I will not
> autonomously spend on enlarging the in-bridge harness against a
> pre-registered minimal-slice terminus.

## What is preserved / validated (unaffected)

Byte-UNMODIFIED + green across the WHOLE compose-bridge build: the
no-confabulation moat (`abstention_gate` + `tests/test_abstention_gate.py`
7/7), `sim/td_value_critic.py`, `sim/compose_temporal_bind.py`,
`sim/kernels.py`, `sim/bridge.py`, `sim/neuromodulators.py`,
`sim/train_checkpoint.py`, `sim/backend.py`,
`sim/dendritic_plasticity.py`,
`research/runners/text_minimal_isolation.py`, every frozen `*_core`
(incl. `compose_bind_core`, `td_critic_core`, `dendritic_fair_core`).
`git diff e8a99a2..HEAD` on the protected set is empty. NO autograd
anywhere. The three prior validated results (TD-critic PASS,
compose-abstract PASS, pop-transfer cheap-GREEN) and their honest
ceilings are unaffected.

## Anti-cheat discipline (why this VOID is trustworthy)

The pre-registered THREE-STATE + V1-instrument-validity-FIRST design
did exactly its job: an instrument whose positive control did not learn
produced VOID, never a fabricated PASS/FAIL. The dedicated adversarial
review (51 tool-uses, real FULL-scale in-bridge probes, RNG-parity +
weight-path tracing) found NO science-integrity holes and PREDICTED
this VOID with evidence before the decisive run. A dead-agent mid-task
API timeout was recovered cleanly (no partial commit; protected
byte-verified; divergent orphan removed; fresh implementer). Every
number recomputed from the single recorded JSON (no re-run, no
bar-tuning); the frozen `_CBR_*` are byte-unchanged; the
deeper-engineering option is handed to the owner rather than
autonomously config-cranked. The validated no-confab moat -- the
project's distinctive contribution -- remained byte-identical and 7/7
green throughout.

## Files / evidence

- Result: `research/findings/raw/compose_bridge_gate.json` (GATE VOID;
  per-seed nogap_td 0.0/0.25/0.375/0.0/0.125 << 0.80 [V1 unmet],
  td/controls ~chance; instrument_valid False).
- Build (6 scoped commits, all controller-verified, protected
  byte-empty in every commit-scoped diff): `f9bb2ff` Task-0 pin ->
  `8f8ae03` `compose_bridge_core.py` (own frozen `_CBR_*` THREE-STATE)
  -> `50165a0` `compose_bridge_gate.py` (kill-safe; reuses
  `build_biological_brain_regions` + the bridge native
  `cp_eligibility_trace` reward path + NM + `train_checkpoint`
  UNMODIFIED; faithful v16-cold-start analog) -> `c02f7c8`
  LOAD-BEARING no-harm.
- Design/plan:
  `docs/plans/2026-05-18-compose-temporal-credit-spiking-integration-{design,implementation}.md`.
- Converges with / does NOT refute:
  `2026-05-18-td-value-critic-temporal-credit-PASS.md`,
  `2026-05-18-compose-temporal-credit-PASS.md` (the thrice-validated
  temporal-credit principle this VOID localizes the remaining blocker
  AWAY from).
