# Q3 engram-prior'd laminar PC-inference generative composition — cheap falsify-first precursor returned VOID-class (instrument not soundly+discriminatingly constructible cheaply); heavy build NOT warranted per the pre-registered rule; 7th-direction triangulation of the arc-wide boundary; NOT a refutation of the durable-sound PC-inference principle; autonomous NON-STOP pivot to Q4 (NO owner-deferral)

## TL;DR

Q2 (two-module constrained decoding) FAILed honestly and the arc
pivoted NON-STOP (CORRECTED OPERATING MODE) to Q3: use the
**durable-SOUND predictive-coding INFERENCE** (the PC arc's empirically
held positive -- the per-step linear-Gaussian PC relaxation update ==
backprop-gradient direction, cos~0.995, 5 seeds; ONLY the PC
training-LOOP accumulation VOIDed) as a generative inference engine
with its top-down prior set by the **independently-validated reward-FREE
Tonegawa engram one-shot bind**, NO PC-training-loop and NO new local
learning rule. The design pre-registered a MANDATORY cheap
falsify-first precursor (Q3c: pure inference-composition, zero
learning) that runs FIRST in the EXACT linear-Gaussian
Whittington-Bogacz setting whose per-step soundness held, with the
pre-registered rule: GREEN -> heavy build; NEGATIVE/VOID -> NO heavy
build, honest propagate + autonomous pivot Q4.

**Cheap precursor result (throwaway pure-numpy probe, NO autograd,
deleted post-decision; recorded evidence
`research/findings/raw/q3_laminar_pc_probe_recorded.txt`):
VOID-class / NEGATIVE.**

- **Run 1 was instrument-INVALID by construction (honestly caught, not
  propagated):** the engram prior was the EXACT ground-truth latent, so
  the `no_inference` control = `W @ prior` = `x_true` = cosine **1.000**
  at every rung -- a non-discriminating control. (This is the inverse
  of the rigging the design explicitly warned against.)
- **ONE root-caused, mode-AGNOSTIC, transparently-logged fix** (the
  established discipline -- mirrors the PC probe's invalid
  `random_feedback` -> faithful `no_inference` replacement): the
  recalled engram prior was made a DEGRADED recall (drop a fraction of
  active units + noise), faithful to the validated ~87.5% Tonegawa
  stim-recall (a perfect prior was the UNFAITHFUL simplification),
  applied IDENTICALLY to every condition; frozen `_Q3_*` science bars
  byte-UNCHANGED.
- **Run 2 (the one allowed re-run): VOID-class.** V1 single-concept
  reconstruction 0.751/0.770/0.765 (< 0.80 -- instrument positive
  control unmet at cheap scale) AND `shuffled_prior`
  0.710/0.799/0.859 (>> 0.50 -- a WRONG-concept prior STILL
  reconstructs the true composed pattern, i.e. the control does NOT
  fail -> NON-DISCRIMINATING). The honest diagnosis: the partial
  bottom OBSERVATION of the already-composed target dominates the
  linear-Gaussian inference, so the top-down engram prior does little
  discriminative work -- one cannot distinguish prior-driven
  generative composition (the actual Q3 question) from
  observation-driven reconstruction. `PC` did beat `no_inference` by a
  decisive +0.20-0.26 and scale-POSITIVELY (0.836->0.906->0.935) --
  the sound PC inference genuinely does real work -- but a
  non-discriminating instrument cannot validly answer the Q3 question
  regardless.

**Verdict (no spin):** cheap precursor **VOID-class** (instrument not
soundly + discriminatingly constructible at the cheap linear-Gaussian
setting: run 1 non-discriminating via a trivially-perfect control; run
2 non-discriminating via observation-dominance + V1 unmet). Per the
pre-registered rule and the iron law (ONE root-caused mode-agnostic
fix only; NO third crank; a non-discriminating instrument is an honest
VOID, not a science refutation), the **heavy Q3a in-sim build is NOT
warranted**; this is propagated and the arc PIVOTS NON-STOP to Q4 (NO
owner-deferral).

## Honest scope (no overclaim, no underclaim, no spin)

- **NOT a refutation of the durable-sound PC-inference principle.**
  The Whittington-Bogacz per-step equivalence (cos~0.995) is
  unaffected and remains the PC arc's durable positive. Indeed in run
  2 the sound PC inference beat prior-readout by +0.20-0.26 and
  scale-positively -- it does real generative work. What is NOT
  cheaply constructible is a SOUND + DISCRIMINATING instrument that
  isolates engram-prior-DRIVEN generative composition from
  observation-driven reconstruction in the linear-Gaussian regime.
- **NOT a science FAIL** in the strong sense (which needs a sound
  discriminating instrument with the mechanism nonetheless failing).
  Nothing was validly isolated: V1 unmet AND a control did not fail.
  Classified VOID-class -- the conservative, non-overclaiming reading.
- **DOES converge with the arc-wide recurring boundary -- now
  7x-triangulated from independent directions:** a principled,
  analytically-checkable signal is repeatedly constructible
  (temporal-credit PASS; PC per-step cos~0.995; Q2's scale-POSITIVE
  non-vacuity 0.333->0.583->0.625), but cheaply turning it into a
  sound, DISCRIMINATING, scale-confident *generative* instrument /
  learner at feasible local scale is the recurring infeasibility
  (dendritic readout-confound BOUNDARY; compose-bridge
  spiking-bootstrap VOID; PC-learning-loop VOID; Q1 in-bridge VOID;
  Q2 frozen-criterion FAIL [scale-positive near-miss]; now Q3
  cheap-precursor VOID). Temporal-credit remains the sole clean
  validated PASS (boundaried at spiking integration).
- **NOT config-cranked.** Exactly ONE root-caused, mode-agnostic,
  transparently-logged probe-construction fix was applied (degraded
  engram recall = MORE faithful to the validated 87.5% stim-recall,
  not less); the frozen `_Q3_*` were byte-UNCHANGED; NO third
  iteration toward an outcome; the heavy build is NOT pursued past the
  pre-registered cheap-NEGATIVE terminus.

## Why NOT owner-deferred (CORRECTED OPERATING MODE)

An honest cheap-precursor VOID/NEGATIVE is propagated (this doc +
pillar n=76 + both remotes) and IMMEDIATELY triggers the autonomous
pivot to the next genuinely-distinct architecture (Q4: concept-level
pretraining objective for the surrogate-grad cortex rewired into the
validated v16 concept-pool substrate -- Phase-2.3a's NEGATIVE was a
char-level-objective mismatch, an architecture fault not mere scale; a
concept/word-prediction objective + concept-pool readout is an
unexplored architecture). NO "handed to the owner" deferral, NO stop.

**Decision-relevant context surfaced for the owner (NOT a re-score,
NOT permission-seeking):** across the 7x triangulation, the single
strongest result toward the owner's scale-confidence deliverable
remains **Q2** -- a SOUND instrument with a REAL, SCALE-POSITIVE
discriminating signal (constrained non-vacuity 0.333->0.583->0.625
monotone, K=12 & K=24 PASS, shuffled-grounding decisively 0.000,
no-confab bit-identical) that failed the FROZEN every-rung-PASS
criterion ONLY because the smallest rung K=6 was below an absolute
bar, where a scale-confidence question is fundamentally about the
TREND. The Q2 findings already recorded (honestly, NOT as a re-score)
that a TREND-PRIMARY scale-confidence criterion is a legitimate
SEPARATE pre-registerable increment. Default per the frozen pivot rule
= proceed to Q4; the trend-primary Q2-refinement remains the recorded
highest-value alternative the owner may elect to prioritize.

## What is preserved / validated (unaffected)

The probe was a single THROWAWAY pure-numpy file (deleted; recorded
numeric output preserved at
`research/findings/raw/q3_laminar_pc_probe_recorded.txt`). NO
protected/validated module created, modified, or touched: the
no-confab moat (`abstention_gate` + test, 7/7), `sim/bridge.py`,
`sim/dendritic_plasticity.py`, `sim/td_value_critic.py`,
`sim/compose_temporal_bind.py`, `sim/kernels.py`, every frozen
`*_core`, all byte-identical. NO autograd anywhere (the PC relaxation
is the hand-derived exact linear-Gaussian MAP gradient -- the
validated setting). All prior validated results
(TD-critic/compose-abstract/pop-transfer PASS, the engram + no-confab
validations, the PC durable cos~0.995 positive) are entirely
unaffected.

## Anti-cheat discipline (why this VOID is trustworthy)

The mandatory cheap falsify-first precursor did its job: run 1's
trivially-perfect `no_inference` control was honestly caught as
instrument-INVALID (not propagated, not spun as a FAIL); exactly ONE
root-caused mode-agnostic fix (degraded engram recall, MORE faithful
to the validated mechanism) was applied with frozen `_Q3_*`
byte-unchanged and transparently logged; the single allowed re-run
returned a non-discriminating (shuffled-control-passes) + V1-unmet
result -> VOID-class, not a fabricated PASS and not a spun FAIL; NO
third crank; the heavy build is NOT pursued past the pre-registered
cheap terminus; the arc PIVOTS non-stop rather than deferring. The
durable-sound PC-inference positive is preserved and never spun as
"Q3 works."

## Files / evidence

- Recorded probe output (both runs):
  `research/findings/raw/q3_laminar_pc_probe_recorded.txt` (run 1
  instrument-invalid no_inf=1.000; run 2 VOID-class V1 0.75-0.77<0.80,
  shuffled 0.71-0.86>>0.50 non-discriminating, PC>no_inf +0.20-0.26
  scale-positive; one root-caused mode-agnostic fix; no third crank).
- Design (durable): `docs/plans/2026-05-18-Q3-laminar-PC-inference-nonPCloop-learning-design.md`
  (pre-registered the cheap precursor + the NEGATIVE->pivot-Q4 rule;
  outcome stamped).
- Converges with / does NOT refute:
  `2026-05-18-predictive-coding-cheap-gate-VOID-with-durable-V1-positive.md`
  (the durable cos~0.995 positive this builds on, unaffected),
  `2026-05-18-Q1-engram-bootstrap-temporal-credit-in-bridge-VOID.md`,
  `2026-05-18-Q2-constrained-decode-FAIL.md` (the scale-positive
  near-miss; the trend-primary refinement is the recorded
  highest-value alternative).
- PIVOT (autonomous, non-stop): Q4 = concept-level pretraining
  objective for the surrogate-grad cortex rewired into the validated
  v16 concept-pool substrate; design to be written next.
