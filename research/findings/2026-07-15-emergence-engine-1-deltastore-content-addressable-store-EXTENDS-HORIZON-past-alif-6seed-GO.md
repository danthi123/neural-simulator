# Emergence-engine #1 de-risk — GO (6-seed): a CONTENT-ADDRESSABLE fast-weight store over the frozen reservoir EXTENDS the memory horizon PAST the fixed-ALIF window, where the fading reservoir provably collapses; the load-bearing cache-bag + key-shuffle anti-cheats both collapse

**Date:** 2026-07-15 (compute unlocked) · **Runner:** `research/runners/_reslm_deltastore_horizon_derisk.py` (reuse-by-import: `RateReservoir` from the eprop harness; numpy-CPU; NO `sim/` edit). Research gate: `2026-07-15-emergence-engine-research-gate-...` (#1 mechanism). **Verdict: GO (6/6) for the horizon-extension headline; the delta-vs-additive refinement is an honest secondary NEGATIVE at this operating point.**

## The frontier this surpasses
The established open frontier (research gate MOVE 1): the frozen-ALIF-reservoir + 2-stage-read-out stack holds/integrates/combines only within the ~5–15-token ALIF window; the genuine irreducible residual is a **non-fading, content-addressable, specific-item STORE** retrievable by current relevance. Passive multi-timescale / longer-τ is a diluting AVERAGE (refuted two ways, 2026-07-11). The biology answer (STP synaptic WM = delta-rule fast weights = Hopfield/CA3 read) supplies off-diagonal binding STRUCTURALLY (pairwise outer-product write) → dissolves the RTRL/e-prop-diagonal wall at O(n²)/step, local, transport-free, no BPTT.

## The test (retrieve-by-content, so a position-cache provably fails)
`[STORE k1 v1 k2 v2  f_1..f_T  PROBE k_j] -> predict v_j` (j random; k/v disjoint content symbols; K=16, chance 0.0625). The model must MATCH the probed key to the stored key and retrieve its PAIRED value. Over each trial a fast-weight `M` is written from the reservoir STATES (`M += eta (v − M k) kᵀ`) and read at PROBE (`v_hat = M q`, q = probe state); `[q ; v_hat]` → a ridge read-out (the "2-stage read-out" as a linear map). ARMS: none (state only, v_hat=0) / delta / additive / **cachebag** (v_hat = mean stored value, position-agnostic) / **keyshuffle** (write keys shuffled → content-addressing broken). All arms share the same feature dim (4n) so the comparison is fair.

## Result (6-seed 42/43/44/100/101/102; chance 0.0625)
| T (dependency length) | none (fading reservoir) | **DELTA store** | cachebag | keyshuffle |
|---|---|---|---|---|
| T=5 (within window)  | 0.36–0.45 | **0.87–0.96** | ~0.50 | ~0.47 |
| T=15 (past window)   | 0.26–0.35 | **0.90–0.98** | ~0.50 | ~0.45 |
| **T=30 (well past)** | **0.16–0.27** (→chance) | **0.85–0.98** | ~0.51 | ~0.46 |
- **HORIZON extension: 6/6 GO.** The store holds ~0.9+ at T=30 while the fading reservoir collapses toward chance. The content-addressing is genuine: **cachebag** (position-agnostic mean) sits at chance-between-values (can't pick which), and **keyshuffle** (same store structure, keys mismatched to values) collapses — so it is the correct key→value binding doing the work, not "having a store" or a positional cache (the load-bearing 2026-07-11 caution is defeated).

## Honest secondary NEGATIVE — the delta-vs-additive refinement is NOT load-bearing here
At the interference operating point (P=6 pairs held at once, T=30): DELTA ≈ ADDITIVE (0.35–0.79, delta not > additive on any seed; 0/6). The research gate's claim that additive SATURATES under interference (so delta's error-correcting write is needed) does NOT bite with single-pass writes over reservoir-state keys — additive suffices because the reservoir states are near-orthogonal enough that one-pass outer-product accumulation does not interfere destructively at P=6. The delta-rule advantage would require correlated keys + multiple write passes (a next rung IF interference capacity becomes the binding limit). ⇒ the CONTENT-ADDRESSABLE STORE is the load-bearing mechanism; the DELTA write is an optional refinement, not required at this scale.

## STP-realization result (6-seed) — the SELECTIVE WRITE is the load-bearing refinement, NOT the delta rule
Added a continuous-write STP mode (bind prev_state→curr_state at EVERY timestep with a per-step tau_f decay — the realistic activity-silent-WM regime where fillers interfere) and swept decay ∈ {1.0, 0.98, 0.9}, 6-seed:
- **Explicit-pairs store (only the k→v pairs written = a PERFECTLY SELECTIVE write): 6/6 horizon-GO** (robust, the headline above).
- **Continuous-write STP store (every transition written, no selection): only 3/6 horizon-GO, and biological tau_f decay HURTS** (delta_stp 0.81→0.45→0.19 as decay 1.0→0.98→0.9 on seed 42; horizon fails on seeds 43/44/101). The `delta_stp > additive_stp` refinement is **1/6** (the 1-seed smoke's TRUE was seed-42-luck; additive_stp ties/beats delta_stp on 5/6).
- **⇒ the mechanism:** the explicit-pairs store is robust because it writes ONLY the relevant bindings; the continuous store is fragile because the k→v binding is swamped by filler-transition interference over reservoir-state keys (the same key-quality/interference issue). **So the SELECTIVE (input-gated) WRITE — store only the salient binding, not every transition — is the load-bearing refinement (the synthesis's Mamba/KOSS point), NOT the delta rule (which did not robustly beat additive).** This SHARPENS the on-bridge design: **RUNG6d's FS-WTA selection IS the selective write — it is not optional; it is what makes the STP store robust.** The delta-rule refinement is deprioritized (additive suffices once the write is selective).

## ⇒ Ladder + next — with the honest a-1 bound (do NOT re-tread the naive Stage-1)
- **What this GO establishes (scoped precisely):** a content-addressable fast-weight store extends the memory horizon past the fixed-ALIF reservoir, 6-seed, anti-cheated, no recurrent credit, no BPTT, O(n²)/step — **WHEN the keys are clean content signatures** (this KV task PRESENTS an explicit key token + re-presents it at PROBE). It validates the research gate's #1 MECHANISM (a non-fading store dissolves the recurrent-credit wall) — given good keys.
- **⚠️ THE a-1 BOUND (checked our own record BEFORE scoping Stage 1): the naive real-language Stage-1 is a KNOWN 3-seed NEGATIVE.** `2026-07-11-cross-sentence-content-addressable-retrieval-NEGATIVE-the-long-range-win-is-a-cache-bag-not-content-addressing`: on real WikiText, a cross-sentence content-addressable store over the reservoir substrate (fixed OR e-prop-learned keys) is NO BETTER than a uniform cache-bag (`content − shuffle ≈ 0`; the uniform bag is the BEST arm), because real-text reservoir states are DIFFUSE linguistic signatures, not the clean content keys this KV task supplies. So "the store on WikiText context-depth buckets" would RE-DERIVE that negative — do not build it.
- **The reconciliation + the TRUE remaining frontier:** the store MECHANISM is sound; the binding limit for real language is **KEY QUALITY** — a learned representation that encodes DISTAL LINGUISTIC structure finely enough for content-addressing to beat a bag. That is the input-representation / deep-credit lever (R3: learning W_in >> W_rec; today's emergent-input-representation gate: directionally real but does NOT beat the tuned bigram at tractable scale → scale/data-bound). So this GO does NOT by itself advance real-language fluency; it confirms the store is the right STRUCTURE and re-localizes the wall to KEY QUALITY.
- **⚠️ SECOND a-1 CATCH — the on-substrate STP realization is ALREADY BANKED (do NOT build it).** `2026-07-13-RUNG6e-onsubstrate-STP-binder-...` + RUNG6f built the on-bridge STP store across TWO architectures (FS-WTA feedforward + Mongillo self-exciting-assembly) over ~15 iterations and BANKED it per the emergence bar: the FS-WTA + rate-window-Hebbian compound has FOUR mutually-coupled tensions (weak inter-region conductance ~1000× under an external current; drive/FS balance; no first-mention differential; Hebbian specificity needs a clean high-rate winner the delicate WTA can't deliver) and "shared FS gives GLOBAL inhibition, not lateral WTA." Tellingly, the **FS-WTA SELECTIVE WRITE this STP result just proved load-bearing is EXACTLY the on-substrate piece RUNG6e/6f could not cleanly deliver** — so my numpy result explains WHY the on-bridge build is hard (it needs a clean lateral-WTA selective winner, which the region-framework compound doesn't give). Building the on-bridge store = re-treading a banked hard problem.
- **⇒ HONEST STATE of the store thread: MECHANISM-COMPLETE in numpy** (RUNG6c/6d + this #1 horizon GO + the selective-write finding), **on-bridge realization BANKED** (RUNG6e/6f, emergence-bar-deprioritized substrate-engineering), **real-language application KEY-QUALITY-BOUND** (2026-07-11 + today's input-repr gate: scale/deep-credit-bound). Both the on-bridge wall (needs a clean lateral-WTA) and the real-language wall (needs distal-discriminating keys) converge on the same deep frontier the research gate + RUNG6e/6f named: **the EMERGENCE ENGINE — a learning substrate that produces better (distal-structure-encoding) representations, from a stream.** The store confirms the STRUCTURE (a content-addressable store dissolves recurrent credit); the residual is representation QUALITY, which is learned, not a retrieval or WTA bolt-on.
- **NEXT (redirected away from both banked/known-negative rungs):** the store thread is closed at the numpy-mechanism level; the genuine frontier is the emergence engine's REPRESENTATION quality (learned keys encoding distal structure), which is the deep-credit/input-representation frontier at scale — the standing priority, not a fresh store build.
- Reuse-by-import; NO `sim/` edit. Runner: `_reslm_deltastore_horizon_derisk.py`.
