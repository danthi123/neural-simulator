# P1.2 GNW workspace + deliberation loop — 6/6-seed GO, ADVERSARIALLY VERIFIED (affect-framing corrected; the GO is the re-entrant loop, NOT affect) (2026-07-24)

## Result
The P1.2 Global Neuronal Workspace + deliberation faculty (Dehaene ignition + re-entrant multi-cycle broadcast,
validated by multi-hop reasoning) closes from a PARTIAL 4/6 to a genuine **6/6-seed GO** (local CPU,
`SIM_BACKEND=numpy`, seeds 42 43 44 100 101 102). `reentrant_3hop_acc=1.000` every seed (chance 0.025); the
re-entrant loop is load-bearing; the no-confab moat holds every seed.

## The fix (runner-only, additive; NO `sim/` edit) — a legitimate statistics improvement, NOT a control-cheat
The 4/6→6/6 flip came ENTIRELY from two **control-granularity false-alarms**, not from the primary signal:
- seed 43 failed `recue_lesion_3hop_acc = 0.125` (> the strict 0.10 bar); seed 102 failed `permuted_3hop_acc = 0.125`.
- Root cause: a single random re-cue / permuted-chase draw over 8 held-out chains coincidentally landed on the 3-hop
  target for exactly 1/8 = 0.125 chains, tripping the 0.10 bar even though the control's TRUE null is ~chance (0.025).
- The fix (`N_CONTROL_SHUFFLES=8`, RNG streams `si*1009`) estimates each control null as the **MEAN over 8 independent
  randomizations** (the textbook null estimate) instead of one noisy draw: seed 43 recue mean 0.031, seed 102 permuted
  mean 0.0625 — both clear 0.10 → 6/6.
- **Why it's legitimate, not a launder** (adversarially verified): the PRIMARY `reentrant_3hop_acc` is 1.000 for every
  seed in BOTH configs — the fix does not touch it. It only sharpens the anti-cheat NULL estimate. Cleanest proof: seed
  102's `permuted_single` (a single draw) is STILL 0.125 (would still fail), yet the 8-shuffle mean 0.0625 passes — the
  seed is rescued purely by estimating the null as a mean. A genuine leak would raise EVERY shuffle (mean stays high, the
  same 0.10 bar still catches it), and the runner also reports `*_max`. MC validation: the reformulated recue null match-
  rate ~0.0249 vs chance 0.025 (unbiased).

## The GNW crux — the re-entrant multi-cycle loop is GENUINELY load-bearing (both controls collapse)
- **One-cycle** (`max_cycles=1` → one PROPOSE→EVALUATE→COMMIT→BROADCAST cycle, reaches only the 1-hop neighbour ch[1],
  scored vs the 3-hop want ch[3]; ch[1]≠ch[3] structurally) → `onecycle_3hop_acc = 0.000` every seed.
- **Lesion** (`attractor_weight=0` → the dense self-recurrent NMDA attractor cannot sustain ignition → hop-0 falls below
  the IGNITE threshold → `committed=None` → abstains) → `lesion_3hop_acc = 0.000` every seed.
- Both collapse while `reentrant_3hop_acc=1.000` ⇒ a single pass does NOT suffice; the workspace's genuine contribution is
  (i) ignition-threshold gating (commit-vs-abstain per hop) and (ii) carrying the committed concept IN SPIKES
  (argmax→assignment round-trip) to re-cue the next hop. The single-step relational reflex (composer's `query_patient`) is
  workspace-independent and survives lesion — the dissociation keystone.

## Seeding integrity (verified)
`cfg.seed = int(seed)` (`:177`), NOT `actual_seed_used`; builds a real `SimulationBridge`; empirically same-seed→
identical / diff-seed→different `cp_neuron_firing_thresholds` hash. Deterministic (OU + heterogeneity off), so every
completed seed reproduced bit-for-bit in independent re-runs.

## ⚠️ HONEST CAVEAT — AFFECT is a HOST-SET salience-scalar STAND-IN, NOT neural affect, and is NOT in the GO gate
The runner reports an `affect_directed_frac` (1.0) vs `affect_lesion_directed_frac` (mean 0.58) diagnostic. Corrected
framing (adversarial verification + the runner's own docstring, L56/L427-435): the "affect" here is a **host-set salience
scalar** — the affect branch routes `IGNITE_PA=2500` to the value candidate vs `2500×DISTRACTOR_FRAC` (~750) to the
non-value (a 3.3× drive), so `affect_directed=1.0` is a **near-tautological** consequence of a strong drive winning the
biased-competition WTA. It is a REPORTED DIAGNOSTIC, **not a term in the `seed_go` gate** — the 6/6 GO comes entirely from
the re-entrant deliberation + the recue/permuted/lesion/moat controls, INDEPENDENT of affect. The **full neural-affect
wiring** (a spiking P0.3 affect-state region biasing the workspace drive) is the follow-on, gated on
`_affect_state_region_derisk.py`. Do NOT overstate this as an affect→workspace integration — it is a placeholder for P0.3.

## Adversarial verification (4-skeptic workflow `verify-p1_2-gnw-workspace-go`, CPU)
All four independent skeptics returned **refuted=false at high confidence**, each backed by independent LIVE
reproductions (bit-matched the artifacts): reproducibility + lever-load-bearing; re-entrance genuinely load-bearing;
controls collapse for the right reason + the shufflefix is a legitimate statistics improvement (not a cheat); seeding
genuine + the affect drop real (but host-scalar). Synthesis verdict: **commit-with-caveat** — no underlying GO fact in
doubt; the caveats are the misleading `shuffle 0 == original` comment (FIXED at commit) and the affect-framing (corrected
above).

## Honest scope
A functional **GNW workspace + re-entrant deliberation correlate** (Dehaene ignition + multi-cycle broadcast), validated
on 3-hop reasoning with the no-confab moat intact. The re-entrant loop + the moat are the genuine faculty; affect is a
host-scalar placeholder for the real P0.3 neural affect-state region. This is a pivot-core faculty verified + committed
alongside **W3 (ToM false-belief)**; with **P0.3 affect (QUALIFIED-GO)** and **gap#4 credit (CPU-rate GO)** it completes
the current 4-faculty cluster. Next integration step: wire the real P0.3 spiking affect state into the workspace drive
(replacing the host salience scalar).
