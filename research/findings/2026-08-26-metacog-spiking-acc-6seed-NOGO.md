---
type: finding
status: no-go
date: 2026-08-26
mechanism: spiking-presynaptic-metacog-confidence-read
---
## Fully-spiking-presynaptic metacognition read (`spiking_acc`) — 6-seed NO-GO (mission_go=False; first-order operating point drifts out of window on 2/6 seeds)

**Set:** `_second_order_metacog_monitor_derisk`, `--confidence-read spiking_acc`, seeds 42/43/44/100/101/102, numpy, 160 trials/seed, calib 96 trials, dynamic ACC features, tuned op point `spiking_acc_drive_pa=320 spiking_acc_syn_w=32` (the mode defaults). Artifact: `research/findings/raw/lanes/metacog/metacog_spiking_acc_3factor_6seed.json` (+`.prov.json`); host ceiling `research/findings/raw/lanes/metacog/metacog_host_learnedacc_6seed_ceiling.json`; sibling plastic GO artifact `research/findings/raw/lanes/metacog/metacog_plastic_3factor_6seed.json`.

**Result: NO-GO — the runner's own verdict is `PARTIAL (4/6 seeds GO)`, `mission_go=False`.** This is the attempted next rung named by the committed `plastic_acc` GO ([`2026-08-18-self-organized-metacog-monitor-GO`](2026-08-18-self-organized-metacog-monitor-GO.md)): install the learned opponent weight `w_diff = w_plus - w_minus` as an all-excitatory ACC→meta projection and let the V+/V- confidence drive EMERGE from ACC spikes through those synapses, so confidence = rate(V+) − rate(V-) is a genuine spiking read of a synaptic integral (Kiani-Shadlen 2009). It does NOT clear the mission bar at the shared operating point. Reporting the runner's verdict, not a metric lifted from it. <!--derived-->

**Why `mission_go=False` — the single failing gate component is the operating window.** `mission_go` = (all three controls collapse 6/6) AND (host_logistic_calls==0) AND (`all_in_window`) AND (host AUC-parity gate) AND (spiking anti-cheat `host_dot_on_read_path==0`). Every component PASSES except **`all_in_window=False`**: two seeds fall below the first-order accuracy floor `type1_acc_lo=0.60`. Seed 44 `type1_acc=0.5938` and seed 101 `type1_acc=0.5062` are out of the `[0.60, 0.90]` window, so their per-seed `go` flags are False → verdict `PARTIAL (4/6)` and `all_in_window=False`. The metacognitive read is only defined in-window (meta-d' is uninterpretable near chance type-1), so this is a genuine NO-GO, not a threshold quibble. <!--derived-->

**What the spiking read machinery DID record (the runner's own sub-verdicts, reported for localization — the overall verdict remains NO-GO).** These are components of a failed gate, not a success claim.
- **Spiking anti-cheat holds on all 6 seeds:** `host_dot_on_read_path==0` (the V+/V- currents come ONLY from ACC synaptic input, never from `monitor.currents_from_features`) and `host_logistic_fit_calls_on_path==0`. `all_host_dot_on_read_zero=True`, `all_host_logistic_calls_zero=True`. The read is a spiking synaptic integral where it runs. <!--derived-->
- **All three controls collapse 6/6:** META-LESION → type2_AUC→0.500, meta-d'→0 on all 6. PERMUTED-CONFIDENCE (200-draw perm test) → `perm_p` ≤ 0.005 on all 6 (real read beats the null). SELF-READ-LESION (shuffled-feedback perm test on the learned mapping margin, 200 draws) → `perm_p` in [0.005, 0.020] on all 6 (the CONTINGENT reward-feature pairing organizes the mapping, not the architecture). WITHIN-CLASS type2_AUC > 0.55 on all 6 (min 0.6077). `all_meta_lesion_collapse`/`all_permuted_collapse`/`all_self_read_lesion_collapse`/`all_within_class_ok` = True. <!--derived-->
- **Host AUC-parity sub-gate passes (5/6):** spiking mean type2_AUC 0.8088 vs host 0.8407 = ratio 0.9620; 5/6 seeds clear 0.85×host_mean (threshold 0.7146) — only seed 100 (0.7112) misses, by 0.003. meta-d' parity 3/6 (mean meta-d' 2.5301 vs host 2.7163, ratio 0.9314). The confidence read reaches host parity where the first-order op-point is valid. <!--derived-->

**Root cause — the `spiking_acc` build shifts the FIRST-ORDER operating point relative to `plastic_acc`, at the same seeds.** In the committed `plastic_acc` GO all 6 seeds were in-window (per-seed type1_acc 0.644–0.825, mean 0.7531). In `spiking_acc` the SAME seeds drift: seed 44 0.7812→0.5938, seed 101 0.6813→0.5062, mean type1_acc 0.7531→0.7031. <!--derived-->
The confidence read is READ-ONLY and cannot change the first-order decision, so the drift is a SUBSTRATE-PERTURBATION artifact: `spiking_acc` builds an extra ACC presynaptic population and installs an ACC→meta projection, which advances the shared substrate RNG stream and shifts the per-seed 2AFC draws. This is the CLAUDE.md "each build advances the global RNG" trap, here shifting the operating point rather than confounding a comparison — `cfg.seed` IS set, but the number/order of substrate ops differs from `plastic_acc`. Two seeds land below the accuracy floor. <!--derived-->

**The residual and the next lever (method banked, capability not deferred).** The spiking-presynaptic confidence READ itself is sound: anti-cheat clean 6/6, controls collapse 6/6, AUC parity 5/6 — where the first-order op-point is valid it matches the host read. The wall is the first-order OPERATING POINT under the `spiking_acc` build, not the read.
The next lever is to re-center the shared operating point for `spiking_acc` mode so the `[0.60, 0.90]` window holds on all 6 seeds — either build the ACC population/projection WITHOUT perturbing the first-order RNG stream (isolate the confidence-path build from the decision substrate), or re-tune `--base-pa`/`--sig-lo`/`--sig-hi` for this mode — then re-run the mission gate. `plastic_acc` remains the current metacognition-monitor GO; `spiking_acc` is a NO-GO at this operating point.

**Honest scope.** The confidence→correctness MAPPING WEIGHTS are self-organized by the local reward-gated three-factor Hebbian rule (no host optimizer), and in `spiking_acc` mode the opponent V+/V- drive is propagated through an installed ACC→meta projection driven by ACC spikes (`host_dot_on_read_path==0`), closing the `plastic_acc` GO's named host-dot READ-path residual on the read path — but the run as a whole does NOT clear the mission bar (`mission_go=False`), so no closure is claimed. The presynaptic feature ENCODING (standardized rate channels) and the Dale's-law E/I split of signed weights remain host/residual; "fully spiking" is NOT claimed. This is a runner-level de-risk, not production integration. FUNCTIONAL metacognition correlate only; no phenomenal claim.
