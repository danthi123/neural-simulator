# EMERGE-24 / toward-language — GO (6/6 seeds): ONLINE GROWTH. The emergent producer LEARNS A NEW GROUNDED FACT LIVE (mid-life, same spiking bridge), PRODUCES it, RETAINS the old facts (no catastrophic forgetting), and keeps the no-confab moat through growth — a brain that grows its knowledge from experience, biology-native. NO `sim/` edit.

**2026-07-02 (autonomous).** Runner `research/runners/_emerge24_online_growth_derisk.py`; CI guard `tests/test_emerge24_online_growth.py` (4 tests). Reuse-by-import (`_emerge14` on-bridge learner + `_emerge12` priming + the EMERGE-23 producer pattern); NO `sim/` edit; CPU numpy-backend; 6-seed (42/43/44/100/101/102).

## The claim
On ONE real spiking bridge, the emergent producer holds the base knowledge ("dog chased ball", "cat ate fish"), is then TAUGHT a new fact LIVE ("fox saw seed") by potentiating its pathway on the SAME bridge (no reset), and afterward:
- **produces the new fact**: `fox → "fox saw seed"` (learned-new **1.00**, all seeds);
- **retains the old facts** — no catastrophic forgetting: `dog → "dog chased ball"`, `cat → "cat ate fish"` (retention **1.00**, all seeds);
- **keeps the moat**: `zzz → <abstain>` (moat **1.00**, all seeds);
- and crucially, **BEFORE teaching, the new subject ABSTAINS**: `fox → <abstain>` (pre-teach-abstain **1.00**) — the fact was genuinely absent, so the post-teach production is real learning, not a pre-existing pathway.

## The mechanism — growth = a permanence-rise on a disjoint pathway
The three-block content-bearing codes (POS-class = grammar / content = the specific word / family = generalization) live on a pre-allocated DENSE cross-column coincidence pool. A fact is learned by the committed `sim/` three-term kernel (`fused_htm_permanence_update`) raising the right permanences. **Growth** = potentiating the new fact's adjacent-pair pathways on the same bridge that already holds the base facts — the new word's columns pre-exist at the sub-connected `p_init`, so learning a new fact is a permanence-rise (exactly as a fixed-topology cortex grows an engram, no runtime edge-adds). Because each fact uses a DISTINCT verb, the new fact's pathway is disjoint from the old ones, so learning it does not overwrite them (no catastrophic forgetting). The new subject (fox) is a fully-disjoint code with no family, so before teaching it drives no coincidence and abstains; after teaching it produces the taught fact — the "learns live" signature.

## Anti-cheats (all airtight, 6/6)
- **pre-teach-abstain 1.00**: the new subject abstains BEFORE teaching (the fact is genuinely learned, not pre-existing).
- **dAP-LESION** (coincidence off): learning collapses to **0.00** — the growth is genuinely the bridge's dendritic-plateau recurrence + the `sim/` kernel, not host bookkeeping.
- **retention 1.00**: the old facts survive the new learning (the no-forgetting check is the retention metric itself).
- **moat 1.00** (confabulation 0): the no-confab moat holds through growth.
- No teacher; multi-seed unanimous.

## Significance — the growth axis of the toward-language chain
This adds the **growth** capability to the emergent producer: the brain does not just produce/generalize/ground/abstain a fixed knowledge set — it GROWS its knowledge from experience (being told a new fact), keeps what it knew, and stays honest (abstains on the still-unknown). This is the master directive's core (a brain that learns + grows) and directly serves the fluid-conversation priority (learn facts live). All spiking, emergent, one brain, NO `sim/` edit.

## Honest scope + next
- The new fact uses a DISTINCT verb, so there is no shared-verb high-order ambiguity in the object slot. Two named next integration steps: (1) growth of a fact that SHARES a verb/object with the base (requiring high-order context to disambiguate which object follows a shared verb — the SeqGenLearner context-allocation combined with the three-block content codes); (2) growth that OVERRIDES a generalization prior (teach fox — a canine — a fact that differs from dog's, so the specific learned fact beats the family default).
- The deep residual (a separate faculty, the next deep-research gate): open-world **SEMANTICS** — knowledge acquisition BEYOND told facts (inference, world model), the experience-driven-learning direction of the master directive.

## Artifacts
`research/runners/_emerge24_online_growth_derisk.py`, `tests/test_emerge24_online_growth.py`, `research/findings/raw/_emerge24_online_growth.json`. Prior: `2026-07-02-emerge23-grammatical-grounded-production-GO.md`, `2026-07-02-emerge14-onbridge-nseq-scaling-R1-surpassed-GO.md`.
