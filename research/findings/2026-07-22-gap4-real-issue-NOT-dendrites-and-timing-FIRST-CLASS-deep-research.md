# gap#4 real issue (NOT dendrites — the frozen credit SIGNAL) + gap#5 timing PROMOTED to first-class: deep-research + biology RAG

**2026-07-22.** Owner pushed back on two things I had been hand-waving: (a) I kept blaming "dendrites" for gap#4 when we've used two-compartment dendrites extensively; (b) whether we've addressed neural TIMING and whether it should be on the roadmap. A 4-agent workflow deep-read our own findings + the credit/timing biology and corrected the record. Sources: Payeur 2021 (Nat Neurosci s41593-021-00857-x), Sacramento 2018 (arXiv:1810.11393), Max et al. 2024 PAL (arXiv:2212.10249), "Does Feedback Alignment Work at Biological Timescales?" (arXiv:2510.18808), Guerguiev-Lillicrap-Richards 2017, Urbanczik-Senn 2014; EMERGE-85/86 + `OrderedPositionWM`.

## gap#4 — the real issue is the CREDIT SIGNAL, not the dendrite; two high-leverage methods were NEVER tested
**Reconciliation (the three framings dissolve into two questions):** credit-ASSIGNMENT (does a local plateau-gated rule move the right weights + can a downstream layer read the right cell?) = **CONFIRMED** (rung 3d 6/6, rung 10 Poisson 10/10 lesion). learn-to-ACCURACY (does credit-training the hidden beat a credit-independent reservoir readout?) = **CLEAN NEGATIVE** (BDSP 0.525 mean-3-seed vs reservoir 0.765). The CAPABILITY the mission needs (grow deep structure from experience by a local rule) = **OPEN + un-wired** (`enable_btsp` in de-risk runners only; fails CLOSED-criterion (e)). The board's "FULLY RESOLVED" is flagged in our own record as *"a scope redefinition, not a result… no experiment closed it"* — the supervised method was PARKED after failing, the capability reassigned to the unsupervised stream cortex.

**Root cause (dendrite topology is NOT to blame):** our 2-compartment dendrite is *topologically faithful* — Payeur/Sacramento/Guerguiev/Urbanczik-Senn ALL use one apical compartment; multi-branch nonlinear subunits (Poirazi-Mel/Beniaguev) are a per-neuron-capacity *enhancement* (RANK 5), not the crux. The load-bearing cause is the **credit SIGNAL**: our apical carries a **FROZEN fixed-random projection of the RAW output error that never falls to zero when the network is already correct** (`sim/dendritic_mlp.py:81` `self.B` set once, never updated; `sim/dendritic_neuron.py:44-55` scalar linear apical→soma). That single choice (i) makes credit-training lose to a random reservoir and (ii) makes the no-confab moat leak (same symptom — an error that never zeroes keeps nudging when correct). Manifests three ways: general = rate-code/point-neuron degeneracy (the SAME algo hits 97%/73% on numpy graded signals, degenerate on sparse spikes); sharpest = cleanxor's rank-1 zero-discriminant (the WRONG instrument — KP has nothing to align to); fidelity = the accurate models don't use a frozen raw error.

**THE TWO UNTESTED FIXES (the concrete open builds):**
1. **Learned interneuron self-predicting microcircuit (Sacramento 2018) — RANK 1, substantial-but-bounded, NEVER built.** SST/PV interneurons with plastic `W^IP`/`W^PI` LEARN to cancel top-down feedback → the apical is SILENT when correct and carries a true prediction error otherwise. Fixes **accuracy AND the moat at once**. The `enable_bdsp_microcircuit` stub exists but its cancellation is runner-supplied per-phase, NOT learned in-engine.
2. **Learned feedback weights (PAL / weight-mirror) — RANK 2, medium, cheapest high-leverage.** Fixed-random feedback-alignment provably plateaus below backprop and *worsens with depth*; PAL "solves complex tasks with fewer neurons." Only ever tested via KP on cleanxor (void — zero discriminant). Untested on a task with informative credit.
Both are no-weight-transport. RANK 3-4 (burst-multiplexing fidelity / graded credit `enable_bdsp_graded_credit` / population size) are small + mostly in-engine. ⇒ **the FA/KP-family "exhausted" verdict UNDER-surveyed the method space; the frontier is NOT method-exhausted.**

## gap#5 TIMING — we BUILT the theta-gamma engine but NEVER wired it to replay → promote to first-class
- **EXISTS + validated on spikes:** EMERGE-85 (rate) + EMERGE-86 (spiking, on `OrderedPositionWM`) theta-gamma multiplexed WM buffer + stack-match (Lisman-Idiart, catalog N.15); `OrderedPositionWM` ordered recall 1.000 to the full 7-slot span at D=256. But only ever applied to WM / recursion / multireferent / word-order — **never** to hippocampal sequence replay.
- **It is the SINGLE named fix for BOTH open ordered-replay threads:** RANK 2 forward-order (4/6, not uniform) AND RANK 3 recombination (co-ignition boundary). The gamma reset that silences the just-fired assembly (de Almeida-Idiart-Lisman E%-max WTA + self-avoidance) DECOUPLES "hold this memory" from "push to the next" — the exact within-vs-chain tension that limited both (RANK 2's `asym` flips reverse when the within-encode is strong; RANK 3 co-ignites).
- **NOT a mega-build (~90% parts exist):** CA3 attractors + BTSP chain (GO) + gamma FS pool (`sim/profiles.py:263-265`) + theta injector (`experiment/stimulus.py:154-156`) + slow-NMDA slot-hold (`bridge.py`) + EMERGE-85 slot bookkeeping. New code = a thin theta→gamma amplitude coupling + a post-fire gamma reset over the CA3 slice. **Days, not months** — reclassify from the board's "deep open frontier / larger mechanism build / bonus rung."
- **Cheapest-first de-risk (NO `sim/` edit):** feed RANK 2's EXISTING forward-chain weights through a numpy gamma-WTA + post-fire silence, gate `forward_frac` 6/6 uniform vs the 4/6 weight-only baseline, reusing RANK 2's SCRAMBLE/NO-NOISE/NO-ENCODE anti-cheats. Isolates "does phase-timing fix the order?" in minutes.

## 5 record corrections (applied to the board)
1. gap#4 "clean-negative" is a METHOD-verdict, capability OPEN (not closed).
2. "FA/KP family exhausted" over-states — the learned interneuron microcircuit + PAL learned-feedback were never/void-tested.
3. cleanxor (zero linear discriminant) may be the wrong instrument for "deep credit can't build accuracy."
4. gap#5 timing is a bounded near-term reuse (days), not a deep-frontier mega-build.
5. gap#5 "completion CLOSED" covers REACTIVATION only; ORDERED replay (RANK 2/3) is OPEN, blocked on the one theta-gamma mechanism.

Full synthesis + per-agent reports: workflow `wf_2b805475-825` journal.
