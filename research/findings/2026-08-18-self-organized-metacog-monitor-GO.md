---
type: finding
status: go
date: 2026-08-18
mechanism: plastic-three-factor-hebbian-metacog-monitor
---
## SYNAPTICALLY SELF-ORGANIZED metacognition monitor (reward-gated three-factor Hebbian) — 6-seed GO

**Set:** `_second_order_metacog_monitor_derisk`, `--confidence-read plastic_acc`, seeds 42/43/44/100/101/102, numpy, 160 trials/seed, calib 96 trials (seed+100003), dynamic ACC features. Artifact: `research/findings/raw/lanes/metacog/metacog_plastic_3factor_6seed.json` (+`.prov.json`); host ceiling `metacog_host_learnedacc_6seed_ceiling.json` (aggregated from the committed banked GO artifacts).

**Result: GO, 6/6 seeds; mission_go=True.** This CLOSES the named honest residual of the committed second-order-metacog GO ([`2026-08-17-wave1-second-order-metacog-6-GO`](2026-08-17-wave1-second-order-metacog-6-GO.md)): "the monitor's WEIGHTS are a host logistic fit". The type-2 confidence→correctness MAPPING is now learned by a LOCAL, reward-gated THREE-FACTOR HEBBIAN rule over the brain's own first-order competition — no host optimizer. mean type2_AUC=0.825, mean meta-d'=2.49, mean M-ratio=1.66, mean type1_acc=0.753 (all in the [0.60,0.90] window). Per-seed type2_AUC in [0.784, 0.857]; meta-d' in [1.89, 3.18]. <!--derived--> (means / per-seed ranges over the 6 cited runs)

**Parity with the host-logistic read (the mission bar).** Plastic mean type2_AUC 0.825 vs host 0.841 = **ratio 0.982**; every seed clears 0.85×host_mean (0.715) → AUC-parity **6/6**, gate PASS. mean meta-d' 2.49 vs host 2.72 = ratio 0.916 (≥0.85 on the mean; per-seed meta-d' parity 4/6 — the two misses are the low-d' seeds 101/102 where the SDT AUC→meta-d' inversion is noisy, not an AUC deficit). The first-order substrate is byte-for-byte the same as the host read (plastic mean type1_acc 0.753 == host 0.753), so this is a like-for-like swap of ONLY the confidence read. <!--derived-->

**Mechanism (three factors; the affect evaluative-conditioning precedent `_affect_evaluative_conditioning_derisk`, a 6/6 self-organization GO, reused).** [pre] the standardized ACC/aPFC evidence-&-conflict read of the brain's own first-order 2AFC competition. [post] the OPPONENT aPFC confidence channel — the spiking meta_schema V+ ("was correct") / V- ("was error") subpools (Namburi-Tye opponent coding), driven by the correctness US in calibration. [third factor] the dopamine/RPE gate δ=correct−V (V a TD-tracked reward baseline; Holroyd-Coles ACC error-monitoring; Fleming-Daw confidence learning): a DA burst on positive surprise writes V+, a DA dip on an error writes V-, eligibility = the presynaptic feature.
w_plus/w_minus = Σ|δ|·z over each reward-gated subset, each normalized by its dopamine mass (Rescorla-Wagner asymptote). Single-pass, LOCAL, reward-gated — no loss minimisation, no matrix solve, no gradient descent. The learned weights drive the spiking V+/V- subpools; confidence = rate(V+)−rate(V-).

**Why it's load-bearing (all controls collapse 6/6):** META-LESION (zero the monitor's drive; the read-only monitor never feeds the workspace, so d'/accuracy are unchanged) → type2_AUC→0.500, meta-d'→0 on all 6. PERMUTED-CONFIDENCE (permutation test, 200 draws) → null mean AUC≈0.50, real read beats it at perm-p=0.005 on all 6. SELF-READ-LESION / no-contingent-learning (re-apply the SAME three-factor rule on SHUFFLED correctness feedback, 200-permutation test) → null mean AUC≈0.50, real read beats it at perm-p≤0.020 on all 6 — the CONTINGENT reward-feature pairing, not the architecture, organizes the mapping. WITHIN-CLASS type2_AUC>0.55 on all 6 (min 0.648) — tracks correctness, not stimulus. <!--derived-->

**Anti-cheats.** (1) NO host optimizer on the plastic path: a module tripwire counts host-logistic fitter calls; `host_logistic_fit_calls_on_path==0` asserted and recorded on all 6 seeds (the plastic path never reaches `_fit_learned_acc_apfc_monitor`). (2) No weight transport: `weight_source=reward_gated_three_factor_hebbian`; the weights derive only from the spike-derived features + reward, never copied from a host fit. (3) Determinism: `cfg.seed` set in the build; two independent full runs gave byte-identical intact per-seed AUCs. Calibration is a SEPARATE block (seed+100003), scored on the held-out 160 — no in-sample leak.

**Instrument note (transparency, not a weakening).** The permuted-confidence and self-read-lesion controls are PERMUTATION TESTS (200 draws), not single draws. A single shuffle of the DISCRETE spiking confidence (a quantized rate difference) has AUC std≈0.05, which false-FAILED the collapse on the two low-d' seeds (single-draw permuted AUC 0.55/0.58) even though permutation FULLY decorrelates confidence from correctness (true null AUC=0.5 by construction). The single-draw value is retained in the artifact. This is the affect precedent's discipline ("a single-draw null is too noisy"). Meta-lesion is deterministic (zero drive → constant confidence → AUC=0.5), so it needs no permutation.

**Honest residual (the next rung, matching the affect precedent's scope).** "self-organized" here scopes to the confidence→correctness MAPPING WEIGHTS (learned by the three-factor rule); the PRESYNAPTIC ACC features are host RATE READS of the brain's own competition (the same reads the `balance`/`learned_acc` modes use), and the learned synaptic sum w·z is injected as the meta subpool current rather than propagated through installed presynaptic synapses. A fully-spiking presynaptic-population read is the remaining shortcut to burn down. This is a de-risk GO that closes the host-logistic residual — NOT production integration (the read is runner-level, like the host learned_acc GO it parallels). FUNCTIONAL metacognition correlate only; no phenomenal claim.
