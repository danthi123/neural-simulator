# NP next-step design gate: the deep-input-encoder idea is a STRUCTURAL no-op (source-verified) — KILLED before building; the REAL mission gap is NP-vs-KP for the FEEDFORWARD input-map credit (NP's unbiasedness vs FA-partial Kolen-Pollack at the ctx1 slice)

**Date:** 2026-07-13
**Gate:** the `np-feedforward-to-mission-next-step` design Workflow (wf_82e0ddec-4a0; 7 agents, research + design + 3 adversarial + verdict). The standing "deep-research-gate new directions" opening move, applied after the deep-credit arc was comprehensively characterized (feedforward NP robust; on-bridge = variance boundary; recurrent = retracted seed artifact).
**Status:** a DECISIVE redirect — it saved a foreordained-null build + found the genuine cleanly-deriskable gap.

## What it KILLED (and why — source-verified, not asserted)
My lead candidate was "a NP-trained DEEP feedforward INPUT ENCODER for language" (NP's proven strength realizing the R3 input-representation lever). The gate REFUTED it from our own source: `_reslm_generalize_rate_check.build_codes` builds each token as `OR(shared-class block, identity-confound dims)` — an ADDITIVE, LINEARLY-SEPARABLE code. The runner header states the W_in lever is pure LINEAR reweighting ("learned W_in up-weights the shared class dims, suppresses the identity confound"), and line 202 records "NO held-out generalization headroom → fixed W_in already generalizes (JL again)." For a linearly-separable additive code, linear reweighting is already optimal ⇒ a deeper nonlinear per-token encoder is a **near-provable NO-OP** — HEADROOM-NULL by structure, not just prediction. Don't build it; don't spend NP on it.

## The REAL gap it found (mission-relevant + cleanly deriskable + NP's valid regime)
`2026-07-11-R3-REFRAME` reaches the +1.258 deep ceiling with a LINEAR W_in, and the biological rule (Kolen-Pollack learned feedback, "kp") plateaus at ~78% — with **ctx1 (position-1 = pure FEEDFORWARD, NO recurrence) = −0.75 vs the oracle's −0.12**. That open ctx1 gap is caused by **feedback-alignment PARTIALITY** (KP's learned feedback is still not weight-transport). **Node perturbation removes FA partiality BY CONSTRUCTION** (unbiased zeroth-order, no feedback matrix), and ctx1 is squarely inside NP's 6+6-seed-validated FEEDFORWARD regime (NOT the retracted through-reservoir recurrent regime). ⇒ **the pivotal test: does NP's unbiased feedforward credit beat FA-partial KP on the ctx1 W_in gap?**

## The de-risk (implementable; the comparison arms EXIST) — the EXACT next action
Runner `research/runners/_np_feedforward_win_credit_derisk.py` (numpy/torch bridge TBD — the arms are in the torch harness; NP is numpy → the np_win arm needs a torch NP impl OR a numpy-instrument port). Reuse: `oracle_win`/`fa_win`/`kp_win` arms already 6-seed-validated in `_emerge_stream_eprop_lm_derisk.py` (`BPTT_frozen_wrec`, `eprop_learn_win`, `eprop_learn_win_kp`); NP mechanism from `_nodepert_deep_credit_derisk.train_nodepert`.
- **GATE-0 (cheap, ceiling-first, NO NP):** confirm depth is a no-op on the real instrument — `deep_encoder − linear_Win ≤ +0.05` on ≥5/6 seeds ⇒ HEADROOM-NULL (expected; the source analysis already forecloses it). Forecloses the deep-encoder premise in minutes.
- **GATE-1 (the deliverable):** train W_in on the **direct/feedforward per-position objective** (reservoir state = a DETACHED context feature → W_in credit is feedforward-only = NP's valid regime). Arms (single variable = the W_in credit RULE): `oracle_win` (ceiling −0.12) · `kp_win` (the R3 bar, ctx1 −0.75) · `fa_win` (plain FA) · **`np_win`** (node perturbation, antithetic k=8, σ∈{0.2,0.35,0.5}). Anti-cheats: `shuffle_dl` (must collapse np_win), `wrong_sign` (must anti-learn). **GO gate (per seed, held-out ctx1 margin):** np_win−shuffle ≥ +0.10 AND np_win−kp_win > 0 AND np_win captures ≥60% of (oracle−fa) AND wrong_sign < fa. **Multi-seed: standard 42/43/44/100/101/102 AND FRESH pre-registered 7/8/9/10/11/12** (the exact gate that caught the recurrent artifact) — Overall GO iff ≥5/6 in BOTH sets AND the FRESH-pooled np_win−kp_win ≥ +0.05.
- Honest scope: runs at the R3 interpretable scale with a CLEAN continuous loss (so NP's variance boundary — which bit the noisy on-bridge spike-count readout — does NOT apply here; this is off-bridge numpy where NP is robust). If np_win ≈ kp_win → NP has no language-mission edge over the already-on-substrate-realizable KP (consolidate NP as feedforward-only); if np_win > kp_win → NP's unbiasedness is a genuine biological-credit upgrade for the language input map.
