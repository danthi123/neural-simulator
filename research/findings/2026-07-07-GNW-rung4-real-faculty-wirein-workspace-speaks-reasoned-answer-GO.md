# GNW Rung-4 — the workspace SPEAKS its REASONED answer via the project's REAL faculties (3-seed GO): breaks Rung-3's by-construction critique, gate-first moat intact, one process, NO training, NO `sim/` edit

**Date:** 2026-07-07
**Runner:** `research/runners/_gnw_rung4_real_faculty_wirein_derisk.py` (reuse-by-import of the GNW workspace + the EMERGE-52/54 reasoner + the EMERGE-67/68 A→W read-out; NO `sim/` edit).
**Verdict:** 3-seed GO (42/43/44), cupy/GPU. The "connect-the-pieces" milestone — the workspace's abstract `report`/`reason` replaced by the project's REAL faculties. Built from the scoping `2026-07-07-real-faculty-wirein-scoping-substrate-requirements.md`.

## The result
The GNW workspace SPEAKS its reasoned answer via three REAL faculties that share ONLY the ignited concept's STRING (not a code vector) — a thin string-keyed orchestration, one cupy process, gate-first:
1. **GNW workspace** ignites a member → reads which report-pop sustains → the concept STRING ("none" ⇒ gate closed).
2. **Real reasoner** (`PerDimensionConsole`, EMERGE-52/54, taught the taxonomy in-script) → `ask_can(concept, prop)` → affirm / override / abstain.
3. **Real A→W read-out** (`UnifiedNeuralSpell(load=True)`, EMERGE-67/68, cached) → spike-spells the frame surface from `language_output` spikes.

Live (3 seeds):
- **owl → "Yes, an owl can fly"** (INHERITED — owl is held-out, never taught fly) → spike-render **"the owl can fly"**.
- **penguin → "No, a penguin walks"** (its own locomotion exception) → spike-render **"the penguin walks"**.
- **zzz (moat) → abstains** → the A→W is invoked **0 times**.

## The gates (all hold, 3 seeds; go=True encodes all four)
- **ROUND-TRIP (report==reasoning):** the string that IGNITES == the string the reasoner reasons about == the string the A→W speaks. Asserted (not just each faculty in isolation).
- **INHERITED:** owl's "Yes" collapses under the reasoner's dAP-lesion → the reasoning substrate is load-bearing (it's inference, not a lookup).
- **SPIKE-SPELLED:** lesion the A→W `pool→language_output` pathway → the spoken surface COLLAPSES → the surface is genuinely decoded from spikes, not a host string.
- **GATE-FIRST MOAT:** a never-taught concept → the reasoner abstains → the A→W spell is invoked 0 times (no-confab by construction).

## Why it matters: breaks Rung-3's by-construction critique
Rung-3's report==reasoning identity was adversarially flagged as by-construction (report and reason were byte-identical symmetric reads of the same source). Rung-4 breaks that for free: `report` is now the A→W concept-pool spiking read-out and `reason` is the HTM-pooler inheritance read — **completely different substrates, different populations, different mechanisms, sharing ONLY the ignited concept's string identity**. The strongest form of "the concepts the brain SAYS are the ones it REASONS with": one ignited workspace assembly selects both the word to speak and the member to reason about.

## Honest scope
The A→W engines are GPU-trained ONCE + cached (a scale/data lever); the smoke uses the vocab INTERSECTION of the A→W-trained words and the reasoner taxonomy (owl/penguin × fly/walk — two fully spike-spellable turns + the moat turn). Full vocab (the fish members / the "breathe" respiration dimension) is a cached-A→W `--train` rebind — a data lever, not a mechanism. The workspace/reasoner/A→W are all substrate-SPECIFIC weights behind the interfaces; the wire-in ITSELF (the string-keyed dispatch + the gate-first moat) is substrate-INDEPENDENT — it survives the coming deep-substrate swap (see the scoping's requirements list).

## Files
`research/runners/_gnw_rung4_real_faculty_wirein_derisk.py`; `research/findings/raw/_gnw_rung4_seed{42,43,44}.json` (3-seed). A→W caches: `bridges/emerge6{7,8}_aw/*.simstate.h5`.
