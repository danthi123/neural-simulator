# GNW Rung-3b — report==reasoning with a GENUINE 2-hop INHERITANCE inference (6-seed GO): a held-out member INHERITS a property via the shared superordinate (never wired), report & reason read DIFFERENT populations — fixing Rung-3's symmetric-readout scope

**Date:** 2026-07-07
**Runner:** `research/runners/_gnw_rung3b_emergent_inheritance_reasoning_derisk.py` (reuse-by-import of Rung-1 assembly-loop + Rung-2 inhibition + Rung-3 dense-projection; NO `sim/` edit).
**Verdict:** 6-seed GO (42/43/44/100/101/102), numpy/CPU. Fixes the adversarial-verify scope of Rung-3 (its `reason` was a byte-identical SYMMETRIC readout of the workspace → the report==reasoning identity was by-construction). Rung-3b makes `reason` a GENUINE 2-hop inheritance inference.
**Adversarial-verify:** Workflow `w2wrsqtw4` — **held-out-genuineness SURVIVES, inference-vs-relay SURVIVES_WITH_SCOPE (no refutation).** CSR-audited on all 6 seeds: **zero direct sparrow→reason edges** (sparrow_member→reason_flies = 0; the ONLY route is sparrow→BIRD [3200 edges] → BIRD→reason_flies [2000 edges, SHARED with robin]); **route-lesion is load-bearing** (zero sparrow→BIRD → sparrow's flies collapses to none while robin still infers flies and salmon swims; zero the shared BIRD→flies → BOTH robin AND sparrow lose flies = the shared-route signature); the superordinate GENUINELY FIRES when sparrow ignites (BIRD late-rate 0.152, identical to robin's; FISH silent — no cross-category leak); and the **extra-hop RATE SIGNATURE** distinguishes genuine inheritance from a taught path — report 0.1515 (1-hop) vs reason 0.0909 (2-hop), a constant 5:3 ratio, disjoint populations (0 overlap); the `teach_held_out_property` control wiring sparrow→flies directly jumps reason to 0.152 (the 1-hop magnitude). The GO gate is a real test (nulls fail: remove member→superord → all reason collapses). SCOPE (folded in below): the taxonomy STRUCTURE is hand-wired (fixed weights, nothing learned) — the genuine, verified result is the 2-hop generalization (a held-out member reaches a property with zero direct wiring, which a 1-hop symmetric readout provably cannot do), which legitimately fixes Rung-3's byte-identical-symmetric-readout critique.

## The result (6-seed)
The workspace ignites ONE member assembly (dense self-recurrent, weight 30, shared inhibition = Rung-2 one-at-a-time). Downstream: `report` reads the member's DISTINCTIVE identity code (member→report, 1 hop = what the brain would SAY); the `superord` category layer reads member→superordinate (robin→BIRD, salmon→FISH, sparrow→BIRD — a member ACTIVATES its category, IT→ATL convergence); `reason` reads superordinate→property (BIRD→flies, FISH→swims). So the reasoning is member→category→property (**2 hops**), reading DIFFERENT populations than report. All 6 seeds:
- **IDENTITY** — ignite robin → report=robin ∧ reason=flies; ignite salmon → report=salmon ∧ reason=swims.
- **HELD-OUT INHERITANCE (the killer anti-cheat)** — `sparrow` is wired sparrow→BIRD but **NEVER** sparrow→flies, yet ignite sparrow → report=sparrow ∧ **reason=flies** (rate 0.091, above threshold) on all 6 seeds → it INHERITS flies purely via the shared BIRD superordinate. **A symmetric readout cannot do this** (there is no sparrow→flies wiring; the only route is sparrow→BIRD→flies) — this is genuine inheritance/generalization (Collins-Quillian / Rogers-McClelland).
- **CAUSAL SWAP** — swapping the ignited member flips report AND the inferred conclusion together.
- **WORKSPACE LESION collapses reason** — with the member self-recurrence → 0 (no sustained ignition), reason goes silent (the inference needs the sustained workspace).
- **DISSOCIATION** — under the SAME lesion, the peripheral DIRECT path keeps `report` alive while `reason` (the inference) stays dead → report-reps == reasoning-reps.
- `identity ∧ held_out_inherits ∧ causal_swap ∧ dissociation ∧ report≠reason-populations` = True on all 6 seeds.

## Why this fixes Rung-3's scope (report != reason, genuine inference)
Rung-3's adversarial-verify found its report and reason were STRUCTURALLY IDENTICAL projections (byte-identical rates) → the identity was by-construction symmetric wiring, not a genuine reasoning computation. Rung-3b breaks that:
- **report and reason read DIFFERENT populations** (report ← member-distinctive, 1 hop; reason ← superordinate, 2 hops) → NOT byte-identical.
- **`reason` is a genuine 2-hop inheritance inference**, and the HELD-OUT generalization (a member reaches a property with NO direct member→property wiring) is a property a 1-hop symmetric readout provably cannot have.

## Honest scope (the next rung)
The category STRUCTURE is FIXED hand-wired: which member belongs to which superordinate (member→category), and which property each category has (category→property), are given fixed projections — nothing is LEARNED from experience here. What is genuine is the INFERENCE (the 2-hop inheritance + the held-out generalization via the shared code) on that structure. The fully-EMERGENT version — the superordinate DISCOVERED from co-occurrence experience (EMERGE-30/32/33) and the property association LEARNED (Hebbian), so the shared representation itself emerges — is the next rung (**Rung-3c**), together with wiring the real A→W read-out as `report` (the brain SPEAKS the inherited answer). That is the direct project payoff: the workspace holds a perceived/heard concept, reasons about it by inheritance through its discovered category, and speaks the grounded answer — one brain, communication north star.

## What this establishes toward the unified spiking GNW
Rung-1 (ignition) + Rung-2 (mutual exclusion) + Rung-3 (broadcast/dissociation) + Rung-3b (genuine inheritance inference over the workspace content, held-out generalization) — the workspace now demonstrably holds a content that is simultaneously REPORTED (identity) and REASONED-FROM (inherited property via the category), with the reasoning genuinely distinct from the report and generalizing to held-out members. NEXT: Rung-3c (learned/emergent structure + real A→W speak).

## Files
`research/runners/_gnw_rung3b_emergent_inheritance_reasoning_derisk.py`; `research/findings/raw/_gnw_rung3b_seed{42,43,44,100,101,102}.json` (6-seed). Adversarial-verify Workflow `w2wrsqtw4`.
