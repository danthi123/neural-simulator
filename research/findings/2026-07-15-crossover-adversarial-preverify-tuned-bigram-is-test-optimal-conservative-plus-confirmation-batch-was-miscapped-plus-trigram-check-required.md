# Crossover adversarial PRE-verification (before trusting any fluency crossover): the tuned bigram is TEST-OPTIMALLY tuned (conservatively strong — good), BUT (1) my first confirmation batch was silently MIS-CAPPED (n_train > n_sentences) and (2) the standing rule's TRIGRAM check is still owed

**Date:** 2026-07-15 (compute unlocked) · **Status:** read-only methodology pre-verification of the fluency-crossover surface (`2026-07-15-fluency-data-lever-...`) BEFORE any GO. Caught two real issues; corrected batch relaunched. NOT a GO.

## What the adversarial pre-verify checked (reading `_reslm_scale_trained_selssm_vectorized_derisk.py`)
1. **Same held-out split for sel and the baseline? YES.** Line 168-169: one `default_rng(seed).permutation`, `ev = last n_eval`, `pool = the rest`. `sel_ce`, `tuned_bi_ce`, `bag_ce` all evaluate through the SAME `ev_ids`. ✓
2. **How is the tuned bigram's k chosen? TEST-OPTIMALLY (line 205-210).** For each k ∈ {0.3,0.1,0.03,0.01,0.003} the CE is computed ON THE EVAL SET and `tuned_bi_ce = min` over k. So the bigram gets the best-case k on the actual test set — a CONSERVATIVELY STRONG baseline that FAVORS the bigram. ⇒ a `sel_over_tuned > 0` crossover is a STRONG, conservative claim (sel beats a test-optimally-tuned bigram), not inflated by a starved baseline. This is the RIGHT direction for rigor (the 3×-caught confound is fully addressed; if anything the crossover is understated). The one caveat — the bigram gets a 1-hyperparameter (k, 5 choices) test-tune that sel does not — is negligible and pro-baseline.

## Issue 1 — my first confirmation batch was silently MIS-CAPPED (caught + fixed)
`load_sentences(path, max_sents)` BREAKS at `max_sents` (a hard cap), and line 172 is `tr = pool[:n_train]`. My confirmation launches passed `--n-train 96000/192000` but NO `--n-sentences` → default 30000 → `pool ≈ 29700` → `pool[:96000]` silently caps at ~29700. So my nt=96000/192000 runs would have trained on ~29700 sentences (mislabeled), NOT the intended data. The pre-existing surface is VALID (its np200/nt96000 stored `n_train=96000` = `len(tr)`, so it loaded ≥96000 sentences via a large `--n-sentences`); only MY confirmation launches were broken. **Killed the 4 misconfigured procs; relaunched with `--n-sentences 250000`** (corpus yields ≥300k filtered sentences — verified — so nt=192000 is achievable) consistently across the set, re-running seed 42 at nt=96000 to validate the config reproduces the +0.025 crossover, with seed 42's nt=96000→192000 trend sharing one eval split. Batch `bkjjcy0zn` (v2 outputs).

## Issue 2 — the TRIGRAM check is still owed (the standing rule requires BOTH)
The runner bakes only the tuned BIGRAM. The standing anti-confound rule requires a "beats the n-gram" claim to also clear a **tuned/interpolated TRIGRAM** — and at nt=96k+ a trigram is far less starved and could beat sel (a trigram, not the bigram, may be the real ceiling at scale). `_ngram_fair_baseline_probe.py` computes the deleted-interpolation trigram on the runner's exact split. **When the v2 batch lands, run it with matching args (`--n-sentences 250000 --n-train 96000 --vocab 120 --n-eval 300 --seed 42`) to compare sel_ce against the interpolated trigram** — the crossover is only a genuine LM improvement if sel beats the trigram too, not just the bigram.

## Net
- The crossover surface's METHODOLOGY is sound where it uses the tuned bigram (same split, conservatively-strong baseline).
- Before ANY crossover GO: (a) the v2 multi-seed + trend batch (properly loaded) must confirm the +0.025 is seed-robust and rising; (b) sel must ALSO beat the interpolated trigram on the same split. Both are in flight / owed. The deep-tail mechanism (sel_over_bag ~+1.0) is unaffected and stands regardless.
