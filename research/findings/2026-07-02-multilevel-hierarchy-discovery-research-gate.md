# Research gate — MULTI-LEVEL discovered taxonomy (robin→bird→animal) on the spiking HTM substrate: SURPASSABLE + cheap; it is STACKING the validated flat pooler, not a new mechanism. Controller-reviewed read-only deep-research.

**2026-07-02 (autonomous).** Read-only deep-research subagent (catalog + Kandel 6e + our findings + literature). Before building a new mechanism CLASS (hierarchical/multi-level pooling — the third Collins-Quillian operation + a richer structure). Controller trust-but-verifies the load-bearing claim via the cheap-first de-risk (EMERGE-44).

## The residual (SURPASS move a)
Most of "multi-level taxonomy" is ALREADY covered by composing what's built:
- **EMERGE-27** did multi-level INHERITANCE (robin inherits "flies" from BIRD + "breathes" from ANIMAL) — but over a HOST-DESIGNED hierarchy (hand-assigned ancestor codes).
- **EMERGE-30** discovered ONE superordinate level from co-occurrence.
- **EMERGE-38..43** built the competitive self-organizing pooler (discovers flat overlapping categories, fully-on-substrate) + inheritance + cancellation over them.
The genuine residual: **can a SECOND pooler layer, taking L1 codons as input, DISCOVER superordinate groupings** (so the hierarchy is learned, not hand-assigned), and does inheritance/transitivity chain through the learned levels? That is stacking the existing pooler + chaining the existing inheritance — **not a new mechanism**.

## Real biology (SURPASS move b)
The brain builds category hierarchy as a CASCADE of local-pool-then-converge stages: the ventral visual hierarchy V1→V2→V4→IT with growing receptive fields + abstraction (Kandel 6e Ch 21, ~p475), then IT→ATL convergence zones (Damasio 1989; Patterson–Lambon Ralph hub-and-spoke) that pool categories-of-features into amodal superordinate concepts. Each stage reuses the same local competitive-pooling motif → stacking the HTM Spatial Pooler is biologically faithful (each cortical level pools the level below).

## Ranked cheap-first de-risks (SURPASS move c)
1. **EMERGE-44 (the critical gate, cheap):** stacked pooler — L1 (EMERGE-38) discovers sub-categories; L2 pools L1 codons of co-occurring same-superordinate members → superordinate codons. GATE: does L2 group by superordinate + does inheritance chain L1→L2→property? (~15% this is a wall — tight-overlap separation.)
2. **2-level inheritance + transitivity** over the learned hierarchy (numpy-ref then spiking): a held-out member inherits both its sub-category and its superordinate property; "robin swims" stays false (no FISH path).
3. **Full 3-level corpus** (24 members, 3 levels), 6-seed, moat.
4. **Fully-spiking** via the EMERGE-40 kernel + EMERGE-41 FS-WTA (reuse).

## Verdict (SURPASS move d)
**SURPASSABLE, cheap.** The one genuine gate: if learned overlapping L1 codons don't separate at L2 (high inter-superordinate leak), stacking degrades → then a decorrelation/separation `sim/` mechanism is needed (2–3 days). Probability wall ~15%. First concrete de-risk: **EMERGE-44 stacked pooler** (this session). Cite: Kandel 6e Ch 21; Patterson–Lambon Ralph 2017; Damasio 1989; Cui-Ahmad-Hawkins 2017 HTM SP.

## Next
Build EMERGE-44 (stacked pooler; trust-but-verify the L2-discovers-superordinates claim), cheap-first, 6-seed, anti-cheats (permuted co-occurrence collapses; lesion the L1→L2 pathway). Reuse EMERGE-38 pooler + the committed kernel; NO `sim/` edit expected.
