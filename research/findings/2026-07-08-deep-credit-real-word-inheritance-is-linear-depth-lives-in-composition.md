# Deep-credit real-task, part 5 (raw-PPMI real-corpus) — the self-correcting Stage-0 gate: real-word CATEGORY-inheritance from PPMI codes is SHALLOW/LINEAR (depth-gap +0.000 every seed; linear-decode 1.000 ≫ chance 0.125) → NOT depth-required. The clarifying reframe: real word embeddings make categories linearly decodable (Levy-Goldberg 2014), so deep credit's depth-benefit does NOT apply to embedding-decoding; the goal-relevant depth-requirement for real language lives in COMPOSITION/REASONING over the (linear) codes — multi-hop chaining, structured binding, systematic recombination.

**Date:** 2026-07-08
**Runner:** `research/runners/_semantic_inheritance_ppmi_deep_credit_derisk.py` (reuse-by-import of the part-2 harness — Stage-0 gate, deep-credit arms, anti-cheats, `_gnw_d1` nets, `sim.dendritic_mlp` oracle; the ONLY change is `make_task_*` swaps the synthetic XOR-pool member features for real PPMI codes). Real-PPMI pipeline reused: `learned_graded_cortex_fair_test.ppmi_matrix` (the exact host PPMI transform) over the real-word 8×8 taxonomy `option_c_real_cooccurrence_derisk.TAXONOMY_8x8`. NO `sim/` edit (git diff sim/ = 0).
**Verdict:** honest boundary + a clarifying reframe (NOT a GO — Stage-1 correctly NOT run on a shallow task). The deep-lever's part-2 compositional traction was on the synthetic XOR-encoding structure, NOT on real-embedding category-inheritance; the depth-requirement for real language is COMPOSITIONAL/RELATIONAL, not embedding-decoding.

## Stage-0 (the self-correcting gate) — real-word inheritance is NOT depth-required
On real-PPMI-coded held-out inheritance, seeds 42/43/44: **linear-oracle {1.000, 0.944, 1.000} == 1-layer == deep-best; depth-gap +0.000 every seed** (< the 0.05 bar) → DEPTH-SEPARATING **False**. The pre-flagged risk is CONFIRMED: **LINEAR category-decode on the PPMI codes = 1.000 ≫ chance 0.125** — real word-embedding codes make the superordinate category linearly present, so any per-super property is linearly reachable. Stage-1 (the deep-credit arms) was deliberately NOT run — reading them on a shallow task is exactly the CIFAR-V1 wrong-instrument trap part-1 identified.

## The clarifying reframe (why this SHARPENS the deep-lever landscape)
- **The synthetic part-2 task's depth-requirement came from its XOR-over-shared-pool encoding** (individual features marginally 50/50, linearly uninformative; the category recoverable only nonlinearly). **Real distributional embeddings do NOT have this property** — a word embedding's whole purpose is to make semantic categories LINEARLY separable (Levy-Goldberg 2014: PPMI-SVD ≈ word2vec; categories are linear directions). So real-word CATEGORY-inheritance is shallow.
- ⇒ **deep credit's depth-benefit does NOT apply to "decode a category from a word embedding"** (that's linear). It applies where depth is genuinely required for language: **COMPOSITION / RELATIONAL REASONING over the (already-linear) codes** — multi-hop transitive chaining, role-filler binding, systematic recombination, nested structure. This is exactly the EMERGE inference territory (EMERGE-26/27/28 inheritance/transitivity, the composer's binding) — and it is where a learned deep/recurrent cortex earns its depth.
- This does not retract part-2: part-2 IS a composition task (XOR-over-pool = a nonlinear composition), correctly depth-required, and the deep-credit rule learned it (0.69, 5/6). The raw-PPMI result just pins that the traction is on COMPOSITION structure, not on embedding-category-decoding — which is the correct, goal-relevant place for it.

## The deep-lever landscape, now sharpened (comprehensive)
- **D1** deep credit ports to spikes · **D2** FA-wall+KP toy-boundary · **CIFAR** wrong instrument (image depth is convolutional) · **compositional-semantics (RATE)** the first real-task traction — on COMPOSITION structure (XOR-over-pool), 0.69/5-6 · **on-bridge spiking** read-out-variance wall (surpass = population coding at the field's K~500 working point = a measured scale-up) · **raw-PPMI** real-word category-inheritance is LINEAR → deep credit's depth-benefit lives in COMPOSITION/REASONING, not embedding-decoding.
- ⇒ the deep-credit mechanism is validated + its depth-benefit is real on COMPOSITION/generalization tasks (the language-relevant setting); the honest next frontier is a real-language COMPOSITION/RELATIONAL-REASONING depth task over the codes (multi-hop/binding/recombination — where depth is genuinely required), and/or the on-bridge scale-up. Category-decoding from embeddings is linear and not the place for deep credit.

## Files
`research/runners/_semantic_inheritance_ppmi_deep_credit_derisk.py` (+ `--codes-cache` for a maximally-real cached-code drop-in, verified to load; the cached `_phaseB_stream_codes_320_*.npy` + `data/corpus/tinystories.txt` are absent in this checkout, so counts are Poisson-generated — but the linearity is a property of the PPMI TRANSFORM, robust across seeds); `research/findings/raw/_semantic_inheritance_ppmi_deep_credit.json`. Part 2: `2026-07-07-deep-credit-real-task-compositional-semantics-GO.md`.
