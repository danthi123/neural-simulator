# To install these dependencies, run the following command in your terminal:
# pip install -r requirements.txt

# Core simulator dependencies
cupy-cuda12x # Or cupy-cuda12x, or another version matching your CUDA installation
numpy
scipy  # REQUIRED for the SIM_BACKEND=numpy CPU path (get_sparse_module -> scipy.sparse). Without it
       # sim/bridge.py:45 catches the ImportError and SILENTLY falls back to `import cupy as cp`, so
       # SIM_BACKEND=numpy runs on the GPU anyway and the documented CPU-portability path is dead.
h5py
dearpygui
PyOpenGL
PyOpenGL_accelerate

# Experiment runner (YAML-driven autonomous sweeps in experiments/*.yaml)
pyyaml>=6.0

# Webapp dashboard (webapp/server.py + static/)
# These are optional for headless / GUI-only use; required for the FastAPI dashboard
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.0

# For CuPy, you need to install the version compatible with your CUDA toolkit.
# Replace 'XXX' with your CUDA version (e.g., 11x for CUDA 11.x, 12x for CUDA 12.x).
# Example for CUDA 11.x: cupy-cuda11x
# Example for CUDA 12.x: cupy-cuda12x
# Visit the CuPy installation guide for more details: https://docs.cupy.dev/en/stable/install.html

# tkinter is usually included with Python standard distributions,
# but if you encounter issues, you might need to install it separately
# depending on your OS and Python environment (e.g., sudo apt-get install python3-tk on Debian/Ubuntu).

# ---------------------------------------------------------------------------------------------------
# OPTIONAL EXTRAS -- not needed for the core simulator, but REQUIRED by parts of the project that are
# documented as working. Audited 2026-07-16 by walking every third-party import in the tree and diffing
# against this file; the gaps below were all real and all silent. See also requirements-dev.txt (tests).
# ---------------------------------------------------------------------------------------------------

# Main-entry niceties. Both are GUARDED (try/except ImportError) in neural-simulator.py, so the GUI runs
# without them -- but their features silently switch off: psutil = memory/CPU stats; hdf5plugin = LZ4
# recording compression (falls back to slower gzip).
psutil
hdf5plugin

# The generative / language arc (torch is a HARD import in sim/tiny_transformer.py and ~48 research runners:
# BPTT-SNN, the TinyStories generator, the spiking-forward conversions, the Qwen faculty). NOT needed to
# import or run the core sim -- sim/__init__.py does not pull them in (verified) -- but needed for that work.
# Install the torch build matching your CUDA (this box: torch 2.11.0+cu128).
# torch
# transformers

# Lazy imports (inside a function -- the module loads fine without them, the FEATURE is what's unavailable):
#   cuda-python   -- viz/renderer.py:305 `from cuda import cudart` (CUDA<->OpenGL interop path)
#   openai        -- sim/llm_adapters.py, the Ollama / OpenAI-compatible endpoint adapter

# Individual research runners / tests (install as needed):
#   scikit-learn  -- clustering/metrics in a few probes
#   spacy         -- corpus frame mining (_bucketB_corpus_mined_frames_derisk)
#   pillow        -- image handling in perception probes
#   datasets      -- _download_simplewiki
#   pypdf         -- tools/rag/extract_reference_pdfs.py (RAG venv; see below)
#   llama-index-core, llama-index-embeddings-huggingface, llama-index-retrievers-bm25,
#   sentence-transformers  -- tools/rag/* (RAG venv; see below)

# The RAG tooling (tools/rag/*) deliberately lives in a SEPARATE venv (.venv-rag) so llama-index's dependency
# resolution cannot churn this env's pinned torch/cupy CUDA stack:
#   python -m venv .venv-rag && .venv-rag/bin/pip install llama-index-core llama-index-embeddings-huggingface \
#       llama-index-retrievers-bm25 sentence-transformers pypdf
