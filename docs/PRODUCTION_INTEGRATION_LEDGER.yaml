# PRODUCTION-INTEGRATION LEDGER — the honest state of the DEFAULT /api/brain-chat turn.
#
# THE GOAL (owner, 2026-08-11): a working all-spiking one-substrate brain with ALL faculties ON BY DEFAULT in
# production, fully functional when chatted with. A faculty is DONE only when WIRED + ON-BY-DEFAULT + SCAFFOLD-RETIRED.
# This file is the tracked truth; tools/gates/production_integration.py (CLASS PI) cross-checks every row's levels
# against live source (anchors) and blocks any doc that CLAIMS a faculty is integrated/wired/on-by-default/one-brain
# beyond what its row supports. Seeded 2026-08-11 from workflow wjgvtko3i (12-agent code-trace, adversarially checked).
#
# LEVELS (each a verified value: YES / PARTIAL / NO):
#   de_risked        validated GO at runner/de-risk level (6-seed etc.)
#   wired            reachable from webapp/server.py /api/brain-chat -> the ChatBrain it builds (not only a runner)
#   on_by_default    runs in the DEFAULT config the owner gets (no opt-in flag, not default-off, not behind rich=True)
#   scaffold_retired the host shortcut it replaces is GONE from the default path (deleted / demoted to test-oracle)
# A row is INTEGRATED only when wired=YES AND on_by_default=YES AND scaffold_retired=YES.
#
# ANCHORS (the machine-checkable part). default_anchor: the gate reads the RHS literal at `assign` and derives the
# expected value FROM the on_by_default LEVEL (NO => must be off_value; YES => must be on_value). scaffold_symbol: while
# scaffold_retired=NO the host symbol MUST be present; =YES it must be absent. If source flips without the ledger moving
# (or the ledger flips without the source), the anchor mismatches and the gate BLOCKS.

headline:
  # THE PRIMARY NORTH-STAR — OWNER-VISIBLE FUNCTION (the 3 acts trace-stamping cannot fake; each needs a LESION test:
  # disable the spiking path -> the default answer must CHANGE, else it is not genuinely doing the act).
  owner_visible_function:
    # EMPIRICALLY MEASURED by running the real default brain: research/runners/_production_lesion_probe.py ->
    # research/findings/raw/_production_lesion/probe.json. A wiring earns credit only when it flips one of these to True
    # on THIS probe AND the lesion of its path flips it back off (load-bearing, not cosmetic).
    choose: PARTIAL  # 2026-08-12 INTEGRATION #1: direct factual questions are decided by the SUBSTRATE (recall or honest
                     # abstain), lesion-verified; the host router's keyword-CONFAB is retired for them. Residual: neural
                     # question comprehension + open-ended topic choice; host still handles self/identity + noisy-anaphora.
    generate: PARTIAL # 2026-08-12 UPGRADED: open-ended NOVEL-proposition generation (#3E) is now WIRED onto the DEFAULT
                     # gate()/render() — an explicit open-ended prompt ("what might X ...", "guess ...") returns a FLAGGED
                     # "perhaps a v p" hypothesis (novel + plausibility-gated over the brain's own fact-graph + moat-verified),
                     # prompt-triggered, on the default turn (no rich toggle). The RICH path (RichAnswerComposer, spiking
                     # elaborate + neural_planner=True) also remains for associative multi-fact replies. DRAW now SPIKING
                     # (B1/F1, 2026-08-13): the generative DRAW is a co-resident vocab-agnostic spiking soft-WTA read off
                     # cp_firing_states (the b2 host-oracle draw is retired); still PARTIAL because the plausibility
                     # LIKELIHOOD + SVO template + RF-composer moat remain host. See the open-ended-generation row.
                     # (The DEFAULT recall path stays byte-identical.)
    learn: YES       # 2026-08-12 INTEGRATION #2 + fix: acquisition moved INTO gate() (an SVO assertion is taught + returned),
                     # so it reaches the /api/brain-chat endpoint; substrate-first recall. Production-path probe (gate+render):
                     # teach "wolf hunt deer" -> recall "wolf hunt deer", lesion-verified. Runtime code allocation for new words.
  # SECONDARY — the composite. default_on_spiking = rows at on_by_default=YES whose spiking path is lesion-load-bearing.
  default_on_spiking_faculties: 16     # + the between-turn CONTINUOUS-LIFE (the self-initiation CA3 wander selection + the affect-ladder re-read on the idle tick, both lesion-load-bearing, flipped default-on 2026-08-21); RF phasor bind/unbind resonate dynamics; the anaphora referent store; the co-resident graded-affect ladder (Gate-B); the co-resident comprehension monitor (Gate-B/D4); the co-resident surprise/mismatch unit (Gate-B/D2); the co-resident balance-of-evidence metacog monitor (Gate-B/E1); the co-resident affective world-model (Gate-B/E2); the co-resident curiosity ASK-pool crave drive (Gate-B/D3-curiosity); the co-resident multi-referent WM bump-attractor buffer (Gate-B/D6); the co-resident hippocampal dAP episodic recall gate (Gate-B/D5); the co-resident vocab-agnostic spiking generative-DRAW WTA bank (B1/F1, 2026-08-13, on the open-ended turn); the co-resident four-FS-WTA discourse event register (D3/F2, 2026-08-13, who-was-before across a connective); the co-resident prospective-memory intention-LATCH + BA10 NMDA-plateau cue-MONITOR (2026-08-13, holds a deferred 'remind me to X when Y' across turns + fires it on the cue); the co-resident W4 depth-2 RSA graded scalar-implicature listener-belief (Task-#12, 2026-08-13, 'some' +> 'some but not all' on a scalar-quantity turn — graded [0,0.73,0.27] vs the leg2_v2 one-hot [0,1,0], collapses to flat under the FS normalization-lesion); the co-resident P1.2 GNW workspace confidence/conflict-GATED deliberation (T1-1 rung d, 2026-08-18, deliberation-until-sure + halt-if-unsure — on a genuine multi-answer conflict >=2 candidates co-ignite / conf collapses -> the keystone acc_conflict_gate ABSTAINS instead of committing the bus's arbitrary first-match; the recurrence-lesion collapses the co-ignition -> re-commits) — all lesion-load-bearing. (The B3 non-contradiction ASSERTION-gate + the B4 reconsolidation belief-revision are ALSO default-on faculties but reuse EXISTING spiking reads — the ONE production recall composer's ask_yes_no polarity WTA, and the D2 surprise window respectively — so they add no NEW spiking substrate. The T1-6 other-repair (a targeted clarification replacing the D4 bare abstain) likewise reuses the D4 SpikingRoleCompetition per-noun sel-pool read — no new bridge. The T1-1 rung-d MULTI-STEP re-entrant deliberation, gnw-multistep-deliberation, on_by_default 2026-08-19, is ALSO a default-on lesion-load-bearing faculty but reuses the SAME P1.2 GNW workspace bridge cycled re-entrantly — same substrate, so no NEW spiking pool.)
  scaffold_retired: 0
  total_faculties: 43   # == the number of `rows` below (bumped 32->38 2026-08-20; 38->39 2026-08-21: + the continuous-state-engine row; 39->40 2026-08-21: + the da-gated-encoding row, the WAVE-0 Gap-4 write-side coupling — de-risked+wired default-OFF, flip gated on a magnitude-store no-regression soak; 40->41 2026-08-21: + the da-gated-curiosity-threshold row, the WAVE-0 Gap-4 coupling (b) — DA/engagement gates the curiosity crave-threshold, de-risked+wired default-OFF; 41->42 2026-08-21: + the gnw-three-organ-bus row, the D4 comprehension monitor added as a THIRD genuinely-distinct load-bearing voter on the GNW ignition bus — de-risked+wired default-OFF; 42->43 2026-08-21: + the continuous-ideation row, the creativity/novelty rung — the between-turn wander OCCASIONALLY GENERATES a novel blended thought (a 2-source attractor recombination flagged as a non-fact idea), de-risked+wired default-OFF)
  note: "most of the live chat's load-bearing cognition is still host/numpy/off-bridge; the spiking set now includes the affect ladder (Gate-B colors WHAT+HOW), the comprehension monitor (Gate-B/D4, honest 'I didn't follow that' abstain), the expectation-violation/surprise unit (Gate-B/D2, honest 'that surprises me' notice), the balance-of-evidence metacog monitor (Gate-B/E1, honest 'low-confidence' hedge), the affective world-model (Gate-B/E2, queryable 'what do you expect' + honest 'that shifts the mood unexpectedly' notice), the curiosity ASK-pool crave drive (Gate-B/D3, honest follow-up QUESTION on a novel-topic abstain — crave, don't refuse), the multi-referent WM buffer (Gate-B/D6, HOLD >=2 discourse referents + honest 'I'm holding 2: dog and cat' read-out), and the hippocampal dAP episodic recall gate (Gate-B/D5, honest 'I recall / I don't recall discussing X' on a referential turn) — all lesion-load-bearing co-resident reads. The non-contradiction ASSERTION-gate (Gate-B/B3, REJECT a contradiction of a held belief) + reconsolidation (Gate-B/B4, PE-gated IN-PLACE belief revision) reuse the existing spiking recall composer / D2 window. Other-repair (Gate-B/T1-6, a targeted clarification NAMING the unresolved role/token instead of the bare D4 abstain) reuses the D4 comprehension per-noun sel-pool read. Appraisal-injection + the Qwen manner-mouth + the comprehension cue-lexicon + the surprise operating-point proxy + the metacog evidence-derivation + the world-model generic-valence pools/persistence-selection + the curiosity SELECTOR/VETO + the D6 host referent-parse/write-marker/argmax-read + the D5 host content/recency + the B3 host negation-detection/morphology remain declared scaffolds; the D5 BTSP WRITE is deferred on numpy (cupy-gated)."

rows:
  - key: content-selection
    faculty: "Content selection — direct factual questions decided by the substrate; host keyword-confab retired"
    de_risked: YES
    wired: YES                  # 2026-08-12 INTEGRATION #1: substrate-first recall + honest abstain on /api/brain-chat
    on_by_default: YES          # default gate behavior; probe CHOOSE True + lesion-load-bearing
    scaffold_retired: PARTIAL   # 2026-08-12 #1 CHOOSE: the question->(agent,action) PARSE is now NEURAL (on-brain BridgeParser role_of) + AUTHORITATIVE for factual-SVO questions -> the host QuestionRouter is RETIRED for them (a declined/lesioned parse ABSTAINS, no keyword-confab). Router still handles self/identity + the anaphora fallback.
    host_scaffold_in_default: "QuestionRouter.match_fact retired for factual-SVO questions (the on-brain parser owns them); still the self/identity + noisy-anaphora fallback"
    scaffold_symbol:
      file: research/runners/brain_chat_tui.py
      symbol: "def match_fact"
    lesion_note: "LESION-LOAD-BEARING (2026-08-12): lesioning parser.role_of (junk role) makes BOTH a taught fact and a build-time fact ABSTAIN (was 'dog chase cat' -> IDK) — the neural parse is doing the comprehension, not the router. The comprehension is on FIRING neurons (the parser's (position,voice)->role conjunction)."
    evidence: "brain_chat_tui.py gate() substrate-first + _neural_question_parse (parser.role_of) authoritative for factual-SVO + __ABSTAIN__; onebrain in-process gate() verified: healthy recall (brain->spikes, dog->cat, taught wolf->deer, anaphora it->cat eat fish, abstain fish/fly) + lesion -> factual CHOOSE abstains; finding 2026-08-12-INTEGRATION-1-CHOOSE-neural-question-parse"

  - key: in-loop-learning
    faculty: "In-loop learning — the owner teaches ARBITRARY new facts by talking; they are recalled from the substrate"
    de_risked: YES
    wired: YES                  # 2026-08-12 INTEGRATION #2+#2b: ChatBrain acquires an SVO assertion (_maybe_acquire->hear) + substrate-first recall on /api/brain-chat
    on_by_default: YES          # default turn behavior; no flag; lesion-verified in the production lesion probe
    scaffold_retired: NO        # the host QuestionRouter remains the FALLBACK (role-blind) — retired only when #1 (spiking selector) lands
    host_scaffold_in_default: "host QuestionRouter is still the fallback recall; the fuller LEARN (a BTSP/plasticity per-turn write for a LASTING trace) is the next depth"
    evidence: "brain_chat_tui.py _maybe_acquire + gate() substrate-first + _substrate_recall; rf_phasor_composer.py runtime code allocation (_filler_phases + _growth_rng); probe LEARN True + lesion-load-bearing; finding 2026-08-12-INTEGRATION2 + 2b addendum"

  - key: open-ended-generation
    faculty: "Generation — the brain VOLUNTEERS a NOVEL grounded proposition on an open-ended prompt (#3E generative replay over its own fact-association graph), moat-verified + flagged as a hypothesis; the RICH path's associative multi-fact reply also remains"
    de_risked: YES
    wired: YES                  # 2026-08-12 INTEGRATION: #3E generative-replay WIRED onto the DEFAULT gate()/render() of /api/brain-chat (not only rich=True). An explicit open-ended prompt ("what might X ...", "tell me something new about X", "what else about X", "guess ...") returns a FLAGGED hypothesis. Verified in-process AND over HTTP (composer=onebrain).
                                # 2026-08-12 FLUENT SURFACE: the flagged guess now renders as FLUENT PROSE via the Qwen mouth ("Maybe <fluent> -- that's a guess from what I've learned, not something I was taught"), SVO-VERIFIED (the re-parse must recover the hypothesis's exact (a,v,p) or it falls back to the raw "perhaps a v p" template). The DEFAULT (rich) path now INTERCEPTS the HypothesisSVO and speaks it as a SINGLE flagged guess — it no longer chained/elaborated stored recall around the guess (which had LEAKED a generated proposition, e.g. "The dog eats cat.", as an asserted fact). Endpoint adds hypothesis/hypothesis_svo/fluent_hypothesis markers and reports recalled_svo=null for a guess. CPU-verified (stub: fluent flagged guess, 0 leaks/battery, moat abstains on unknown, recall/abstain unregressed); Qwen fluent surface confirmed live on GPU (main process). See finding 2026-08-12-INTEGRATION-open-ended-generation-renders-FLUENT-flagged-guess.
                                # 2026-08-12 BRAIN-NATIVE SPIKING SURFACE (burn-down A1a): the GENERATE-channel hypothesis SURFACE is now spoken by FIRING NEURONS, not the external transformer. render_hypothesis_verified renders a structured (transitive SVO) hypothesis via the composed spiking BROCA — "perhaps the <S> <V-3sg> the <O>", word order = the per-pool spiking-RATE ranking on a real Izhikevich SimulationBridge (EMERGE-59/61 × the #3E draw, reuse-by-import from _spiking_fluent_surface_derisk, 6-seed GO), TRANSFORMER-FREE, replacing the agrammatic host f-string. Re-parse VERIFIED (the SAME moat the recall path uses -> recovers the drawn SVO) or it falls back to the raw flagged template (0 leaks); the guess stays FLAGGED ("perhaps ... [a guess ...]"). DEFAULT-ON (BRAIN_SPIKING_MOUTH=0 escape reverts to the Qwen/stub mouth, byte-identical), lesion-load-bearing (OFF -> reverts). Verified SYNCHRONOUSLY numpy-CPU: end-to-end "what might a dog chase" -> "perhaps the dog chases the hare  [a guess ...]" (moat-recovered), 4/4 direct verbs grammatical+faithful, INDEPENDENT held-out parser recovers the SVO, discriminative verify (wrong-object/wrong-subject rejected), no torch on the path, recall/abstain/anaphora/discourse unregressed (byte-identical pristine==modified smoke verdict). RESIDUAL (still Qwen): open ARBITRARY prose the spiking Broca can't frame = the banked deep-context wall. See finding 2026-08-12-INTEGRATION-generate-channel-surface-brain-native-spiking-broca.
    on_by_default: YES          # fires on the DEFAULT single-fact turn — no flag, not behind rich=True. PROMPT-TRIGGERED by design (an explicit open-ended lead-in); EVERY other question is byte-identical (recall/abstain/learn/anaphora untouched; verified pristine==modified smoke).
    scaffold_retired: NO        # DRAW now SPIKING (B1, F1, 2026-08-13): the generative DRAW is a genuinely-SPIKING vocab-agnostic soft-WTA read off cp_firing_states of a co-resident Izhikevich bank (VocabAgnosticSpikingSampler, role pools induced from the brain's OWN stored-fact concepts — no taxonomy, no KeyError on runtime vocab) -> the b2 host-oracle DRAW shortcut is RETIRED. REMAINING residual host shortcuts: the plausibility LIKELIHOOD matrix is host-computed over the brain's own facts; the SVO template + the RF-composer moat are host; the WTA bank is co-resident (rides the one-brain merge, burn-down #1); non-contradiction is inert on the onebrain composer (negation not stored as retrievable 'no' — the reused gate DOES fire on the rf composer); broader novel-sentence generation (emerge stream-cortex) unwired. SURFACE (A1a, 2026-08-12): the STRUCTURED transitive-SVO hypothesis surface is BRAIN-NATIVE SPIKING (the composed spiking BROCA, transformer-free); Qwen renders ONLY the open-arbitrary-prose residual (the banked deep-context wall).
    host_scaffold_in_default: "the DEFAULT recall path is unchanged (recall/abstain); open-ended generation is a prompt-triggered branch whose DRAW is now a co-resident spiking soft-WTA (B1, F1); the plausibility LIKELIHOOD is still host-computed over the brain's own fact co-occurrence graph, the SVO template + RF-composer moat host; onebrain negation unstored so the non-contradiction gate is inert there; broader emerge stream-cortex unwired"
    default_anchor:                # 2026-08-18 CLEAN MASTER SWITCH added: BRAIN_GENERATE_CHANNEL, DEFAULT-ON (matches this committed integration + the codebase default-ON/=0-escape convention); BRAIN_GENERATE_CHANNEL=0 disables the whole channel byte-identically (the row STAYS on_by_default:YES). The pre-existing BRAIN_SPIKING_DRAW/BRAIN_SPIKING_MOUTH flags only control HOW the channel draws/speaks, not WHETHER it fires; this is the WHETHER switch the channel previously lacked.
      - file: research/runners/brain_chat_tui.py
        assign: _GENERATE_CHANNEL_DEFAULT_ON
        off_value: "False"
        on_value: "True"
        count: 1
    evidence: "brain_chat_tui.py _parse_open_ended + _build_generation_proposer (clean co-occurrence over stored facts) + _generate_hypothesis (REUSES b2 GenerativeReplayProposer _plausible/_contradicts/_sample_weighted) + render() HypothesisSVO. DRAW (B1/F1, 2026-08-13): _generate_hypothesis installs VocabAgnosticSpikingDrawOrgan onto the proposer (flips use_spiking_sampler=True; the unchanged loop then draws off cp_firing_states). onebrain in-process gate(): 'what might dog eat?' -> 'perhaps dog eat cat' (novel+plausible+moat-verified), 0/30 confab leaks, unknown-topic abstains; HTTP /api/brain-chat composer=onebrain returns the flagged guess. B1 verify (research/runners/_vocab_agnostic_spiking_generation_production_organ_verify): VERDICT GO (draw 0 host-rng/>0 spiking; LESION plausible-frac 0.828->0.035, 95.7% attributable; noise-ablation ou_std=0 -> deterministic argmax; FLAG-OFF 16==16 byte-identical) + real handler (5/6 prompts return flagged spiking-drawn hyps, 400 spiking draws/0 host-rng, LESION collapses the handler proposer's plausible-frac 0.862->0.009, FLAG-OFF a no-op). commit 6670bda25. finding 2026-08-12-INTEGRATION-open-ended-generation-brain-volunteers-novel-propositions-on-default-chat. MASTER SWITCH (2026-08-18): BRAIN_GENERATE_CHANNEL default-ON with a =0 byte-identical escape; verified through the REAL ChatBrain.gate handler 6-seed (byte-id-off smoke identical SHA + gate() fully suppressed + proposer never built; ON: novel+moat-safe 0 leaks/0 negated-reproposed/untaught-abstain 20/20; plausibility gate lesion-load-bearing). NOTE the handler's plausibility is the brain's own sparse heard-fact co-occurrence -> a modest 2.1-3.4x advantage over random (NOT the 3E corpus-PPMI's 14-24x; strengthening it is a mapped follow-on). runner research/runners/_generate_channel_wiring_verify.py; finding 2026-08-18-generate-channel-wired-brain-chat-GO"

  - key: neural-render
    faculty: "Neural word-order articulation (Broca-like; enable_neural_render)"
    de_risked: PARTIAL
    wired: PARTIAL
    on_by_default: NO
    scaffold_retired: NO
    host_scaffold_in_default: "off-bridge Qwen2.5-0.5B transformer (or host templates); enable_neural_render=False on all builders"
    default_anchor:
      - file: webapp/server.py
        assign: enable_neural_render
        off_value: "False"
        on_value: "True"
        count: 2
    evidence: "server.py enable_neural_render=False x2"

  - key: one-brain-substrate
    faculty: "One shared substrate (vs per-op bridges + host glue); composer_kind — the GENUINELY-SPIKING recall"
    de_risked: YES             # 2026-08-12: the onebrain full chat now does CHOOSE+LEARN+abstain+anaphora end-to-end;
    #                            the wrap-vs-inner cleanup-codebook bug (a runtime-taught word stored but never recalled)
    #                            is FIXED via recruit-an-assembly (OneBrainComposer vocab_headroom, a reserve of
    #                            uncommitted cleanup slots). Genuinely-spiking recall + runtime LEARN COMPOSE (resonate
    #                            step per query, NOT the numpy _scan fast path). Byte-identical at vocab_headroom=0.
    wired: YES                  # 2026-08-12: the webapp /api/brain-chat tiny-demo brain builds composer_kind=onebrain
    on_by_default: YES          # the production webapp default is now onebrain (BRAIN_COMPOSER_KIND defaults to 'onebrain'); BRAIN_COMPOSER_KIND=rf is the escape to the numpy fast path
    scaffold_retired: NO        # the rf numpy fast path remains as the CPU/test oracle + the env escape
    host_scaffold_in_default: "none for the recall MECHANISM (the resonate-and-fire onebrain store is the default); the rf numpy _scan fast path is the opt-out oracle (BRAIN_COMPOSER_KIND=rf), not the default"
    default_anchor:                # 2026-08-26 REPAIRED: the old _ck anchor put an os.environ.get(...) EXPRESSION in
                                   # off/on_value, which _count_assign (a NAME = "literal" matcher) can never resolve —
                                   # so it read on=0/off=0 and silently passed while sub-check A was dead. Re-pointed at
                                   # the named production-default constant _COMPOSER_KIND_DEFAULT = "onebrain" (server.py,
                                   # the value os.environ.get("BRAIN_COMPOSER_KIND", ...) reads); flipping it to "rf" now blocks.
      - file: webapp/server.py
        assign: _COMPOSER_KIND_DEFAULT
        off_value: "rf"
        on_value: "onebrain"
        count: 1
    lesion_note: "The one-brain property is a MECHANISM claim (recall on FIRING NEURONS via resonate, not numpy _scan) — proven by the HTTP activity trace (composer='onebrain', 45730 readout neurons, frac_fired ~1.7%% on a match / ~0 on abstain), NOT by an answer change (onebrain and rf give the same answers by design; the point is the substrate computing them). The TUI/smoke _build_tiny_demo default stays 'rf' (fast GPU-free smoke); only the webapp production default flipped."
    evidence: "webapp/server.py BRAIN_COMPOSER_KIND default 'onebrain'; _build_tiny_demo(composer_kind=) plumbing; one_brain_composer.py vocab_headroom recruit-an-assembly; HTTP-verified on /api/brain-chat: CHOOSE 'The dog chases cat.' + abstain + LEARN 'wolf hunt deer'->'The wolf hunts deer.' (matched_fact_index=5) + rich GENERATE 'The dog chases cat. The cat eats fish.', all composer='onebrain'; finding 2026-08-12-onebrain-spiking-store...RESOLVED"

  - key: onebrain-merge-organs
    faculty: "ONE shared SUBSTRATE for the co-resident production organs (the burn-down-#1 residual named in surprise-monitor + worldmodel-forward). BRAIN_ONEBRAIN_MERGE default-ON builds the D2 SURPRISE + E2 WORLD-MODEL organs on ONE SimulationBridge (one cp_membrane_potential_v, N=1584) via the rung-1 MergedSubstrate, instead of two separate bridges; BRAIN_ONEBRAIN_MERGE=0 is the escape to separate bridges."
    de_risked: "YES"            # rung-1 6/6 GO (2026-08-13-merge-production-integration-rung1-GO): both organs on one bridge, reads byte-identical merged-vs-co-resident, functional, deterministic.
    wired: "YES"                # POOL #1: surprise/worldmodel get_organ inject get_merged_substrate(seed) when merge_enabled(); the /api/brain-chat handler reads them via _get_surprise_organ()/_get_worldmodel_organ(). POOL #2 (2026-08-13): metacog/pragmatic get_organ inject get_merged_substrate2(seed) when merge2_enabled() -> the handler's metacog judge() + pragmatic interpret() read the shared pool #2 by default. Verified in-process: metacog._shared IS pragmatic._shared IS get_merged_substrate2 singleton.
    on_by_default: "YES"        # POOL #1 merge_enabled() defaults to _MERGE_DEFAULT_ON=True; POOL #2 merge2_enabled() defaults to _MERGE2_DEFAULT_ON=True (2026-08-13 flip); BRAIN_ONEBRAIN_MERGE=0 / BRAIN_ONEBRAIN_MERGE2=0 are the escapes to separate bridges (byte-identical to the pre-flip production). Answer-PRESERVING vs pre-flip 6/6: pool #1 surprise `surprised`+world-model `pred_sign`; pool #2 pragmatic implicature + metacog `confident` (the metacog nmda_norm read, de-noised + re-draw-invariant). Each build self-calibrates its threshold.
    scaffold_retired: "PARTIAL" # RETIRED: the separate-bridge co-residency for BOTH proven param-het clusters — pool #1 (surprise+world-model, param-het-OFF) AND pool #2 (metacog+pragmatic, param-het-ON) — each now shares ONE pool by default (2026-08-13; POOL #2 FLIP GO 6/6, metacog answer-preservation 1/6 -> 6/6 once its confidence read became the divisive-normalized NMDA-conductance nmda_norm read). NOT full: (a) 4 of the ~7 proven organs are pooled; affect is a STRUCTURAL exclusion (region-name collision workspace/workspace_fs/meta_schema with metacog + a global OU/neuromod cfg -> its own pool / the recall-composer bridge), comprehension (per-region dt) + causal/curiosity (plasticity/neuromod) remain separate arcs; (b) the two pools are merged with EACH OTHER, not yet with the RECALL composer bridge (one-brain-substrate). finding 2026-08-13-onebrain-pool2-flip-GO (resolves 2026-08-13-onebrain-second-pool-SCOPED).
    host_scaffold_in_default: "affect / comprehension / causal / curiosity remain on their own separate bridges (affect: region-name collision + global OU/neuromod cfg; comprehension: per-region dt); pool #1 (surprise+world-model) and pool #2 (metacog+pragmatic) each share one pool but neither is yet merged with the recall composer bridge"
    default_anchor:
      - file: research/runners/onebrain_merge_production.py
        assign: _MERGE_DEFAULT_ON
        off_value: "False"
        on_value: "True"
        count: 1
      - file: research/runners/onebrain_merge_production2.py
        assign: _MERGE2_DEFAULT_ON
        off_value: "False"
        on_value: "True"
        count: 1
    lesion_note: "One shared cp_membrane_potential_v is a SUBSTRATE-consolidation claim (assert: surprise.bridge IS worldmodel.bridge IS the MergedSubstrate bridge; the two organs' get_organ share ONE MergedSubstrate). NOT an answer change (the pair is answer-preserving vs the pre-flip separate bridges by design — the point is the two organs computing on ONE pool). The numeric Hz/margin reads DO shift merged-vs-pre-flip (inherent: one global RNG cannot reproduce both organs' standalone threshold draws), but no classification crosses a threshold."
    evidence: "research/runners/onebrain_merge_production.py merge_enabled() default _MERGE_DEFAULT_ON=True (BRAIN_ONEBRAIN_MERGE=0 escape); MergedSubstrate holds both organs on ONE bridge; surprise_production_organ.get_organ / worldmodel_production_organ.get_organ inject get_merged_substrate when merge_enabled(). Verified research/runners/_onebrain_production_flip_verify.py (SIM_BACKEND=numpy, 6 seeds): A one shared pool 6/6 (surprise.bridge IS worldmodel.bridge, N=1584); B MERGED==CORESIDENT byte-id 6/6 (surp/wm delta 0.0); C answer classes preserved vs today 6/6 (all `surprised`+`pred_sign` identical); D documented residual (surp <=1.16 Hz, wm margin <=129, no class flip). Default-path check: no env -> merge_enabled True, both get_organ share one cp_membrane_potential_v. No regression: pytest tests/test_determinism.py 9/9; brain_chat_tui --smoke byte-identical pre-vs-post flip. finding 2026-08-13-onebrain-production-default-flip-SCOPED"
    evidence_pool2: "POOL #2 (2026-08-13): onebrain_merge_production2.py merge2_enabled() default _MERGE2_DEFAULT_ON=True (BRAIN_ONEBRAIN_MERGE2=0 escape); MergedSubstrate2 holds metacog+pragmatic on ONE bridge (N=450); metacog/pragmatic get_organ inject get_merged_substrate2(seed) by default; metacog default read is nmda_norm (BRAIN_METACOG_READ=balance escape). Verified _onebrain_production_flip2_verify (SIM_BACKEND=numpy, 6 seeds): A 6/6, B byte-id Δ=0.0 6/6, C both organs 6/6 (metacog nmda_norm), D residual metacog<=0.0204 pragmatic<=0.0658, FLIP-GO 6/6. No regression: determinism 9/9; smoke byte-identical default-vs-escape; pool #1 untouched (_MERGE_DEFAULT_ON=True). finding 2026-08-13-onebrain-pool2-flip-GO"

  - key: discourse-planner
    faculty: "Discourse planner (dlPFC neural planner; multi-sentence content)"
    de_risked: YES
    wired: YES                   # 2026-08-12 INTEGRATION: the DEFAULT /api/brain-chat turn now runs the RICH multi-sentence path (RichAnswerComposer, neural_planner=True), not the single-SVO host path. The single-SVO path is now the OPT-OUT escape (rich=False in the body, or BRAIN_RICH=0).
    on_by_default: YES           # the fluent MULTI-SENTENCE reply is the default turn: BrainChatRequest.rich defaults to None -> _brain_rich_default() -> True; the neural dlPFC planner (relevance-by-latency ordering) runs every default turn. Escape: rich=False / BRAIN_RICH=0 -> byte-identical single-SVO.
    scaffold_retired: NO         # residual scaffold: the fluent PROSE surface is the external Qwen-0.5B mouth (requires SIM_BACKEND=cupy; GPU-free falls back to the multi-sentence TEMPLATE stub); the gather/relevance/breadth/stop are host heuristics steering WHICH grounded facts to bring up (the moat + per-sentence VERIFY are on the substrate path).
    host_scaffold_in_default: "the fluent multi-sentence SURFACE is the off-bridge Qwen-0.5B mouth (SIM_BACKEND=cupy; GPU-free -> multi-sentence template stub); host gather/order/relevance/stop heuristics pick WHICH facts to raise (content recall + per-sentence VERIFY moat are on the onebrain substrate)"
    default_anchor:
      - file: webapp/server.py
        assign: neural_planner
        off_value: "False"
        on_value: "True"
        count: 1
    evidence: "webapp/server.py: BrainChatRequest.rich default None + _brain_rich_default() (env BRAIN_RICH, default True) -> the omit-default turn takes _get_rich_composer (neural_planner=True). Live HTTP-verified (SIM_BACKEND=cupy) on /api/brain-chat: default 'what are you' -> 'The brain uses spikes for communication. The brain learns words.' (renderer=qwen, rich=True, n_sentences=2, composer=onebrain); moat 'capital of france' abstains; teach 'wolf hunt deer' -> recall 'A wolf hunts deer.'; escape rich=False -> single-SVO 'The dog chased the cat.' finding 2026-08-12-INTEGRATION-default-chat-turn-is-fluent-multi-sentence"

  - key: semantic-recall
    faculty: "Semantic fact recall (VSA bind/unbind)"
    de_risked: YES
    wired: YES
    on_by_default: YES          # genuinely runs every turn
    scaffold_retired: NO        # but on the numpy VSA fast path by default, not the spiking store
    host_scaffold_in_default: "host VSA unbind (enable_substrate_store=False -> numpy composite + host bind/unbind)"
    evidence: "core_sim_composition.py:428; rf enable_substrate_store default False"

  - key: moat-verify
    faculty: "No-confab moat / VERIFY (recall-verify + abstain; CLAIM-LEVEL entailment over the multi-fact set)"
    de_risked: YES
    wired: YES
    on_by_default: YES          # the spiking recall-verify genuinely runs — one of the 2 default-on-spiking
    scaffold_retired: NO        # the abstain firewall + clause decomposition/coverage around the substrate role-parse are host
    host_scaffold_in_default: "host keyword abstain gate + host clause decomposition/coverage bracket the on-substrate per-clause role-parse"
    # 2026-08-12 CLAIM-LEVEL GENERALIZATION WIRED (default-ON): the RichAnswerComposer multi-fact path now verifies each
    # rendered sentence with the de-risked ClaimEntailmentVerifier over the SET of facts the turn gathered (IMPORTED from
    # _moat_claim_entailment_derisk, NOT reimplemented; per-clause role assignment is the SAME on-brain parser the
    # single-triple verify uses). So genuinely free-form MULTI-CLAUSE grounded prose survives the moat — the old check
    # required the prose to re-parse to EXACTLY one gated SVO. Strict SUPERSET: a single grounded sentence still passes
    # byte-identically; any response carrying even one ungrounded/contradictory asserted clause is rejected (0 leaks).
    # Escape BRAIN_CLAIM_MOAT=0 + the single-fact turn both revert to the single-triple _verify. RESIDUAL: the clause
    # decomposition + coverage + synonym/negation/hedge bookkeeping are a HOST verification harness.
    evidence: "brain_chat_tui.py ChatBrain._verify_claim_set/_build_claim_verifier (reuses _moat_claim_entailment_derisk.ClaimEntailmentVerifier on self.inner.parse); rich_answer_composer.py _verify_rendered routes the >1-fact turn through it. Verified numpy-CPU seed42 THROUGH the production wiring: grounded 'The cat eats fish and the dog chases the cat.' PASSES where old single-triple rejects; full de-risk suite 30 cases/14 leak-cases -> 0 confab leaks; LESION BRAIN_CLAIM_MOAT=0 -> multi-clause grounded rendering reverts to reject (load-bearing); single-fact recall/abstain/learn unregressed. finding 2026-08-12-INTEGRATION-claim-level-entailment-moat-wired-multiclause"

  - key: anaphora-wm
    faculty: "Anaphora working memory (referent store)"
    de_risked: YES
    wired: YES
    on_by_default: YES          # spiking V1 referent store runs — the 2nd default-on-spiking
    scaffold_retired: NO        # host pronoun detect/substitute around it; single-attractor (ties on >=2 referents)
    host_scaffold_in_default: "host pronoun detection + string substitution; SpikingLoopContextBuffer single-slot"
    evidence: "brain_chat_tui.py:253-267 host pronoun ops; SpikingLoopContextBuffer"

  - key: discourse-register
    faculty: "Discourse event register (who-was-doing-it-BEFORE across a connective; D3) — the PAIR (a_curr,p_curr | a_prev,p_prev) held on four FS-WTA spiking attractor slots; the who-was-before answer is read off cp_firing_states of the held prev slot (a single-event register structurally cannot answer it)"
    de_risked: YES
    wired: YES                  # 2026-08-13 Gate-B/D3 (F2): /api/brain-chat brain_chat runs the register (PART B, placed after the D6 block, before COMPREHENSION): (i) an ADDITIVE FOLD of a discourse SVO clause (a connective marks the boundary -> SHIFT; pure side-effect, reply byte-identical) + (ii) a DISJOINT before/now QUERY short-circuit answered off the held spiking slot + the moat abstain. PART A: both register-construction sites (_build_tiny_demo + _load_self_knowledge) build make_discourse_register (the validated spiking PairEventRegister twin) instead of PairEventRegister(spiking=False). research/runners/d3_discourse_event_register_production_organ.py, reuse-by-import. Verified numpy-CPU: organ verify seed42 ALL_OK + through the REAL server handler.
    on_by_default: YES          # default-ON; BRAIN_DISCOURSE_REGISTER=0 -> the register is built spiking=False (today's rate register) AND the endpoint block is skipped -> byte-identical. CONVERSATION-SCOPED fold state (_SESSION_DISCOURSE, cleared on reset).
    scaffold_retired: NO        # declared residuals (ride existing burn-down): (a) the transition delta (prev-slots+clause-code -> per-slot scores) is a rate-learned RNN (multislot_rnn); only the re-discretization (score -> one-of-K winner) is on spikes; (b) the connective/boundary DETECTION + the referent/verb PARSE are host (vocab-ceiling, same class as comprehension/d6); the SHIFT the connective triggers IS on the substrate; (c) CO-RESIDENT on its own four FS-WTA bridges, not merged onto the recall bridge — rides the one-brain merge (burn-down #1).
    host_scaffold_in_default: "the transition-delta RNN (multislot_rnn, rate-learned) + the boundary/connective detection + the referent/verb parse are host; the co-resident four-FS-WTA register is a separate bridge (rides the one-brain merge). The genuinely-SPIKING part is the four slot re-discretizations + the held prev-slot read (the load-bearing who-was-before answer)."
    lesion_note: "LESION-LOAD-BEARING (2026-08-13): BRAIN_DISCOURSE_REGISTER_LESION=1 silences the spiking HOLD of the prior event (the _PrevSilencePairRegister forces the two prev slots to the identity every clause) -> who-was-before collapses (organ: BEFORE 0.900->0.150, 83% attributable to the spiking hold; through the real handler with a discriminating discourse before='cat' -> the answer flips 'cat'->'dog'(identity), the register type flips to _PrevSilencePairRegister proving the flag propagates at build); NOW is preserved (only the prev hold is silenced)."
    evidence: "brain_chat_tui.py _build_tiny_demo + _load_self_knowledge -> _DR.make_discourse_register(refs, seed); webapp/server.py brain_chat D3 block (note_turn additive fold + maybe_answer disjoint before/now short-circuit) + _SESSION_DISCOURSE state + reset. research/runners/d3_discourse_event_register_production_organ.py wraps the spiking PairEventRegister twin (four FS-WTA slots). Verified numpy-CPU: _d3_discourse_event_register_organ_verify seed42 ALL_OK (INTACT BEFORE 0.900/NOW 0.917, recency 0.300, naive 0.450; LESION BEFORE->0.150 NOW 1.000, 83% attributable; flag-off rate BEFORE 0.950; no-boundary abstain 1.000; source branch 42/43/44 all ALL_OK) + real handler (DEFAULT-ON 'dog was.'/'cat was.'+'bird is.'; LESION collapse + regtype flip; FLAG-OFF block skipped -> normal path) + TUI --smoke flag-off BYTE-IDENTICAL to pristine HEAD, default-ON discourse resolves (before='dog was.', now='bird is.', discourse_ok). finding: commit d817effd0."

  - key: episodic-memory
    faculty: "Episodic memory (Gate-B, D5) — recall of PAST TURNS. A referential turn ('you mentioned X') decides whether topic X was actually discussed THIS conversation by a genuinely-SPIKING hippocampal pattern-completion (two-compartment apical dAP UP-state read), NOT a host list scan; a completion → honest disclosure, a non-completing cue → honest 'I don't recall discussing X' (never a confabulation)"
    de_risked: YES              # kt=8 EpisodicDapMemory, 6/6-GO gap#5 dAP readout (n_ca3=2000), both backends
    wired: "YES"                # 2026-08-12 Gate-B/D5: /api/brain-chat Hook A (referential recall READ, referential-first so comprehension/surprise/B3 can't pre-empt) + Hook B (BTSP store WRITE, gated behind cupy) + Hook C (reset). research/runners/d5_episodic_production_organ.py, reuse-by-import. Verified numpy-CPU: organ verify ALL_OK + through the REAL handler (referential-first, honest not-in-memory, IN-MEMORY fire after a store, lesion collapse).
    on_by_default: "YES"        # default-ON; BRAIN_EPISODIC=0 → the referential turn falls through to the normal path (byte-identical). CONVERSATION-SCOPED (one EpisodicDapMemory per session, cleared on reset).
    scaffold_retired: "NO"      # declared residuals: (a) the fact CONTENT surfaced on a completed topic is the host-oracle chat recall the moat already governs (the GATE is spiking); (b) temporal/recency ORDER is a host store-index (no spiking WHEN pool); (c) the sleep-replay CAPSTONE is offline consolidation, NOT this per-turn path; (d) LATENCY — Hook B BTSP store is ~430-510s/topic on numpy@2000 so it is GATED behind cupy (BRAIN_EPISODIC_STORE=1 forces it); numpy defers the WRITE (the recall GATE stays spiking); (e) CO-RESIDENT on its own dAP bridge (n_ca3=2000), rides burn-down #1.
    lesion_note: "LESION-LOAD-BEARING (2026-08-12): BRAIN_EPISODIC_LESION=1 reads the referential cue through the UNFORMED baseline recurrent weights → the dAP completion collapses 0.909→0.000 → the gate falls to 'not in memory' (attributable_to=1.0: 100% of the recall carried by the BTSP-formed assembly, 0% by glue). The read is the cp_v_apical UP-state fraction after real _run_one_simulation_step() steps, NOT a host formula."
    evidence: "webapp/server.py brain_chat: d5_episodic_production_organ Hook A (is_referential+extract_referent→recall→recall_disclosure, short-circuits FIRST) + Hook B (note_topic on an answered VERIFIED SVO, _episodic_store_ok gates behind cupy) + reset_episodic_organ. research/runners/d5_episodic_production_organ.py wraps EpisodicDapMemory (kt=8). Verified numpy-CPU: _verify_d5_episodic_organ ALL_OK seed42 (intact cue=0.909 perm=nocue=lesion=0, attribution 1.0, wall 790s; 6-seed committed GO both backends) + real handler htestA/htestB (referential caught by Hook A not comprehension; not-in-memory honest disclosure; IN-MEMORY fire after forced store; lesion collapse). finding 2026-08-12-D5-episodic-production-organ-spiking-recall-gate-wireable"

  - key: wm-binding-advanced
    faculty: "Advanced WM/binding (Gate-B, D6) — HOLD >=2 discourse referents across a turn/span on a genuinely-SPIKING multi-register bump-attractor buffer (R disjoint slow-NMDA banks, ONE shared FS); the read-back reports what a single-attractor store CANNOT (it ties to one)"
    de_risked: YES              # MultiSlotHold + RUNG6c HebbianBinder, 6-seed GO (k=2 all_recovered 1.000, lesion 0.000, ceiling k=5)
    wired: "YES"                # 2026-08-12 Gate-B/D6: /api/brain-chat MAINTAIN (a >=2-referent coordinated-NP turn LOADS+HOLDS, write-only) + READ-OUT (a 'who/what are we keeping in mind' query READS BACK off cp_firing_states → honest functional read-out, short-circuit). research/runners/d6_multiref_wm_production_organ.py, reuse-by-import. PER-SESSION buffer (the organ singleton's referent codebook is process-global → a shared buffer leaks other sessions' referents). Verified numpy-CPU: organ verify PASS + real handler (n=2 all_recovered, hold-query holds dog+cat, lesion collapse, flag-off byte-identical).
    on_by_default: "YES"        # default-ON; BRAIN_MULTIREF=0 → fully skipped (byte-identical). Out-of-scope (<2 referents, no hold-query) → None → unchanged.
    scaffold_retired: "NO"      # declared residuals: (a) the learned SPIKING WRITE-GATE is the open rung — register assignment is a role-by-position host MARKER (gap#4 credit-assignment); (b) referent EXTRACTION is a host regex+lexicon (vocab-ceiling); (c) the register READ is a host argmax over each bank's firing rates (read-out instrument), binder-capped at 6 (ceiling k=5); (d) cross-turn persistence of WHICH referents is a host codebook (the load-bearing spiking part is the within-span HOLD); (e) CO-RESIDENT on its own MultiSlotHold bridge (rides burn-down #1).
    lesion_note: "LESION-LOAD-BEARING (2026-08-12): BRAIN_MULTIREF_LESION=1 builds the buffer with recur=0 (the slow-NMDA recurrence killed) → the sustained bumps die over the span → the >=2 read-back collapses (all_recovered 1.000→0.000, hold_alive_min→0.0). The host referent parse + write-marker are byte-identical across arms; only recur differs, so the discrimination is caused by the spiking hold."
    evidence: "webapp/server.py brain_chat + _get_multiref_organ (PER-SESSION, next to _get_worldmodel_organ; _SESSION_MULTIREF, popped on reset): d6_multiref_wm_production_organ.judge(msg) after the world-model block, before comprehension — is_hold_query→readout short-circuit / else MAINTAIN pass-through. research/runners/d6_multiref_wm_production_organ.py reuses MultiSlotHold + RUNG6c HebbianBinder. Verified numpy-CPU: _d6_multiref_wm_production_verify PASS (k=2/3/4 all_recovered=1.000, lesion=0.000, 100% attributable, superposed-single ~1/k) + real handler htestA (maintain n=2 all_recovered; hold-query 'holding 2: dog and cat'; lesion collapse; flag-off multiref:null). finding 2026-08-12-D6-multiref-WM-production-organ-holds-two-plus-referents-lesion-load-bearing"

  - key: affect-coloring
    faculty: "Affect/emotion coloring (Gate-B) — the brain's live spiking MOOD colors WHAT it volunteers (mood-congruent forthcomingness) + HOW it phrases it (warmer/curter prose); moat-safe, lesion-load-bearing"
    de_risked: YES              # the co-resident graded-affect LADDER is a 6-seed GO for the valence SIGN + a bistable ladder (2026-08-08); the content-bias (forthcomingness) is lesion-verified in the Stage-A integration
    wired: YES                  # 2026-08-12 Gate-B: /api/brain-chat reads the live mood NEURALLY off the co-resident graded-affect ladder (research/runners/affect_production_organ.py) and colors the DEFAULT turn — forthcomingness (RichAnswerComposer max_sentences/elaborations) + prose MANNER (the Qwen mouth's constrain prompt). HTTP-verified composer=onebrain.
    on_by_default: YES          # default-ON; BRAIN_AFFECT=0 is the byte-identical-oracle escape. The moat/recall/abstain paths are UNCHANGED (affect colors only an already-matched answer; it can never manufacture a fact or flip an abstain).
    scaffold_retired: NO        # 2026-08-12: the appraisal VALUE is now the DR-2 LEARNED distributional valence (leave-one-out label-propagation over the brain's learned co-occurrence graph; 6-seed held-out r~0.81; sourced from a cached map, NOT a hardcoded lookup) -- closes the audit's #1 over-credit (the value was a raw Warriner lookup mislabeled "DR-2 learned"). Declared scaffolds STILL host: (1) the affect-word SALIENCE GATE + the SEED norms are Warriner (DR-2 is SEEDED from them -- the lexicon is propagated, NOT retired; a full learned-gate drop-in colors plain cat/sit/run -> breaks neutral-default, measured), the LEARNING is numpy not spiking, the injection is host -- next rung = a fully-spiking opponent V+/V- appraisal population; (2) the Qwen MOUTH the manner conditions is external/host (burn-down A1); (3) the affect organ is a co-resident bridge alongside the recall composer, not merged (burn-down #1); (4) the held value is a quantized bistable LADDER (sign+level), not a smooth continuum.
    host_scaffold_in_default: "appraisal VALUE = DR-2 learned map (not a hardcoded lookup); RESIDUAL host = the affect-salience GATE + seed norms (Warriner-seeded, numpy label-prop not spiking) + the host injection into the ladder; the external Qwen mouth for the manner surface (A1); the affect organ on its own co-resident bridge (not yet merged with the recall composer's bridge). 2026-08-19 COVERAGE FIX: the seed GATE lexicon (140 words, TinyStories-curated) MISSED common adult strong-emotion words, so strongly-worded emotion read valence 0.0 and BOTH affect faculties were DORMANT when emotion was strongest; +~40 adult emotion words added (Warriner-approximate, sign-correct) -> strong emotion now crosses threshold + drives the reply end-to-end, neutral queries stay neutral (finding 2026-08-19-affect-appraisal-lexicon-missed-strong-emotion). Still a curated seed, not a learned/spiking appraisal."
    lesion_note: "LESION-LOAD-BEARING (2026-08-12): set_transmission_gate('affect_out',0) (BRAIN_AFFECT_LESION=1) collapses the neural differential to 0 -> the mood coloring collapses (content reverts to the neutral forthcomingness default, manner to bare) while the matched-fact CONTENT ('dog chase cat') is byte-identical and the abstain behaviour is unchanged. The READ-BACK through affect_out is the load-bearing spiking part. HTTP-verified: NEG-mood-ON = 1 terse sentence; NEG-mood-LESIONED = differential 0.0, 2 neutral sentences, same recalled fact."
    evidence: "webapp/server.py brain_chat: appraise_text + _update_session_mood + organ.read_differential -> content_plan (rich max_sentences/elaborations) + MoodConditionedRenderer manner; research/runners/affect_production_organ.py signed ladder read (build_one_brain co_resident_affect_ladder=True). HTTP /api/brain-chat composer=onebrain: POSITIVE mood diff +0.039 -> 2 sentences warm ('The dog chased the cat. Cat is eating fish!'); NEGATIVE mood diff -0.036 -> 1 terse sentence ('The dog chased the cat.'); affect_out lesion -> diff 0, coloring collapsed, fact byte-identical; 0 moat leaks under strong mood. finding 2026-08-12-GateB-affect-colors-production-chat"

  - key: comprehension-monitor
    faculty: "Comprehension MEASUREMENT (Gate-B, D4) — the brain reads a SPIKING role-binding margin off cp_firing_states BEFORE acting on an incoming transitive assertion, and honestly ABSTAINS ('my role-binding didn't resolve — I didn't follow that') on OOV / content-ambiguous input; moat-STRENGTHENING, lesion-load-bearing"
    de_risked: "YES"            # the co-resident SpikingRoleCompetition sel-pool semantic margin is a 6/6-GO D4 de-risk (type-2 AUC=1.000, lesion->0.500, reads COMPREHENSION not word-order), 2026-08-12
    wired: "YES"                # 2026-08-12 Gate-B/D4: /api/brain-chat reads the semantic sel-pool margin off cp_firing_states (research/runners/comprehension_production_organ.py, reuse-by-import) BEFORE the rich/single split; a LOW-margin competent transitive -> honest 'I didn't follow that' abstain. Verified in-process numpy-CPU (composer=rf), 7/7 checks.
    on_by_default: "YES"        # default-ON; BRAIN_COMPREHENSION_GATE=0 is the byte-identical-oracle escape. NON-REGRESSIVE by scope: fires ONLY on a competent 3-token transitive (fully cue-covered OR fully OOV); questions/self-queries/anaphora/open-ended/real-but-untabled vocab are OUT OF SCOPE -> unchanged. GUARD: never abstains on a KNOWN (agent,action) (what_does truthy).
    scaffold_retired: "NO"      # declared residuals: (1) CO-RESIDENT on its own SpikingRoleCompetition bridge, not merged onto the ONE recall bridge (burn-down #1); (2) VOCAB CEILING — the cue lexicon (ANIMACY/VERB_SELECTS) is the toy 2-noun transitive scope, a graded/near-threshold battery + a LEARNED cue lexicon are the next rung; (3) structural malformedness (no verb/wrong arity) still a host arity check.
    host_scaffold_in_default: "the toy cue lexicon (ANIMACY/VERB_SELECTS) bounds the monitor's competence; a light base-form lemmatizer + the extract-transitive shape check are host; the monitor runs on its own co-resident bridge (not merged with the recall composer's bridge)"
    lesion_note: "LESION-LOAD-BEARING (2026-08-12): BRAIN_COMPREHENSION_LESION=1 zeroes the learned cue->role synapses -> the semantic margin collapses on a well-formed input (0.338 -> 0.000, comprehended True->False) while the host cue VALUES are byte-identical -> the discrimination is caused by the learned spiking competition, not the host cue table. The read is on FIRING neurons (comp._noun_role_rates over cp_firing_states); the host _semantic_contrast dot-product is never called."
    evidence: "webapp/server.py brain_chat: comprehension_production_organ.judge(msg, brain_vocab) -> on a competent low-margin transitive that the brain does not already know, returns the honest 'didn't follow' abstain BEFORE the rich/single path. research/runners/comprehension_production_organ.py: SpikingRoleCompetition semantic sel-pool margin, hard per-turn reset (history-independent), build-time threshold calibration (~0.249). Verified numpy-CPU in-process (composer=rf) 7/7: comprehensible 'the dog eats the bone' m=0.338 PASSES; OOV 'wug blicket toma' m=0.026 + ambiguous 'apple push rock' m=0.142 -> honest didn't-follow abstain; known-but-ambiguous 'dog chase cat' m=0.088 HONORED (known=True); recall/untaught questions comprehension:null (out of scope, unregressed); lesion 0.338->0.000; flag-off comprehension:null. finding 2026-08-12-GateB-comprehension-monitor-production-chat"

  - key: other-repair
    faculty: "Conversational OTHER-REPAIR (Gate-B, T1-6) — on a low-comprehension turn the D4 gate would ABSTAIN on, the brain asks a TARGETED clarification NAMING what did not resolve (the unresolved thematic ROLE, or the OOV token) instead of the bare 'I didn't follow that'. A repair SEQUENCE, not a dead-end. Composes the D4 monitor: the SAME per-noun spiking sel-pool read (a0,a1 off cp_firing_states) localises the failed role; moat-safe (a QUESTION, never a fact — the turn stays an abstain), lesion-load-bearing"
    de_risked: "YES"            # reuses the 6/6-GO D4 SpikingRoleCompetition semantic sel-pool read (_spiking_comprehension_monitor_derisk.py, type-2 AUC=1.000, lesion->0.500, 6 seeds); the per-noun role localisation is an ADDITIVE read on that same substrate. Role-lean robustness across the 6 D4 seeds: two-inanimate net-lean -> AGENT 72/72; two-animate net-lean ~0 -> generic 72/72.
    wired: "YES"                # 2026-08-13 Gate-B/T1-6: at the D4 comprehension-abstain point /api/brain-chat calls comprehension_production_organ.repair_target(msg) (the per-noun a0,a1 read) + repair_production_organ.clarification_question(target) -> replaces the bare abstain with the targeted clarification. NO new bridge (reuses D4's SpikingRoleCompetition); NO sim/ edit. Verified numpy-CPU in-process (composer=rf, stub) 6/6 + 9-turn byte-identical regression.
    on_by_default: "YES"        # default-ON; BRAIN_REPAIR=0 is the byte-identical escape (the bare abstain, no repair key). Additive + moat-safe: fires ONLY inside the existing D4 abstain (never creates an abstain, never manufactures a fact, never flips the abstain into an answer); the turn stays abstained=True and only the surface text changes.
    scaffold_retired: "NO"      # declared residuals: (1) the OOV TOKEN branch is a HOST-LEXICAL scaffold — NOT load-bearing on the spiking read (only the ROLE branch is); a spiking unknown-word read is the next rung; (2) two-animate direction is UNDETERMINED by the substrate -> a GENERIC (still targeted) role-swap question is the honest read; (3) the clarification WORDING is a fixed host language template (only the DECISION + ROLE TARGET are brain-surfaced); (4) D4-SCOPE INHERITANCE — the repair fires on exactly the D4-abstain set (inherits D4's OOV-transitive edge cases); (5) CO-RESIDENT — reuses D4's own bridge (rides burn-down #1).
    host_scaffold_in_default: "the clarification QUESTION template + the OOV-token identity are declared host language/lexical scaffolds (like curiosity's wh-frame + topic extractor); the ROLE TARGET (sign(a0+a1) over the per-noun sel-pool firing + the pair-max commitment gate) is the load-bearing spiking part; no new spiking substrate is added (reuses the D4 read)"
    lesion_note: "LESION-LOAD-BEARING (2026-08-13): BRAIN_COMPREHENSION_LESION=1 (the SAME D4 lesion — zeroes the learned cue->role synapses) collapses the per-noun role evidence pair-max to 0.000 (< the build-calibrated role_floor ~0.033) -> repair_target returns None -> the turn reverts to the BARE abstain (repaired=False). The role clarification is caused by the SPIKING per-noun sel-pool firing, not a host role-parse; the OOV token branch (host-lexical) is declared NOT load-bearing."
    evidence: "webapp/server.py brain_chat: at the D4 (not cj['comprehended']) and (not known) abstain, calls corg.repair_target(msg, brain_vocab, lesion) + repair_production_organ.clarification_question(target) -> replaces the bare _CO.didnt_follow_message with the targeted clarification (payload['repair'] added; BRAIN_REPAIR=0 -> key absent). research/runners/comprehension_production_organ.py repair_target (per-noun a0,a1 off cp_firing_states, role_floor/lean_margin calibrated in the SAME threshold loop so D4 stays byte-identical) + research/runners/repair_production_organ.py (host QUESTION template). Verified numpy-CPU (composer=rf, stub) research/runners/_gateB_repair_production_verify.py 6/6 ALL_OK: 2-inanim 'book carries cup' -> role=AGENT clarification; 2-animate 'wolf watches owl' -> generic; fully-OOV 'wug blickets glorp' -> token naming; comprehensible 'wolf bites apple' -> no false repair; lesion -> bare abstain; flag-off -> bare abstain no-key. 9-turn regression: flag-off pristine-identical (0 diffs), default-ON identical on recall/moat/anaphora/D2/comprehensible-D4 (differs only where D4 already abstained). finding 2026-08-13-T1-6-other-repair-production-chat"

  - key: surprise-monitor
    faculty: "Expectation-violation / SURPRISE (Gate-B, D2) — when an asserted (agent,action,patient) contradicts a stored expectation, a spiking predictive-coding MISMATCH unit fires (cp_firing_states[surprise]) and the brain PREPENDS an honest NOTICE ('that surprises me — I'd learned <stored>'); moat-safe, lesion-load-bearing"
    de_risked: "YES"            # the spiking predictive-coding mismatch unit is a 6/6-GO D2 de-risk at the robust operating point (confirm ~0 Hz vs contradict 7.5-9.9 Hz, lesion-decisive), 2026-08-12
    wired: "YES"                # 2026-08-12 Gate-B/D2: /api/brain-chat runs the mismatch circuit (research/runners/surprise_production_organ.py, reuse-by-import) when the user asserts a fact for which the brain holds a stored (agent,action)->patient; on a firing surprise it PREPENDS the honest notice. The expected patient is RECALLED by the brain's own spiking what_does; the mismatch is a cp_firing_states read (no host recalled==asserted compare). Verified in-process numpy-CPU (composer=rf), 8/8 checks.
    on_by_default: "YES"        # default-ON; BRAIN_SURPRISE=0 is the byte-identical-oracle escape. Additive + moat-safe: runs ONLY when a stored expectation exists; never manufactures a fact, flips an abstain, or enters the certainty band — it only PREPENDS a notice to the turn's normal answer.
    scaffold_retired: "NO"      # declared residuals: (1) burn-down #1 PARTIAL (2026-08-13): the mismatch circuit now shares ONE bridge with the E2 WORLD-MODEL organ BY DEFAULT (BRAIN_ONEBRAIN_MERGE, ledger row onebrain-merge-organs; one cp_membrane_potential_v, N=1584) — the pair's separate-bridge co-residency is retired — but it is NOT yet merged onto the ONE recall composer bridge; (2) PRECISION COMPANION WIRED (2026-08-13, default-ON): the per-block HOMEOSTATIC PREDICTION-GAIN equalizer now runs in SurpriseProductionOrgan.ensure_built (BRAIN_SURPRISE_HOMEOSTAT=0 escape) — het_vote_rate 0.9375->1.0 (every FAMILIAR edge reads below threshold) with surprise specificity preserved BY CONSTRUCTION (topographic block-diagonal prediction inhibits only its own confirm block); RESIDUAL: a BUILD-TIME host-orchestrated calibration loop (an ONLINE spiking homeostatic-plasticity rule, Vogels 2011, is the further step); a fully-learned all-to-all CA3 recall is the unchanged separate rung; (3) the (agent,action) recall + patient-block map key on surface tokens (light inflection tolerance).
    host_scaffold_in_default: "the patient-concept->circuit-block mapping + the extract-assertion shape check are host sensory-encoding; the per-block prediction gain is now homeostatically EQUALIZED at build (the precision companion, default-ON) — a build-time host-orchestrated calibration loop (an online spiking rule is the next rung), NO LONGER a fixed-weight proxy; the mismatch unit runs on its own co-resident bridge (not merged with the recall composer's bridge)"
    lesion_note: "LESION-LOAD-BEARING (2026-08-12): BRAIN_SURPRISE_LESION=1 zeroes the patient_expected->surprise prediction edges -> the subtractive inhibition is gone -> the SAME CONFIRM input ('wolf hunt deer', stored==asserted) that is ~0 Hz intact now FIRES (surprised True) -> the confirm/contradict separation collapses. The surprise is a cp_firing_states[surprise] read; current_reward_signal==0; no host compare of the asserted vs expected codes produces the signal."
    evidence: "webapp/server.py brain_chat: surprise_production_organ.judge(agent,action,p_stored,p_asserted) AFTER the D4 gate, BEFORE the rich/single split -> on surprised, PREPEND surprise_notice to the answer. research/runners/surprise_production_organ.py: predictive-coding mismatch circuit at cue_to_expected_weight=0.8, trained then FROZEN, build-time confirm-vs-contradict threshold (~2.73 Hz). Verified numpy-CPU in-process (composer=rf) 8/8: teach 'wolf hunt deer' surprise:null; CONFIRM 'wolf hunt deer' 0.00 Hz not-surprised no-notice; CONTRADICT 'wolf hunt rock' 5.61 Hz surprised -> honest notice prepended; contradict>=3x confirm; LESION makes the SAME confirm fire; flag-off surprise:null. finding 2026-08-12-GateB-surprise-expectation-violation-production-chat. PRECISION COMPANION WIRED (2026-08-13): SurpriseProductionOrgan.ensure_built runs the per-block homeostatic prediction-gain equalizer (_homeostat/_install_block_gains; the identical routine the de-risk _surprise_organ_homeostat_derisk validated 6/6, finding 2026-08-13-surprise-organ-homeostat-GO). Verified research/runners/_surprise_organ_homeostat_production_verify.py: (A) 6-seed organ het-vote 1.0 (8/8) ON with specificity 1.0/1.0, load-bearing (>=1 seed <1.0 OFF); (B) real D2 handler CONFIRM not-surprised / CONTRADICT surprised / calib.homeostat True by default / LESION confirm fires / BRAIN_SURPRISE_HOMEOSTAT=0 escape no-regression."

  - key: metacog-monitor
    faculty: "Self-model / METACOGNITION (Gate-B, E1) — the brain reads a SPIKING balance-of-evidence confidence of the answer it is about to give (the workspace WTA margin |rate(asm1)-rate(asm0)| off cp_firing_states) and, on a LOW-confidence answer, honestly QUALIFIES it with a functional hedge ('my decision-margin reads this as low-confidence'); moat-safe (only qualifies an already-produced answer), lesion-load-bearing"
    de_risked: "YES"            # the pure-spiking balance-of-evidence confidence read is a 6/6-GO E1 de-risk (meta-d'>0 on every seed, type2_auc 0.67-0.82, permuted collapses, within-class holds), 2026-08-12
    wired: "YES"                # 2026-08-12 Gate-B/E1: /api/brain-chat reads the confidence of the answer AFTER the gate/moat produced it (both rich + single-fact paths) off the co-resident balance-of-evidence monitor (research/runners/metacog_production_organ.py, reuse-by-import); the evidence is the brain's OWN mean role-decode confidence; a LOW-confidence answer gets an honest hedge PREPENDED. Verified in-process numpy-CPU (composer=rf), 11/11 checks.
    on_by_default: "YES"        # default-ON; BRAIN_METACOG=0 is the byte-identical-oracle escape. Additive + moat-safe: runs ONLY after the gate/moat already produced an answer; an abstain (no answer) is skipped; it only QUALIFIES, never manufactures a fact, changes WHICH answer, or flips an abstain.
    scaffold_retired: "NO"      # declared residuals: (1) CO-RESIDENT on its own metacog-workspace bridge, not merged onto the ONE recall bridge (burn-down #1); (2) EVIDENCE = the parse confidence (a COMPONENT of answer confidence), not a full recall-vs-alternatives balance (rf magnitude/frac saturated on the tiny-demo) — a richer recall-margin evidence is the next rung; (3) NOT type-1/type-2 DISSOCIABLE (balance is an ENCODING read; the dissociable margin_abs comparator is seed-fragile = the next rung).
    host_scaffold_in_default: "the evidence DERIVATION (mean role-decode confidence -> evidence scalar, a stretch-normalization) is a declared host boundary; the balance MARGIN read + the threshold decision are the load-bearing spiking part; the monitor runs on its own co-resident bridge (not merged with the recall composer's bridge)"
    lesion_note: "LESION-LOAD-BEARING (2026-08-12): BRAIN_METACOG_LESION=1 removes the evidence DIFFERENTIAL from the workspace (drives both assemblies at base) -> the settled margin collapses (~0) regardless of the answer's evidence -> the SAME high-confidence recall ('what does the brain use', balance 0.00189 confident) FLIPS to hedged (balance 0.00089, confident False). The lesion changes only the workspace drive, not the raw role-conf scalar -> the confident/uncertain discrimination is caused by the SPIKING margin, not a host threshold. FUNCTIONAL correlate, NOT a phenomenal claim."
    evidence: "webapp/server.py brain_chat: _metacog_qualify(activity, no_answer) reads the answer's mean role-decode confidence -> metacog_production_organ.judge(evidence) -> on confident=False, PREPEND hedge_prefix. research/runners/metacog_production_organ.py: build_metacog_bridge(confidence_read='balance') (reused), build-time high/low-evidence threshold calibration (~0.00142), averaged 8-rep read. Verified numpy-CPU in-process (composer=rf) 11/11: high-conf recall (role-conf 0.476, balance 0.00189) NO hedge; low-conf recall (role-conf 0.400, balance 0.00095) HEDGED; abstain skipped (metacog null); LESION flips high->hedge; flag-off null+byte-identical; recall/anaphora/abstain/D2/D4 unregressed. finding 2026-08-12-GateB-metacog-confidence-readout-production-chat"

  - key: worldmodel-forward
    faculty: "Internal worldview / AFFECTIVE WORLD-MODEL (Gate-B, E2) — a co-resident 2-channel spiking predictive-coding VALENCE forward model: the next-turn-affect prediction is QUERYABLE ('what do you expect / how is this going' -> a two-pool spike-rate read), and an affect-trajectory VIOLATION fires a genuinely-SPIKING surprise (cp_firing_states[surprise]) that prepends an honest 'that shifts the mood unexpectedly' notice; moat-safe (only reads/notices), lesion-load-bearing"
    de_risked: "YES"            # the spiking valence forward model is a 6/6-GO E2 de-risk (predicted-valence acc 1.00, expected 0 Hz vs violated 37-46 Hz, lesion 3/3, dual-scored shuffle), 2026-08-12
    wired: "YES"                # 2026-08-12 Gate-B/E2: /api/brain-chat answers a 'what do you expect / how is this going' query with the two-pool spiking prediction (early return) and prepends an honest surprise notice on an affect-trajectory violation (reuse-by-import from research/runners/worldmodel_production_organ.py). Verified in-process numpy-CPU (composer=rf), 15/15 checks.
    on_by_default: "YES"        # default-ON; BRAIN_WORLDMODEL=0 is the byte-identical-oracle escape. Additive + moat-safe: E2 only READS (a queryable expectation) or NOTICES (an honest surprise notice); it never manufactures a fact, flips an abstain, or changes WHICH answer the recall produced.
    scaffold_retired: "NO"      # declared residuals (the mission's named next rungs): (1) GENERIC pos/neg pools — NOT bound to the ACTUAL interlocutor affect (the P0.3 valence latch + W5 ToM channel) — the next rung, un-wired; (2) the persistence state-SELECTION is a host mapping over the model's learned predictions; (3) Markov-1 first-order transition (HTM-TM high-order is a rung); (4) teacher-driven (not self-organized); (5) burn-down #1 PARTIAL (2026-08-13): the forward model now shares ONE bridge with the D2 SURPRISE organ BY DEFAULT (BRAIN_ONEBRAIN_MERGE, ledger row onebrain-merge-organs; one cp_membrane_potential_v, N=1584); NOT yet merged onto the ONE recall composer bridge.
    host_scaffold_in_default: "the valence APPRAISAL (now the DR-2 LEARNED map -> sign; 2026-08-12) + the persistence state-SELECTION are declared host boundaries (the de-risk's legitimate environment boundary; the appraisal VALUE is learned-from-corpus, the salience gate + seed norms + numpy label-prop remain host -> fully-spiking opponent-population appraisal is the next rung); the prediction READ + the mismatch READ are the load-bearing spiking part; the model runs on its own co-resident bridge (not merged with the recall composer's bridge)"
    lesion_note: "LESION-LOAD-BEARING (2026-08-12): BRAIN_WORLDMODEL_LESION=1 zeroes the learned state->pred transition -> (a) the QUERYABLE prediction margin COLLAPSES (410.8 -> 0.0 through the handler); (b) on the production organ the SAME expected observation that is 0.00 Hz (not surprised) intact fires 52.08 Hz (surprised) under lesion -> the expected/violated separation collapses. The discrimination is caused by the learned SPIKING prediction, not the host state/observation drive; the surprise is a cp_firing_states[surprise] read; current_reward_signal==0."
    evidence: "webapp/server.py brain_chat: worldmodel_production_organ.expectation(context_sign) answers 'what do you expect / how is this going' (early return); read_surprise(context_sign, observed_sign) on a normal turn -> on surprised, PREPEND worldmodel_surprise_notice. research/runners/worldmodel_production_organ.py: build_world_model_circuit + train_transition (reused), build-time state selection + surprise threshold (~12.2 Hz). Verified numpy-CPU in-process (composer=rf) 15/15: queryable +411 Hz margin -> 'keep going positive'; violation 24.3 Hz fires the notice; persistence no-surprise; LESION collapses margin 411->0 + expected 0->52 Hz; flag-off null + query not-intercepted; recall/anaphora/abstain/D2/D4/E1 unregressed. finding 2026-08-12-GateB-worldmodel-affective-forward-model-production-chat"

  - key: curiosity-followup
    faculty: "Curiosity (Gate-B, D3) — an honest FOLLOW-UP QUESTION on a NOVEL topic (crave, don't refuse). On an ABSTAIN (the brain holds no answer -> a maximal epistemic gap) the gap drives the from_novelty neuromodulator -> a spiking ASK pool; the wanting is read off cp_firing_states[ask]; when it CRAVES the brain APPENDS a follow-up QUESTION ('My curiosity is piqued — I haven't learned about <topic> yet: what can you tell me about <topic>?'). Moat INVERTED, not broken (the abstain still confabulates nothing; the added text is a QUESTION); moat-safe, lesion-load-bearing"
    de_risked: "YES"            # the DR-1 crave-DRIVE is on-bridge 6-seed CPU GO (corr(gap,SPIKING-want)=+0.996 reproduced numpy-CPU, lesion->0 asks; 6/6-SAFE crave-on-spikes in Stage-A step-3), 2026-07-23/24 + 2026-08-07
    wired: "YES"                # 2026-08-12 Gate-B/D3: /api/brain-chat reads the ASK-pool crave off cp_firing_states[ask] (research/runners/curiosity_production_organ.py, reuse-by-import) on an abstain (both rich + single-fact paths); on a craving read it APPENDS an honest follow-up question. Verified in-process numpy-CPU (stub renderer), 24/24 checks.
    on_by_default: "YES"        # default-ON; BRAIN_CURIOSITY=0 is the byte-identical-oracle escape. Additive + moat-safe: runs ONLY on an abstain (there is no answer to corrupt) and only APPENDS a QUESTION; a FAMILIAR recall is OUT OF SCOPE -> byte-identical. Never manufactures a fact or flips an abstain into an assert.
    scaffold_retired: "NO"      # declared residuals (the named next rungs): (1) ONLY the crave-DRIVE is wired — the learning-progress SELECTOR is a CPU-proxy host formula (on-bridge memory seed-fragile 1/6) + the noisy-TV VETO is a host ELP TD tracker (survives the critic lesion), NEITHER wired (a single-topic chat follow-up needs no multi-armed LP selection nor a noise veto); (2) NOVELTY = the ABSTAIN (a binary epistemic gap), a declared host boundary — a graded Bogacz-Brown familiarity novelty + curiosity on a low-confidence RECALL (already metacog-hedged) are next rungs; (3) the wh-FRAME is a host language scaffold (only the topic CONTENT is brain-surfaced); (4) CO-RESIDENT on its own ASK bridge (burn-down #1).
    host_scaffold_in_default: "the NOVELTY derivation (the abstain = the brain's own memory read, the same signal the moat uses) + the wh-FRAME + the topic content-word extractor are declared host boundaries; the ASK-pool firing read + the threshold decision are the load-bearing spiking part; the LP SELECTOR + noisy-TV VETO are host (un-wired); the organ runs on its own co-resident bridge (not merged with the recall composer's bridge)"
    lesion_note: "LESION-LOAD-BEARING (2026-08-12): BRAIN_CURIOSITY_LESION=1 removes the curiosity drive PATHWAY (curiosity_excit_sensitivity=0 -> the from_novelty modulator no longer drives the ASK pool) -> the SAME novel abstain's ASK-pool want COLLAPSES 129.2 -> 5.4 Hz (< threshold 65.9) -> curious=False -> NO follow-up (bare 'I don't know about that.'). The follow-up is caused by the SPIKING ASK-pool firing, not by the abstain flag; the read is on cp_firing_states[ask]."
    evidence: "webapp/server.py brain_chat: _curiosity_followup(abstained) reads curiosity_production_organ.judge(novelty) on an abstain -> on curious, APPEND followup_question(extract_topic(msg)). research/runners/curiosity_production_organ.py: build_curiosity_bridge (reused from _curiosity_seek_learn_onbridge_derisk, the DR-1 crave-drive) + build-time novel-vs-familiar threshold calibration (~65.9 Hz), N-rep drift-free read. Reproduced numpy-CPU: corr(gap, cp_firing_states[ask])=+0.996, lesion->0 asks. Verified numpy-CPU in-process (stub renderer) 24/24: novel abstain 'what does the dragon breathe' -> want 129.2 Hz >= 65.9 -> curious -> follow-up appended (topic=dragon); familiar recall curiosity:null no-suffix; LESION want 129.2->5.4 no-follow-up; flag-off null + additive:default==off+suffix; single-fact path too; affect/D2/D4/E1/E2 keys unregressed. finding 2026-08-12-GateB-curiosity-followup-production-chat"

  - key: noncontradiction-gate
    faculty: "Non-contradiction ASSERTION-gate (Gate-B, B3) — when the user ASSERTS a transitive fact whose POLARITY contradicts the brain's stored polarity for the EXACT same SVO ('the dog eats grass' vs a stored 'a dog does NOT eat grass'), the brain REJECTS it instead of silently overwriting a held belief; the load-bearing recall is the spiking polarity WTA (ask_yes_no → cp_firing_states), moat-INVERTING, lesion-load-bearing"
    de_risked: "YES"            # _burndown_B3_onebrain_negation_moat_derisk, 6-seed GO D=128 (intact 6/6 rejected 0 false-accepts; storage-lesion 0→18 false-accepts; the onebrain composer DOES recall a stored NEGATE on the substrate, enable_spiking_cleanup)
    wired: "YES"                # 2026-08-12 Gate-B/B3: /api/brain-chat runs the gate AFTER D2 surprise, BEFORE the store/gate (a reject returns before _maybe_acquire overwrites); research/runners/b3_noncontradiction_production_organ.check(chat.inner.is_it_true, msg), reuse-by-import. STORE-SIDE: brain_chat_tui._maybe_acquire now acquires with the DETECTED polarity via extract_polar_assertion (a heard negation stores as NEGATE), guarded legacy AFFIRM path when disabled. Verified numpy-CPU: organ verify 6/6 ALL_OK + real handler (reject on the contradiction; lesion inert; flag-off no-op).
    on_by_default: "YES"        # default-ON; BRAIN_NONCONTRADICTION_GATE=0 → the whole block skipped (byte-identical). Moat-INVERTING: an unknown SVO → accept (never a fabricated rejection). MUTUALLY EXCLUSIVE with D2 surprise (patient-mismatch → 'unknown' → accept). NO co-resident bridge added (reads the ONE production recall composer directly).
    scaffold_retired: "NO"      # declared host upstream residuals: negation DETECTION (detect_polarity parses not/n't/never) + verb MORPHOLOGY (surface-first + single trailing-s/ies lemma fallback) are input surface-tagging (the composer already RECALLS polarity on the substrate); a learned spiking polarity classifier + the shared D4 lemmatizer are the next rungs. The gate boolean stored!=asserted is host (the accepted moat pattern, identical to _contradicts).
    lesion_note: "LESION-LOAD-BEARING (2026-08-12): BRAIN_NONCONTRADICTION_LESION=1 bypasses the spiking polarity recall (every recall → 'unknown') → the gate goes INERT (contradictions slip through, rejections→0). Isolates the spiking ask_yes_no recall (cp_firing_states WTA over the resonator cleanup membrane) as the cause of the rejection: organ verify intact 12 rejections vs recall-lesion 0 → attributable_to=1.0. The de-risk STORAGE lesion (store all AFFIRM) also loses the negation on the substrate (0→18 false-accepts)."
    evidence: "webapp/server.py brain_chat: b3_noncontradiction_production_organ.check(chat.inner.is_it_true, msg) after the surprise block, before use_rich → on reject, return the rejection_message (short-circuit). research/runners/brain_chat_tui.py _maybe_acquire: extract_polar_assertion(q) stores a heard negation as NEGATE (was hard-coded AFFIRM + exactly-3-token). research/runners/b3_noncontradiction_production_organ.py reuses _assert_gate from the 6-seed-GO de-risk. Verified numpy-CPU: _b3_noncontradiction_organ_verify 6/6 ALL_OK + real handler htestA (teach 'the dog does not eat grass'→NEGATE; assert 'the dog eats grass'→REJECT stored NEGATE vs asserted AFFIRM; lesion slips through; flag-off nc:null). finding 2026-08-12-B3-noncontradiction-production-organ-built-and-verified"

  - key: reconsolidation
    faculty: "Reconsolidation / belief revision (Gate-B, B4) — a PE-gated IN-PLACE fact UPDATE: when the asserted patient CONTRADICTS the stored one (the D2 spiking surprise window is OPEN), the stored fact is UPDATED IN PLACE (no contradictory duplicate) instead of the append-only stale-first; moat-safe, lesion-load-bearing"
    de_risked: "YES"            # reconsolidation_production_organ verify GO (rf 6/6 + onebrain 3/3; window 5.61 Hz open on contradict vs 0 Hz re-statement; belief revision 100% attributable; flag-off byte-identical append-only). Nader-Schafe-LeDoux; PE-necessity (Sevenster-Beckers-Kindt)
    wired: "YES"                # 2026-08-12 Gate-B/B4: /api/brain-chat runs reconsolidation INSIDE the D2 surprise block (reusing its ONE cp_firing_states[surprise] read — zero extra spiking reads) when the asserted patient != the stored one; the in-place rewrite reuses the composer's OWN update_on_mismatch (rf + onebrain). research/runners/reconsolidation_production_organ.py, reuse-by-import. Verified numpy-CPU: organ verify GO + real handler (INTACT rewrite dog-go north→south, ONE fact; lesion→stale; flag-off append-only).
    on_by_default: "YES"        # default-ON; BRAIN_RECONSOLIDATION=0 → the update path is never invoked, the store stays APPEND-ONLY (byte-identical). Runs strictly AFTER D2 surprise and ONLY when sj.surprised; only PREPENDS an honest notice (order: worldmodel, surprise, reconsolidation).
    scaffold_retired: "NO"      # declared residuals: reactivation SELECTS the fact by the same host kb cue-match recall performs (the composer-as-idealization layer ALL recall carries; rides the one-brain merge); the synaptic-literal engram tag-and-capture + a cupy production-scale full-6-seed onebrain sweep are the next rungs. INTEGRATION FIX (2026-08-12): the in-place rewrite now RECRUITS a runtime-novel corrected patient (composer._recruit_word, the exact _store_fact pattern) BEFORE the rewrite — without it update_on_mismatch→_patient_prediction_error KeyErrored on an un-coded word ('actually south' when only 'north' was taught) and fell back to append-only.
    lesion_note: "LESION-LOAD-BEARING (2026-08-12): BRAIN_RECONSOLIDATION_LESION=1 fires the window but BLOCKS the in-place update (action='lesioned_nowrite') → append-only fallback → recall returns the STALE fact (north persists). The corrected answer (south) is exactly the part the in-place update produced; the belief revision is 100% attributable to the update (treatment 1.0 vs lesion control 0.0), on BOTH composers."
    evidence: "webapp/server.py brain_chat: INSIDE the D2 surprise block (after surprise_prefix, inside if p_stored), reconsolidation_production_organ.reconsolidate(chat.inner.composer, a,v, p_stored, p_asserted, sj=surprise_info) when sj.surprised and p_asserted!=p_stored → on action=='rewrite', PREPEND reconsolidation_notice. research/runners/reconsolidation_production_organ.py reuses the D2 SurpriseProductionOrgan window + the composer's update_on_mismatch; _inplace_rewrite recruits the novel patient first. Verified numpy-CPU: _reconsolidation_production_organ_verify GO (rf 6/6, onebrain 3/3) + real handler htestA/htestC (INTACT novel-patient rewrite north→south ONE fact no dup; lesion lesioned_nowrite stale persists; flag-off append-only). finding 2026-08-12-reconsolidation-production-organ-belief-revision-wireable"

  - key: self-model-reward-residual
    faculty: "Self-model / reward (residual of the old curiosity/self-model/reward bundle — curiosity split to its own row above; self-model/metacognition is the E1 metacog-monitor row above)"
    de_risked: YES              # Phase-0 6-seed GOs (self-schema); reward batteries GO
    wired: PARTIAL              # curiosity WIRED (row above); self-model = the E1 metacog hedge (row above); the standalone REWARD/value signal on the live turn remains un-wired
    on_by_default: NO
    scaffold_retired: NO
    host_scaffold_in_default: "no standalone reward/value neuromodulator drives the live chat turn (curiosity now via curiosity_production_organ; self-model via metacog_production_organ); a live-turn reward/value signal is un-wired"
    evidence: "grep of the chat modules: curiosity + metacog are now wired; no standalone reward/value reference drives the turn"

  - key: perception-motor
    faculty: "Perception / motor (sensory encoding, motor pools)"
    de_risked: PARTIAL          # nav/perception de-risks; not part of the text-chat turn
    wired: NO
    on_by_default: NO
    scaffold_retired: NO
    host_scaffold_in_default: "not exercised by the text chat turn (world/body faculties; legitimate host I/O boundary aside)"
    evidence: "text chat has no perception/motor path"

  - key: gnw-bus-shadow
    faculty: "Organ-COMBINATION by the substrate (GNW N-organ ignition bus) vs host Python — the co-resident organs in brain_chat are fused by an `if recalled == p`; the bus makes the substrate do the combining. PROMOTED from SHADOW to the DEFAULT combination 2026-08-13 (key kept as a stable id)."
    de_risked: YES              # _gnw_norgan_bus_derisk 6/6 GO (the substrate combines N>=3 subthreshold organ reads via consensus-ignition + WTA + re-entry; EVERY organ load-bearing, moat abstains); finding 2026-08-13-gnw-norgan-ignition-bus-substrate-combines-N-organ-reads
    wired: YES                  # 2026-08-13 FLIP (SHADOW -> DEFAULT): webapp/server.py brain_chat installs webapp/gnw_bus_shadow.install_bus_gate on the ChatBrain -> the wrapper re-authors the organ-COMBINATION verdict through the substrate ignition bus (consensus + WTA). Covers BOTH the single-fact path AND the default rich path (both funnel their direct recall through chat.gate). The ignited patient IS the answer; no ignition IS the abstain (the moat as a substrate property). The host `if recalled == p` verdict is OVERRIDDEN.
    on_by_default: YES          # DEFAULT-ON: the substrate authors the combination on every routable turn unless BRAIN_GNW_BUS_HOST=1 (escape -> the original host gate(), byte-identical to pre-flip production). BRAIN_GNW_BUS_LESION=1 is the honest-negative lever (collapses the answer). BRAIN_GNW_BUS is now ONLY the observability flag (attach the gnw_bus debug block; default-OFF -> no key -> byte-identical response). Earned on a broad byte-identical panel (research/runners/_gnw_bus_default_flip_verify.py: 22/22 covered classes).
    scaffold_retired: NO        # the host gate() CODE still executes (the wrapper calls it for the (agent, action) EXTRACTION + the acquisition/anaphora/open-ended side effects; its `if recalled == p` combination result is COMPUTED-then-OVERRIDDEN, not removed). Full retirement = a ChatBrain.gate_via_bus that never computes the host combination (the named follow-on, design §3). So the DECISION is the substrate's by default, but the host combination code is not yet deleted.
    host_scaffold_in_default: "ChatBrain.gate() still RUNS host Python for the (agent, action) extraction + its own (now-overridden) `if recalled == p` combination; the substrate ignition bus AUTHORS the combination verdict by default, but the host combination code is not yet deleted (gate_via_bus follow-on)"
    scaffold_symbol:
      file: research/runners/brain_chat_tui.py
      symbol: "def gate"
    lesion_note: "LESION-LOAD-BEARING on the ANSWER (2026-08-13 flip, production composer): with the bus authoring by default, BRAIN_GNW_BUS_LESION=1 (bus_combine lesion=True) zeroes the workspace assembly self-recurrence -> the consensus cannot ignite -> the COMBINED ANSWER collapses to abstain (intact commits the patient), WHILE the forward-recall reflex (composer.query_patient, never routed through the workspace) still returns it — the dissociation proving the SUBSTRATE, not a residual host read, does the combination. Attribution to the workspace ignition = 1.0."
    evidence: "webapp/server.py brain_chat: `from webapp import gnw_bus_shadow; install_bus_gate(chat)` wraps chat.gate so the SUBSTRATE authors the organ-combination by default (both the single-fact + rich paths call chat.gate). webapp/gnw_bus_shadow.py bus_authored_svo/install_bus_gate reuse-by-import of build_workspace_bridge/_ignite_and_read/norgan_hop/D_SUB_UNANIMITY/_pick_decoy (NO sim/ edit); the read-only organ probes save+restore composer.last_trace so `activity` stays byte-identical. Verified numpy-CPU through the REAL production ChatBrain + handler: _gnw_bus_default_flip_verify GO — BYTE-IDENTICAL bus-vs-host 22/22 across stored/self/unstored/inconsistent/open-ended/acquisition/anaphora; MOAT holds; LESION collapses the ANSWER + reflex survives (attribution 1.0); ESCAPE BRAIN_GNW_BUS_HOST=1 byte-identical to the bus default through the real handler. The shadow audit (_gnw_bus_shadow_production_verify) still GO 9/9. finding 2026-08-13-gnw-bus-default-flip-substrate-authors-organ-combination"

  - key: gnw-two-organ-bus
    faculty: "Two GENUINELY-DISTINCT organs combined by ignition (composer recall + the production SurpriseProductionOrgan, a spiking predictive-coding mismatch monitor) — closing caveat #1 of the N-organ bus (whose three reads all came from the composer). A covered routable recall is committed only when recall AND the surprise-check AGREE (coincidence-ignition); conflict -> consensus-veto -> abstain. PROMOTED to DEFAULT-ON 2026-08-20."
    de_risked: YES              # _gnw_two_distinct_organs_derisk 6/6 GO (finding 2026-08-20-gnw-workspace-integrates-two-genuinely-distinct-organs-6seed-GO); organ B a real spiking read discriminating ~0.13 Hz agree vs ~2.74 Hz conflict; every anti-cheat 0.0
    wired: YES                  # webapp/gnw_two_organ_bus.py install_two_organ_gate on the ChatBrain, hooked in webapp/server.py brain_chat; backend-aware gate (_organ_discriminates): numpy always + cupy when backend_neutral_izh_initialization on (default), else safe inert byte-identical fallback. NO sim/ edit (reuse-by-import of the de-risk workspace machinery).
    on_by_default: YES          # DEFAULT-ON 2026-08-20 (BRAIN_GNW_2ORGAN default '' -> 'on'; escape =0/off byte-identical to the pre-bus path). Promotion gate GREEN both backends: production wired end-to-end cupy+numpy GO (agree->commit, lesion->abstain, discrimination 0.434<<1.401<<2.488 Hz), real server handler HTTP 200 off+on, byte-identity-off, no-regression 263/57/2 identical off-vs-on. Unblocked on cupy by backend_neutral_izh_initialization (the surprise organ's thresholds were backend-RNG-dependent). Levers BRAIN_GNW_2ORGAN_WS_LESION / _ORGANB_LESION. finding 2026-08-20-gnw-two-organ-bus-DEFAULT-ON-in-production-both-backends
    scaffold_retired: NO        # layers the genuinely-distinct SECOND organ ONTO the N-organ bus (gnw-bus-shadow); the host gate() extraction + side-effects still run once (mirrors gate_via_bus). Full retirement shares gnw-bus-shadow's follow-on.
    host_scaffold_in_default: "ChatBrain.gate() still runs host Python for the (agent, action) extraction + side effects; the two-organ coincidence-ignition authors the covered-recall combination verdict by default"
    scaffold_symbol:
      file: webapp/gnw_two_organ_bus.py
      symbol: "def install_two_organ_gate"
    lesion_note: "LESION-LOAD-BEARING both backends: BRAIN_GNW_2ORGAN_ORGANB_LESION=1 (zero the surprise organ's patient_expected->surprise prediction edges) or _WS_LESION=1 (zero the workspace self-recurrence) -> the coincidence cannot ignite -> the covered answer collapses to abstain WHILE the forward-recall reflex still returns the patient (dissociation). cupy 5/5 + numpy 5/5."
    evidence: "webapp/server.py brain_chat installs webapp/gnw_two_organ_bus.install_two_organ_gate when two_organ_enabled() (default-ON). Verified through the REAL ChatBrain.gate on BOTH backends (research/runners/_gnw_two_organ_production_verify.py, banked in a follow-up once it emits a tools.verdict preconditions block + attributable_to): cupy GO 5/5 commit + lesion abstain + discrimination 0.434<<1.401<<2.488 Hz; numpy GO; real server handler HTTP 200 bus-off AND bus-on; byte-identity-when-off both backends; no-regression 263/57/2 identical off-vs-forced-on. finding 2026-08-20-gnw-two-organ-bus-DEFAULT-ON-in-production-both-backends"

  - key: gnw-three-organ-bus
    faculty: "A THIRD genuinely-distinct organ on the GNW ignition bus (2026-08-21): organ C = the 6/6-GO D4 COMPREHENSION monitor (a Wong-Wang SpikingRoleCompetition sel-pool WTA read off cp_firing_states), added to organ A (composer recall) + organ B (the SurpriseProductionOrgan). The workspace ignites — the brain commits a covered routable recall — ONLY when it RECALLS it AND is NOT surprised by it AND COMPREHENDED the recalled proposition (Q=3 UNANIMITY consensus). A recall the brain retrieved + was not surprised by, but whose thematic ROLES its own substrate could not resolve (role-ambiguous), is VETOED -> abstain — a consensus-veto the 2-organ bus could not make. DEFAULT-OFF (review gate)."
    de_risked: YES              # composes THREE already-6-seed-GO in-repo organs via the already-6/6-GO N-organ consensus hop: D4 comprehension (AUC 1.000, synaptic lesion 0.500; finding 2026-08-12-comprehension-production-monitor-wired-into-gate-b), the N-organ ignition bus (6/6 GO, finding 2026-08-13-gnw-norgan-ignition-bus-substrate-combines-N-organ-reads), the 2-organ bus (6/6 GO). Production-wiring verify GO (research/runners/_gnw_three_organ_bus_verify.py, seed 42): organ C's cp_firing_states margin separates HIGH (>=0.338) from LOW (<=0.138) around thr 0.249; Q=3 window on the shared workspace bridge (2 votes subthreshold, 3 votes supra); low-comprehension abstain 100% attributable to organ C's veto (intact vs organ-C-lesion). finding 2026-08-21-gnw-three-organ-bus-d4-comprehension-GO
    wired: YES                  # webapp/gnw_three_organ_bus.py install_three_organ_gate on the ChatBrain, hooked in webapp/server.py brain_chat behind the DEFAULT-OFF BRAIN_GNW_3ORGAN flag (unset -> the block is skipped, the module is never imported, byte-identical to the 2-organ-bus production path). Backend-aware (reuses the 2-organ bus's _organ_discriminates gate). Guarded so a wiring failure degrades to the wrapped gate. NO sim/ edit (reuse-by-import of gnw_two_organ_bus + _gnw_norgan_bus_derisk.norgan_hop + comprehension_production_organ).
    on_by_default: YES          # FLIPPED default-ON 2026-08-21. The flip-blocking over-veto is FIXED: organ C's D4 comprehension monitor now reads real-vocab entity/role competence — a recalled fact whose agent/action/patient-head are all in the brain's OWN learned vocabulary CORROBORATES (known recall = comprehended, no veto), and only genuine out-of-vocab non-comprehension consults the spiking D4 SpikingRoleCompetition WTA and vetoes (lesion-severable). Composed re-verify GO: research/runners/_wave4_composed_flip_noregression.py isolating BRAIN_GNW_3ORGAN over the 8-turn out-of-scope panel -> n_diverged=0 (the dog-chase/cat-eat recalls no longer abstain), while genuine OOV still vetoes. BRAIN_GNW_3ORGAN=0 is the byte-identical escape (delegates to the 2-organ bus); the install is fail-safe + now logs on failure (no silent no-install for a default-on faculty). fix code webapp/gnw_three_organ_bus.py (real-vocab _comprehension_vote), NO sim/ edit. finding 2026-08-21-gnw-three-organ-realvocab-flip-GO. artifact research/findings/raw/_gnw_realvocab_reverify/composed_noregression.json.
    scaffold_retired: NO        # layers the genuinely-distinct THIRD organ ONTO the 2-organ bus (gnw-two-organ-bus) + the N-organ bus (gnw-bus-shadow); the host gate() extraction + side-effects still run once (mirrors gate_via_bus / two_organ_gate_via). Full retirement shares gnw-bus-shadow's follow-on.
    host_scaffold_in_default: "DEFAULT-OFF, so nothing runs in the default path (the 2-organ bus authors the combine, byte-identical); when ON, ChatBrain.gate() still runs host Python for the (agent, action) extraction + side effects, the 3-way consensus-ignition authors the covered-recall verdict, and organ C's competence gate is the declared toy-cue-lexicon boundary"
    scaffold_symbol:
      file: webapp/gnw_three_organ_bus.py
      symbol: "def install_three_organ_gate"
    lesion_note: "LESION-LOAD-BEARING: BRAIN_GNW_3ORGAN_ORGANC_LESION=1 silences organ C's veto (it corroborates unconditionally, its cp_firing_states read bypassed) -> the Q=3 consensus reduces to organ A + organ B (the 2-organ decision) -> the low-comprehension abstain (dog-chase-cat / cat-eat-fish) REVERTS to the 2-organ commit (n_votes 2 -> 3). The veto is attributed to organ C's active spiking participation, not a host if margin<x (a host branch would still fire when the organ is lesioned). The veto's SELECTIVITY is caused by the learned cue->role spiking competition (the D4 synaptic lesion collapses the well/ill AUC 1.000 -> 0.500; host cue VALUES byte-identical)."
    evidence: "webapp/server.py brain_chat: `if BRAIN_GNW_3ORGAN truthy: from webapp import gnw_three_organ_bus; install_three_organ_gate(chat)` after install_two_organ_gate (default-OFF -> skipped). webapp/gnw_three_organ_bus.py three_organ_combine/three_organ_gate_via/install_three_organ_gate: organ A = composer.query_patient; organ B = organ_b_confirms (cp_firing_states[surprise]); organ C = ComprehensionProductionOrgan.read_margin (cp_firing_states sel-pool WTA) on the reconstructed (agent,action,cand) transitive, gated by competent(); norgan_hop with 3 candidates at D_SUB_UNANIMITY[3]. reuse-by-import (NO sim/ edit). Verified numpy-CPU through the REAL production ChatBrain.gate (research/runners/_gnw_three_organ_bus_verify.py, GO): (A) OFF byte-identical 10/10; (B) high-comp/deferred stored query 3-organ==2-organ, low-comp probe (dog-chase-cat margin 0.108 / cat-eat-fish margin 0.138 < thr 0.249) -> 3-organ ABSTAINS while 2-organ commits; (C) ORGANC_LESION reverts both to commit; (D) moat holds (fish-fly / cat-chase abstain both arms). finding 2026-08-21-gnw-three-organ-bus-d4-comprehension-GO"

  - key: causal-whatif
    faculty: "Causal WHY / WHAT-IF forward model (T1-4) — a real 'what happens if <agent> <action>?' (forward-SIMULATION of an unseen consequence: the substrate rolls dog-go-east -> reach-river -> drink-water though dog->drink was NEVER taught) and 'why did <agent> <action>?' (the directed cause that survives a Pearl DO-probe: dog-wake reads sun-rise, never the correlate bird-sing) — the reasoning rung a host triple-JOIN cannot serve; moat-safe (every surfaced fact composer.query_patient-confirmed), lesion-load-bearing"
    de_risked: YES              # grounded causal forward model 6/6 GO (real-fact why/what-if over the production RF-VSA composer; edge + grounding lesions load-bearing; DO-prune 106% attributable). finding 2026-08-13-grounded-causal-forward-model-real-fact-why-whatif-6seed-GO
    wired: YES                  # 2026-08-13 T1-4: webapp/server.py brain_chat runs a DISJOINT why-did/what-happens-if turn class (after affect/episodic/worldmodel/multiref/discourse, before comprehension/surprise/rich) routed through the co-resident spiking forward model (research/runners/causal_whatif_production_organ.py, reuse-by-import — NO sim/ edit). Verified numpy-CPU through the REAL handler.
    on_by_default: YES          # default-ON; BRAIN_CAUSAL=0 -> the block is fully skipped (byte-identical oracle). Additive + moat-safe: the consequence/cause is emitted ONLY when composer.query_patient CONFIRMS it; an unconfirmed/unmapped causal query ABSTAINS to the honest _honest_causal_answer disclaimer (0 confab); a causal query about an UNKNOWN agent falls through unchanged.
    scaffold_retired: NO        # declared residuals (named next rungs): (a) GROUNDING-BY-DERIVATION not shared-substrate-merge — the events are DERIVED from + gated by the LIVE composer's moat recall (read-only), but the composer's unbind SPIKES do not yet directly DRIVE the forward-model event blocks in ONE merged bridge (co-resident forward-model bridge, rides the one-brain merge #1); (b) the DA sign + causal episode ORDER are teacher-delivered (a spiking-mismatch DA is the next rung); (c) FIRST-ORDER + the canonical CHAIN/CONFOUND causal STRUCTURE is teacher-rendered (wired scope = the validated chain-source what-if + confound why). The honest causal disclaimer remains the abstain fallback.
    host_scaffold_in_default: "the forward-model event blocks are grounded by DERIVATION (gated by the live composer's moat recall) not by a shared-substrate spike-merge; the DA sign + causal episode order are teacher-delivered; the canonical causal STRUCTURE is teacher-rendered; the load-bearing spiking parts are the directed forward-simulation (cp_firing_states rollout) + the DO-probe cause read; the organ runs on its own co-resident forward-model bridge (not merged with the recall composer's bridge)"
    lesion_note: "LESION-LOAD-BEARING (2026-08-13): BRAIN_CAUSAL_LESION=1 zeroes the learned cross-block forward edges -> the forward-simulation cannot roll A->B->D (what-if collapses) and the DO-probe predecessor of Y is no longer C (why collapses) -> BOTH why/what-if fall to the honest _honest_causal_answer abstain. The answers are caused by the learned SPIKING forward edges, not the host drive."
    evidence: "webapp/server.py brain_chat: causal_whatif_production_organ.is_causal_query(msg) + extract_cue -> on a known-agent why/what-if, corg.what_if/why(chat.inner.composer) short-circuits with the moat-confirmed answer, else _honest_causal_answer abstain (INTEGRATION #5). research/runners/causal_whatif_production_organ.py reuses _causal_forward_model_grounded_derisk (enumerate_events/why_cause/_recalled) + _causal_forward_model_derisk (build_forward_model/train/unseen_consequence/do_intervention/_lesion_xblock). Verified numpy-CPU through the REAL production ChatBrain + handler: _causal_whatif_production_organ_verify GO — WHAT-IF moat-confirmed consequence (dog->drink water); WHY DO-surviving cause (sun rose not bird sang); unmapped + grounding-unconfirmed abstains (0 confab); LESION collapse; byte-identical-when-off (recall/abstain panel flag on==off + causal query flag-off no key) + canonical brain_chat_tui --smoke byte-identical (server.py stashed vs present). finding 2026-08-13-causal-whatif-production-organ-wired-into-brain-chat"

  - key: prospective-memory
    faculty: "Prospective memory (Gate-B, 2026-08-13) — a spiking intention-LATCH + BA10 NMDA-plateau cue-MONITOR that HOLDS a deferred intention ('remind me to X when Y') across intervening turns and RELEASES it only when the cue appears. A FORMATION turn LATCHES the intention (a self-sustaining cortex<->dlpfc attractor) + a disjoint acknowledgement; on later turns the held x cue coincidence is READ off cp_firing_states (rel >= FIRE_THR) and, on a fire, the reminder is PREPENDED to the normal turn (which still answers what the user said). Moat-safe (a formation ack / a reminder prefix — never a fabricated fact or an abstain flip); lesion-load-bearing"
    de_risked: "YES"            # 2026-08-13 fire_on_cue CLOSES 6/6 (finding 2026-08-13-prospective-sfa-nmda-amplifier-GO): the intention-latch + cue-monitor arc is a full 6/6 (persistence + cue-specificity + wrong-cue + no-intention + lesion silence all 6/6), the release amplitude closed by a per-pool intrinsic-plasticity homeostat + a supralinear POOL-GATED NMDA/dendritic-plateau COINCIDENCE amplifier (the plateau owns the closure; SFA not load-bearing). Runners _pmem_intention_latch_derisk / _pmem_perpool_homeostat_derisk / _pmem_sfa_nmda_amplifier_derisk.
    wired: "YES"                # 2026-08-13 Gate-B: /api/brain-chat HOLDS the intention across turns on a PER-SESSION co-resident spiking latch bridge (research/runners/prospective_memory_production_organ.py, reuse-by-import of the GO SFANmdaProspectiveMemory; NO sim/ edit) and reads the held x cue coincidence off cp_firing_states each subsequent turn; a FORMATION turn short-circuits with an ack, a cue FIRE prepends the reminder (both rich + single-fact paths). Verified numpy-CPU through the REAL handler: _prospective_memory_production_verify GO (A fires-on-cue + silent-before, B wrong-cue + spiking specificity, C lesion collapse, D byte-identical-off).
    on_by_default: "YES"        # default-ON; BRAIN_PMEM=0 -> the whole block is skipped + no `prospective` key (byte-identical oracle). Additive + moat-safe: a FORMATION turn is a disjoint acknowledgement class; a cue FIRE only PREPENDS an honest reminder to the normal turn (never manufactures a fact or flips an abstain). A turn with no held intention is byte-identical. The intention is CONSUMED only on DELIVERY on a main path (a cue turn that hits a disjoint short-circuit keeps it held to fire on the next main-path cue mention).
    scaffold_retired: "PARTIAL"  # 2026-08-13 the cue->action CONTENT-binding INSTALL is RETIRED (residual #1 CLOSED): the binding is now LEARNED via a ONE-SHOT HEBBIAN potentiation at intention-formation (Gollwitzer implementation-intention; default-ON, BRAIN_PMEM_HEBBIAN=0 escape to the install) — absent-before, ~canonical after, load-bearing on the formation event. REMAINING declared residuals (PARTIAL, like content-selection): (2) the intention/cue TEXT->slot mapping + the cue-PRESENCE detection are a host language/sensory boundary (a legitimate host I/O boundary, like the surprise organ's assertion extraction + curiosity's wh-frame) + the formation goal-activation drive to rel_X + the developmental operating-point calibration; (3) CO-RESIDENT on its own latch bridge (rides the one-brain merge, burn-down #1); (4) engine-native STDP realization of the same local rule is the further step. The build-time synaptic install scaffold is GONE from the default path.
    host_scaffold_in_default: "the cue->action CONTENT binding is now LEARNED one-shot at formation (the build-time synaptic install is RETIRED); the intention/cue TEXT->slot mapping + the cue-presence detection + the formation goal-activation drive + the operating-point calibration are declared host language/sensory/developmental boundaries; the HOLD (attractor persistence) + the cue-monitoring (NMDA-plateau coincidence) + the RELEASE (accumulator crossing threshold) + the cue->action BINDING are the load-bearing spiking part; the organ runs on its own co-resident latch bridge (not merged with the recall composer's bridge)"
    lesion_note: "LESION-LOAD-BEARING (2026-08-13): BRAIN_PMEM_LESION=1 zeroes the intention's attractor edges at formation (lesion_latch -> held_after_lesion=0.0) -> the held assembly collapses -> the SAME cue does NOT fire -> NO reminder. Intact fires (rel>=FIRE_THR) vs lesion silent -> the fire is caused by the SPIKING latch (the held x cue coincidence), not the host cue-match (which the lesion leaves unchanged). The read is on cp_firing_states."
    evidence: "webapp/server.py brain_chat: after the AFFECT block, prospective_memory_production_organ.parse_intention(msg) -> on a formation, _get_pmem_organ(cache_key).form_intention(..., hebbian_lesion=pmem_hebbian_lesioned()) + a disjoint acknowledgement short-circuit; else on a held intention, porg.read_turn(msg) reads the held x cue coincidence and on a fire sets pmem_prefix (prepended in both the rich + single-fact paths, consumed on delivery). research/runners/prospective_memory_production_organ.py: _ensure_pm builds the de-risked HebbianBindingProspectiveMemory (default; reuse-by-import of _pmem_hebbian_binding_derisk) so form_intention LEARNS the cue->action binding one-shot at formation (form_intention_hebbian); BRAIN_PMEM_HEBBIAN=0 -> SFANmdaProspectiveMemory install (byte-identical); BRAIN_PMEM_HEBBIAN_LESION=1 -> latch without the event (binding absent -> no fire). Verified numpy-CPU through the REAL production ChatBrain + handler: _prospective_memory_production_verify GO — (A) formation ack + 3 intervening turns held 0.3438 silent + the cue FIRES rel>=0.20 reminder prepended WITH the binding LEARNED; (B) wrong-cue silent + rel_A=0.0 slot-B cue; (C) BRAIN_PMEM_LESION=1 held_after_lesion=0.0 no fire; (D) byte-identical-when-off 4-turn recall/abstain panel no key; (E) LEARNED binding |w| before=0.0 -> 17760.0 (~canonical) after, cue fires; BINDING lesion silent; BRAIN_PMEM_HEBBIAN=0 install escape fires. findings 2026-08-13-prospective-memory-production-wired + 2026-08-13-prospective-hebbian-binding-GO"

  - key: pragmatic-implicature
    faculty: "Scalar-implicature pragmatic belief (Task-#12, D-pragmatics, 2026-08-13) — the W4 depth-2 RSA GRADED-implicature listener-belief wired as the production BELIEF SOURCE for a scalar-quantity turn class. On a scalar-quantity claim/probe ('I ate some of the cookies', 'not all of them') the brain FORMS a graded belief over the interpretations {none, SBNA, all} for the utterance ('some' +> 'some but not all', SBNA ~0.73 preferred, 'all' left ~0.27-possible) and PREPENDS an honest functional pragmatic reading — replacing the leg2_v2 WTA ONE-HOT [0,1,0] that falsely rules 'all' out. SCOPED (the pipeline had NO pragmatic slot before this); moat-safe (only prepends a reading; never a fabricated fact or an abstain flip); lesion-load-bearing"
    de_risked: "YES"            # 2026-08-13 the W4 pragmatic arc CLOSES 6/6 (finding 2026-08-13-w4-detector-operating-point-homeostat-GO): the graded RSA belief beats the one-hot on the informativeness objective 6/6 (belief-magnitude read + informativeness objective + detector operating-point homeostat). Belief source verified faithful (calibration ~12x better than one-hot, collapses under the FS normalization-lesion) in _pragmatic_graded_belief_source_derisk.
    wired: "YES"                # 2026-08-13 Task-#12: /api/brain-chat FORMS the graded belief on a scalar-quantity turn via research/runners/pragmatic_production_organ.py (reuse-by-import of graded_belief_sources / onehot_belief_sources; the spiking Izhikevich RSA substrate, plasticity off, built once + frozen; NO sim/ edit) and prepends the reading (both rich + single-fact paths). Verified numpy-CPU through the REAL handler: _w4_pragmatic_belief_production_verify GO (A default graded belief source, B byte-identical-off, C normalization-lesion collapse, D additive/moat-safe).
    on_by_default: "YES"        # default-ON; BRAIN_PRAGMATIC=0 -> the whole block is skipped + no `pragmatic` key (byte-identical oracle). Additive + moat-safe: fires ONLY on a scalar-quantity context (partitive '<scalar> of' / 'not all' / a some-vs-all probe); a casual 'some' filler is out of scope; it only PREPENDS a reading, never manufactures a fact or flips an abstain.
    scaffold_retired: "NO"      # declared residuals (the named next rungs): (1) NO general pragmatic comprehension front-end — this is one LEXICAL scalar family {none,some,all} in a partitive/probe context; embedded/downward-entailing environments, non-lexical scalars, and the Q-under-discussion are the mapped gap; (2) the surface-scalar->RSA-utterance mapping + the scalar-context detection are a host language/sensory boundary (like the surprise organ's assertion extraction); (3) the graded belief is a BUILD-TIME spiking read at a fixed operating point cached per process (plasticity off, as the W4 GO specifies) — a live per-turn re-read is identical (no learning); (4) CO-RESIDENT on its own RSA bridge (rides the one-brain merge, burn-down #1).
    host_scaffold_in_default: "NO prior host pragmatic pipeline existed to retire (the pipeline formed no belief over interpretations); the graded BELIEF is the substrate's spiking RSA read (FS divisive normalization; collapses under lesion) — the residual host boundary is the scalar-term detection + surface->utterance mapping + the (build-time-cached) frozen operating point; a general pragmatic front-end that lets this belief drive arbitrary pragmatic responses is the named gap"
    lesion_note: "LESION-LOAD-BEARING (2026-08-13): BRAIN_PRAGMATIC_LESION=1 uses the normalization-lesion belief (RSA_FS_EXC_W=0) -> belief('some') collapses from the graded [0,~0.73,~0.27] to flat [0,0.5,0.5] -> the implicature margin falls from ~0.46 to ~0 -> the reading is SUPPRESSED (no pragmatic notice). The graded implicature content is caused by the substrate's FS divisive normalization, not host-injected; the belief is a substrate read, the one-hot baseline (which rules 'all' out) is the leg2_v2 WTA comparison arm."
    evidence: "webapp/server.py brain_chat: after the COMPREHENSION block, pragmatic_production_organ.pragmatic_enabled() gates a _get_pragmatic_organ().judge_text(msg, lesion=pragmatic_lesioned()) read; on a detected scalar-quantity turn it sets pragmatic_info + pragmatic_prefix (the honest functional reading), attached as the `pragmatic` block + prepended in both the rich + single-fact paths. research/runners/pragmatic_production_organ.py reuses graded_belief_sources/onehot_belief_sources from _pragmatic_graded_belief_source_derisk (the W4 faithful graded RSA posterior + the leg2_v2 one-hot baseline). Verified numpy-CPU through the REAL production ChatBrain + handler: _w4_pragmatic_belief_production_verify GO — (A) 'some' -> graded [0,0.73,0.27], implicature represented (margin 0.46), better-calibrated than one-hot, residual 'all' 0.27 retained (one-hot 0.0), reading on the surface; (B) byte-identical-when-off 4-turn non-scalar panel no key + 'some' filler out-of-scope; (C) BRAIN_PRAGMATIC_LESION=1 flat [0,0.5,0.5] margin 0 reading suppressed; (D) additive — the scalar turn's recall (abstained/recalled_svo/verified) matches flag-off, only the reading + block differ. finding 2026-08-13-w4-pragmatic-belief-production-scoped"

  - key: self-initiated-utterance
    faculty: "Self-initiated utterance (2026-08-18) — the first INTERNALLY-GENERATED turn class. On an IDLE/EMPTY turn (empty message, or a bare 'say something / what's on your mind' lead-in) the brain SELECTS a stored concept ITSELF (a noise-seeded, curiosity-biased CA3 wander — the multibasin self-initiation GO, 66% attributable — on cupy; the mouth's curiosity-top decodable concept on the numpy-deferred path) and SPEAKS it through the OneBrainComposer mouth as a self-initiated remark/question. Internally-SELECTED content on an idle-turn trigger (NOT fully autonomous proactive speech); moat-safe (speaks ONLY a stored concept; an emptied store abstains); lesion-load-bearing"
    de_risked: YES              # loop-closing 6-seed GO (2026-08-13-self-initiated-utterance-GO): the noise CA3 wander SELECTS a curiosity-biased basin (multibasin GO 2026-08-13-self-initiation-multibasin-GO, 66% attributable) routed through the OneBrainComposer mouth -> a coherent SVO utterance ABOUT it; about-selected 1.00 vs SCRAMBLE 0.00, NO-NOISE 0, STORE-LESION 0 every seed. Runner _self_initiated_utterance_derisk.py.
    wired: "YES"                # 2026-08-18: /api/brain-chat brain_chat runs a DISJOINT idle/empty-turn class (before the empty-422) short-circuiting to self_initiated_production_organ.speak() -> the self-initiated remark/question; PER-SESSION organ (_SESSION_SELFINIT, cleared on reset). research/runners/self_initiated_production_organ.py, reuse-by-import (NO sim/ edit). Verified numpy-CPU through the REAL handler.
    on_by_default: "YES"        # default-ON; BRAIN_SELF_INITIATE=0 -> the idle block is fully skipped (empty->422, 'say something'->normal path; byte-identical). CONVERSATION-SCOPED. The idle turn is a DISJOINT class; every reactive (non-idle) turn is byte-identical.
    scaffold_retired: "NO"      # declared residuals: (a) LATENCY — the heavy n_ca3=2000 CA3 wander is ~seconds on cupy but minutes on numpy@2000, so on numpy it is DEFERRED (the light path speaks the mouth's curiosity-top decodable concept; BRAIN_SELF_INITIATE_STORE=1 forces the full wander) — the SAME residual d5-episodic declares for its BTSP write; (b) the basin<->lexical-concept BINDING + each concept's stored FACT are the learned store/ENVIRONMENT; (c) the curiosity want->recurrent-gain PROJECTION is a host neuromodulatory scaling (the one-brain-merge rung); (d) the QUESTION/REMARK-template wrapper + any natural-language FLUENCY are the Broca/Qwen articulation scaffold; (e) the TIMING is still HTTP-triggered — a truly proactive NO-HTTP idle-tick is the named deferred rung; (f) CO-RESIDENT on its own selection + mouth bridges (rides burn-down #1).
    host_scaffold_in_default: "on numpy the heavy CA3 selection wander is DEFERRED (the light path speaks the mouth's curiosity-top decodable concept; the full curiosity-biased CA3 competition runs on cupy / BRAIN_SELF_INITIATE_STORE=1); the basin<->concept binding + stored facts are the environment; the curiosity want->gain projection is a host neuromodulatory scaling; the remark/question template + fluency are the Broca/Qwen scaffold; the TIMING is HTTP-triggered (the proactive idle-tick is deferred). The load-bearing spiking part is the OneBrainComposer mouth's on-bridge RF decode (render_fact) + the CA3-selection wander (cupy)."
    default_anchor:
      - file: webapp/server.py
        assign: _SELF_INITIATE_DEFAULT_ON
        off_value: "False"
        on_value: "True"
        count: 1
    lesion_note: "LESION-LOAD-BEARING (2026-08-18): BRAIN_SELF_INITIATE_LESION=1 is the store NO-ENCODE control (an emptied RF store, not a host flag) -> render_fact abstains for every concept -> the utterance stream collapses (n_utt intact 4 -> lesion 0 every seed) -> the honest neutral idle fallback. The mouth's on-bridge RF decode is the load-bearing spiking read tested live on numpy; the full CA3 selection wander's NO-ENCODE collapse (member->chance, 0 utterances every seed) is the committed cupy 6-seed GO."
    coverage_note: "ALL-N-BASINS COVERAGE CLOSED (2026-08-18): the finding's declared 3-of-4-basins residual (the LAST-encoded CA3 basin never ignited, so a self-initiated turn could never select the tail concept) is CLOSED by folding the DMN per-basin encode-EQUALIZATION GO (2026-08-17-dmn-per-basin-encode-equalization) into the organ's cupy store WRITE: a post-encode CONSOLIDATION settle (the substrate's OWN zero-input BTSP; selfinit_consolidate() default-ON; BRAIN_SELF_INITIATE_CONSOLIDATE=0 reverts) converts the last basin's slow eligibility trace like every other basin -> ALL N=4 disjoint basins ignite and the previously-dead TAIL concept is now reliably self-initiable (coverage 4/4 on 6/6 seeds vs the pre-integration 3/4). Verified through the production organ (research/runners/_dmn_consolidated_selfinit_verify PASS/GO): consolidated 4/4 6/6 (tail spoken every seed) vs consolidation-off full coverage only 2/6 (utterance magnitude 82% attributable, n_utt 2206 vs 388) + store NO-ENCODE lesion collapse 6/6; basins disjoint (overlap 0), recall byte-frozen, reactive panel byte-identical (the consolidation is a cupy WRITE-path property only). NO sim/ edit; NO webapp/server.py edit; reuse-by-import of _run_wander(encode_mode='consolidated'). finding 2026-08-18-dmn-consolidated-store-folded-into-self-init-production-GO."
    evidence: "webapp/server.py brain_chat: _SI.is_selfinit_trigger(req.message) (empty / 'say something' lead-in) short-circuits to _get_selfinit_organ(cache_key).speak(lesion=selfinit_lesioned()) -> self_initiated_text / idle_fallback_text, placed BEFORE the empty-422; _SESSION_SELFINIT + reset; the _SELF_INITIATE_DEFAULT_ON default-ON anchor. research/runners/self_initiated_production_organ.py reuses _self_initiated_utterance_derisk (_lexicon/_build_mouth/_utterance_stream) + _self_initiation_multibasin_derisk (_run_condition/_selection) + _self_initiated_spontaneous_thought_derisk._curiosity_wants + OneBrainComposer. Verified numpy-CPU through the REAL production ChatBrain + handler: _self_initiated_production_verify PASS — (A) 6/6 seeds coherent (n_utt=4, curiosity-top concept, mouth fidelity, about-rate 1.0) + handler idle 'Something's been on my mind - cat eat worm. What does cat eat?'; (B) byte-identical reactive panel flag-ON == pristine-HEAD == BRAIN_SELF_INITIATE=0 (SEPARATE processes, hashed) + no self_initiated key on any reactive turn; (C) BRAIN_SELF_INITIATE_LESION=1 collapses n_utt 4->0 every seed + handler neutral idle fallback; (D) moat-safe (unknown subject abstains; reactive abstain byte-identical). de-risk 6/6 committed GO _self_initiated_utterance_derisk.json. finding 2026-08-18-self-initiated-utterance-wired-brain-chat-GO"

  - key: gnw-deliberation
    faculty: "Confidence/conflict-GATED deliberation (THE KEYSTONE, T1-1 rung d, 2026-08-18) — the 'ACT on the conflict/confidence signals we only REPORT' audit item, wired. After the GNW ignition bus commits, the WORKSPACE'S OWN spiking conflict read (n_ignited off cp_firing_states + the nmda_norm confidence balance off cp_conductance_g_nmda) DECIDES commit-vs-abstain. On a genuine multi-answer conflict (>=2 facts share the SAME (agent, action) with DIFFERENT patients — today's bus commits the arbitrary FIRST-match), the distinct candidates are driven EQUALLY into the P1.2 GNW workspace; a sustained co-ignition / low-confidence read (the keystone acc_conflict_gate, theta self-calibrated) makes the brain ABSTAIN instead of committing the shaky answer (deliberation-until-sure + halt-if-unsure). Moat-safe (only ADDS abstentions on a genuine conflict; never un-abstains, never invents, never flips a confident single-answer recall); lesion-load-bearing"
    de_risked: YES              # the keystone re-entrant metacog-gated deliberation is GO-caveat 6/6 (finding 2026-08-18-gnw-reentrant-metacog-gated-deliberation-GO-caveat): the re-entrant cycle count EMERGES from the substrate's own spiking n_ignited/conf read (not a host counter); the decisive read is n_ignited (ignition/conflict count), A3 per-trial halt-timing + A4 lesion dissociation carry substrate control. Runner _gnw_reentrant_metacog_gated_deliberation_derisk.py; the global-STOP companion (distributed overwrite workspace) is 6/6 GO in 2026-08-18-gnw-distributed-overwrite-workspace-PARTIAL.
    wired: YES                  # 2026-08-18 T1-1 rung d: webapp/server.py brain_chat installs webapp/gnw_deliberation.install_deliberation_gate on the ChatBrain (AFTER install_bus_gate) -> the wrapper reads the DISTINCT candidate patients under (agent, action) off the live composer (PROPOSE, read-only), drives them EQUALLY into the P1.2 GNW workspace, and lets the keystone acc_conflict_gate (n_ignited + nmda_norm conf, off cp_firing_states/cp_conductance_g_nmda) AUTHOR commit-vs-abstain. Covers the covered-class factual recall the bus authors (authored_by=='bus'); out-of-scope classes (open-ended/acquisition/self) pass through unchanged. reuse-by-import of _gnw_reentrant_metacog_gated_deliberation_derisk + _p1_2_workspace_deliberation_loop_derisk (NO sim/ edit). Verified numpy-CPU through the REAL production ChatBrain + handler.
    on_by_default: YES          # default-ON; BRAIN_GNW_DELIBERATE=0 -> the wrapper is a pure PASS-THROUGH (returns the bus svo untouched) -> byte-identical to today. Additive + moat-safe: only a >=2-competing-candidate turn can change the outcome (ABSTAIN); a single-answer recall drives ONE slot -> n_ignited==1 / conf~1 -> ADVANCE -> unchanged; a bus abstain stays an abstain. The standard reactive panel (unique (agent,action) keys) is byte-identical.
    scaffold_retired: NO        # declared residuals (named next rungs): (1) the DECISIVE piece wired is the single-hop confidence/conflict-gated ABSTAIN (halt-if-unsure); the full MULTI-HOP 'deliberation-until-sure' (the variable-depth transitive chase whose re-entrant CYCLE COUNT emerges across a CHAIN of inferences) stays the de-risk fixture and is the named deferred rung (on a single production hop RETRY re-drives the SAME deterministic read -> a sustained conflict resolves to ABSTAIN after the budget); (2) the candidate ENUMERATION (which distinct patients compete) is a host modular-processor PROPOSE boundary (the same boundary the keystone/coincidence-integrator declare) — the composer's unbind SPIKES do not yet DIRECTLY drive the workspace slots in ONE merged bridge (rides the one-brain merge #1); (3) the ABSTAIN renders as the standard honest 'I don't know' — a conflict-SPECIFIC honest hedge ('I have more than one answer') is a named refinement; (4) CO-RESIDENT on its own P1.2 workspace bridge (not merged with the recall composer's bridge). The load-bearing spiking part is the workspace WTA co-ignition + the n_ignited/nmda_norm read + the acc_conflict_gate.
    host_scaffold_in_default: "the candidate ENUMERATION (distinct competing patients under (agent,action)) is a host modular-processor PROPOSE boundary (read-only composer unbind); the abstain renders as the standard 'I don't know' (a conflict-specific hedge is a named refinement); the MULTI-HOP deliberation-until-sure stays the de-risk fixture (single-hop halt-if-unsure wired); the organ runs on its own co-resident P1.2 workspace bridge — the load-bearing spiking part is the workspace co-ignition + the n_ignited/nmda_norm conflict read + the acc_conflict_gate deciding commit-vs-abstain"
    default_anchor:
      - file: webapp/server.py
        assign: _GNW_DELIBERATE_DEFAULT_ON
        off_value: "False"
        on_value: "True"
        count: 1
    lesion_note: "LESION-LOAD-BEARING (2026-08-18): BRAIN_GNW_DELIBERATE_LESION=1 runs the conflict read on the P1.2 workspace built with the assembly self-recurrence ZEROED -> the competing candidates cannot sustain co-ignition (n_ignited collapses) -> the acc_conflict_gate can no longer DETECT the conflict -> the brain COMMITS the shaky first-match again (intact ABSTAINS). The abstain is caused by the SPIKING workspace competition read off cp_firing_states, not a host `if len(candidates) >= 2`. Instrument-first probe: intact 2-cand -> ABSTAIN (conf 0.0, n_ignited 2), 1-cand -> ADVANCE (conf 1.0, n_ignited 1); lesion 2-cand -> COMMIT (n_ignited 0)."
    evidence: "webapp/server.py brain_chat: after install_bus_gate, `if _GNW_DELIBERATE_DEFAULT_ON: from webapp import gnw_deliberation; install_deliberation_gate(chat)` wraps chat.gate so the substrate conflict read gates commit-vs-abstain on a genuine multi-answer conflict. webapp/gnw_deliberation.py: all_candidate_patients (PROPOSE, read-only composer unbind, save/restore last_trace) + conflict_gate (drive N slots EQUALLY at IGNITE_PA -> _ignite_and_read_nmda/_conf_from_nmda -> acc_conflict_gate, theta self-calibrated via calibrate_theta) + deliberate_svo/install_deliberation_gate; reuse-by-import of _gnw_reentrant_metacog_gated_deliberation_derisk + _p1_2_workspace_deliberation_loop_derisk (NO sim/ edit). Verified numpy-CPU through the REAL production ChatBrain + handler: _gnw_deliberation_wired_verify GO — (A) abstain-on-conflict (pristine bus COMMITS dog->chase->cat, wired ABSTAINS; n_candidates==2) + real handler default ABSTAINS while BRAIN_GNW_DELIBERATE=0 + _LESION=1 COMMIT; (B) reactive panel byte-identical wired gate == pristine gate (in-process) + real handler md5 default-ON == flag-0 (separate processes); (C) BRAIN_GNW_DELIBERATE_LESION=1 re-commits the shaky answer (workspace load-bearing); (D) moat-safe (a single clean answer is not over-abstained/flipped). finding 2026-08-18-gnw-deliberation-wired-brain-chat-GO"

  - key: gnw-multistep-deliberation
    faculty: "MULTI-STEP re-entrant deliberation (THE KEYSTONE'S DEFERRED RUNG, T1-1 rung d, 2026-08-19) — the 'deliberation-until-sure over a CHAIN of inferences' half the single-hop gnw-deliberation named as DEFERRED. On an explicit chase-form question ('what does X <action> all the way / to the end?') the P1.2 GNW workspace cycles the partial answer back through itself (re-entrant broadcast), re-igniting, and the substrate's OWN spiking n_ignited read (off cp_firing_states, via the keystone acc_conflict_gate) decides how many cycles to run — NOT a host query_chain(cue, actions) counter — halting when the leaf collapses ignition. The brain works through a multi-step transitive inference whose DEPTH it discovers itself, LIVE. Moat-safe (abstains on an unstored/over-run chase, never un-abstains, never invents); lesion-load-bearing (the recurrence-zeroed workspace collapses the multi-step answer)"
    de_risked: "YES"            # 6/6-seed live GO, adversarially verified (finding 2026-08-19-gnw-multistep-reentrant-deliberation-GO): reentrant_acc 1.00 (variable depth L in {1,2,3,4}, depth NOT told) vs single-pass 0.25; workspace-silence LESION 0.00 while the 1-hop reflex 1.00 (full dissociation); DIFFICULTY-GRADED (spearman(resolved_hops, depth)=1.00, no halt at H_cap). Runner _gnw_multistep_reentrant_deliberation_derisk.py; keystone offline _gnw_reentrant_metacog_gated_deliberation_derisk.py.
    wired: "YES"                # 2026-08-19 T1-1 rung d: webapp/server.py brain_chat installs webapp/gnw_multistep_deliberation.install_multistep_gate on the ChatBrain (AFTER install_deliberation_gate) -> on a chase-form question the wrapper strips the marker, runs the UNCHANGED inner gate for (agent, action) + first hop ONCE, then drives confidence_gated_chase on the LIVE composer + the SHARED P1.2 workspace; the emergent cycle count is the substrate's n_ignited read. reuse-by-import of _gnw_reentrant_metacog_gated_deliberation_derisk + _p1_2_workspace_deliberation_loop_derisk (NO sim/ edit). Verified numpy-CPU through the REAL production ChatBrain + handler.
    on_by_default: "YES"        # 2026-08-19 PRODUCTION-DEFAULT FLIP (board #68): _GNW_MULTISTEP_DEFAULT_ON=True installs the gate by default + multistep_enabled() default-ON; BRAIN_GNW_MULTISTEP=0 -> the installed wrapper is a pure PASS-THROUGH (returns the inner gate's svo untouched) -> byte-identical to the pre-flip default. Additive + moat-safe: only an explicit chase-FORM question can change the outcome (ADD the chain terminal); every non-chase turn is byte-identical (the wrapper is inert without a chase marker). Verified through the REAL handler: default-ON chase -> terminal 'munt'; =0 -> first-hop 'blib' byte-identical to pre-flip; LESION -> abstain; non-chase panel md5 identical across all arms.
    scaffold_retired: "NO"      # declared residuals (unchanged by the flip; SAME boundaries as the single-hop gnw-deliberation): (1) the chase-form DETECT + the (agent, action) EXTRACT are host comprehension of the teacher/world utterance (the boundary the SVO question parser occupies); (2) PROPOSE is composer.query_patient (the declared modular-processor boundary — the composer's unbind SPIKES do not yet directly drive the workspace slots in ONE merged bridge; rides the one-brain merge #1); (3) PER-HOP-RESET form only (snapshot-restore wash-out; the continuous no-reset train-of-thought is gated on the unbuilt async attractor); (4) CO-RESIDENT on the SHARED P1.2 workspace bridge (not merged with the recall composer's bridge). The load-bearing spiking part — the whole novelty — is the CYCLE COUNT / when-to-halt as a spiking n_ignited read (off cp_firing_states) replacing the host query_chain counter.
    host_scaffold_in_default: "the chase-form DETECT + (agent, action) EXTRACT are host comprehension of the teacher/world utterance (the SVO-parser boundary); PROPOSE is composer.query_patient (the declared modular-processor boundary, read-only unbind); per-hop-reset form only (the continuous train-of-thought is deferred on the unbuilt async attractor); the organ runs on its own co-resident P1.2 workspace bridge — the load-bearing spiking part is the workspace re-entrant WTA ignition + the n_ignited read + the acc_conflict_gate deciding ADVANCE-vs-COMMIT-vs-ABSTAIN (the emergent cycle count)"
    default_anchor:
      - file: webapp/server.py
        assign: _GNW_MULTISTEP_DEFAULT_ON
        off_value: "False"
        on_value: "True"
        count: 1
    lesion_note: "LESION-LOAD-BEARING (2026-08-19): BRAIN_GNW_MULTISTEP_LESION=1 runs the chase on the P1.2 workspace built with the assembly self-recurrence ZEROED -> a hop cannot sustain ignition -> n_ignited collapses -> the loop cannot advance/converge -> the multi-step terminal is NOT reached (the brain abstains). The multi-step answer is caused by the SPIKING workspace re-entry read off cp_firing_states, not a host `for _ in range(depth)`. Through the REAL handler (default-ON): intact chase -> 'The zorp chases munt' (terminal); lesion -> 'I don't know' (abstains); the ordinary/one-hop non-chase panel is byte-identical under the lesion (the lesion touches only the chase path). De-risk dissociation: reentrant 1.00 vs recurrence-zeroed lesion 0.00 while the 1-hop reflex survives 1.00, 6/6 seeds."
    evidence: "webapp/server.py brain_chat: after install_deliberation_gate, `if _GNW_MULTISTEP_DEFAULT_ON: from webapp import gnw_multistep_deliberation; install_multistep_gate(chat)` wraps chat.gate so a chase-form question runs the substrate emergent-count multi-step chase. webapp/gnw_multistep_deliberation.py: detect_chase (strip the marker) + deliberate_multistep/multistep_chase -> confidence_gated_chase on the LIVE composer + the SHARED warm P1.2 workspace (_get_bridge/_get_theta reused from gnw_deliberation); multistep_enabled() default-ON (BRAIN_GNW_MULTISTEP=0 pass-through). reuse-by-import of _gnw_reentrant_metacog_gated_deliberation_derisk + _p1_2_workspace_deliberation_loop_derisk (NO sim/ edit). PRODUCTION-DEFAULT FLIP verified numpy-CPU through the REAL production ChatBrain + handler (research/findings/raw/_multistep_production_default/verify.json, all_four_pass): (1) default-ON no-env -> chain TERMINAL 'The zorp chases munt' [zorp,chase,munt], not first-hop 'blib'; (2) BRAIN_GNW_MULTISTEP=0 -> non-chase panel md5 AND full chase response md5 byte-identical to the pre-flip base fa300e1a0 default (first-hop 'blib'); (3) BRAIN_GNW_MULTISTEP_LESION=1 collapses the chase to abstain while ordinary turns unchanged (disable degrades to first-hop) — load-bearing; (4) no regression: default-ON non-chase panel md5 identical to pre-flip (the chase detector does not hijack normal turns). de-risk 6/6 GO finding 2026-08-19-gnw-multistep-reentrant-deliberation-GO; flip finding 2026-08-19-multistep-production-default-GO"

  - key: gnw-thought-swap
    faculty: "GNW neural thought-swap into the LIVE held-topic workspace (the OBSERVE-ONLY #77 tracker) — a topic-change user turn (a salient mismatch between the new input and the currently-held topic) swaps the workspace (old topic evicted, new ignited); a same-topic follow-up holds. The cross-turn 'current thought' the earlier GNW gates lacked (they restore their workspace each turn). ⚠️ SUPERSEDED by `swap-drives-response` (board #85, on_by_default:YES): #85 reuses this SAME #77 machinery by import (webapp/swap_drives_chat.py -> webapp/gnw_thought_swap.py) AND additionally DRIVES the response (the neural swap verdict prepends a topic-transition lead), so it is a strict superset. RETIRED 2026-08-20: the observe-only fallback (the `elif` that ran gnw_thought_swap.observe_turn when swap-drives was OFF and BRAIN_GNW_SWAP was on) is REMOVED from webapp/server.py brain_chat — swap-drives is now the SOLE swap path (exactly one neural swap per turn). This row is KEPT (ratchet count) as the historical #77 observer entry; the LIVE swap faculty is swap-drives-response. Byte-identical-live verified vs HEAD (all 16 fast+rich turns, combined md5 92f157a1). webapp/gnw_thought_swap.py stays on disk (imported by swap_drives_chat.py)."
    de_risked: YES              # the 3 swap de-risks each 6/6 GO — eviction (recurrence weaken), admission (neural vacancy gate), decision (neural swap-intention mismatch/salience detector); reused-by-import unchanged.
    wired: YES                  # 2026-08-19 board#77: was reachable from /api/brain-chat via the per-turn observe fallback. SUPERSEDED + the fallback RETIRED 2026-08-20 (see faculty note): the LIVE swap machinery now runs via swap-drives-response (#85), which imports + reuses THIS module's ThoughtSwapWorkspace/run_intention_swap. `_GNW_SWAP_DEFAULT_ON`(False) is retained in server.py as this row's default_anchor + it still gates the DEFAULT-OFF `gnw_swap` observability key (BRAIN_GNW_SWAP=1 now surfaces swap-drives' own per-turn read). Kept YES: the mechanism is wired-in-production (as #85's reused core), not removed.
    on_by_default: NO           # DEFAULT-OFF (a reversible flag pending owner review): _GNW_SWAP_DEFAULT_ON=False + the BRAIN_GNW_SWAP opt-in. Flag off -> the block is fully skipped (no workspace built, no gnw_swap key) -> byte-identical to pre-wiring.
    scaffold_retired: NO        # host residuals (named, unchanged from the de-risk): (1) the mm->boost COUPLING is a host rate read-out (no engine primitive for presynaptic-firing U-modulation) — a neuromodulator-like scaffold; (2) the cross-turn CONTINUITY of the held thought is a host label (held_slot) re-established each turn via run_intention_swap(isolate=True) — the swap-vs-hold VERDICT is neural every turn, but the between-HTTP-turn persistence is host bookkeeping; (3) per-pattern labeled-line topic routing (N_PATTERNS=3 slots, LRU reuse) — the topic identity is the world's input.
    host_scaffold_in_default: "the swap is fully OFF by default (no cross-turn held-topic workspace in the default turn). When enabled: the mm->boost coupling is a host rate read-out; the cross-turn held-thought continuity is a host label (held_slot); the incoming topic is host-extracted from the user message (the SVO-parser comprehension boundary); per-pattern labeled-line topic routing."
    default_anchor:
      - file: webapp/server.py
        assign: _GNW_SWAP_DEFAULT_ON
        off_value: "False"
        on_value: "True"
        count: 1
    lesion_note: "The swap DECISION (swap-vs-hold each turn) is the spiking mismatch/salience detector's, not a host `if`: a DIFFERENT salient topic fires mm -> the recurrence-depression boost evicts the incumbent -> the vacancy gate admits the newcomer (SWAP); the SAME topic is vetoed by the pred held-content interneuron -> no boost -> the incumbent holds (NO swap). The de-risk's trigger-lesion (silence mm) makes a salient input FAIL to swap (incumbent holds), 6/6 seeds. Verified through the REAL handler: a continuous conversation swaps on every topic-change turn (rate 1.00) and holds on every same-topic/no-topic turn (rate 0.00); with the flag off the response is byte-identical (no gnw_swap key) and with it on the answer is unchanged (only the additive gnw_swap read appears)."
    evidence: "webapp/gnw_thought_swap.py (ThoughtSwapWorkspace + observe_turn + the grounded-topic extractor) reuses-by-import build/run_intention_swap/MultiLoopSTD/SALIENT_PA/N_PATTERNS from research/runners/_gnw_neural_swap_intention_derisk (6/6 GO, NO sim/ edit). webapp/server.py brain_chat: after msg is computed, `if _GNW_SWAP_DEFAULT_ON or _gnw_swap_flag_on(): gnw_thought_swap.observe_turn(chat, msg)`; both main return paths attach resp['gnw_swap'] only when _gnw_swap_flag_on(). Verified numpy-CPU through the REAL /api/brain-chat handler (research/findings/raw/_gnw_swap_chat/verify.json): (A) continuous conversation dog->dog(hold)->brain(swap)->brain(hold)->cat(swap)->tell-me-more(hold)->dog(swap) — topic-change swap rate 1.00, same-topic/hold rate 0.00, each swap shows old_residual~0 + new_rate 0.333 + n_ignited==1; (B) a 6-turn OFF/ON panel — OFF carries no gnw_swap key and ON-minus-gnw_swap is byte-identical (md5) to OFF on every turn (answer/recall/abstain/multi-step/self unchanged). de-risk findings 2026-08-19-gnw-neural-swap-intention-GO / -gnw-neural-vacancy-gate-GO / -gnw-recurrence-weaken-swap-GO; wiring finding 2026-08-19-gnw-swap-into-chat-GO"

  - key: affect-drives-response
    faculty: "Graded affect DRIVES the live response (board #84) — the #81 interoceptive graded-affect LADDER (a Koulakov robust integrator reading the #49 body-state as a SMOOTH valence x arousal off cp_firing_states) is read live each turn and made LOAD-BEARING on how the brain responds: the felt valence picks a graded affective-EXPRESSION lead the reply leads with (warmth/curtness), the felt arousal its emphasis. The anti-hollow-integration counterpart to the observe-only faculties — the affect read is neural AND it demonstrably shapes the surface, and the surface change VANISHES under the neural embodiment lesion."
    de_risked: YES              # rides the #81 6/6-seed GO (2026-08-19-graded-affect-attractor-GO): the graded ladder read is Pearson +0.97/+0.95 valence/arousal, the embodiment (intero->ladder) lesion collapses it to 0. Reused-by-import unchanged (GradedAffectBrain/read_body).
    wired: YES                  # 2026-08-19 board#84: reachable from /api/brain-chat — the handler's per-turn block appraises the message (Gate-B DR-2 valence, the SVO-parser comprehension boundary), EMA-folds it into a persistent per-session body-state, runs read_body on the #81 ladder (PRIVATE-RNG isolated, host RNG restored — the #77 footgun), and prepends the graded affective-expression lead OUTERMOST to the answer on BOTH main return paths; the additive affect_drives read is attached.
    on_by_default: YES          # 2026-08-19 PRODUCTION-DEFAULT: _AFFECT_DRIVES_DEFAULT_ON=True; BRAIN_AFFECT_DRIVES=0 -> the block is fully skipped (no affect_drives key, no lead) -> byte-identical oracle. Verified through the REAL handler: A/B/C all PASS + byte-identical-off.
    scaffold_retired: NO        # host residuals (named): (1) the message->valence APPRAISAL is host (a language-comprehension boundary, the SVO-parser boundary — DR-2 learned valence gated by Warriner salience norms) — the felt READ (body-state -> graded valence x arousal off cp_firing_states) + its embodiment dependence ARE the #81 neural mechanism (lesion-proven); (2) the level->EXPRESSION-MARKER map is a host conditioned-articulation scaffold (the mouth) DRIVEN by the neural read (owner-sanctioned articulation-crutch — the lesion collapses the marker); a brain-native spiking prosody mouth is the next rung; (3) the #81 ladder runs on its OWN co-resident bridge, not merged onto the recall bridge (the one-brain merge); (4) orthogonal to the Gate-B BRAIN_AFFECT prose-manner path (a different 2026-08-08 Stage-A ladder) — unifying the two affect reads is a follow-on.
    host_scaffold_in_default: "the message->valence appraisal is host (the SVO-parser comprehension boundary, DR-2 learned valence + Warriner salience gate); the level->expression-marker STRING is a host conditioned-articulation template (the mouth), DRIVEN by the neural ladder read; the #81 ladder runs on its own co-resident bridge (not merged with the recall composer's bridge). The load-bearing spiking part is the graded valence x arousal READ off cp_firing_states + its embodiment dependence (the lesion-proven #81 mechanism) driving the expression."
    default_anchor:
      - file: webapp/server.py
        assign: _AFFECT_DRIVES_DEFAULT_ON
        off_value: "False"
        on_value: "True"
        count: 1
    lesion_note: "LESION-LOAD-BEARING (2026-08-19): BRAIN_AFFECT_DRIVES_LESION=1 cuts the interoceptive->ladder synapses (the #81 embodiment lesion) on every read -> the neural mood differential collapses to ~0 -> the staircase level is 0 -> the affective lead VANISHES and the answer surface reverts to the coupling-off base. So the surface change RIDES the SPIKING ladder read, not a host `if valence>0`. Through the REAL handler (message FIXED, induced pos vs neg): intact pos 'Wonderful — The dog chases cat.' vs neg 'Frankly! The dog chases cat.' (content identical); lesion -> BOTH collapse to the bare 'The dog chases cat.' (mood +0.0000), the tone-difference GONE."
    evidence: "webapp/affect_drives_chat.py (AffectDrivesWorkspace + observe_turn + the level->expression map) reuses-by-import GradedAffectBrain/read_body/I_BODY_PA from research/runners/_graded_affect_attractor_derisk (board #81 6/6 GO, NO sim/ edit) and the appraisal from affect_production_organ.appraise_text. webapp/server.py brain_chat: after msg, `if _affect_drives_on(): affect_drives_chat.observe_turn(chat, msg)`; both main return paths prepend affect_drives_lead OUTERMOST + attach resp['affect_drives'] (guarded, additive). Verified numpy-CPU through the REAL /api/brain-chat handler (research/findings/raw/_affect_drives_chat/verify.json, verdict GO): (A) an emotionally-varied conversation moves the neural mood sensibly (baseline -0.003, pos +0.069, neg -0.029) and a neutral probe HOLDS it (persistence), felt-arousal rises 0->0.074; (B) message FIXED, induced pos vs neg -> the reply lead differs (Wonderful vs Frankly) with the base sentence + content fields byte-identical, and the difference VANISHES under the intero->ladder lesion (both mood->0, leads gone, answers == base); (C) content fields (abstained/recalled/verified) affect-invariant across {off,+pos,+neg} + byte-identical-off (OFF no key; a neutral un-induced ON-minus-key == OFF md5). A+B also confirmed on the FULL default-organ config + the rich answer path (full_default_config_AB_transcript.txt). de-risk finding 2026-08-19-graded-affect-attractor-GO; wiring finding 2026-08-19-affect-drives-chat-load-bearing-GO"

  - key: swap-drives-response
    faculty: "The #77 neural thought-swap DRIVES the live response (board #85) — the GNW held-topic swap (a spiking mismatch/salience detector fires for a salient TOPIC-CHANGE proposal that does NOT match the held coalition -> recurrence-depression evicts the incumbent -> the neural vacancy gate ignites the newcomer) is read live each turn and made LOAD-BEARING on what the brain SAYS: on a SWAP the reply LEADS with a topic-transition acknowledgment naming the newly-held coalition ('On <newtopic>, then — <answer>'); a same-topic follow-up holds silent. The anti-hollow-integration counterpart to the #77 OBSERVER (gnw-thought-swap) — the swap read is neural AND it demonstrably shapes the surface, and the surface change VANISHES under the neural mismatch-detector lesion."
    de_risked: YES              # rides the 3 swap de-risks each 6/6 GO (eviction/admission/decision) via #77's ThoughtSwapWorkspace; reused-by-import unchanged (the observe_turn gains only an additive lesion= kwarg, default False -> #77 observer byte-identical).
    wired: YES                  # 2026-08-19 board#85: reachable from /api/brain-chat — the handler runs ONE neural swap per turn via swap_drives_chat.observe_turn (reuse of #77 gnw_thought_swap.observe_turn, lesion threaded) and prepends the topic-transition lead OUTERMOST to the answer on BOTH main return paths; the additive swap_drives read is attached. This SUPERSEDES the #77 observer block (never both -> no double-advance of the workspace).
    on_by_default: YES          # 2026-08-19 PRODUCTION-DEFAULT: _SWAP_DRIVES_DEFAULT_ON=True; BRAIN_SWAP_DRIVES=0 -> the block is fully skipped (no swap_drives key, no lead) -> byte-identical oracle. Verified through the REAL handler: A/B/C all PASS + byte-identical-off.
    scaffold_retired: NO        # host residuals (named): (1) the verdict->TRANSITION-STRING map is a host conditioned-articulation scaffold (the discourse mouth) DRIVEN by the neural swap (owner-sanctioned articulation-crutch — the lesion collapses the lead); a brain-native spiking discourse-transition mouth is the next rung; (2) the coupling is SWAP-only (mark shifts not continuations; a bidirectional 'Still on <held>' continuity lead is a possible enrichment, kept off so the mm-lesion vanish stays byte-identical); (3) the topic EXTRACTION is host comprehension (the SVO-parser boundary) — the swap DECISION + the winning coalition's identity are the substrate's; (4) inherited #77 residuals: the mm->boost coupling is a host rate read-out (functional correlate, NOT phenomenal), the cross-turn held-thought continuity is a host label (held_slot), per-pattern labeled-line topic routing; (5) CO-RESIDENT on its own swap bridge (rides the one-brain merge #1)."
    host_scaffold_in_default: "the topic EXTRACTION is host (the SVO-parser comprehension boundary); the verdict->transition-STRING is a host conditioned-articulation template (the discourse mouth), DRIVEN by the neural swap verdict; the swap runs on its own co-resident bridge (not merged with the recall composer's bridge); inherited #77 host scaffolds (mm->boost rate read-out, held_slot continuity label, labeled-line routing). The load-bearing spiking part is the swap-vs-hold VERDICT + the winning coalition's identity off the #77 mismatch/eviction/admit chain (lesion-proven) driving the transition lead."
    default_anchor:
      - file: webapp/server.py
        assign: _SWAP_DRIVES_DEFAULT_ON
        off_value: "False"
        on_value: "True"
        count: 1
    lesion_note: "LESION-LOAD-BEARING (2026-08-19): BRAIN_SWAP_DRIVES_LESION=1 threads trigger_lesion=True into the reused run_intention_swap -> the spiking mismatch/salience detector is given NO proposal drive (mm never fires -> the STD boost stays 0) -> a salient TOPIC-CHANGE can no longer swap (swapped collapses to False) -> the transition lead VANISHES and the surface reverts to the byte-identical no-lead base. So the surface change RIDES the SPIKING mm read, not a host `if topic_changed`. Through the REAL handler (message FIXED 'what does the dog chase?', held-topic context varied): intact swap-ctx(held=cat) swapped=True 'On dog, then — The dog chases cat.' vs hold-ctx(held=dog) swapped=False 'The dog chases cat.' (base + content identical); lesion swap-ctx -> swapped=False reason=mismatch_held_no_swap mm_peak=0.011 lead gone -> 'The dog chases cat.' == base. FUNCTIONAL correlate, NOT a phenomenal claim."
    evidence: "webapp/swap_drives_chat.py (transition_lead + observe_turn) reuses-by-import #77's webapp/gnw_thought_swap.observe_turn (ThoughtSwapWorkspace + the grounded-topic extractor + the neural swap), which reuses build/run_intention_swap/MultiLoopSTD from research/runners/_gnw_neural_swap_intention_derisk (6/6 GO, NO sim/ edit). webapp/gnw_thought_swap.py gains an additive lesion= kwarg threading trigger_lesion into run_intention_swap (default False -> #77 observer byte-identical). webapp/server.py brain_chat: after msg, `if _swap_drives_on(): swap_drives_info = swap_drives_chat.observe_turn(chat, msg)` (else the #77 observer runs — never both); both main return paths prepend swap_drives_lead OUTERMOST + attach resp['swap_drives']. Verified numpy-CPU through the REAL /api/brain-chat handler (research/findings/raw/_swap_drives_chat/verify.json, verdict GO): (A) a topic-changing conversation dog->dog(hold)->brain(swap 'On brain, then — ')->brain(hold)->cat(swap 'On cat, then — ')->tell-me-more(hold)->dog(swap 'On dog, then — ') — topic-change swap-rate 1.00, same-topic/no-topic 0.00, the transition lead present IFF the neural swap fired; (B) message FIXED, held-topic context swap vs hold -> the reply lead differs ('On dog, then — ' vs '') with the base sentence 'The dog chases cat.' + content fields byte-identical, and the difference VANISHES under the neural mm-lesion (swapped->False, mm_peak 0.011, lead gone, answer == base); (C) content fields (abstained/recalled/verified) swap-invariant across {off,swap,hold} (md5 350812dd) + byte-identical-off (OFF no key; a no-swap ON-minus-key == OFF md5 194897f8/7f15f541). de-risk findings 2026-08-19-gnw-neural-swap-intention-GO / -gnw-neural-vacancy-gate-GO / -gnw-recurrence-weaken-swap-GO; #77 observer 2026-08-19-gnw-swap-into-chat-GO; wiring finding 2026-08-19-swap-drives-chat-load-bearing-GO"

  - key: da-mode-drives-response
    faculty: "The brain's OWN spiking dopamine MODE DRIVES the live response (board #79) — the #76 spiking DA nucleus (the snc population self-produces the tonic DA LEVEL from a reward/context afferent, off the neuromodulator bus via from_region_firing_signed on [snc] -> concentration -> D1(+)/D2(-) reconfiguration; NEVER a host set_concentration) is read live each turn and made LOAD-BEARING on HOW forthcoming the brain responds: the message's engagement drives the SNc afferent, the SNc self-produces the DA LEVEL, the level is binned to a MODE (rest/focus/arousal), and the mode picks a graded ENGAGEMENT SUFFIX the reply appends (rest/neutral -> none/withhold, focus -> forthcoming, arousal -> emphatic). The anti-hollow-integration counterpart to the #76 DEFAULT-OFF de-risk (the DA mode is neural AND it demonstrably shapes the surface, and the surface change VANISHES under the neural SNc-nucleus lesion). A distinct axis (engagement/arousal) + a SUFFIX, vs the #84 valence-LEAD + the #85 topic-LEAD. Closes the ledger gap 'no standalone reward/value neuromodulator drives the live chat turn'."
    de_risked: YES              # rides the #76 6/6-seed GO (2026-08-19-neuromod-spiking-da-mode-GO): a spiking DA nucleus self-produces the mode (SNc fires more + DA level higher appetitive than aversive, gap>0 every seed; direct/indirect double dissociation), and silencing the SNc nucleus collapses the self-produced level byte-for-byte (max|dF|==0). Reused-by-import unchanged (PM.build/make_manager/measure_self_driven/BASELINE from _neuromod_spiking_da_mode_derisk).
    wired: YES                  # 2026-08-19 board#79: reachable from /api/brain-chat — the handler's per-turn block reads the message engagement (novelty+richness, the SVO-parser comprehension boundary), EMA-folds it into a persistent per-session engagement scalar, maps it to the SNc afferent, runs measure_self_driven on the #76 substrate (PRIVATE-RNG isolated, host RNG restored — the #77 footgun; a full cp_* snapshot makes each read history-independent), bins the self-produced DA level to a mode, and APPENDS the graded engagement suffix to the answer on BOTH main return paths; the additive da_drives read is attached.
    on_by_default: YES          # 2026-08-19 PRODUCTION-DEFAULT: _DA_DRIVES_DEFAULT_ON=True; BRAIN_DA_DRIVES=0 -> the block is fully skipped (no da_drives key, no suffix) -> byte-identical oracle. Verified through the REAL handler: A/B/C all PASS + byte-identical-off.
    scaffold_retired: NO        # host residuals (named): (1) the message->ENGAGEMENT scalar (novelty+richness -> the SNc reward/context afferent) is host (a language/sensory-comprehension boundary, the SVO-parser boundary) — the DA LEVEL (set by SNc spikes off the bus) + its SNc-nucleus dependence ARE the #76 neural mechanism (lesion-proven); the #76 finding itself flags the reward/context scalar's ORIGIN as the residual (computing it from the brain's own sensory stream is a separate faculty, the next rung); (2) the mode->SUFFIX-STRING map is a host conditioned-articulation scaffold (the discourse mouth) DRIVEN by the neural level (owner-sanctioned articulation-crutch — the lesion collapses the suffix); a brain-native spiking engagement mouth is the next rung; (3) operating-point + reward-origin residuals inherited from #76 (the reconfiguring regime str tone 40 + the SNc->DA gain are tuned so the self-produced level spans the mode band); (4) FUNCTIONAL correlate, NOT a phenomenal claim; (5) CO-RESIDENT on its own #76 BG substrate (rides the one-brain merge #1)."
    host_scaffold_in_default: "the message->engagement scalar (novelty+richness) is host (the SVO-parser comprehension boundary) mapped to the SNc reward/context afferent; the mode->suffix STRING is a host conditioned-articulation template (the discourse mouth), DRIVEN by the neural DA level; the #76 BG substrate runs on its own co-resident bridge (not merged with the recall composer's bridge); inherited #76 residuals (the reward/context scalar's ORIGIN is host, the operating point is tuned). The load-bearing spiking part is the SELF-PRODUCED DA LEVEL off the SNc nucleus (from_region_firing_signed reading SNc spikes off the bus; no set_concentration) + its SNc-nucleus dependence (lesion-proven) driving the engagement suffix."
    default_anchor:
      - file: webapp/server.py
        assign: _DA_DRIVES_DEFAULT_ON
        off_value: "False"
        on_value: "True"
        count: 1
    lesion_note: "LESION-LOAD-BEARING (2026-08-19): BRAIN_DA_DRIVES_LESION=1 silences the spiking SNc nucleus (the #76 anti-cheat-2: clamp its input context-independently) on every read -> the reward/context can no longer reach the DA level -> the self-produced level collapses to its sub-firing floor REGARDLESS of the engagement afferent -> the mode is REST -> the engagement suffix VANISHES and the surface reverts to the byte-identical no-suffix base. So the surface change RIDES the SPIKING SNc->DA read, not a host `if engagement>x`. Through the REAL handler (message FIXED 'what does the dog chase?', DA mode induced by the SNc afferent): intact focus-induce(1300pA) self-produced DA=1.239 mode=arousal 'The dog chases cat. — there's plenty more to dig into here!' vs rest-induce(100pA) DA=0.046 mode=rest 'The dog chases cat.' (base + content identical); lesion focus-induce+SNc-silenced -> DA=0.046 mode=rest suffix gone -> 'The dog chases cat.' == base. FUNCTIONAL correlate, NOT a phenomenal claim."
    evidence: "webapp/da_mode_drives_chat.py (DaModeDrivesWorkspace + observe_turn + the engagement->afferent map + the level->mode bins + the mode->suffix map) reuses-by-import PM.build/make_manager/measure_self_driven/BASELINE/SNC_SILENCE_CLAMP from research/runners/_neuromod_spiking_da_mode_derisk (board #76 6/6 GO, NO sim/ edit) -> _neuromod_reconfiguration_derisk (#64) + _perturb_and_measure_derisk (#63). webapp/server.py brain_chat: after msg, `if _da_drives_on(): da_drives_info = da_mode_drives_chat.observe_turn(chat, msg)`; both main return paths APPEND da_drives_suffix to resp['answer'] + attach resp['da_drives'] (guarded, additive). Verified numpy-CPU through the REAL /api/brain-chat handler (research/findings/raw/_da_drives_chat/verify.json, verdict GO): (A) an engagement-varied conversation moves the self-produced DA mode sensibly — hi/ok(rest, DA 0.046, no suffix) -> 'what does the dog chase'(focus, DA 0.701) -> novel/rich(arousal, DA 1.025/1.041) — reached arousal, engaged-DA >= 0.701 > rest-DA <= 0.046 (monotone), suffix present IFF mode focus/arousal; (B) message FIXED, induced focus vs rest -> the reply suffix differs (arousal suffix vs '') with the base sentence 'The dog chases cat.' + content fields byte-identical, and the difference VANISHES under the neural SNc-nucleus lesion (DA 1.239->0.046, mode arousal->rest, suffix gone, answer == base); (C) content fields (abstained/recalled/verified) mode-invariant across {off,focus,rest} (md5 350812dd) + byte-identical-off (OFF no key; a rest ON-minus-key == OFF md5 af57eeaf). de-risk findings 2026-08-19-neuromod-spiking-da-mode-GO / 2026-08-19-neuromod-reconfiguration-GO; wiring finding 2026-08-19-da-mode-drives-chat-load-bearing-GO"

  - key: d5-live-consolidation
    faculty: "Learn-through-use consolidation (D5) — between turns the idle tick re-activates the memory the last turn RECALLED, opens the step-2 self-terminating apical-plateau window, and the substrate's OWN plateau-gated BTSP strengthens it on the organ's real store. WIRED default-OFF. The prior handler-visible-recall FLATNESS IS RESOLVED: EpisodicDapMemory.recall now emits a GRADED apical read (depth_hold = IS_post) BESIDE the binary in_memory gate, and the surfaced recall STRENGTH rises with use through the real handler (5/5 completing seeds) where the binary UP-fraction saturated. NOT flipped: a soak surfaced a real no-regression violation (emergent-assembly crosstalk) that blocks default-on."
    de_risked: YES              # (a) robustness reserve: _d5_live_consolidation_integration_derisk 6/6 GO (cupy), crash-rollback FOUND+FIXED+verified. (b) conversation-visibility: _d5_graded_production_flip_verify — through the REAL EpisodicRecallOrgan.recall+recall_disclosure at the PRODUCTION encode (te=40), depth_hold rises with use load-bearing 5/5 completing seeds (faithful+moat+byte-id-off, strict-monotone 3/6 saturating-tail), 1/6 self-ignites (moat-abstained). finding 2026-08-21-d5-graded-apical-read-conversation-visible-in-production-flip-blocked-on-emergent-assembly-crosstalk
    wired: YES                  # webapp/continuous_engine.py consolidate_used_memory + tick_idle_sessions D5 branch (rollback-safe, _D5_EPISODES=1) + _CONSOLIDATED_TOPICS set (populated on successful consolidate, cleared on forget_session) + topic_consolidated(); research/runners/_episodic_dap_dialogue_memory.py EpisodicDapMemory.recall emits the graded read (byte-identical binary gate); research/runners/d5_episodic_production_organ.py recall_disclosure(cache_key=) surfaces the strength gated PER CONSOLIDATED TOPIC (default reply byte-identical to HEAD when off / for un-consolidated topics); webapp/server.py mark_recall on the referential recall + passes cache_key to recall_disclosure. NO sim/ edit.
    on_by_default: YES          # 2026-08-21 FLIPPED DEFAULT-ON: continuous_engine.d5_consolidate_enabled() returns _D5_CONSOLIDATE_DEFAULT_ON=True when BRAIN_D5_CONSOLIDATE is unset; BRAIN_D5_CONSOLIDATE=0 is the byte-identical escape (no consolidation + no surfaced strength -> recall reply byte-identical to HEAD). GATED by research/runners/_d5_graded_flip_soak.py (sep_bias=0, cupy): 5/6 GO (42/43/44/100/101) + s102 the one moat-abstaining self-ignition build (dog+bird cue=1.0 but in_memory=False, no confab), OFF byte-identical (off_store_flat 6/6), crash-rollback 6/6, dog strength rises 0.43-0.87 mV conversation-visible. THE PRIOR NO-GO WAS A MISDIAGNOSIS: the neighbour 'bird' read byte-identical OFF-vs-ON on 6/6 (apical_cue Δ0.0, depth_hold Δ0.0 on 5/6); the 0/6 was an over-broad surfacing that appended ", recall strength X mV" to EVERY in_memory reply under the flag. FIX: the strength is gated PER CONSOLIDATED TOPIC (_CONSOLIDATED_TOPICS set + recall_disclosure(cache_key=)), so consolidating one memory changes ONLY its own reply. The DG separator (sep_bias) is NOT armed by default — per-topic gating makes its graded-read-crosstalk purpose moot and its winner-fatigue only shrinks assemblies (both sep_bias=0 and =1000 soak 5/6; s102 self-ignites either way). finding 2026-08-21-d5-learn-through-use-flip-GO-per-topic-strength-surfacing-the-prior-NO-GO-was-a-surfacing-artifact-not-substrate-crosstalk.
    scaffold_retired: NO        # a NEW capability (not a host-shortcut replacement); the arc's snapshot/restore determinism guard + the full-strength one-shot encode + the per-topic surfacing gate (host bookkeeping of which memory was consolidated) are declared idealizations. s102 self-ignition is an emergent-assembly-quality residual (moat abstains correctly).
    lesion_note: "LESION-LOAD-BEARING (Level-3) on BOTH the store's robustness reserve AND the conversation: BRAIN_D5_CONSOLIDATE=0 -> consolidate_used_memory returns None -> store byte-identical + NO topic enters _CONSOLIDATED_TOPICS -> the recall reply byte-identical to HEAD (the graded strength is not surfaced for any topic); DEFAULT-ON -> the substrate BTSP weight change (w_dog +within-assembly) raises the surfaced depth_hold and the used memory's reply gains 'recall strength X mV' (rise 0.43-0.87 mV, on_dog_rose 5/6), and both vanish exactly when the flag is off (off_store_flat 6/6). Faithful: formation-lesion (baseline weights) collapses the graded read to 0.000. A NEIGHBOUR never consolidated keeps a reply byte-identical (bird_reply_same + bird_gate_same 6/6) -> consolidating one memory changes only its own reply."
    evidence: "research/runners/_d5_graded_flip_soak.py (4-turn OFF-vs-ON no-regression + crash-rollback, cupy, te=40) WITH the per-consolidated-topic strength gate: sep_bias=0 (production default) 5/6 GO (42/43/44/100/101) + s102 moat-abstaining self-ignition; sep_bias=1000 also 5/6 (s102 self-ignites either way -> separator does not fix it). bird byte-identical OFF-vs-ON on 6/6 (apical_cue Δ0.0; depth_hold Δ0.0 at sep0 on 6/6). dog strength rises 0.43-0.87 mV; crash-rollback 6/6 (hash_pre==hash_post + topic drained + re-raised); off_store_flat 6/6. Reproduce of the prior NO-GO on this branch: research/findings/raw/_d5_ltu_lever2/reproduce_s42.json (NO_REGRESSION=False, bird read byte-identical, reply differed only by the appended strength clause). artifacts research/findings/raw/_d5_ltu_lever2/sep0|sep1000/soak_summary_6seed.json. Earlier arc: _d5_live_consolidation_integration_derisk.py 6/6 GO (robustness reserve) + _d5_graded_production_flip_verify.py (depth_hold rises 5/5). finding 2026-08-21-d5-learn-through-use-flip-GO-per-topic-strength-surfacing-the-prior-NO-GO-was-a-surfacing-artifact-not-substrate-crosstalk (supersedes the 2026-08-21 NO-GO)."
    default_anchor:                # 2026-08-21 FLIPPED default-ON; BRAIN_D5_CONSOLIDATE=0 is the byte-identical escape. Mirrors continuous-state-engine / affect-drives.
      - file: webapp/continuous_engine.py
        assign: _D5_CONSOLIDATE_DEFAULT_ON
        off_value: "False"
    host_scaffold_in_default: "the per-topic surfacing gate (host bookkeeping of which memory was consolidated, _CONSOLIDATED_TOPICS) + the idle-tick scheduler (host clock) + the reactivate loop's snapshot/restore determinism guard + the single full-strength one-shot encode; the load-bearing spiking part is the plateau-gated BTSP weight change + the depth_hold (IS_post) graded read"

  - key: tiered-knowledge-ltm
    faculty: "LLM-scale KNOWLEDGE in the live chat (board #66/#109) — the brain can hold + query a large body of facts (100k-1M) beside the small conversation working-set, past the k_max=32 co-resident cap. The hippocampal-buffer / cortical-LTM split: the live agent's small flat composer stays the recent-conversation BUFFER; a routed ShardedPhasorStore is the cortical LONG-TERM store (a query touches only ONE shard ~K/S facts -> sub-second at ANY K). A TieredFactStore drop-in implements the exact composer READ+WRITE API and reads buffer-then-routed-LTM; a write goes to the buffer. WIRED default-OFF (opt-in) into the production load path. Delivers the owner's #1 priority: teach the fundamental knowledge an LLM has, then chat + learn over it daily."
    de_risked: YES              # capacity: research/runners/_knowledge_scale_sharding_verdict.py GO (14.2x @ K=2000/S=16, 0 answer-mismatches vs unsharded, recall+moat byte-identical; real Wikidata 2413: 22.6x; clean to 100k concepts @ D128) -> finding 2026-08-20-sharded-fact-store-removes-the-O-K-query-wall-knowledge-scales-to-LLM-scale. DROP-IN: research/runners/_knowledge_tiered_livechat_derisk.py 3/3 GO (N=5000, through the REAL BrainConversationalAgent) -> finding 2026-08-20-tiered-fact-store-wires-LLM-scale-knowledge-into-the-live-chat.
    wired: YES                  # research/runners/developed_brain_io.load_developed_brain(ltm_bundle=<path>) builds a ShardedPhasorStore LTM + installs TieredFactStore as the agent composer; webapp/server.py brain-chat load path exposes it as BRAIN_LTM_BUNDLE (unset -> the plain flat composer, byte-for-byte). NO sim/ edit.
    on_by_default: YES          # 2026-08-26 FLIPPED DEFAULT-ON (board #133): the curated 15k wikidata knowledge CORE ships as the default cortical LTM. webapp/server.py _resolve_ltm_bundle(): BRAIN_LTM_BUNDLE unset -> the shipped core (_LTM_SHIP_DEFAULT_ON=True + the core dir present on disk); BRAIN_LTM_SHIP_DEFAULT=0 (or BRAIN_LTM_BUNDLE=off) is the byte-identical no-LTM escape; a checkout without the data lake degrades to no-LTM (isdir guard). GATED by the core no-regression SOAK go=True 6/6 seeds (42/43/44/100/101/102): research/findings/raw/_knowledge_core/core_bundle_soak_verdict.json -> 0 byte-identity mismatches vs the plain-flat UNSHARDED oracle, 0-confab moat, first-match recall 1.0, latency median ~0.40s (vocab 7,032, well under 1s), and the REAL production load path load_developed_brain(ltm_bundle=<core>) recall+buffer+moat GO; re-confirmed on main single-seed (research/findings/raw/_knowledge_core/core_bundle_soak_confirm_main_s42.json). D5 learn-through-use consolidation stays in the path (the buffer tier keeps its per-topic plasticity) -> biological memory, not static RAG. Finding 2026-08-26-knowledge-core-shipped-as-default-cortical-ltm-soak-go-byte-identical.
    scaffold_retired: NO        # host residuals: (1) the ShardedPhasorStore's agent-hash ROUTER is a declared capacity-de-risk scaffold (the faithful version is a learned/spiking cue->sub-population router); (2) the RF composer + moat are the numpy VSA fast path (the co-resident spiking store is a separate rung); (3) cross-tier multi-hop chains are not merged; (4) promote_buffer_to_ltm (the sleep-replay hippocampal->cortical consolidation) is an explicit hook, not auto/replay-driven yet.
    lesion_note: "LOAD-BEARING via the tier fallthrough + the ltm=None degrade (a routing/capacity wiring, not a spiking-organ lesion): with ltm=None the TieredFactStore is ANSWER-IDENTICAL to the plain buffer (degrade_identical True, 3/3) -> the LTM answers only ever come from the installed cortical store, never from decoration; a bulk-knowledge query the buffer cannot answer (buffer abstains, string-compares unbound facts, no shared codebook) is answered IFF the routed LTM shard holds it. The no-confab moat holds because BOTH tiers must abstain for a non-answer (moat 20/20 unknown subjects abstain; end-to-end with real Wikidata: snarklebee->None, elephant-isa-fish->unknown). Recent buffer facts SHADOW a contradicting LTM fact (recency wins). NOT a phenomenal claim."
    default_anchor:                # 2026-08-26 FLIPPED: _LTM_SHIP_DEFAULT_ON=True is the ship default; BRAIN_LTM_SHIP_DEFAULT=0 is the byte-identical escape. Mirrors continuous-state-engine / gnw-multistep.
      - file: webapp/server.py
        assign: _LTM_SHIP_DEFAULT_ON
        off_value: "False"
        on_value: "True"
        count: 1
    evidence: "research/runners/tiered_fact_store.py (TieredFactStore + build_ltm_from_facts + auto_n_shards) composes RFPhasorComposer (buffer) + ShardedPhasorStore (LTM, research/runners/sharded_phasor_store.py) behind the composer API. developed_brain_io.load_developed_brain gains ltm_bundle=/ltm_n_shards=/ltm_seed=/ltm_D= (additive, default None -> byte-identical); webapp/server.py brain-chat load reads BRAIN_LTM_BUNDLE (default unset -> off). Verified numpy-CPU: (drop-in) _knowledge_tiered_livechat_derisk 3/3 GO N=5000 through agent.what_does/who_does/is_it_true — recall 50/50, routed query sub-second at 5000 facts, teach-in-conversation lands in the buffer (LTM untouched), recency shadows, moat 20/20, k_max lifted, ltm=None degrades identical; (end-to-end wired) a tiny developed brain loaded with the real 2413-fact Wikidata bundle as ltm_bundle answers aardvark-isa->mammal (LTM) + dog-chases->cat (buffer) + snarklebee->abstain through the production load path. findings 2026-08-20-sharded-fact-store-removes-the-O-K-query-wall-knowledge-scales-to-LLM-scale + 2026-08-20-tiered-fact-store-wires-LLM-scale-knowledge-into-the-live-chat"

  - key: continuous-state-engine
    faculty: "The between-turn CONTINUOUS LIFE (board Trunk-A) — the brain is not inert between messages. An idle tick does two spiking reads: (A) a THOUGHT WANDERS — the self-initiation organ's CA3 selects a concept off its own store (full 4000-step op point on cupy); recent_wander() hands it to the next live turn so the brain can LEAD with what it was mulling. (B) the FELT MOOD RELAXES toward baseline and RE-READS the spiking affect ladder, so idling measurably changes the affect/tone the next turn is built on. This is the substrate's ALIVENESS — learn-through-use / feeling / trains-of-thought living in the brain's continuous life, the LLM-surpassing differentiator (owner 2026-08-19 reframe). WIRED default-OFF behind BRAIN_CONTINUOUS (+ BRAIN_CONTINUOUS_DRIVES for the wander-lead); the flip is the mission-defining default-on."
    de_risked: YES              # BOTH drive-couplings lesion-verified load-bearing on cupy: research/runners/_continuous_drive_loadbearing_cupy.py (wander-drive: recent_wander non-None ON / None when BRAIN_CONTINUOUS=0 / consumed-once; feeling-drive: the relaxed-mood affect read differs from the original-mood read, and is unchanged with no tick) + research/runners/_continuous_tick_cupy_verify.py (the tick runs end-to-end on the cupy production substrate: affect read_differential + selfinit CA3 wander both succeed at the full op point).
    wired: YES                  # webapp/continuous_engine.py (continuous_enabled anchor + tick_session/tick_idle_sessions/mark_request/recent_wander + the D5 consolidate_used_memory branch). webapp/server.py brain_chat: the BRAIN_CONTINUOUS_DRIVES block (server.py:4152) leads the next turn with a recent_wander when one is pending; the between-turn idle loop calls tick_idle_sessions to evolve mood while idle. Default-OFF: continuous_enabled() reads BRAIN_CONTINUOUS default '0' -> the tick loop is inert + recent_wander returns None regardless -> byte-identical to today. NO sim/ edit.
    on_by_default: YES          # 2026-08-21 FLIPPED DEFAULT-ON (the mission-defining flip): continuous_enabled() returns _CONTINUOUS_DEFAULT_ON=True when BRAIN_CONTINUOUS is unset; webapp/server.py's BRAIN_CONTINUOUS_DRIVES lead-block defaults "1". BRAIN_CONTINUOUS=0 (and/or BRAIN_CONTINUOUS_DRIVES=0) is the byte-identical escape to the pre-flip inert loop. GATED by research/runners/_continuous_default_flip_soak_cupy.py GO through the REAL brain_chat handler (cupy): no-regression 0/7 diverged (ON byte-identical to the =0 escape on ordinary turns) + no GPU-memory pileup (VRAM drop 304MiB < floor; cupy pool settled 46->72->72MB, a one-time warm-up not a per-turn leak — the prior %-threshold false-alarm was corrected to a VRAM-stability + pool-settle check) + drive still load-bearing + byte-identical-BY-CONSTRUCTION (the drive only prepends a lead when recent_wander() is non-None, which needs a prior idle tick). The brain now WANDERS + FEELS between turns BY DEFAULT — the substrate's aliveness is live in production.
    scaffold_retired: NO        # host residuals (named): (1) the idle-tick SCHEDULER is a host background timer ("WHEN to think" is host-clocked, not a neural default-mode oscillation); (2) the wander SELECTION rides the self-initiation organ's co-resident CA3 (one-brain merge #1); (3) the mood-relax EMA constants + the affect operating point are host-tuned; (4) FUNCTIONAL correlate of an inner life, NOT a phenomenal claim. The load-bearing spiking parts are the CA3 wander selection + the affect-ladder re-read; the drives are lesion-proven to ride them.
    host_scaffold_in_default: "the idle-tick scheduler (a host background timer) + the mood-relax EMA constants + the affect operating point; the wander runs on the self-initiation organ's co-resident CA3 bridge (not merged with the recall composer's bridge)"
    lesion_note: "LESION-LOAD-BEARING on cupy (both couplings): BRAIN_CONTINUOUS=0 -> continuous_enabled() False -> the tick loop is inert AND recent_wander() returns None regardless -> the next turn gets no lead and the mood is unchanged -> byte-identical to today. ON -> (A) a recorded wander is handed to the next turn ONCE then consumed (w1 non-None, w2 None), and is None under the lesion; (B) the affect differential read at the RELAXED mood differs from the read at the ORIGINAL mood (idling changed the next read), and is unchanged with no tick. Both proven in _continuous_drive_loadbearing_cupy.py. FUNCTIONAL correlate, NOT a phenomenal claim."
    default_anchor:                # 2026-08-21 FLIPPED: continuous_enabled() returns _CONTINUOUS_DEFAULT_ON=True when BRAIN_CONTINUOUS is unset; BRAIN_CONTINUOUS=0 is the byte-identical escape. server.py's BRAIN_CONTINUOUS_DRIVES lead-block defaults "1" (=0 escape). Mirrors affect-drives / gnw-multistep.
      - file: webapp/continuous_engine.py
        assign: _CONTINUOUS_DEFAULT_ON
        off_value: "False"
        on_value: "True"
        count: 1
      - file: webapp/server.py             # 2026-08-26 REPAIRED: the old anchor put the os.environ.get(...) EXPRESSION in
                                           # `assign`, which _count_assign can never resolve (it needs NAME = "literal";
                                           # server.py:4260 is an inline `if os.environ.get(...)`, not an assignment) —
                                           # so it read on=0/off=0 and silently passed while sub-check A was dead. Re-pointed
                                           # at the named production-default constant _CONTINUOUS_DRIVES_DEFAULT = "1".
        assign: _CONTINUOUS_DRIVES_DEFAULT
        off_value: "0"
        on_value: "1"
        count: 1
    evidence: "webapp/continuous_engine.py + webapp/server.py brain_chat (BRAIN_CONTINUOUS_DRIVES lead block :4152 + the idle tick loop). de-risk: research/runners/_continuous_drive_loadbearing_cupy.py (BOTH couplings load-bearing + lesion-verified on cupy) + research/runners/_continuous_tick_cupy_verify.py (tick end-to-end on cupy). SOAK (the default-on safety gate, in flight 2026-08-21): research/runners/_continuous_default_flip_soak_cupy.py — multi-session no-regression (ON byte-identical to OFF on ordinary turns) + no GPU-memory pileup, through the REAL brain_chat handler. board Trunk-A."

  - key: continuous-ideation
    faculty: "Continuous IDEATION — the brain OCCASIONALLY GENERATES a NOVEL blended thought between turns (the creativity/novelty rung of the continuous life). Today the idle wander SELECTS one stored basin and speaks its concept (recall); behind BRAIN_CONTINUOUS_IDEATE the wander every Nth tick instead drives a BLENDED cue of the TWO most curiosity-active basins into a sparse associative-attractor, which settles into a NOVEL recombination that was NEVER stored (novelty from the DYNAMICS, reusing the GO generative-attractor-wander de-risk + its ca3_ff_inhib dynamic threshold). The novel idea is FLAGGED (kind=novel-association, is_fact=False) and surfaced on a channel DISJOINT from recall, so the next turn frames it as 'a thought that occurred to me', never a stored fact. WIRED default-OFF, strictly additive."
    de_risked: YES              # mechanism GO: the 2-source blend is a novel balanced recombination at 6 seeds x 2 scales (blend_balance 0.724, novelty/max-overlap-any-single 0.724 vs a single recall's 1.000, overlap-other-basin 0.153), with clean blend-not-single (single-cue balance-on-the-two-sources 0.093), blend-not-noise (noise balance-on-the-two-sources 0.107), and untrained-W=0 (0.000) controls — all separating from the blend by > 0.15. Reuses research/runners/_generative_attractor_wander_derisk.py verbatim (6-seed GO 2026-08-20). verify: research/runners/_continuous_ideation_verify.py GO.
    wired: YES                  # webapp/continuous_engine.py (ideation_enabled anchor + _ideation_wander/_ideation_blend_settle/recent_ideation + the tick_session ideation branch, additive). webapp/server.py brain_chat: a default-OFF ideation_drives lead block (right after the wander-drives block) prepends the FLAGGED idea in both response paths when recent_ideation() is non-None. Default-OFF: ideation_enabled() reads BRAIN_CONTINUOUS_IDEATE default '0' -> the branch is never entered, no ideation key, recent_ideation() None -> byte-identical to the live default-on continuous wander. NO sim/ edit.
    on_by_default: YES          # FLIPPED default-ON 2026-08-21 (owner "run the soaks, proceed"). SAFE BY CONSTRUCTION: ideation fires ONLY on an IDLE between-turn tick and prepends a FLAGGED non-fact idea (is_fact False, recall-channel empty, store unchanged) — it NEVER touches a live recall/self answer. The composed no-regression (research/runners/_wave4_composed_flip_noregression.py, out-of-scope panel, no idle tick) confirms byte-identical on live turns. BRAIN_CONTINUOUS_IDEATE=0 is the byte-identical escape (anchor webapp/continuous_engine.py:364, default '1'). RESIDUALS (named, NOT flip-blockers): (a) the numpy blend attractor is a declared on-substrate-CA3 stand-in — a burn-down for scaffold_retired, not a correctness gate (the mechanism is 6-seed GO); (b) whether the occasional idle idea reads as a welcome creative aside vs noise is an owner UX judgment made live + reversible via =0. finding 2026-08-21-three-wave-faculties-flip-decision.
    scaffold_retired: NO        # host residuals (named): (1) the fast standalone NUMPY attractor is the de-risked stand-in for the on-substrate CA3 blend (the SAME latency residual the self-init organ declares — cupy CA3 ~seconds, numpy@scale ~minutes); (2) the every-Nth-tick cadence is a host-timed scheduler (WHEN to ideate, like the idle-tick clock); (3) the SELECTION of the two source basins rides the organ's spiking curiosity gains (co-resident, one-brain merge #1). The load-bearing NOVELTY is the attractor dynamics; the surfaced idea is FLAGGED non-fact. FUNCTIONAL creativity correlate, NOT a phenomenal claim.
    host_scaffold_in_default: "the numpy sparse-Hopfield blend attractor (a de-risked stand-in for the on-substrate CA3 blend) + the every-Nth-tick host scheduler; the source-basin selection rides the self-initiation organ's co-resident curiosity gains, not merged onto the recall composer's bridge"
    lesion_note: "OFF byte-identical (the load-bearing protection of the live flip): BRAIN_CONTINUOUS_IDEATE unset -> the ideation branch is never entered, the tick records NO ideation key, recent_ideation() returns None, and the recall wander == today's single-basin curiosity selection (verified through the real tick_session: no key, recall 'cat', flag-on-a-non-ideation-tick is a no-op). ON -> the tick records a FLAGGED novel-association and recent_ideation() surfaces it tagged (is_fact=False), consumed once. HONESTY: the ideation NEVER enters the recall channel (recent_wander None on an ideation tick) and writes NO store / manufactures NO fact (the organ's store fingerprint is unchanged). FUNCTIONAL correlate, NOT a phenomenal claim."
    evidence: "webapp/continuous_engine.py (ideation_enabled/_ideate_every_n/_is_ideation_tick/_ideation_blend_settle/_ideation_wander/recent_ideation + the tick_session ideation branch) + webapp/server.py brain_chat (the default-OFF ideation_drives lead, both response paths). reuse: research/runners/_generative_attractor_wander_derisk.py (the blend attractor + ca3_ff_inhib dynamic threshold, 6-seed GO). verify: research/runners/_continuous_ideation_verify.py GO (numpy-CPU, foreground) — (A) OFF byte-identical (no ideation key, recent_ideation None, recall == today's 'cat', flag-on-non-ideation-tick a no-op); (B) MECHANISM 6 seeds x 2 scales the 2-source blend is a novel balanced recombination + INTEGRATION the real tick surfaces a flagged novel-association (sources cat+dog); (C) blend-not-single/blend-not-noise/untrained controls all separate by > 0.15; (D) honesty preserved (is_fact False, recall-channel empty, store unchanged). artifact research/findings/raw/_continuous_ideation/verify.json. finding 2026-08-21-continuous-ideation-novel-between-turn-thought-GO. board Trunk-A (continuous life)."

  - key: da-gated-encoding
    faculty: "DA-gated ENCODING (board WAVE-0, Gap-4 write-side) — the brain's OWN self-produced tonic dopamine scales the WRITE MAGNITUDE of a taught fact AT STORE TIME (Lisman-Grace hippocampal-VTA loop; Kandel D.16: dopamine gates entry into LONG-TERM memory — a fact heard while the SNc bursts is encoded STRONGER + more stable). The counterpart to the #76/#79 DA-mode READ-side: the same self-produced tonic DA that colors HOW the brain answers now also sets HOW STRONGLY it records what it is told. WIRED default-OFF into the live chat store."
    de_risked: YES              # mechanism GO: research/runners/_burndown_I7_dopamine_encoding_deploy_derisk.py (production OneBrainComposer, stored |w| ratio == the DA-derived gain, lesion-nulled, moat-preserved, byte-identical default) + research/runners/_phaseB_dopamine_encoding_gain_derisk.py (6-seed within-fact recall lift under read damage) + research/runners/_consolidation_probe2_limbic.py (the gain reading the SHARED spike-derived dopamine off the merged bridge). wire-in: research/runners/_da_encoding_wired_verify.py GO — through the REAL brain_chat handler the live self-produced DA (high INDUCE=1300 -> DA 1.239 -> g 2.477; low INDUCE=100 -> DA 0.046 -> g 0.5-floor) reaches the composer's store hook with g_high>g_low, OFF byte-identical (no key + encoding_gain_fn None), lesion pins g=1.0. finding 2026-08-21-da-gated-encoding-wired-into-chat-GO
    wired: YES                  # webapp/da_encoding_drives_chat.py (da_encoding_enabled/da_encoding_lesioned/install_encoding_gain, gain map reused verbatim from the I-7-b de-risk); webapp/server.py brain_chat installs composer.encoding_gain_fn right after the DA-mode read (chat._last_da_drives['da_level'] fresh) + before the gate/acquire that stores; the additive da_encoding trace on the response. Default-OFF: BRAIN_DA_ENCODING unset -> the block is skipped, encoding_gain_fn stays None, no da_encoding key -> the store + reply are byte-identical to HEAD. 2026-08-25: the on-substrate Turrigiano homeostat lives on OneBrainComposer (homeostatic_scaling flag + apply_homeostatic_scaling; additive, DEFAULT-OFF = byte-identical) with the driver's apply_substrate_homeostasis(chat) consolidation entry point (da_encoding_substrate_enabled, default '1' when the coupling is on); install_encoding_gain arms it on the live composer. The composer edit is additive/guarded (NOT a sim/ edit); byte-identical when the flag is off.
    on_by_default: NO           # FLIP GATE now GO (2026-08-25) — held only for a coordinated verifier update + owner sign-off, NO LONGER for missing evidence. The magnitude-store no-regression soak (research/runners/_da_encoding_leansoak.py --substrate-scaling, 6-seed cupy) is GO with the ON-SUBSTRATE Turrigiano synaptic-scaling homeostat: moat_introduced=0, clean=0, genuine (target-block-attributed) stress-net violations=0, derivation cross-check byte-equal to a real production build; the on-substrate rule IMPROVES recall across the damage knee (sigma 1.5: on 24 vs off 13 of 54). RESOLVED the prior UNDEFINED (research/findings 2026-08-21-...-UNDEFINED...): its lone raw stress-net violation was a FOREIGN-BLOCK CONFABULATION at the sigma-6 noise floor (OFF "recalls" via a non-target engram's damaged decode that coincidentally matches; ON abstains — the moat working, not a regression), proven by target-block attribution. The prior "rf fast-path magnitude-invariant" instrument objection is superseded — this soak reads the onebrain MAGNITUDE store (store_conns) at the I-7-b knee. Finding 2026-08-25-da-encoding-substrate-turrigiano-scaling-FLIP. REMAINING before flipping THIS default to YES (owner product call): the wire-in verifier + wave4 baseline OFF arms pin BRAIN_DA_ENCODING=0 explicitly (the byte-identical baseline moves UNSET->=0), and the substrate homeostat's live consolidation-trigger wiring (apply_substrate_homeostasis runs at a consolidation event; the per-write path already carries the recall-safe floor so live is safe meanwhile).
    scaffold_retired: NO        # host residuals (named): (1) the message->engagement->SNc-afferent scalar is the same host sensory/comprehension boundary the #79 DA-mode read names as its residual (the DA LEVEL itself is the brain's own spiking SNc read); (2) on the rf numpy fast-path recall used for the handler's speed the stored recall is magnitude-INVARIANT, so the gain is a write-side reserve THERE — the load-bearing WRITE-magnitude effect lives on the magnitude-carrying onebrain default; (3) the gain MAP constants (k_DA, g_min/g_max) are host-tuned (== the I-7-b operating point). REDUCED 2026-08-25: the homeostatic companion is NO LONGER a host-arithmetic proxy — the on-substrate Turrigiano rule OneBrainComposer.apply_homeostatic_scaling() SENSES each engram's readout activity on the substrate (resonate + read the readout neurons' |Z|) and multiplicatively rescales the store SYNAPSES toward the unit set-point (a genuine homeostatic synaptic-scaling rule; the scale is computed from measured NEURAL ACTIVITY, not a DA-scalar EMA). Default homeostat when the coupling is on (BRAIN_DA_ENCODING_SUBSTRATE, default '1'; =0 falls back to the lever-2 host-proxy EMA). Residual on THIS piece is now only the live consolidation-TRIGGER wiring (the pass is a slow/offline op). FUNCTIONAL memory-strength correlate, NOT a phenomenal claim.
    lesion_note: "LESION-LOAD-BEARING via BRAIN_DA_ENCODING_LESION=1 (pin the encoding gain to 1.0 regardless of the DA level — DISTINCT from BRAIN_DA_DRIVES_LESION which collapses the LEVEL): the high-vs-low write-gain differential VANISHES (g_high==g_low==1.0) even though the self-produced DA level still varies -> the write magnitude rides the DA READ, not a host if-engaged>x. OFF byte-identical: BRAIN_DA_ENCODING unset -> encoding_gain_fn None -> the byte-identical unit-magnitude write (recall content unchanged, verified through the real handler). The gain colors trace STRENGTH (on a magnitude store), never WHICH fact — the moat/recall/abstain verdict is unchanged. FUNCTIONAL correlate, NOT a phenomenal claim."
    evidence: "webapp/da_encoding_drives_chat.py + webapp/server.py brain_chat (install right after the DA-mode read, before the gate/acquire store). de-risks: _burndown_I7_dopamine_encoding_deploy_derisk.py (GO, production OneBrainComposer magnitude mechanism + lesion + moat + regression) + _phaseB_dopamine_encoding_gain_derisk.py (6-seed within-fact lift) + _consolidation_probe2_limbic.py (shared spike-derived DA on the merged bridge). wire-in verify: research/runners/_da_encoding_wired_verify.py GO (through the REAL brain_chat handler, numpy-CPU rf recall) — (A) OFF byte-identical (no da_encoding key, encoding_gain_fn None, recall 'grass'); (B) ON load-bearing g_high 2.477 (DA 1.239) > g_low 0.5 (DA 0.046) + the same g writes a measurably stronger trace on a magnitude store (stored |w| ratio 4.955 == g-ratio); (C) lesion pins g=1.0 (differential vanishes). artifact research/findings/raw/_da_encoding_wired/verify.json. finding 2026-08-21-da-gated-encoding-wired-into-chat-GO. board WAVE-0 Gap-4."

  - key: da-gated-curiosity-threshold
    faculty: "DA/ENGAGEMENT-gated CURIOSITY crave-threshold (board WAVE-0, Gap-4 coupling (b)) — the brain's OWN self-produced tonic dopamine (its engagement/arousal state) modulates the CURIOSITY crave decision: ENGAGED (DA above tonic) LOWERS the effective crave-threshold so the spiking ASK pool crosses it more readily (the brain asks an honest follow-up on a novel topic it would otherwise let pass); disengaged raises it. A coupling of two EXISTING spiking faculties (re-invents neither): the DR-1 spiking curiosity ASK-pool crave-drive (curiosity_production_organ, corr(gap,want)=+0.996) and the #76/#79 spiking SNc->DA read (da_mode_drives_chat). The engagement-axis counterpart to the #79 DA-mode READ-side: the same self-produced DA that colors HOW forthcoming the reply is now also sets HOW READILY the brain craves to learn more (Aston-Jones-Cohen adaptive-gain / tonic-DA vigor account: DA/arousal raises exploratory drive). WIRED default-OFF into the live chat."
    de_risked: YES              # mechanism GO: the DR-1 spiking curiosity crave-drive it reads is the 6-seed / 6/6-SAFE de-risk research/runners/_curiosity_seek_learn_onbridge_derisk.py (corr(gap,SPIKING-want)=+0.996) reused by curiosity_production_organ; the DA LEVEL is the #76/#79 spiking SNc read (research/runners/_neuromod_spiking_da_mode_derisk.py, 6/6-seed GO). wire-in: research/runners/_curiosity_da_threshold_verify.py GO — through the REAL brain_chat handler the live self-produced DA (high INDUCE=1300 -> DA 1.239 -> gain 2.108 -> eff crave-threshold 31.3 Hz; low INDUCE=100 -> DA 0.046 -> gain 0.319 -> eff crave-threshold 206.4 Hz) flips WHETHER the honest follow-up QUESTION is appended (want held fixed at 126.9 Hz), OFF byte-identical (no curiosity_da key), lesion pins gain=1.0. finding 2026-08-21-da-gated-curiosity-threshold-wired-GO
    wired: YES                  # webapp/da_curiosity_drives_chat.py (da_curiosity_enabled/da_curiosity_lesioned/crave_decision; da_crave_gain = clip(0.2,3.0,1+1.5*(DA-0.5)), tonic 0.5 -> gain 1.0); webapp/server.py brain_chat _curiosity_followup recomputes the crave decision with the live DA (chat._last_da_drives['da_level'] fresh from the DA-mode read earlier in the turn) + attaches the additive curiosity_da trace, ONLY when enabled. Default-OFF: BRAIN_CURIOSITY_DA unset -> the block is skipped, the organ's calibrated threshold decides `curious` unchanged, no curiosity_da key -> the turn is byte-identical to HEAD. NO sim/ edit (a host decision over the live spiking reads).
    on_by_default: YES          # FLIPPED default-ON 2026-08-21 (owner "run the soaks, proceed"). SAFE BY CONSTRUCTION: the coupling changes ONLY whether the honest follow-up QUESTION is appended on an ABSTAIN — the content fields (answer/recalled_svo/abstained) are byte-identical on/off (moat preserved). The composed no-regression (research/runners/_wave4_composed_flip_noregression.py, out-of-scope panel) confirms it does not SPURIOUSLY fire on a confident recall/self turn. BRAIN_CURIOSITY_DA=0 is the byte-identical escape (anchor webapp/da_curiosity_drives_chat.py:80, default '1'). RESIDUAL (UX taste, not a GO/NO-GO): whether the DA-modulated follow-up cadence reads as helpful vs nagging under real engagement is an owner judgment best made live + reversible via =0 — NOT an unrun objective soak. finding 2026-08-21-three-wave-faculties-flip-decision.
    scaffold_retired: NO        # host residuals (named): (1) the NOVELTY the organ reads is the ABSTAIN (a declared host boundary, per the curiosity organ); (2) the ENGAGEMENT->SNc-afferent scalar is the same host sensory/comprehension boundary the #79 DA-mode read names as its residual (the DA LEVEL itself is the brain's own spiking SNc read, lesion-proven); (3) the crave-gain constants (K_DA=1.5, g_min/g_max) are host-tuned; (4) the curiosity organ is co-resident on its own ASK bridge (rides the one-brain merge, burn-down #1), not merged onto the recall bridge. FUNCTIONAL engagement->curiosity correlate, NOT a phenomenal claim.
    lesion_note: "LESION-LOAD-BEARING via BRAIN_CURIOSITY_DA_LESION=1 (pin the crave-gain to 1.0 regardless of the DA level — DISTINCT from BRAIN_CURIOSITY_LESION which removes the curiosity DRIVE pathway / collapses the WANT, and from BRAIN_DA_DRIVES_LESION which silences the SNc / collapses the LEVEL): the high-vs-low follow-up decision becomes IDENTICAL (both fire, the organ's own decision) even though the self-produced DA level still varies -> the crave decision rides the DA READ, not a host if-engaged>x. OFF byte-identical: BRAIN_CURIOSITY_DA unset -> no curiosity_da key -> the organ's calibrated threshold decides unchanged (verified through the real handler: a novel abstain fires the follow-up, a familiar recall does not). The coupling gates only WHETHER the honest follow-up QUESTION is appended — never WHICH fact, never the abstain (moat/recall/verify verdict unchanged). FUNCTIONAL correlate, NOT a phenomenal claim."
    evidence: "webapp/da_curiosity_drives_chat.py + webapp/server.py brain_chat (_curiosity_followup recomputes the crave decision with the live DA after the organ's spiking judge, before appending the follow-up). couples: curiosity_production_organ (the DR-1 spiking ASK-pool crave-drive, _curiosity_seek_learn_onbridge_derisk 6-seed GO) + da_mode_drives_chat (the #76/#79 spiking SNc->DA read, _neuromod_spiking_da_mode_derisk 6/6-seed GO). wire-in verify: research/runners/_curiosity_da_threshold_verify.py GO (through the REAL brain_chat handler, numpy-CPU rf recall) — (A) OFF byte-identical (no curiosity_da key; the novel abstain still fires the follow-up, a familiar recall does not); (B) ON load-bearing, the SAME message with novelty held fixed (want 126.9 Hz): HIGH-DA (1.239, gain 2.108, eff crave-threshold 31.3 Hz) FIRES the follow-up where LOW-DA (0.046, gain 0.319, eff crave-threshold 206.4 Hz) does NOT; (C) lesion pins gain=1.0 -> the high-vs-low decision is identical (DA-dependence vanishes). artifact research/findings/raw/_curiosity_da_threshold/verify.json. finding 2026-08-21-da-gated-curiosity-threshold-wired-GO. board WAVE-0 Gap-4 coupling (b)."
