# Vikunja board reconcile stamp — updated by the `vikunja` skill after each board reconcile.
#
# The vikunja-freshness gate (tools/gates/vikunja_freshness.py, CLASS VF) blocks a finding-adding
# commit once THRESHOLD (6) findings have landed since this file was last touched. Stamping this file
# (in the reconcile commit) = "the board at https://vikunja.dant123.com was reconciled" and resets the
# budget. This is the mechanical enforcement the board previously lacked (the roadmap sync was gated,
# the board sync was only remembered — so the board drifted while the docs stayed fresh, 2026-08-20).
#
# Update this on every board reconcile (the `vikunja` skill does it): bump the date + at_commit + note.

last_reconciled: 2026-08-25
at_commit: pending-vision75-readout-v2
note: Reconciled board #75 (vision object-"which" fully-spiking readout) for the dense signed-linear
  readout finding on branch research/vision75-readout-v2. Task 75 STAYS OPEN (capability not achieved):
  the DIFFERENT readout (learned DENSE SIGNED linear discriminant, spike-ported E + feedforward-inhibition
  LIF class populations + temporal G-glimpse integration) SOLVES the quantization wall (spike-port gap
  +0.0243 vs config-C centroid ~0.19) and makes readout LEARNING load-bearing 6/6 (learned 0.4375 vs
  random 0.2552), clears the 0.34 NO-GO 6/6, but does NOT beat V1-direct 0.4184 (capability_go 0/6) — the
  wall RELOCATED to the ~0.47 z-normed C2 feature ceiling. Created next-rungs #75a (lighter s2_norm +
  lower s2_gain joint op-point sweep) and #75b (nonlinear 2-layer spiking readout). SCOPE NOTE: this
  reconcile covered board #75 only (the subagent's task); the prior 2026-08-26 harvest note's other rows
  (#133/#132/#129/#130/#134) are unchanged and still current. Finding
  2026-08-25-vision-signed-linear-discriminant-spiking-readout-solves-quantization-wall-relocates-to-feature-ceiling.
