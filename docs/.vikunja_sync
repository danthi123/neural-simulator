# Vikunja board reconcile stamp — updated by the `vikunja` skill after each board reconcile.
#
# The vikunja-freshness gate (tools/gates/vikunja_freshness.py, CLASS VF) blocks a finding-adding
# commit once THRESHOLD (6) findings have landed since this file was last touched. Stamping this file
# (in the reconcile commit) = "the board at https://vikunja.dant123.com was reconciled" and resets the
# budget. This is the mechanical enforcement the board previously lacked (the roadmap sync was gated,
# the board sync was only remembered — so the board drifted while the docs stayed fresh, 2026-08-20).
#
# Update this on every board reconcile (the `vikunja` skill does it): bump the date + at_commit + note.

last_reconciled: 2026-08-20
at_commit: ec5b9f96
note: reconciled after the D5 learn-through-use arc (steps 1-5) + GNW two-organ default-on + the SWR
  memory-replay arc + the knowledge-loading arc scoping. Board changes: #71 updated (de-risked, learn-through-use
  advanced end-to-end + conversation-visible, still default-off); #76 marked done (dopamine-mode-in-chat, default-on);
  #108 created (give the brain a real body of knowledge to talk about).
  Follow-up 2026-08-20: #108 advanced — first real knowledge bundle built (2413 Wikidata facts, loads+answers+abstains); still de-risked (standalone, not in the always-on chat).
  Follow-up 2026-08-20b: #71 -> learn-through-use now RELIABLY conversation-visible (graded read, step-6); #109 -> knowledge-scale sharding de-risked GO (sub-second at any K). Both banked.
  Follow-up 2026-08-20c: #109 -> knowledge-scale sharding now WIRED into the live chat (tiered hippocampal-buffer/cortical-LTM split, opt-in default-off; 3/3 GO drop-in through the real agent + end-to-end with the real 2413-fact Wikidata bundle). de-risked label set; remaining before default-on = a 100k+ real-fact bundle (rate-limited fetch in flight) + a soak. New ledger row tiered-knowledge-ltm (wired=YES on_by_default=NO).
