# Vikunja board reconcile stamp — updated by the `vikunja` skill after each board reconcile.
#
# The vikunja-freshness gate (tools/gates/vikunja_freshness.py, CLASS VF) blocks a finding-adding
# commit once THRESHOLD (6) findings have landed since this file was last touched. Stamping this file
# (in the reconcile commit) = "the board at https://vikunja.dant123.com was reconciled" and resets the
# budget. This is the mechanical enforcement the board previously lacked (the roadmap sync was gated,
# the board sync was only remembered — so the board drifted while the docs stayed fresh, 2026-08-20).
#
# Update this on every board reconcile (the `vikunja` skill does it): bump the date + at_commit + note.

last_reconciled: 2026-08-25
at_commit: 0fe3c3888
note: DA-GATED ENCODING flipped to production default-ON (board #116 DONE). The faculty
  (webapp/da_encoding_drives_chat.da_encoding_enabled, _DA_ENCODING_DEFAULT_ON=True) now scales every taught
  fact's write magnitude by the brain's own self-produced dopamine by default; BRAIN_DA_ENCODING=0 is the
  byte-identical escape. Gated by the 6-seed substrate-scaling soak GO + two prep rungs (OFF-arm verifiers
  pinned to =0; the Turrigiano scaling pass wired onto the between-turn idle tick). Flip-verified GO through
  the real handler (default drives g_high 2.48 > g_low 1.0; lesion severs; =0 byte-identical) + wave4 composed
  no-regression GO (8 turns, 0 diverged). #138 CREATED (fire the scaling pass on a sleep/NREM event, next rung).
  Ledger row da-gated-encoding on_by_default:YES. finding 2026-08-25-da-encoding-faculty-default-on-flip.
  PRIOR HARVEST note preserved in git history (was 2026-08-26 @ 01bb124c: #133 DONE knowledge core, #132/#75/#129/#130 advanced, #134 created).
