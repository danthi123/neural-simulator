# Host-decision seam map — the default `/api/brain-chat` turn (DRAFT, local-model runbook task 1)

**Status: DRAFT — facts only, no verdict. Claude reviews after Tuesday.**

Scope: ONE default chat turn, traced in execution order from `POST /api/brain-chat`
(`webapp/server.py:4526`, `def brain_chat`) through `brain_reply` (the shared full-faculty pipeline,
`webapp/server.py:4631`; `webapp/brain_reply.py` is a thin surface that delegates to it). One table row per
place where Python (the host) decides something FROM NEURAL OUTPUT (input-side host decisions are included but
marked, because the host-decision instrument needs the full inventory). Categories: candidate generation /
scoring / admission-filtering / winner selection / commitment / composition / routing. "Neural signal already
represents the choice?" cites the evidence when known; "unsure" where not certain.

Trace order (complete): handler (server.py:4526-4628) → `brain_reply` (4631) → GNW gate-wrapper installs
(4650-4769, wiring; the host decisions live INSIDE the wrapped `chat.gate` — Part 2) → composer trace flag +
`_read_activity` (4777-4793) → swap-drives (4801-4831) → self-initiation (4833-4869, idle/empty turns only) →
vision-identity (4876-4920, `req.percept` only) → bg-action-selection (4922-4957, content-empty turns only) →
AFFECT (4959-5018) → feel-query (5020-5029, disjoint) → open-ended (5031-5231, default-OFF) → affect-drives #84
(5233-5253) → W5 ToM (5255-5280, default-OFF) → DA-mode #79 (5282-5303) → common ground (5305-5327, default-OFF)
→ DA-encoding (5329-5350, default-OFF) → tag-capture (5352-5363, default-OFF) → wander #86 (5365-5389) →
ideation (5391-5416, default-OFF) → PMem (5418-5477) → episodic D5 (5479-5551, referential turns) → worldmodel
E2 (5553-5645) → multiref D6 (5647-5767) → silent WM (5769-5809) → discourse register D3 (5811-5846) →
false-belief ToM (5848-5887, default-OFF) → causal (5889-5959, causal-query turns) → pragmatic (5961-5994) →
comprehension D4 (5996-6109) → surprise D2 (6111-6183) → #129 crossedge (6184-6218, default-OFF) → B3
(6220-6254) → metacog E1 (6256-6307) → curiosity D3 (6309-6388) → **RICH path (6390-6773, the default turn)** →
single-fact path (6775-7132, NOT on the default turn — mirror, see below).

## Part 1 — rows that fire on the default content turn (execution order)

| # | file:line | operation (what the host computes) | neural input just before | the decision | category | neural signal already represents the choice? |
|---|-----------|------------------------------------|--------------------------|--------------|----------|----------------------------------------------|
| 1 | webapp/server.py:4565 | `renderer = (req.renderer or _default_brain_renderer()).lower()` | none (request field) — NOT a neural-input decision; listed because it routes the rest of the turn | which renderer produces the surface form | routing | n/a (input is the request, not neural output) |
| 2 | webapp/server.py:4827 (+ webapp/swap_drives_chat.py) | `observe_turn(chat, msg)` runs one spiking swap/hold detector per turn against the grounded topic; the host maps the spiking verdict to a topic-shift lead; the lead is prepended OUTERMOST at 6699-6702 (rich) / 7015+ (single-fact) | spiking mismatch/salience verdict + the grounded topic (host parse of the message — declared boundary) | whether to announce a topic shift, and which topic to name | composition | the swap/hold verdict is spiking; the lead TEXT is host composition (internal mapping: Part 2) |
| 3 | webapp/server.py:4983 | `appr = _AO.appraise_text(msg)` — host lexicon appraisal of the message → valence/arousal | none — input-side (declared host boundary) | the turn's affective appraisal | scoring | n/a (input is the message, not neural output) |
| 4 | webapp/server.py:4984 | `mood = _update_session_mood(cache_key, appr)` — host EMA over the per-session mood state; the mood then conditions the neural organ | none — host state (feeds the organ's conditioning) | the session mood carried into the next read | commitment (state update) | n/a (host state; the organ read is the next row) |
| 5 | webapp/server.py:4986-4988 | `read = organ.read_differential(mood["valence"]); diff = float(read["differential"]); level = _AO.tone_level(diff)` — host bucketing of the spiking differential into a discrete tone level | `read["differential"]` (spiking pos/neg rate differential, mood-conditioned) | the discrete tone level | scoring → commitment | the differential is graded spiking; the level is a host threshold bucket — unsure whether the organ's spiking ladder already encodes the discrete level (Part 2: `affect_production_organ.tone_level`) |
| 6 | webapp/server.py:4989 | `affect_plan = _AO.content_plan(level)` — host mapping level → `{max_sentences, max_elaborations}` (the forthcomingness ceiling) | via level (spiking differential) | how many gate-matched facts to volunteer + elaboration depth | commitment | host mapping from a spiking read (Part 2: `content_plan`) |
| 7 | webapp/server.py:4990, 5006-5014 | `manner_tmpl = _AO.manner_template_for(level)` — host mapping level → phrasing template; installed on `MoodConditionedRenderer` (5007-5011) and on the spiking recall mouth via `chat._mood_tone_level = int(level)` (5014) | via level (spiking differential) | the surface manner (warmer/curter phrasing) | composition | host mapping from a spiking read (Part 2: `manner_template_for`) |
| 8 | webapp/server.py:4995 | `valence_sign = ("+" if diff > tol else ...)` with `tol = _AO.SA.LADDER_NEUTRAL_TOL` — host ±tol threshold | spiking differential | the trace valence sign (JSON field only — NOT on the answer surface) | scoring (trace) | host threshold on a spiking read |
| 9 | webapp/server.py:5249 (+ webapp/affect_drives_chat.py) | `observe_turn` reads the felt valence × arousal off the #81 graded-affect ladder (neural, off `cp_firing_states`) → a graded warmth/curtness lead + trace. The lead is computed + recorded EVERY turn; the surface gate `BRAIN_AFFECT_MARKER_SURFACE` is default-OFF (6674-6683) — so by default the lead is NOT prepended | the graded ladder read (spiking) | which affective expression to attach (recorded by default; spoken only when the flag is on) | composition | the ladder read is graded spiking; the lead-text selection is a host mapping (Part 2) |
| 10 | webapp/server.py:5299 (+ webapp/da_mode_drives_chat.py) | `observe_turn` reads the self-produced DA level off the spiking DA nucleus → a graded engagement suffix (host level → suffix text); the suffix is appended at 6729-6732 (rich) | the DA level (spiking) | the engagement suffix text | composition | the level is spiking; the level → text mapping is host (Part 2) |
| 11 | webapp/server.py:5383 (+ webapp/continuous_engine.py) | `recent_wander(cache_key)` consumes the last idle-wandered concept (selected BETWEEN turns by the curiosity-biased spiking selection on the self-initiation organ; the record is host-stored) → lead "(I'd been mulling over %s.)"; prepended at 6714-6717 (rich) | the wandered concept (spiking selection between turns) | whether to surface the wander, and its text | composition | the selection is spiking (between turns); the surfacing text is host |
| 12 | webapp/server.py:5449 | `_PM.parse_intention(msg)` — host text→slot mapping (intention + cue clause + cue keywords); declared HOST-SCAFFOLD (comment 5435-5438) | none — input-side (declared host scaffold) | formation vs monitor routing | routing | n/a (input is the message) |
| 13 | webapp/server.py:5467-5471 | `rd = porg.read_turn(msg)` (spiking held-intention × cue coincidence, rel ≥ FIRE_THR); `if rd["fired"]: pmem_prefix = _PM.reminder_text(...)` — the prefix is prepended at 6487 (rich) / 6916 (single-fact) | the spiking coincidence read | whether to release the reminder + its text | commitment (release) + composition | the fire is a spiking threshold; the text is host |
| 14 | webapp/server.py:5591-5592 | `obs_val = float(_wm_appraise(msg).get("valence", 0.0)); obs_sign = 1 if obs_val > 0.02 else (-1 if obs_val < -0.02 else 0)` — host lexicon appraisal + host ±0.02 threshold → valence sign; declared host boundary / RESIDUAL (comment 5558-5560) | none — input-side (declared residual) | the turn's valence sign | scoring | n/a (input is the message) |
| 15 | webapp/server.py:5619-5621 | `held = wm_state.get("expected_sign"); if held is not None and obs_sign != held: sj = worg.read_surprise(...)` — host persistence-prior comparison (sign flip vs the held expectation) decides whether the spiking surprise read runs at all | the held `pred_sign` (from the prior turn's spiking `expectation()` read, 5637-5639) + the host-computed `obs_sign` | whether to read surprise this turn | admission-filtering | the held expectation is a spiking read; the flip comparison is host state-selection (declared residual) |
| 16 | webapp/server.py:5624-5625 | `if sj["surprised"]: worldmodel_prefix = _WM.worldmodel_surprise_notice(int(held))` — prepended at 6487 | the spiking surprise read | the surprise-notice text | composition | the fire is a spiking threshold; the text is host |
| 17 | webapp/server.py:5637-5639 | `exp = worg.expectation(obs_sign, ...); wm_state["context_sign"] = int(obs_sign); wm_state["expected_sign"] = int(exp["pred_sign"])` — the next-turn prediction is a spiking two-pool read; the host stores it | the spiking `pred_sign` | the held expectation for the next turn | commitment (state update) | the prediction is spiking; the storage is host bookkeeping |
| 18 | webapp/server.py:5756 | MAINTAIN: `jm = d6org.judge(msg, lesion=d6les)` — spiking load+hold of ≥2 named referents (write-only; `None` when out-of-scope → byte-identical) | the organ's own scope decision | whether to load+hold this turn's referents | admission (load gate) | unsure where the referent extraction happens (inside the organ, or a host lexicon) — Part 2 |
| 19 | webapp/server.py:5802-5806 | MAINTAIN: `_sw_refs = _SW._extract_refs(msg)` (host referent lexicon — declared residual, reuses D6's); `if _sw_refs: sworg.write_referent(_sw_refs[0])` else `sworg.note_distractor()` | none — input-side host lexicon (declared residual) | which referent to hold silently (the FIRST named) vs a distractor turn | candidate selection + admission | n/a (host lexicon pick; the hold itself is spiking facilitation) |
| 20 | webapp/server.py:5834 | `_DR.note_turn(msg, chat.agent, dstate, actions=chat.actions_set)` — host SVO/clause parse + connective detection → updates the running spiking register pair (side effect; the reply is unchanged) | the brain's known-action set (`chat.actions_set`) constrains the parse | the event-boundary (SHIFT) decision that drives the spiking register | routing | the boundary detection is host; the register update is spiking |
| 21 | webapp/server.py:5989 | `pj = _get_pragmatic_organ().judge_text(msg, ...)` — spiking RSA belief over {none, SBNA, all}; `if pj is not None:` gates the notice (a scalar-implicature turn class was detected) | the spiking graded belief | whether the turn carries a scalar implicature | routing + admission | the belief is spiking; the turn-class detection + notice are host (Part 2: `pragmatic_production_organ.judge_text`) |
| 22 | webapp/server.py:5991-5992 | `pragmatic_prefix = _PR.pragmatic_notice(pj)` — host composes the honest functional reading; prepended at 6487 | the spiking belief | the pragmatic-notice text | composition | host mapping from a spiking read |
| 23 | webapp/server.py:6023-6024 | `cj = corg.judge(msg, brain_vocab=..., wm_focus=...)` — spiking role-competition sel-pool margin → `comprehended`; the scope decision (in-scope transitive vs out-of-scope → `None`) is host (Part 2: `comprehension_production_organ`) | the spiking margin | whether the turn is in scope, and whether it resolved | admission-filtering | the margin is spiking; the scope gate is host |
| 24 | webapp/server.py:6031 | `known = bool(chat.inner.what_does(a_c, v_c))` — the brain's OWN spiking recall (not a host lookup) checks whether the brain knows this (agent, action) → the guard: never abstain on a known fact | the spiking recall | the known-fact guard | admission-filtering (guard) | the recall is spiking; the truthiness guard is host |
| 25 | webapp/server.py:6054 | `if (not cj["comprehended"]) and (not known):` — host conjunction of the spiking margin + the spiking recall → the abstain decision (early return, the honest "I didn't follow that") | both spiking | whether to abstain for non-comprehension | commitment (abstain) | both inputs are spiking; the conjunction is host logic |
| 26 | webapp/server.py:6063-6073 | repair (default-ON, `BRAIN_REPAIR`): `tgt = corg.repair_target(...)` (spiking per-noun read of the unresolved role) → `q = _RP.clarification_question(tgt)` (host composes the clarification question); early return at 6107 | the spiking target | the clarification-question text | composition | the target is spiking; the question text is host |
| 27 | webapp/server.py:6138 | `asrt = _SO.extract_assertion(msg)` — host SVO extraction (agent, action, patient) | none — input-side | whether the turn is an assertion | routing | n/a (input is the message) |
| 28 | webapp/server.py:6144 | `p_stored = chat.inner.what_does(a_s, v_s)` — the expected patient is RECALLED by the brain's own spiking recall (not a host lookup) | the spiking recall | the stored expectation | candidate generation | the recall is spiking |
| 29 | webapp/server.py:6147 | `if p_stored:` — host truthiness gate: the surprise path runs only when a stored expectation exists | the spiking recall | whether to run the mismatch unit | admission-filtering | the recall is spiking; the truthiness gate is host |
| 30 | webapp/server.py:6149-6152 | `sj = sorg.judge(a_s, v_s, p_stored, p_asserted, ...)` — spiking mismatch unit (GABA_A subtractive inhibition; windowed `cp_firing_states` rate, NO host recalled==asserted compare); `if sj["surprised"]: surprise_prefix = _SO.surprise_notice(...)` — prepended at 6487 | the spiking mismatch read | the surprise fire + notice text | commitment (fire) + composition | the fire is spiking; the text is host |
| 31 | webapp/server.py:6169-6170 | `if (_rc_on and sj.get("surprised") and str(p_asserted).lower() != str(p_stored).lower()):` — host string compare (asserted ≠ stored) + the spiking window → the in-place rewrite decision | the spiking window (`sj`) | whether to reconsolidate (rewrite the stored fact in place) | commitment (belief revision) | the window is spiking; the ≠ compare is host (the accepted no-confab moat pattern) |
| 32 | webapp/server.py:6177-6179 | `if res.get("action") == "rewrite": reconsolidation_prefix = _RC.reconsolidation_notice(...)` — prepended at 6487 | the organ's action result | the reconsolidation-notice text | composition | host mapping from the organ's result |
| 33 | webapp/server.py:6241-6245 | `nres = _get_noncontradiction_organ().check(chat.inner.is_it_true, msg, ...)` — spiking polarity WTA recall (`is_it_true` → `_spiking_select` off `cp_firing_states`) + "the gate proper is the ONE host boolean `stored != asserted`" (comment 6225-6226); `if nres["reject"]:` → rejection short-circuit | the spiking polarity recall | whether to reject the asserted SVO | commitment (reject) | the polarity recall is spiking; the reject boolean is a host compare |
| 34 | webapp/server.py:6247 | `_NC.rejection_message(nres["svo"], nres["stored_polarity"])` — host composes the rejection text | the organ's result | the rejection text | composition | host mapping from the organ's result |
| 35 | webapp/server.py:6291-6292 (in `_metacog_qualify`, 6279+) | `mrc = _MC.mean_role_confidence(activity); ev = _MC.evidence_from_role_conf(mrc)` — host mean over the spiking role-decode confidences (from the recall trace) → evidence | the spiking role confidences | the metacognitive evidence value | scoring | the confidences are spiking; the mean + mapping are host |
| 36 | webapp/server.py:6302-6305 | `j = _get_metacog_organ().judge(ev, ...)` — spiking balance-of-evidence monitor (`|rate(asm1)-rate(asm0)|`); `("" if j["confident"] else _MC.hedge_prefix())` | the spiking margin | whether to hedge + the hedge text | commitment (qualify) + composition | the confident decision is spiking; the hedge text is host |
| 37 | webapp/server.py:6332 | `topic = _CU.extract_topic(msg)` — host topic extraction | none — input-side | the curiosity topic | routing | n/a (input is the message) |
| 38 | webapp/server.py:6341 | `novelty_val = _CU.NOVEL_SIGNAL` — host CONSTANT: every abstain is treated as maximally novel (in place of a graded read; the graded version is default-OFF, 6342-6347) | none — a host constant | the novelty signal | scoring | NO — this is a host constant, not a neural read (the graded read exists but is default-OFF) |
| 39 | webapp/server.py:6348 | `j = _get_curiosity_organ().judge(novelty=novelty_val, ...)` — spiking ASK-pool read (want_hz vs threshold); `curious = bool(j["curious"])` | the spiking ASK-pool read | whether to crave (follow up) | commitment (crave) | the crave is a spiking threshold (fed by the host constant above) |
| 40 | webapp/server.py:6385 | `return _CU.followup_question(topic), info` — host composes the follow-up question from the topic (on a curious abstain) | the spiking crave decision | the follow-up question text | composition | host mapping from the topic |
| 41 | webapp/server.py:6400 | `use_rich = req.rich if req.rich is not None else _brain_rich_default()` — host mode resolution (request/env, fluent-ON default) | none — request input | rich vs single-fact path | routing | n/a (input is the request) |
| 42 | webapp/server.py:6407-6410 | `if affect_plan is not None: rich.max_sentences = int(affect_plan["max_sentences"]); rich.max_elaborations = int(affect_plan["max_elaborations"])` — the host installs the affect content plan (from the neural mood, via level) into the composer as the forthcomingness ceiling | via mood → differential → level (spiking) | how much content the composer may produce | commitment | host mapping from a spiking read (where affect bites content) |
| 43 | webapp/server.py:6431-6434 | confidence-forthcoming #94 (default-ON): `_cf_floor = _CF.floor_override() or (rich.max_sentences, rich.max_elaborations); rich.max_sentences, rich.max_elaborations = _CF.reach_plan(*_cf_floor)` — the host requests ONE extra "reach" fact | the floor just set (rows 42 / composer default) | the reach ceiling | commitment (reach) | host plan (the cap below is where the neural read decides keep/truncate) |
| 44 | webapp/server.py:6436 | `r = rich.answer(msg)` — `RichAnswerComposer`: its own gate + multi-hop chain + elaboration + per-sentence verify (Part 2 — the largest host-decision cluster: candidate generation, chain selection, sentence verification, abstain) | the spiking gate + recall (inside) | the core answer content | candidate generation + winner selection + commitment | Part 2 |
| 45 | webapp/server.py:6456-6460 | the cap: `_cf_activity = _read_activity(); _cf_mc_prefix, _cf_mc_info = _metacog_qualify(_cf_activity, False); r, _cf_trace = _CF.apply_cap(rich, r, _cf_floor[0], (_cf_mc_info or {}).get("confident"))` — if the spiking confidence read is not confident, the reach's extra fact is TRUNCATED back to the floor | the spiking confidence read (the SAME read the E1 hedge reuses) | keep vs truncate the reach fact | winner selection | the confidence is spiking; the truncate decision is host (`_CF.apply_cap`, Part 2) |
| 46 | webapp/server.py:6474-6477 | episodic store gate: `if _episodic_on and (not r["abstained"]) and facts and not _rich_derived and _episodic_store_ok():` → `_EP.get_episodic_organ(...).note_topic(facts[0][0])` — write-only BTSP form (no reply change); host conjunction + the cupy-only latency gate | the composer's verified facts (spiking gate hits) | whether to BTSP-store the answered topic | admission (write gate) | the facts are spiking-sourced; the conjunction + the backend gate are host |
| 47 | webapp/server.py:6486-6488 | `answer = pmem_prefix + worldmodel_prefix + surprise_prefix + reconsolidation_prefix + pragmatic_prefix + r["answer"]` — host composes the final surface from the prefixes + the core (the accretion ORDER is host) | the prefix set (each from a spiking read above) + the core | the final answer surface (and its order) | composition | host accretion over spiking-sourced parts |
| 48 | webapp/server.py:6495 | `recalled_svo = (None if _rich_derived else (facts[0] if facts else None))` — host decides what to report as the recalled fact (the FIRST supporting fact; null for a derived answer) | the composer's verified fact list (spiking gate hits) | which fact is reported as the recall | candidate selection | the facts are spiking-sourced; the "first" pick is host |
| 49 | webapp/server.py:6504 | `verified: (not r["abstained"]) and bool(facts)` — host verdict of verified-by-construction | the composer's abstain + facts | the verified flag | commitment | host conjunction over spiking-sourced fields |
| 50 | webapp/server.py:6566-6573 | self-schema authorship (default-ON; hypothesis turns only): `_ss_read = _get_self_schema_organ().read_author(authored=True, ...)`; `if _ss_read.get("is_self"): resp["answer"] = _SS.authorship_marker() + resp["answer"]` | the spiking is_self read | whether to mark the answer as self-authored | composition | the is_self read is spiking; the marker prepend is host |
| 51 | webapp/server.py:6611-6616 | metacog hedge on the rich path: `_metacog_qualify(resp.get("activity"), ...)`; `if _mc_prefix: resp["answer"] = _mc_prefix + resp["answer"]` — reuses the #94 confidence read (row 45) | the spiking confidence read | the hedge prefix on the rich answer | composition | same as row 36 |
| 52 | webapp/server.py:6624-6626 | curiosity suffix on the rich path (same decision as rows 39-40, applied as a suffix here instead of an early return) | the spiking crave decision | the curiosity suffix | composition | same as rows 39-40 |
| 53 | webapp/server.py:6674-6683 | affect-drives surface gate: `_affect_surface_on = _ADC_surf.affect_marker_surface_enabled()` (default-OFF, the 2026-09-25 owner decision) → `if affect_drives_lead and _affect_surface_on: resp["answer"] = affect_drives_lead + resp["answer"]` | the recorded affect lead (row 9) | whether the affect lead is spoken (default: recorded, not spoken) | composition (gated) | the lead is spiking-sourced; the surface gate is a host flag |
| 54 | webapp/server.py:6699-6702 | `if swap_drives_lead: resp["answer"] = swap_drives_lead + resp["answer"]` — the host prepends the swap lead OUTERMOST (default-ON) | the swap lead (row 2) | the final surface with the topic-shift lead | composition | host accretion |
| 55 | webapp/server.py:6714-6717 | `if wander_lead: resp["answer"] = wander_lead + resp["answer"]` — the host prepends the wander lead (default-ON) | the wander lead (row 11) | the final surface with the wander lead | composition | host accretion |
| 56 | webapp/server.py:6729-6732 | `if da_suffix: resp["answer"] = resp["answer"] + da_suffix` — the host appends the DA engagement suffix (default-ON) | the DA suffix (row 10) | the final surface with the engagement suffix | composition | host accretion |
| 57 | webapp/server.py:6758-6769 | GNW global stop (default-ON): `_gnw_stop_mod.observe_turn(chat, msg)` → the spiking distributed-overwrite clear-all decision; the host prepends the lead OUTERMOST when a clean clear fired | the spiking clear-all read | whether to announce the global stop | composition | the clear-all is spiking; the lead text is host (Part 2) |

## Disjoint classes (a host parse routes a DIFFERENT turn type; not on the default content turn)

Same table format; each of these short-circuits the turn with its own response when the host parse matches.

| # | file:line | operation | neural input | the decision | category | neural signal already represents the choice? |
|---|-----------|-----------|--------------|--------------|----------|----------------------------------------------|
| D1 | webapp/server.py:4833-4869 | self-initiation (default-ON; idle/empty turns only): host trigger parse (`is_selfinit_trigger`) → the spiking self-initiation organ selects a topic → host composes the self-initiated turn | the spiking topic selection | whether to self-initiate + the topic | routing + composition | the topic selection is spiking; the trigger parse + text are host |
| D2 | webapp/server.py:4876-4920 | vision-identity (default-ON; `req.percept` only): the spiking identity read → host composes the identification | the spiking identity read | the identification text | composition | unsure (Part 2) |
| D3 | webapp/server.py:4922-4957 | bg-action-selection (default-ON; content-empty turns only): the spiking selection → host composes the action announcement | the spiking selection | the announced action | composition | unsure (Part 2) |
| D4 | webapp/server.py:5022-5029 | feel-query (default-ON): host parse `is_feel_query` → `feel_readout(differential, valence, arousal)` — host composes the honest functional read-out from the spiking differential + host mood state | the spiking differential + host mood state | the feel read-out text | composition | the differential is spiking; the text is host (a functional read, never a phenomenal claim) |
| D5 | webapp/server.py:5450-5462 | PMem formation (default-ON; intention turns only): host parse (row 12) → spiking latch + `acknowledgement_text` (host) → early return | the parsed slots (host) | the acknowledgement text | composition + commitment | n/a (the latch is spiking; the parse + text are host) |
| D6 | webapp/server.py:5499-5549 | episodic D5 referential (default-ON; "did we discuss X" turns): host `is_referential` + `extract_referent` (host, against the brain's topic set) → spiking `eorg.recall(ref)` pattern completion → **host first-match content selection**: loop `_kb_iter` (composer kb / D6 visible_kb), first fact with agent==ref + action + patient → `content = chat.render([a,v,p])` (5528-5539) → `recall_disclosure` (host text); `abstained = not in_memory` | the spiking completion verdict + the kb fact set | which stored fact to surface (the FIRST match) + the disclosure text | candidate selection + commitment | the completion is spiking; the content pick is a HOST first-match loop — unsure whether the kb iteration order is a learned/neural ranking (Part 2) |
| D7 | webapp/server.py:5578-5587 | worldmodel expectation-query (default-ON; "what do you expect" turns): spiking `expectation()` two-pool read → `expectation_readout` (host text) | the spiking prediction | the expectation read-out text | composition | the prediction is spiking; the text is host |
| D8 | webapp/server.py:5676-5754 | D6 hold-query (default-ON; "who/what are we talking about" turns): spiking `judge` readout of the held referents → host composes; the xedge curiosity-d6 qualifier (default-OFF) appends a self-consuming qualifier when a recent crave clears the floor | the spiking buffer readout | the hold read-out text | composition | the readout content is the spiking buffer; the sentence is host |
| D9 | webapp/server.py:5792-5800 | silent-WM recall-query (default-ON; temporal-recall turns): spiking ping reactivation of the silent buffer; `abstained = (jq.get("recovered") is None)` — the NO-CONFAB gate: the abstain decision comes from the spiking recovered verdict, the host maps it | the spiking recovered verdict | abstain vs answer + the read-out text | commitment (abstain) + composition | the recovered verdict is spiking; the abstain mapping + text are host |
| D10 | webapp/server.py:5836-5844 | discourse before/now-query (default-ON; "who was doing it before/now" turns): spiking slot read off the register; the moat abstain (a single-event register structurally abstains — the abstain decision is partly host/structural: no boundary opened → abstain) | the spiking register slot | the before/now answer or the abstain | commitment + composition | unsure where the structural abstain is decided (inside `_DR.maybe_answer` or host) — Part 2 |
| D11 | webapp/server.py:5916-5948 | causal what-if/why (default-ON; causal-query turns): host `is_causal_query` + `extract_cue` (host; the known-agent gate `c_agent is not None` admits only brain-known agents) → host event-identity check (`evt == _CA.A` / `evt == _CA.Y`) picks the validated branch → spiking `corg.what_if/why` (forward simulation / DO-probe; moat-confirmed by `composer.query_patient`) → on unconfirmed: `_honest_causal_answer` (host text) + `abstained = not confirmed` | the spiking simulation + the moat confirmation | the causal answer or the honest abstain | commitment + composition | the simulation + confirmation are spiking; the parse + branch pick + abstain text are host |
| D12 | webapp/server.py:5479-5551 (5499) | (see D6) — listed once above | | | | |

## Default-OFF blocks (flag + the seam it would add when flipped)

| block | flag (default-OFF) | the seam it would add |
|-------|--------------------|-----------------------|
| open-ended state-driven generation (5031-5231) | `BRAIN_OPEN_ENDED` | a generated (non-recalled) reply path with a host verify post-filter + the R1 rung-2/3 state-write folds (5140-5230) |
| W5 affective ToM (5255-5280) | (W5 flag) | a host-interpreted ToM read prepended to the surface |
| common ground (5305-5327) | (CG flag) | a CG lead prepended at 6707-6710 |
| DA-gated encoding (5329-5350) | (DA-encoding flag) | the spiking DA level → a host encoding gain `g` scaling the write magnitude |
| DA tag-and-capture (5352-5363) | (tag-capture flag) | a DA-gated trace field |
| ideation (5391-5416) | (ideation flag) | an ideation lead prepended at 6721-6724 |
| false-belief ToM (5848-5887) | (false-belief flag) | a false-belief read-out short-circuit |
| #129 surprise→provenance crossedge (6184-6218) | `BRAIN_ONEBRAIN_XEDGE_SURPRISE_EPISODIC` | a crossedge diagnostic field |
| xedge curiosity→D6 (5709-5748) | `BRAIN_ONEBRAIN_XEDGE_CURIOSITY_D6` (+ `..._SEMANTIC_DROP`) | a host threshold `shift_w0 <= -INTACT_FLOOR` (5721-5723) that appends a qualifier / drops a referent from the read-out |
| xedge surprise→worldmodel C1 (5595-5635) | `BRAIN_ONEBRAIN_XEDGE_SURPRISE_WORLDMODEL` | a second spiking surprise read gating one Hebbian co-fire step |
| source-provenance #129 (single-fact path, 6837-6896) | (source-provenance flag) | a provenance suffix on the single-fact answer |
| transitive route (single-fact path, 6800-6816) | (transitive flag) | a host route check before `chat.gate` |
| OEG (6444-6445) | (OEG flag) | a host BG speak/abstain + off-KB reply applied to the rich result |
| affect-marker surface (6674-6683) | `BRAIN_AFFECT_MARKER_SURFACE` | the affect lead (row 9) is prepended instead of only recorded |
| affect-marker congruence gate A2 (6656-6666) | (A2 flag) | a host congruence check between the lead and the answer |
| graded novelty (6342-6347) | (graded-novelty flag) | a graded spiking novelty read in place of the `NOVEL_SIGNAL` constant (row 38) |
| curiosity DA-gate | (curiosity-DA flag) | a DA-gated threshold on the ASK-pool read |
| D6 engram readtime / focus-bind (5759-5765) | (D6 flags) | an anaphor resolution read off the held state |
| GNW bus/swap observability (6639-6649) | (GNW observability flags) | trace fields only (no surface change) |
| composer read-only trace (4777-4787) | (trace flag) | a trace field (no surface change) |

## The single-fact path (6775-7132) — NOT on the default turn

`use_rich` is fluent-ON by default (row 41), so the default turn takes the RICH path above. The single-fact
path (reached only via an explicit `rich=False`) is a MIRROR of the rich path with duplicated call sites (the
R1 residual): `gate_svo = chat.gate(msg)` at 6816 → `if gate_svo is None: answer, abstained, verified = "I
don't know about that.", True, False` (6817-6818) → `answer = chat.render(gate_svo)` (6820-6824) with
`verified = "[unverified render" not in answer` (a host string check on the renderer's output) → the same
prefix concat (6916-6918), metacog + curiosity (6922-6931), response assembly (6942-6996), and the same
lead/suffix accretion cascade (7015-7128). Every seam above recurs here at a different line; the map tracks
the default (rich) turn.

## Part 2 — the called components (where most host decisions actually live)

[SKELETON — to be filled:]

- **A. `ChatBrain` core** (`research/runners/brain_chat_tui.py:717`): `gate` / `gate_extract` / `render` /
  `what_does` / `is_it_true` / `_spiking_select` — the recall core the wrapped `chat.gate` runs.
- **B. `RichAnswerComposer.answer`** (`research/runners/rich_answer_composer.py:303`): its own gate +
  multi-hop chain selection + elaboration + per-sentence verify + abstain — the largest host-decision cluster.
- **C. the GNW gate-wrapper stack** (installed at server.py:4650-4769, around `chat.gate`):
  `webapp/gnw_bus_shadow.py` (ignition bus), `webapp/gnw_two_organ_bus.py`, `webapp/gnw_three_organ_bus.py`
  (consensus), `webapp/gnw_deliberation.py`, value-driven choice (`value_choice_production_organ`),
  `webapp/gnw_multistep_deliberation.py` — consensus/veto/commit logic.
- **D. the observe_turn family** (the lead mappers): `webapp/swap_drives_chat.py`,
  `webapp/affect_drives_chat.py`, `webapp/da_mode_drives_chat.py`, `webapp/gnw_global_stop.py`,
  `webapp/continuous_engine.py` (`recent_wander`).
- **E. the organ mapping/text functions** (host mappings from spiking reads to text/numbers):
  `affect_production_organ` (`appraise_text` 104, `tone_level` 343, `content_plan` 348,
  `manner_template_for` 369, `is_feel_query` 408), `d5_episodic` (`recall_disclosure`),
  `worldmodel_production_organ` (`expectation_readout`, `worldmodel_surprise_notice`),
  `surprise_production_organ` (`surprise_notice`, `extract_assertion`), `pragmatic_production_organ`
  (`judge_text`, `pragmatic_notice`), `metacog_production_organ` (`mean_role_confidence`,
  `evidence_from_role_conf`, `hedge_prefix`), `curiosity_production_organ` (`extract_topic`,
  `followup_question`, `NOVEL_SIGNAL`), `prospective_memory` (`parse_intention`, `reminder_text`,
  `acknowledgement_text`), `b3_noncontradiction_production_organ` (`check`, `rejection_message`),
  `d3_discourse_event_register_production_organ` (`note_turn`, `maybe_answer`),
  `comprehension_production_organ` (`judge`, `repair_target`), `repair` (`clarification_question`),
  `activity_silent_wm_production_organ` (`_extract_refs`, `judge`), `d6_multiref_wm_production_organ`
  (`judge`), `causal` organ (`is_causal_query`, `extract_cue`, `what_if`, `why`, `_honest_causal_answer`),
  `self_schema` organ (`read_author`, `authorship_marker`), `confidence_forthcoming_chat` (`reach_plan`,
  `apply_cap`, `floor_override`).
